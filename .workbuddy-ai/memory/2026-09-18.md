# 2026-09-18

## PDF base-pattern audit (course «دورهٔ آلفا» p.105-108) vs BaseKnotTool

User asked whether the course's base text (FTR/ETR/CTR/OTR + ABO/EBO/CBO/OBO,
RBR/DBD) is implemented. Answer: yes for the first half, deliberately no for the
second. No code changed — this is a findings record.

Mapping found (useful for future questions):

- ABO (Failure To Return)   -> `nd.returned == false`      -> `BK_STATE_FRESH`
- EBO (Engulf To Return)    -> `nd.returned == true`       -> `BK_STATE_TESTED`
- CBO/CO (Clean To Return)  -> `nd.rebreaks > 0`           -> `BK_STATE_TESTED`
- OBO (Out To Return)       -> `nd.crossed == true`        -> `BK_STATE_CONSUMED`

The four names are NOT printed and NOT a type. `BaseKnotNodeName` returns the
short name alone (`BaseKnotTool.mqh:3501-3506`); the course's pairs are prose in
the header comment at `BaseKnotTool.mqh:285-288`.

Type ownership today (P-BK-75/78/79): `BaseKnotNodeKindOfLength`
(`BaseKnotTool.mqh:3192`) = the BOX HEIGHT against three ATRs off the node's own
time — `h <= mid(trig,pat)` FTR, `<= mid(pat,str)` ETR, `<= str` CTR, else OTR.
Abilities pushed in per (TF, anchor) by the pump; audit mirror in
`tools/base-count-audit.py:1422-1433`.

Retired in place (do not resurrect without the user):
- `BKNODEKIND-OFF` (`BaseKnotTool.mqh:3438-3444`) — the story classifier
  (`!returned`->FTR, `crossed`->OTR, `rebreaks`->CTR, else ETR).
- `BKNODERUNG-OFF` (`:3217+`) — the rung-count classifier.
- `BKPAT-OFF` (`:3400-3411`) — RBR/RBD/DBR/DBD, deleted by the user himself.

Gap worth raising with the user: the PDF sizes CTR/OTR by the RETURN magnitude,
the code sizes them by the BOX HEIGHT. `breakStep`/`retStep` are read (in ATR
steps) but "SIZE the numbers, decide nothing".

## Node-time read: the 16-bar box read «M1 base» (open question, no code changed)

User's screenshot: EURUSD M1, note `[BUY · EngSL M5 0.8 | 3.9 | 16 bars · M1 base · ETR]`.
He says 16 bars must be counted as the HIGHER time (his words: «با قانون 3 کندل
… میشه 12 کندل باید برای تایم بالا حساب بشه»).

Findings — the length condition he asked for EXISTS, but it measures the wrong span:

- `BaseKnotBaseTFRead` (`BaseKnotTool.mqh:2918`) walks the ladder top-down with
  TWO gates: `if(3 * rung > spanMin) continue;` (`:2934`, the length gate) and
  `BaseKnotRungHoldCount(...) >= BK_BASE_RUNG_MIN` (`:2935/2847`, the hold gate —
  the rung's OWN candle bodies must sit inside the band).
- `spanMin = (b2 - b1) / 60` (`:2926`) and the call site passes the BOX' two
  anchors (`:4703` `BaseKnotBaseTFMin(still, t1, t2, top, bot)`) — so the length
  gate measures the DRAWN BOX, not the base. P-BK-44 already ruled the other way
  for the count («ملاک که سقف و کف بیس هستش نه داخل باکس»).
- 16 M1 candles => spanMin = 15 => `3 * 5 = 15` passes by exactly zero margin.
  One candle narrower and M5 is refused outright.
- The note's number is `sp.life` (`:3977`, 16 bars); the class read gets
  `sp.still` (`:3977`), and the hover prints `still`. Two different numbers.

Diagnostic that tells the two gates apart — the box hover's Base line
(`:2999-3005`): "... stood still in the band" => the LENGTH gate refused the
higher rung; "no rung of the ladder stood still in it" => the HOLD gate did.

Still open: which gate blocked this box, and which rule should win. Options put
to the user — (A) length gate reads the base's own length, (B) the hold gate
stops vetoing, (C) length gate reads max(box span, base length).

## RESOLVED — P-BK-84 / P-BK-85 (same day, after the user's follow-up)

The user closed the open question above with three calls:

1. «اون سه کندل درجا زدن یکم قانونش خیلی سخت و خشک هستش اگر این سه کندل ملاک
   بشه عالی میشه ولی مولتی تایم که هر گره یک تایم بیشتر نداشته باشه در هر تایم
   فریمی که بودیم» — the three candles stay the criterion, but the stand-still
   veto is too rigid; one knot must have ONE time on every chart TF.
2. «کندل ها رو که از قبل داره شمارش میکنه دیگه فقط یک مقایسه میمونه» — the
   candles are already counted; only a comparison is left.
3. «باید اطمینان صدردصدی داشته باشیم از کندل ورود و خروج» — the entry and exit
   candles must be provably exact.

What was implemented (both in `Biotak/BaseKnotTool.mqh`):

- **P-BK-84 — the class' span is the base's OWN entry..exit ∪ the box' anchors.**
  New helper `BaseKnotClassSpan(sp, b1, b2, c1, c2)` (`:438`): the box is a
  FLOOR, never the measurement. Four call sites now compute the union:
  `:895` (hover, `cs1/cs2`), `:4072` (note, `ws1/ws2`), `:4223` (live sync,
  `ls1/ls2`), `:4801` (sync, `bs1/bs2`).
- **P-BK-84 — the LENGTH is the sole criterion.** `BaseKnotBaseTFRead` (`:2989`)
  now walks `for(i = n-1; i >= 0; i--) if(3 * rungs[i] <= spanMin) return rungs[i];`
  and reads NO series at all — the class' cost on the Sync path is now zero bars.
  The stand-still veto (`held >= BK_BASE_RUNG_MIN` / `unread`) is RETIRED IN PLACE
  as `BKHOLD-OFF` (`:3019-3039`), commented, never deleted. Restore = uncomment +
  re-teach `base-count-audit`'s RUNG gate.
- The count is still read and REPORTED, never a decider: the hover prints
  «; N of them stood still in the band» (`BaseKnotBaseLine`).
- **P-BK-85 — the exit candle is VERIFIED, not assumed.** New
  `BaseKnotBarCloseOut(shift, top, bot)` (`:2372`) plus one line in
  `BaseKnotBarCount`: `int end = (head > 1 ? head - 1 : head); if(!BaseKnotBarCloseOut(end, top, bot)) end = head;`
  So a candle that pokes out of the band and closes back inside is no longer
  named the exit.

Verification:
- `tools/base-count-audit.py` — gates clean, `--selftest` 140/140 faults caught.
  New gate: the union is COMPUTED at all four classing sites, not merely declared.
- All other audits green (panel-wiring, write-budget, gesture-budget, zorder,
  ui-text, chart-label, panel-colour, panel-mt4-sim --audit, submenu_geometry_check).
- Every `.mq4` compiles: `Result: 0 errors, 0 warnings`.

## Tooling trap — duplicate proxy variables break the compile

`compile-th3.ps1:691` calls `Start-Process`, which builds the child environment
from a case-INSENSITIVE dictionary. This machine's process env carries BOTH cases
of the same name — `HTTP_PROXY`/`http_proxy` and `HTTPS_PROXY`/`https_proxy`
(each `http://127.0.0.1:6590`) — so the compile dies with:

    Start-Process : Item has already been added.
    Key in dictionary: 'http_proxy'  Key being added: 'HTTP_PROXY'

`Set-Item env:... -Value $null` is NOT enough: the PowerShell `env:` provider is
case-insensitive and does not remove both native entries. The call that works,
in the SAME PowerShell invocation as the compile (shell state does not persist
between tool calls):

    foreach($n in 'HTTP_PROXY','HTTPS_PROXY','http_proxy','https_proxy',
                  'ALL_PROXY','all_proxy','NO_PROXY','no_proxy'){
      [System.Environment]::SetEnvironmentVariable($n, $null, 'Process')
    }
    $env:APPDATA = [Environment]::GetFolderPath('ApplicationData')
    & .\compile-th3.ps1 -Project all *> "$env:TEMP\_all.txt"

Also this session: the PowerShell tool returned NO stdout at all (every call
reported only "exit code 0"). Redirect to a file (`*>` writes UTF-16, read with
`iconv -f UTF-16LE`) and read that file with the Read/Bash tool instead.

`-Project all` compiles only the MAIN entry (workspace + installed). Compiling
every `.mq4` needs `-SourceFile <full path>` per file, per AGENTS.md.

## The audit's model must mirror the code — a lesson from P-BK-85

`tools/base-count-audit.py` keeps a Python model of the walk (`walk_span`) beside
the static gates. When a change lands in `BaseKnotBarCount`, the MODEL has to
move with it. P-BK-85 changed the exit from `end = head - 1` (assumed) to a
verified CLOSE, and the model kept the old line — so the model would have
certified a reading the code no longer produces. That is the failure mode to
watch: a stale model is worse than no model, because it passes.

What the model needed: a second series. `flags[i]` is the body being inside the
band; `closed[i]` is the CLOSE being outside it. Both are needed because the
body test and the close test disagree on exactly one candle — the one that poked
out and closed back inside — and that candle is the whole point of P-BK-85.
`walk_span(flags, right, closed=None)` defaults `closed` to `not flags`, which is
the retired assumption, so old scenes keep their old numbers.

The scene pair that pins it: `reclose` (shift 39 did NOT close outside → 18) and
`exitok` (the same tape, shift 39 DID → 19). One bit moved, two different
numbers, so neither can be reached by accident.

## Verified on the user's own chart — the «M1 base» box now reads M5

His new screenshot: `[SELL · node EngSL 1.1 | 4.8 | 17 bars · M5 base · ETR]`.

Measured from the PNG (`Pillow` in the managed venv at
`~/.workbuddy-ai/binaries/python/envs/default`):

- The drawn box is **113 px** wide (x 754..867) and 149 px tall (y 249..398).
- The M1 candle pitch on that chart is **8 px** (32 of 36 adjacent body gaps;
  and the body pattern is 5-on / 3-off). So the box is **14.1 minutes**.
- The base is **17 bars**.
- `3 × M5 = 15`: the box (14.1) does **not** reach it, the base (17) does.
  `3 × M15 = 45` is far out of reach. So **M5 is the highest rung that fits** —
  and the answer can only come from the base's own span, i.e. P-BK-84 working.

### Reading candle colours out of an MT4 screenshot

Exact RGB values in this template, needed if this has to be measured again:

| thing | colour |
|---|---|
| the drawn box' border | `(0,0,139)` darkblue |
| candle BODY, bear / bull | `(178,34,34)` firebrick / `(50,205,50)` limegreen |
| candle WICK, bear / bull | `(255,99,71)` tomato / `(0,100,0)` darkgreen |
| the level lines | `(255,0,0)` red / `(46,139,87)` seagreen |

**Trap:** `(255,0,0)` is BOTH the red level line and the ATR panel's own text
(`(27.6 - 32.2 - 36.8)`, `H4: 18.4`). Counting red pixels as candles invents
candles that are not there. Count **body** colours only.

### The chart's M1 series has gaps — bar count ≠ minutes

The body-colour scan shows holes: **x 734..752** (a ~3-bar hole, right at the
box' left edge — nothing but the level line crosses it) and **x 801..818**
(1 bar, inside the box). So this chart is missing M1 history.

That does not break the class: `BaseKnotBaseTFRead` measures **wall-clock
minutes** (`(b2 - b1) / 60`), and three M5 candles are 15 minutes of TIME
whatever the bar count is. But it does mean a base's BAR COUNT and its MINUTE
span can differ — a 17-bar base over a 3-bar hole spans 20 minutes. Worth
knowing when a reading looks one rung too high.

## P-BK-86 — the box-side note wears its own plate

User: «این اطلاعات میره داخل کندل ها خونده نمیشه اینو باید چیکارش کنیم که
هیچ تداخل نداشته باشه و همیشه بتونه خونده بشه».

**Cause.** The box-side note was a CHART-SPACE `OBJ_TEXT`. Two consequences,
one cause:

1. MT4 paints chart art in that same layer, so candles and level lines came out
   over the glyphs. Measured on his crop: in `_note1.png` the red level line
   (rows 189-191) is drawn across the note's rows (184-220), cutting "16 bars".
2. `OBJ_TEXT` cannot carry a background, so there was nothing to read the ink
   against. `Z_CHART_LABEL` (100) did not help: the custom price line rides the
   same rung, and at equal ZORDER the later-created object wins.

**Fix — one move, both answers.** The box-side home is now a SCREEN PAIR:
`OBJ_LABEL` for the ink (the panel layer is above every chart-space rung) plus
`OBJ_RECTANGLE_LABEL` as a plate behind it, filled with
`CHART_COLOR_BACKGROUND`. Invisible over empty chart, a cut-out where the
candles are. The corner row (rung 2) is untouched.

**New owners in `Biotak/BaseKnotTool.mqh`:**

| owner | job |
| --- | --- |
| `BK_NOTE_PLATE` `"_BG"` | the plate's name = the note's name + this |
| `BaseKnotInfoPlateName(infoName)` | the ONE spelling of that suffix |
| `BaseKnotInfoWipe(pfx)` | deletes the PAIR — used by all three delete sites |
| `BaseKnotNotePlateH()` | `PnlRawLineH(BKInfoFontPt()) + 2*PAD_Y` |
| `BaseKnotNotePlateTop(...)` | the two top-left corners from the projected corner |
| `BaseKnotNotePlatePx(...)` | the plate's box, width from `PnlRawTextW` |
| `BaseKnotNotePlateDraw(...)` | the plate itself, rung `Z_BOX_INFO` (60) |
| `BaseKnotNotePlateFollow(pfx,t2,top)` | re-projects, read-guarded |

**The follower is the price of going screen-space.** A chart-space object
follows the chart by itself; a screen one does not. It is called from the
child-move path (a drag) and from `BaseKnotPlaceBadges` (which the 500 ms pump
and `CHART_CHANGE` both reach), so scroll, zoom, resize and a new bar are all
covered. It writes only when the pixels moved — four terminal reads otherwise.

**Upgrade path.** `ObjectCreate` never re-types an object it finds, so a
pre-plate build's `OBJ_TEXT` note is purged by one `OBJPROP_TYPE` read.

**Two properties worth keeping:**

- the plate's fill and the note's ink both come off `CHART_COLOR_BACKGROUND`
  (the ink through `BaseKnotFgForBg()`), so they cannot disagree about which
  way round they are;
- the geometry has ONE owner (`BaseKnotNotePlateTop`), so the writer and the
  follower cannot place the plate and the ink by two spellings of one sum.

**Gates.** `tools/base-count-audit.py` grew `check_note_plate` (13 rows) and
8 mutants; two older gates were re-taught: the rung gate in `check_note_life`
(the plate's rung now lives in its own drawer) and the "only the last lines
differ" gate in `check_note_home` (both homes are screen objects now, so
`OBJPROP_PRICE`/`OBJPROP_TIME` must NOT appear in the writer).

**Result.** 11/11 `.mq4` compile `0 errors, 0 warnings`; all 11 audits green
(base-count `151/151`).

## P-BK-86b — the plate was measured against a guess (the advance table)

**Found while verifying the plate.** The plate's width is
`PnlRawTextW(text, BKInfoFontPt())`, and `PnlAdvUnits` (the ONE owner of "the
width Arial Bold draws this character at") had no entry for `[`, `]` or `|` —
all three of the note's own punctuation. They fell through to its
"unknown -> a mid-weight capital" value of 611, so the plate came out
**+1273 units = +1.27 em too wide** (+14 px at 8pt/96dpi, +20 px at 8pt/144dpi):
an empty tail on the one surface whose whole point is that its box fits its ink.

**Measured, not guessed.** `tools/font-adv-check.py` (new) parses
`C:\Windows\Fonts\arialbd.ttf` itself — `head`/`hhea`/`hmtx` plus the format-4
`cmap` — and reads the real advances: `[` `]` = 333, `|` = 280. Both are now in
the table (`Biotak/UtilityFunctions.mqh`). After the fix the plate is
**+55 units (+1 px at 8pt/96dpi) wider** than the ink — the safe direction.

**The rest of the table is sound.** Only 2 of its 88 entries differ from the
font: `'` (278 vs 238) and `Y` (722 vs 667). Left alone — both lean wide, and
`Y` is in the note's own `BUY`.

**Nothing else measured those three.** The panels split their option lists on
`|` before measuring a single option (`PnlDdOptText`), so the change closes a
gap instead of moving a layout. Confirmed: `panel-mt4-sim --audit` still ends
`overlap gate: clean`.

**A gate, and the trap in it.** `check_note_advances` (2 rows) walks the
literals the writer passes to `OBJPROP_TEXT` and requires each to have a
measured advance. Its first version compared `set("".join(lits))` — characters
— against a set of code points, so the subtraction removed nothing and every
literal read "unmeasured": a permanent red no source change could ever fix.
Fixed to `ord(c) not in known`. 3 mutants; the first drops the middle dot (183)
precisely so the row-1 machinery has to fire, not row 2's value check.

**Result.** 11/11 `.mq4` compile `0 errors, 0 warnings`; base-count selftest
`154/154`, plain run clean; the rest of the battery green
(panel-wiring 139/139, probe-budget 258, write-budget 56, gesture-budget 18/18,
zorder 10/10, ui-text 11/11, chart-label 31/31, panel-colour 19/19,
panel-mt4-sim clean, submenu 1440 cases PASS).

## P-BK-87 — the note is kept inside the window

**The hole P-BK-86 opened.** Going screen-space traded one collision for another. A
chart-space object is clipped by the chart; a SCREEN object is not — it is projected
to whatever pixel the box' corner lands on, and off-window pixels do not paint. So a
box scrolled to the top edge (or to the right one) took the note with it. The user's
own words for the whole line of work are «همیشه بتونه خونده بشه» — a note that
vanishes at the window edge is not readable.

**Where it is fixed.** ONE place, and it is already the owner of the pair's position:
`BaseKnotNotePlateTop` now takes `pw` and fits the pair into the window — top and left
`BK_NOTE_EDGE_MARGIN` (4), right `cw - MathMax(8, inpLabelsMarginLeft)` (the label
family's own boundary, the one its countdown tag already uses; past it sits the price
scale), bottom `ch - BK_NOTE_BOTTOM_SAFE` (20, for the date-scale bar).

**Why the bottom reserve is spelled here rather than reused.** `PNL_BOTTOM_SAFE` (30)
lives in `BiotakPanels.mqh` — the UI half, which the Lite entry does not compile. A
domain-half module that read it would break Lite (P-BUILD-01). Lite compiles green, so
the copy is the honest choice.

**A second hole, found while fixing the first.** The follower only ever MOVED the
plate; it never resized it. The note's own text is not constant (the bar count, the
risk, the type), so a plate that only moves is sooner or later too narrow for the ink
it carries — the one thing a plate exists to prevent. `BaseKnotNotePlateW` is now the
width's ONE owner, and the follower measures with it and writes XSIZE/YSIZE. Steady
state went from four terminal reads to six; still reads only.

**The gate was re-taught, not loosened.** `check_note_home` asserted this module reads
NO `inpLabelsMargin*` at all — its point was that the corner slot is PUSHED IN from
above, never guessed here. The fit is not the column, so the row now strips the fit's
own body before searching: the module still never places the corner row with a margin.

**Gates.** `check_note_plate` grew 5 rows (the fit, its right boundary, its bottom
reserve, the width's one owner, the follower's size step) and 5 mutants. One of those
mutants earned its keep immediately: the first version of the size row only looked for
the property NAME, and the follower's read-guard also mentions XSIZE — so the mutant
walked straight through it. The row now requires the actual writes.

**Result.** 11/11 `.mq4` compile `0 errors, 0 warnings` (Lite included — that IS the
P-BUILD-01 gate); base-count selftest `159/159`, plain run clean; panel-wiring 139/139,
write-budget 56, gesture-budget 18/18, zorder 10/10, ui-text 11/11, chart-label 31/31,
panel-colour 19/19, panel-mt4-sim clean, submenu 1440 cases PASS.

## Which build the terminal is actually running

Checked with the method the skill describes: the terminal's own log
(`…\Terminal\A1660DA4CB596E740BE3B3233E577E1B\logs\20260918.log`) against the `.ex4` mtime.

| fact | value |
| --- | --- |
| terminal started | 06:12:59 |
| the ONLY reload that day (all four indicators) | **08:00:13** |
| `BaseKnotTool.mqh` last edit | 08:50:01 |
| `Biotak Trigger TH3.ex4` written | 08:51:35 (Lite 08:51:32) |

So the build on the chart does NOT contain P-BK-86b or P-BK-87 — the user has to remove
and re-add. And the reload at 08:00:13 was HIS, not ours: an external
`metaeditor.exe /compile` does not make MT4 reload (08:36 and 08:51 wrote the `.ex4` and
produced no reload line). That is now written into the build skill, because it is the
difference between "the fix did not work" and "you tested the previous build".

## Performance verdict — is there anything left that blocks closing development?

User: «پروژه از نظر پرفورمنس ایرادی داره یا نه که دیگه برنامه نویسی قطع بشه دولوپش».
**Answer: no. No code was changed for this question** — it is a findings record.

Method: the terminal's own Experts logs, all six
(`…\MQL4\Logs\20260913..20260918.log`, 74 546 lines), scanned by
`%TEMP%\perf_verdict.py` (a throwaway; never inline regex in Bash).

| day | lines | `[W][PERF]` | overrun lines |
| --- | --- | --- | --- |
| 09-13 | 9 596 | 727 | 45 |
| 09-14 | 13 139 | 199 | 58 |
| 09-15 | 17 040 | 61 | **1** |
| 09-16 | 27 996 | 115 | 4 |
| 09-17 | 6 033 | 85 | 3 |
| 09-18 | 742 | 44 | 2 |

**The hot path is clean.** `OnCalculate` CPU warnings (`CPU_WARNING_MS` 50,
`CPU_CRITICAL_MS` 200): **0** on 09-15..09-18. The only two in the whole week are on
09-14 12:16 (94 ms, 63 ms) and both carry an all-zero internal breakdown
(`base=0 atr=0 hist=0 levels=0 labels=0 overlay=0`) — the cost was the terminal's, not
the indicator's.

**Every surviving overrun shape is attach-time warm-up.** `OnDeinit reason=4` (TF
switch, 276 ms avg, 78 samples) fires only 09-13/14/15. `OnInit` 156/235 ms vs a 150 ms
budget: 2 samples, both 09-17. `OBJ_CLICK 531 ms`: one sample, 09-13. `MOUSE_MOVE
78/79 ms`: 09-14 only. And every `CHART_CHANGE` overrun in the week sits within the first
~55 lines of its log — 09-18 L13/L54, 09-17 L13, 09-16 immediately after `initialized`.
It is the first chart-change after attach.

**The one live residual, and why it is smaller than it reads.** On 09-18:
`chart event CHART_CHANGE(id=9) [indicator=47..62 ui=0 settle=0 panels=0] took 47-62ms
(budget 40ms)`. The breakdown puts the 47 ms in the label relayout's ATR leg
(`labels relayout: clear=0 atr=47 th=0 trade=0 tail=0`, `count=0 paint=0` — no label was
even built). And `GetTickCount()` steps in **15.625 ms**, so 47 = 3 ticks and 62 = 4 —
the real value is in `(31.25, 46.875]`, i.e. **at most one tick above the 40 ms budget**.

**The coop pump is not doing heavy work.** Three of its four jobs report `ran=0ms` in
*every* sampled instance all week (`obj-cleanup`, `label-expiry`, `status-text`). Only
`heavy-frame` ever worked: its `ran` daily max fell 79 → 47 → 16 → 47 → **absent** →
**absent**. On 09-17 and 09-18 no job crossed `COOP_WARN_MS` at all.

### The `waited=` column is a log defect — root-caused, zero user impact

283 of 892 coop samples print a **negative** `waited`. A monotone clock cannot produce
one. Mechanism, now proven: the pump's owed-by-definition branch
(`EventHandlers.mqh:4585-4590`) stamps `s_coopOwedMs[HF]` itself but does **not** set
`s_coopOwed[HF]` — so the frame body, running later in the same iteration, re-asks
through `ScheduleHeavyFrame` (`:1837`) and **rewrites the stamp mid-run**. Line `:4620`
then computes `t0 - stamp` against a timestamp from the future and underflows the `uint`.

Proof: in **all 283** negative samples `-waited <= ran` holds (0 violations), and all 283
are `heavy-frame`. i.e. `waited` = minus the elapsed part of the run. The pattern
`waited=-15 ran=15` is the re-ask landing at the very end of the frame.

Read `ran=` (work done), never `waited=` (queue latency, and broken). It is silent on
the newest build only because the line no longer fires there — **the bug is unfixed**.

### Tooling note

The **PowerShell tool silently cannot launch python on this machine** — the exe exists,
`--version` returns nothing, `$LASTEXITCODE` is empty, and no output file appears. The
**Bash tool runs it fine**. So python probes go through Bash here; the console-window
rule (memory) is about the `rm`/`rmdir`/`unlink` shims, not about python.

## Deploy round closed — committed and pushed

User: «کامیت و پوش کن که دیپلوی دیگه فعلا تمام کنیم».

| fact | value |
| --- | --- |
| commit 1 | `a2a5c22` `chore(memory): log the performance verdict and the coop waited= defect` |
| commit 2 | `743c20d` `chore(memory): record the deploy round and the two push traps` |
| pushed | `e2181b5..a2a5c22`, then `a2a5c22..743c20d` |
| GitHub `refs/heads/main` (ls-remote, authoritative) | `743c20d` |
| working tree | clean — memory files only, no source touched |

**Two traps worth remembering (both now in the build skill):**

1. **`git push` hung and died by `SIGTERM` with no output.** Cause:
   `credential.helper = helper-selector` (PortableGit's system gitconfig) — a shim that
   opens a **GUI** credential dialog. Network was fine; `ls-remote` worked. The
   credential was already in the Windows store
   (`LegacyGeneric:target=git:https://github.com`), so the push works with the chain
   reset and `wincred` substituted:
   `git -c credential.helper= -c credential.helper=wincred push origin main`
   (the empty value FIRST resets the multi-valued list; a bare
   `-c credential.helper=wincred` just appends to the hanging selector).
2. **The local `origin/main` is stuck at `afe24a7`** (an Aug-21 boundary commit) while
   GitHub is current, so `git status` keeps reporting `[ahead 197]`. The repo is a
   **shallow clone** and the tracking ref lives only in `.git/packed-refs`. It is NOT
   fixable from this session: `git update-ref refs/remotes/origin/main <sha>` exits 0
   and changes nothing (no loose ref is created at all), and the Write tool's copy of
   that file lands but is reverted again by the sandbox within a command or two. Both
   `git fetch` and `git push` report updating it and it reverts too.
   **Leave it.** The user can clear it from their own terminal with
   `git fetch --prune`, where the ref write persists.

Rule for next time: confirm a push with `git ls-remote origin refs/heads/main`, never
with local refs. The local tracking ref is not trustworthy in this sandbox.
