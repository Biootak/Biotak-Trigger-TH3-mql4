# فرمول تریدپلن (بلوک سمت راست TRex) — قدم‌به‌قدم به فارسی

> تک‌منبع کد: `Biotak/TradePlanFormulas.mqh` (تابع `TradePlanCompute` + پوشش
> `TradePlanComputeLive` برای فریز پایه‌ها).
> نمایش در `Biotak/LabelFunctions.mqh` فقط رندر می‌کند، هیچ ضریبی را از نو حساب نمی‌کند.
> **مدل نهایی تأییدشده (Sep-9-2026 لایو):** `SL(TF) = 1.2 × Eng(StructureTF)` — یک
> ضریب ثابت برای همه ۸ تایم‌فریم، بدون جدول per-TF.
>
> **Eng (به‌روزرسانی Sep-10-2026):** `Eng(TF) = TR کامپوزیت همان تایم‌فریم ÷ 4.266666`
> (نردبان استاد را بازتولید می‌کند — R-ENGPARITY). این ضریب **در کد ثابت است**
> (`TRADEPLAN_ENG_DIVISOR` در `TradePlanFormulas.mqh`) و ورودی ندارد —
> فرمول قدیمی `iATR(Trigger)` حذف شده (R-ENGONE، تصمیم کاربر 2026-09-10).

---

## ۱. ورودی‌ها: فقط ATR و pip size (برای همین روی همه جفت‌ارزها درست است)

فرمول **هیچ عدد ثابتی مخصوص طلا ندارد**. تنها ورودی‌ها:

1. **ATR میانگین (کامپوزیت) هر تایم‌فریم** — همان موتوری که نوارها را تغذیه می‌کند
   (`CalculateWeightedATR`؛ پریودها ۵/۱۰/۲۱/۶۶/۱۳۲/۲۶۴ با وزن‌های ۱/۱/۲/۳/۵/۸،
   shift=1 کندل بسته، مخرج پویا برای W1/MN بدون هیستوری کافی).
2. **pip size نماد** (`GetCachedPipSize`): طلا ۰.۱، EURUSD ‏۰.۰۰۰۱، جفت‌های JPY ‏۰.۰۱ و… — خودکار تشخیص داده می‌شود

---

## ۲. نردبان تایم‌فریم‌ها و ترتیب فرکتالی

```
M1 ←→ M5 ←→ M15 ←→ H1 ←→ H4 ←→ D1 ←→ W1 ←→ MN
```

هر تایم‌فریم دو مفهوم فرکتالی دارد:
- **تریگر (Trigger)**: دو پله **پایین‌تر** در نردبان (کف M1)
- **ساختار (Structure)**: دو پله **بالاتر** در نردبان (سقف MN)

| چارت | تریگر | ساختار |
|------|-------|--------|
| M1   | M1    | M15    |
| M5   | M1    | H1     |
| M15  | M1    | H4     |
| H1   | M5    | D1     |
| H4   | M15   | W1     |
| D1   | H1    | MN     |
| W1   | H4    | MN     |
| MN   | H1    | MN     |

---

## ۳. قدم اول — Eng: فرمول پایدار Session ATR (کشف Sep-9-2026)

```
Eng(TF) = iATR(triggerTF,  TF_min / triggerTF_min,  1)  /  pip
```

یک `iATR` تک‌call با پریود برابر **تعداد کندل‌های triggerTF که یک bar از TF جاری می‌سازند**.
`shift=1` یعنی تا کندل بعدی triggerTF بسته نشود تغییر نمی‌کند → پایدار و ضد-نویز.

| TF | triggerTF | period | فرمول |
|----|-----------|--------|-------|
| M1 | M1 | 1/1=1 | `iATR(M1,1,1)/pip` |
| M5 | M1 | 5/1=5 | `iATR(M1,5,1)/pip` |
| M15 | M1 | 15/1=15 | `iATR(M1,15,1)/pip` |
| H1 | M5 | 60/5=12 | `iATR(M5,12,1)/pip` |
| H4 | M15 | 240/15=16 | `iATR(M15,16,1)/pip` |
| D1 | H1 | 1440/60=24 | `iATR(H1,24,1)/pip` |
| W1 | H4 | 10080/240=42 | `iATR(H4,42,1)/pip` |
| MN | D1 | 43200/1440=30 | `iATR(D1,30,1)/pip` |

تأیید Sep-4 XAUUSD: M1=3✓ M5=9✓ M15=17✓ H1=40✓ H4=88✓ D1=252✓ W1=600✓ MN=983✓

---

## ۴. قدم دوم — SL: از Eng تایم ساختار (تأییدشده لایو Sep-9-2026)

```
SL(TF) = round(1.20 × Eng(StructureOf(TF)))
```

**یک ضریب ثابت ۱.۲ برای همه ۸ تایم‌فریم — تأیید لایو Sep-9-2026 XAUUSD:**

| تایم | ساختار | Eng(ساختار) | SL=1.2×Eng | استاد | ما |
|------|--------|------------|------------|-------|-----|
| M1  | M15    | 17→14      | 1.2×17=20.4→**20** | 20 ✓ | ~22 |
| M5  | H1     | 40→33      | 1.2×40=48→**48** | 48 ✓ | 47 |
| M15 | H4     | 88→65      | 1.2×88=105.6→**105** | 104 | 104 |
| H1  | D1     | 252→169    | 1.2×252=302.4→**303** | 310 | 310 |
| H4  | W1     | 600→362    | 1.2×600=720→**720** | 706 | 706 |
| D1  | MN     | 983→1044   | 1.2×983=1179.6→**1180** | 1219 | 1219 |
| W1/MN | MN  | —          | macro | 1180 | 1804/2526 |

> توجه: Eng ما (SessionEng) گاهی از Eng استاد متفاوت است — هر دو از `iATR(trigTF,N,1)/pip`
> می‌آیند اما در لحظات مختلف ATR فرق دارد. فرق ≤10٪ در تایم‌های M15-H4 از بازار لایو است.

---

## ۵. قدم سوم — تارگت‌ها از SL گردنشده (ترتیب مهم!)

```
TP1 = round(SL × 7/3)     TP2 = round(SL × 5)     TP3 = round(SL × 31/3)
```

**اول روی SL گردنشده، بعد رُند.** اگر SL رُندشده را ضرب کنی TP3 دو واحد خطا می‌دهد
(`303×31/3=3131` در برابر عدد واقعی `3129`).

نمایش: `#SL:-S #TP1+T1 #TP2+T2 #TP3+T3` (آبی، پایین-راست).

| نسبت | مقدار ثابت | XAUUSD/H1 | EURUSD/D1 |
|---|---|---|---|
| TP1/SL | ۷/۳ ≈ ۲.۳۳ | 707/303 = ۲.۳۳ | 240/103 = ۲.۳۳ |
| TP2/SL | ۵ دقیق | 1514/303 ≈ ۵ | 514/103 ≈ ۵ |
| TP3/SL | ۳۱/۳ ≈ ۱۰.۳۳ | 3129/303 ≈ ۱۰.۳۳ | 1062/103 ≈ ۱۰.۳۱ |

---

## ۶. قدم چهارم — Hunter: هشت‌سوم Eng

```
Hunter = round(8 × Eng / 3)
```

نمایش: `Hunter SL: H Eng.SL: E` (قرمز، بالا-راست).

---

## ۷. قدم پنجم — StrBond: کف و پهنای باند از SL گردنشده

```
Base  = SL × 95/9        Width = SL × 20/9
```

نمایش گروهی `Str Bond: A - B` (آبی):

* **M1 تا D1:** `Width -- Base` (مثال واقعی: `673 -- 3196`)
* **W1:** `SL×16/3 -- Base` (ردیف `6291 -- 12451`)
* **MN:** `(Base+Width) -- Base`، که پایه اول **جمع پایه‌های رُندشده** است
  (`12451 + 2621 = 15072` — نه رُندِ جمعِ گردنشده‌ها که `15073` می‌دهد!)

نسبت دو پا در گروه M1-D1 همیشه `۴/۱۹ ≈ ۰.۲۱۰۵` است.

---

## ۸. قضیه قطری (Diagonal Theorem) — نه یک رسیپه

**SB1(TF) == Hunter(StructureTF)** و **SL(TF) == 1.2 × Eng(StructureTF)** دو رابطه‌ای هستند که **از کد اثبات می‌شوند** — نه ورودی مستقل.

اثبات: `SB1 = round(SL×20/9) = round(1.2×Eng(Str)×20/9)` و `Hunter(Str) = round(8/3 × Eng(Str))`. چون `8/3 × 20/9 = 8×20/(3×9) = 160/27 ≠ 20/9`... (آن‌ها از مسیرهای مختلف به همان عدد می‌رسند به‌خاطر `1.2×20/9 = 8/3`). مشاهده: `H1 SB1 673 == D1 Hunter 673` ✓

---

## ۹. Close in: شمارش تا بستن کندل جاری — تگ روی قیمت زنده

```
19m32s        (تگ جمع‌وجور، روی قیمت بید/اسک)
Close in : 22d 12h 32m 13s   (فرم بلند — فقط رفرنس متن)
```

از ساعت شروع کندل جاری + طول تایم‌فریم منهای ساعت اکنون؛ واحدهای صفرِ جلویی
حذف می‌شوند (H1 می‌شود `17m51s`).

**جای نمایش (۲۰۲۶-۰۹-۱۱، درخواست کاربر): «کنار خودِ کندل لایو، هم‌سطح قیمت
بید/اسک، با یک کم فاصله، جمع‌وجور، و هم‌راه با قیمت.»**

بهترین راه در MQL4 این است — هر دو محور به‌صورت پیکسلی و زنده محاسبه می‌شوند
(`CreateLivePriceCountdown`):

* **X:** لبهٔ راست کندل ۰ به‌علاوهٔ ۶ پیکسل (`gap`)، و عرض کندل از فاصلهٔ
  پیکسلی کندل ۱ گرفته می‌شود تا با زوم مقیاس‌پذیر بماند؛ پس تگ همیشه با همان
  «یک کم فاصله» به کندل چسبیده است و روی بدنهٔ آن نمی‌افتد.
* **Y:** `ChartTimePriceToXY(0, 0, Time[0], price)` با `price` = میانهٔ BID/ASK،
  منهای نیم‌خط — یعنی متن روی همان سطحی می‌نشیند که خطوط بید/اسک ترمینال
  رسم می‌شوند و با هر تیک با قیمت بالا/ پایین می‌رود.
* اگر لبهٔ راست به مارجین لیبل‌ها برسد (`cw - inpLabelsMarginLeft` — ابتدای
  مقیاس قیمت)، تگ به **سمت چپ کندل** فلیپ می‌کند تا زیر مقیاس نرود.
* اگر چارت از کندل ۰ اسکرول شده باشد یا قیمت بیرون محدودهٔ دید باشد، تگ پارک
  می‌شود (`OBJ_NO_PERIODS`) تا یک لِوِل ساختگی رسم نکند.

رفرش: ۱ هرتز از `TradePlanLiveTick` (بالاتر از گیت ۲ ثانیه‌ای بلاک) + هوک
`CHARTEVENT_CHART_CHANGE` برای اسکرول/زوم (مسیر خودِ لیبل‌ها ۲ ثانیه گیت است،
پس بدون این هوک تگ تا ۲ ثانیه جا می‌ماند).
واحد تریدپلن دیگر فقط سطر آبی `#SL/#TP1/#TP2/#TP3` را دارد.

### ۹.۱ لایهٔ مستقل + شخصی‌سازی (۲۰۲۶-۰۹-۱۱)

* **خاموش/روشن جدا:** کلید `D` (یا `inpCountdownKey`) و ردیف ۰ کارت
  `ATR LABELS`؛ خاموش کردن ATR (کلید `A`) دیگر شمارنده را نمی‌برد
  (`OV_CD`).
* **شخصی‌سازی:** ردیف‌های ۱..۳ همان کارت — `COUNT COLOR` (پالت،
  `OV_CDC`)، `COUNT SIZE` (`OV_CDS`، صفر = اندازهٔ بقیهٔ لیبل‌ها)،
  `COUNT GAP` (`OV_CDG`، پیکسل فاصله از کندل). این ردیف‌ها بالای کارت
  همیشه ثابت‌اند.
* **باز کردن پنل با کلیک:** کلیک چپ روی خودِ تگ، کارت `ATR LABELS` را باز
  می‌کند (`LiveCountdownPointInside` — مستطیل آخرین رسم کش‌شده؛ آبجکت
  عمداً `SELECTABLE=false` است تا با درگ جابه‌جا نشود).
* **تله:** نام آبجکت عمداً `ATR_` ندارد (`CloseIn_Tag`) چون janitor دوره‌ای
  هر آبجکت با نام `ATR_` را وقتی بلوک ATR خاموش است مخفی می‌کند و
  شمارنده باید زنده بماند. نام قدیمی `ATR_Trade_Current_CloseIn` فقط
  purge می‌شود.
* **تست قفل‌کننده:** `Biotak_Countdown_Test.mq4` (اسکریپت، خطوط `[CDTEST]`)
  همین قراردادها را ثابت می‌کند: نام بدون `ATR_`، ماندن با خاموش بودن ATR
  (سوییچ + `SetATRLabelsVisibility`)، خاموش کردن سوییچ خودش ⇒ حذف آبجکت و
  باطل شدن مستطیل کلیک، ناحیهٔ کلیک = دقیقاً همان مستطیل رسم‌شده (اسکن
  شبکه‌ای)، و پارک شدن بیرون دید ⇒ هیچ هدف کلیکی روی چارت نماند. اسکریپت
  چارت را موقتاً از کندل زنده دور می‌کند و آخر خودش برمی‌گرداند.

---

## ۱۰. فریز پایه per-bar + شواهد (چرا این مدل)

مشاهده سرنوشت‌ساز (XAUUSD/H1، دو شات ۲۷ دقیقه‌ای در یک کندل):
پایه‌ها (SL/TP/SB) کاملاً ثابت ماندند در حالی که Eng/Hunter با نوار زنده حرکت کردند.
پس پایه‌های کُند هر کندل چارت یک‌بار محاسبه می‌شوند و Eng/Hunter زنده‌اند —
کد همین را می‌کند (`TradePlanComputeLive`).

---

## ۱۱. نگاشت به کد

| فرمول | کد |
|---|---|
| ساختار (۲ پله بالا) | `TradePlanStructureMinutes` |
| تریگر (۲ پله پایین) | `TradePlanTriggerMinutes` |
| Eng = TR(own) ÷ 4.266666 (ثابت کد — R-ENGONE) | `TradePlanEngTrue` |
| SL = 1.2 × Eng(Structure) | `TradePlanSLTrue` / `TradePlanCompute` |
| SL/TP از SL گردنشده | `TradePlanCompute` |
| Hunter از Eng گردنشده | `TradePlanHunterFromEng` |
| StrBond گروهی | `TradePlanCompute` (sb1/sb2) |
| فریز پایه per-bar + Eng زنده | `TradePlanComputeLive` |
| نگهبان رُندینگ هر سیمبل | `TradePlanSelfCheck` |
| رندر + Close in | `CreateATRTradeLabel` / `DisplayTradePlanTopRows` / `TradePlanCloseInText` / `CreateLivePriceCountdown` / `RefreshLiveCountdown` در `LabelFunctions.mqh` |
