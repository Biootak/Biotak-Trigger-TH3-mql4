# فرمول تریدپلن (بلوک سمت راست TRex) — قدم‌به‌قدم به فارسی

> تک‌منبع کد: `Biotak/TradePlanFormulas.mqh` (تابع `TradePlanCompute`).
> نمایش در `Biotak/LabelFunctions.mqh` فقط رندر می‌کند، هیچ ضریبی را از نو حساب نمی‌کند.
> کشف‌شده از اسکرین‌شات‌های TRex استاد روی XAUUSD (هر ۸ تایم‌فریم) و تأییدشده روی EURUSD روزانه.

---

## ۱. ورودی‌ها: فقط ATR و pip size (برای همین روی همه جفت‌ارزها درست است)

فرمول **هیچ عدد ثابتی مخصوص طلا ندارد**. تنها ورودی‌ها:

1. **ATR** (دیتای زنده بازار، همان weighted-iATR خودمان با پریودهای ۵/۱۰/۲۱/۶۶/۱۳۲/۲۶۴)
2. **pip size نماد** (`GetCachedPipSize`): طلا ۰.۱، EURUSD ‏۰.۰۰۰۱، جفت‌های JPY ‏۰.۰۱ و… — خودکار تشخیص داده می‌شود

پس روی هر سیمبلی ATR همان سیمبل می‌آید و با pip همان سیمبل به پیپ تبدیل می‌شود.
قدرمطلق اعداد با نوسان بازار عوض می‌شود (طلا پایه روزانه ≈۱۹۶ پیپ، یورو ≈۱۷ پیپ)،
ولی **نسبت‌ها همیشه ثابت‌اند** — درستی فرمول یعنی همین نسبت‌ها.

---

## ۲. قدم اول — پایه پلن (B): ATR تایم‌فریم جاری با سقف روزانه

```
B = ATR پیپیِ min(تایم‌فریم چارت، روزانه)
```

یعنی چارت‌های ماهانه/هفتگی/روزانه هر سه **پلن روزانه** را نشان می‌دهند
(روی هر سه چارت XAUUSD دقیقاً یک ست دیده شد: `1180 / 2752 / 5898 / 12189`)؛
استاد بالای روزانه ترید نمی‌کرده. تایم‌های پایین‌تر پلن خودشان را دارند.

---

## ۳. قدم دوم — SL و TPها: پایه × (۶، ۱۴، ۳۰، ۶۲)

```
SL  = round(B × 6)     TP1 = round(B × 14)
TP2 = round(B × 30)    TP3 = round(B × 62)
```

نمایش: `#SL:-S #TP1+T1 #TP2+T2 #TP3+T3` (آبی، پایین-راست).

نسبت‌های ثابت (چک روی هر دو سیمبل):

| نسبت | مقدار ثابت | XAUUSD روزانه (B≈۱۹۶.۶) | EURUSD روزانه (B≈۱۷.۱۳) |
|---|---|---|---|
| TP1/SL | ۷/۳ ≈ ۲.۳۳ | 2752/1180 = ۲.۳۳ | 240/103 = ۲.۳۳ |
| TP2/SL | ۵ دقیق | 5898/1180 = ۵ | 514/103 = ۴.۹۹ |
| TP3/SL | ۳۱/۳ ≈ ۱۰.۳۳ | 12189/1180 = ۱۰.۳۳ | 1062/103 = ۱۰.۳۱ |

این همان خانواده سطر بالای چارت است: آنجا `TH × (۳، ۷، ۱۵)` و اینجا
`ATR × ۲ × (۳، ۷، ۱۵، ۳۱)` — جمله ۳۱ ادامه همان تصاعد (۳، ۷، ۱۵، ۳۱) است.

---

## ۴. قدم سوم — تایم‌فریم تریگر: دو پله پایین‌تر (کف M1)

```
تریگر = دو پله پایین‌تر در نردبان (M1 M5 M15 H1 H4 D1 W1 MN)، کف M1
```

یعنی تریگرِ روزانه = H1، تریگرِ H4 ‏= M15، تریگرِ H1 ‏= M5، تریگرِ M15 ‏= M1
(دقیقاً همان نقشه `GetTriggerDurationSeconds` خودمان).

---

## ۵. قدم چهارم — Eng: ضریب فرد × ATR تریگر

```
Eng = round(ضریب × ATR پیپی تریگر)        ضریب: M1→۱ ، M5→۳ ، بقیه→۵
```

چرا ۱/۳/۵؟ عمق نردبان زیر چارت: هرچه تایم پایین‌تر، پله کمتری زیرش هست
(M1 هیچی → ۱، M5 فقط M1 ‏→ ۳، بقیه → ۵). هر ۸ تایم XAUUSD با خطای زیر یک واحد خورد:
`5×50.9=254.5→254` ،`5×120=600` ،`5×196.6=983` ،`3×8.29≈11` ،`1×3.79≈4`.

---

## ۶. قدم پنجم — Hunter: هشت‌سومِ Engِ گردنشده (ترتیب مهم!)

```
Hunter = round(8 × Eng_گردنشده / 3)
```

**اول ×۸/۳، بعد رُند.** اگر Eng رُندشده را ضرب کنی H1 دو واحد خطا می‌دهد
(`8×41/3=109` در برابر عدد واقعی `111`؛ ولی `8×41.45/3=110.5→111` درست است).
نمایش: `Hunter SL: H Eng.SL: E` (قرمز، بالا-راست).

---

## ۷. قدم ششم — StrBond: دو پا از دو ATR

```
پایه دوم = round(190 × B / 3)        (= TP3 بعلاوه 4/3×B)
پایه اول = round(40 × ATR تایم‌چارت / 3)   (بدون سقف روزانه، بدون استثنا)
```

نمایش: `Str Bond: A - B` (آبی). نسبت دو پا همیشه `۴/۱۹ ≈ ۰.۲۱۰۵` است
(روی هر ۶ تایم M1 تا D1 در هر دو سیمبل).

درباره «Hunter دو پله بالاتر»: تصادفیِ ظاهری بود، نه قانون — چون
Hunterِ تایم بالاتر و پایه‌اولِ چارت هر دو مساوی `40/3 × ATR` همان تایم‌اند،
روی M1 تا D1 بایت‌به‌بایت یکی درمی‌آمدند. قانون واقعی همین `40/3` است که روی
تایم فراکتالی هم تعریف می‌شود (lookup نردبانی آنجا جا می‌ماند).

---

## ۸. Close in: شمارش تا بستن کندل جاری

```
Close in : 22d 12h 32m 13s
```

از ساعت شروع کندل جاری + طول تایم‌فریم منهای ساعت اکنون؛ واحدهای صفرِ جلویی
حذف می‌شوند (H1 می‌شود `17m 51s`). قرمز، بالای سطر SL/TP.

---

## ۹. اثبات چندطرفه (چرا مطمئنیم فرمول اصلی همین است)

هر عدد از دو-سه راه مستقل به دست می‌آید و همه به هم می‌رسند:

**حلقه بسته EURUSD** (زیباترین شاهد — فقط با پایه H1 ≈ ‏۲.۳۵ شروع کن):
- سطر پایین D1 ‏(`141/329/704`) تقسیم بر ۱۰ = پلن H1 → پایه H1 ‏≈ ۲.۳۵
- `Eng روزانه = 5 × 2.35 = 11.75 → 12` ← دقیقاً عدد روی چارت
- `Hunter = 8/3 × 11.75 = 31.33 → 31` ← دقیقاً عدد روی چارت
- `پایه‌اول باند = 40/3 × 17.13 = 228.4 → 228` ← دقیقاً عدد روی چارت
- `پایه‌دوم = 190/3 × 17.13 = 1084.9 → 1085` ← دقیقاً عدد روی چارت

**XAUUSD:** هر ۷ عدد ماهانه فقط با یک ATR روزانه ≈ ‏۱۹۶.۶ بازتولید می‌شوند
(پنجره مشترک غیرتهی: ‏۱۹۶.۵۸۹ تا ‏۱۹۶.۶۰۵)؛ سطر پایین هر تایم دقیقاً
۱۰ برابر پلن دو پله پایین‌تر است؛ سطر اول بالا تصاعد حسابی است
(`HIGH = 2×MID − TH` با خطای حداکثر یک واحد رُند روی هر ۸ تایم هر دو سیمبل).

---

## ۱۰. نویز زنده ±۱ طبیعی است

ATR زنده با هر تیک می‌لرزد (روی M1 در ۱۵ دقیقه `TP2: 114→113` و
`StrBond: 51-240 → 50-238` دیده شد ولی Hunter/Eng ثابت ماندند چون رُند،
لرزش کوچک را قورت داد). پس اختلاف یک واحدی بین دو لحظه، خطا نیست.

---

## ۱۱. چه چیزی مانده برای تأیید چشمی

- پایه‌اول StrBond روی چارت **هفتگی و ماهانه**: پیش‌بینی قانون یکدست همان
  `6291` و `15072` است (یعنی ATR هفتگی ≈ ‏۴۷۲ و ماهانه ≈ ‏۱۱۳۰). اگر همان آمد،
  پرونده بسته است؛ اگر نه، عدد واقعی را بده.
- نسخه‌های مختلف TRex روی اسکرین‌شات‌ها (۳.۲ تا ۳.۶) یعنی ممکن است `6291`
  (نسخه ۳.۲) باگ قدیمی باشد — قانون بدون‌استثنا ملاک است، نه آن یک عدد.

---

## ۱۲. نگاشت به کد

| فرمول | کد |
|---|---|
| پایه پلن + سقف روزانه | `TradePlanBaseMinutes` |
| SL/TP ×(۶،۱۴،۳۰،۶۲) | `TradePlanCompute` |
| تریگر + ضریب ۱/۳/۵ | `TradePlanTriggerBack` / `TradePlanEngTrue` |
| Hunter از Eng گردنشده | `TradePlanHunterFromEng` (ورودی `engTrue`) |
| StrBond (۱۹۰/۳ و ۴۰/۳) | `TradePlanCompute` (`sb2` از `basePips`، `sb1` از `ownPips`) |
| نگهبان رُندینگ هر سیمبل | `TradePlanSelfCheck` |
| رندر راست‌چین + Close in | `CreateATRTradeLabel` / `DisplayTradePlanTopRows` / `TradePlanCloseInText` در `LabelFunctions.mqh` |
