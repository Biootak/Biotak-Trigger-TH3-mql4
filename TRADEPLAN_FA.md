# فرمول تریدپلن (بلوک سمت راست TRex) — قدم‌به‌قدم به فارسی

> تک‌منبع کد: `Biotak/TradePlanFormulas.mqh` (تابع `TradePlanCompute` + پوشش
> `TradePlanComputeLive` برای فریز پایه‌ها).
> نمایش در `Biotak/LabelFunctions.mqh` فقط رندر می‌کند، هیچ ضریبی را از نو حساب نمی‌کند.
> مدل: دستورالعمل مهندسی‌شده ۲۰۲۶-۰۹-۰۹ (جدول مولت per-TF + ماکرو روزانه)؛
> زنجیره D1 (‏۱۰۶۶←۱۱۸۰←۶۲۹۱/۱۲۴۵۱/۱۵۰۷۲) و نسبت‌های هر دو بلاک واقعی تأییدشده‌اند،
> مولت H1 و پایه‌های W1/MN در انتظار تأیید فوروارد هستند (بخش ۱۱).

---

## ۱. ورودی‌ها: فقط ATR و pip size (برای همین روی همه جفت‌ارزها درست است)

فرمول **هیچ عدد ثابتی مخصوص طلا ندارد**. تنها ورودی‌ها:

1. **ATR میانگین (کامپوزیت) خودمان** — همان موتوری که نوارها را تغذیه می‌کند
   (`GetATRForTimeframe`؛ پریودها/وزن‌ها در `TH_ATR_FA.md` بخش ۴). پایه‌ای که
   هیستوری کافی ندارد از میانگین حذف می‌شود (مخرج پویا — مهم برای W1/MN).
2. **pip size نماد** (`GetCachedPipSize`): طلا ۰.۱، EURUSD ‏۰.۰۰۰۱، جفت‌های JPY ‏۰.۰۱ و… — خودکار تشخیص داده می‌شود

پس روی هر سیمبلی ATR همان سیمبل می‌آید و با pip همان سیمبل به پیپ تبدیل می‌شود.
قدرمطلق اعداد با نوسان بازار عوض می‌شود، ولی **نسبت‌ها همیشه ثابت‌اند** —
درستی فرمول یعنی همین نسبت‌ها.

---

## ۲. قدم اول — استاپ پایه (SL) با جدول مولت per-TF + سقف ماکرو

```
M1:  SL = 1.50 × ATR(M1)      M5:  SL = 1.20 × ATR(M5)
M15: SL = 1.35 × ATR(M15)     H1:  SL = 1.75 × ATR(H1)   (= گام Control)
H4:  SL = 2.00 × ATR(H4)      (= گام LS)
D1/W1/MN: SL = 1.107 × ATR(D1)   (سقف ماکرو روزانه)
```

زنجیره تأییدشده ماکرو (XAUUSD): `1.107 × 1066 = 1180.06 ← SL=1180`.
⚠️ مولت H1 در انتظار تأیید فوروارد است: `1.75 × 176 = 308` در برابر `303` دیده‌شده؛
توضیح محتمل فریز پایه per-bar است (بخش ۹) — اگر رول کندل بعدی خلافش را ثابت کرد،
این مولت بازبینی می‌شود. M30 و تایم‌های فراکتال به M15 فلور می‌شوند.

---

## ۳. قدم دوم — تارگت‌ها از SL گردنشده (ترتیب مهم!)

```
TP1 = round(SL × 7/3)     TP2 = round(SL × 5)     TP3 = round(SL × 31/3)
```

**اول روی SL گردنشده، بعد رُند.** اگر SL رُندشده را ضرب کنی TP3 دو واحد خطا می‌دهد
(`303×31/3=3131` در برابر عدد واقعی `3129`).

نمایش: `#SL:-S #TP1+T1 #TP2+T2 #TP3+T3` (آبی، پایین-راست).

نسبت‌های ثابت (چک روی هر دو سیمبل):

| نسبت | مقدار ثابت | XAUUSD/H1 | EURUSD/D1 |
|---|---|---|---|
| TP1/SL | ۷/۳ ≈ ۲.۳۳ | 707/303 = ۲.۳۳ | 240/103 = ۲.۳۳ |
| TP2/SL | ۵ دقیق | 1514/303 ≈ ۵ | 514/103 ≈ ۵ |
| TP3/SL | ۳۱/۳ ≈ ۱۰.۳۳ | 3129/303 ≈ ۱۰.۳۳ | 1062/103 ≈ ۱۰.۳۱ |

---

## ۴. قدم سوم — تایم‌فریم تریگر: دو پله پایین‌تر (کف M1)

```
تریگر = دو پله پایین‌تر در نردبان (M1 M5 M15 H1 H4 D1 W1 MN)، کف M1
```

یعنی تریگرِ روزانه = H1، تریگرِ H4 ‏= M15، تریگرِ H1 ‏= M5، تریگرِ M15 ‏= M1
(دقیقاً همان نقشه `GetTriggerDurationSeconds` خودمان).

---

## ۵. قدم چهارم — Eng از SL تریگر: `SL(تریگر)/1.2`

```
Eng = round(SL_تریگر / 1.2)     تریگر = دو پله پایین‌تر، کف M1
```

چون SL تریگر خودش `۱.۲ × ATR` است (پایه M5)، این ۱.۲ خنثی می‌شود و عملاً
`Eng = ATR نوارِ تریگر` است. سند: روی XAUUSD/H1 استاد `Eng=43` دقیقاً مساوی
ATR نوار M5 یعنی `43` است. قانون ضریب فرد (۱/۳/۵) بازنشسته شد.
(`TradePlanEngTrue` — همان ATR نوار که تریگر را تغذیه می‌کند.)

---

## ۶. قدم پنجم — Hunter: هشت‌سومِ Engِ گردنشده (ترتیب مهم!)

```
Hunter = round(8 × Eng_گردنشده / 3)
```

**اول ×۸/۳، بعد رُند.** اگر Eng رُندشده را ضرب کنی خطا می‌دهد
(`8×43/3=114.67→115` در برابر عدد واقعی `114`؛ ولی با Eng گردنشده ≈۴۲.۶
می‌شود `113.6→114` که درست است).
نمایش: `Hunter SL: H Eng.SL: E` (قرمز، بالا-راست).

---

## ۷. قدم ششم — StrBond: کف و پهنای باند از SL گردنشده

```
Base  = SL × 95/9        Width = SL × 20/9
```

نمایش گروهی `Str Bond: A - B` (آبی):

* **M1 تا D1:** `Width -- Base` (مثال واقعی: `673 -- 3196`)
* **W1:** `SL×16/3 -- Base` (ردیف `6291 -- 12451`)
* **MN:** `(Base+Width) -- Base`، که پایه اول **جمع پایه‌های رُندشده** است
  (`12451 + 2621 = 15072` — نه رُندِ جمعِ گردنشده‌ها که `15073` می‌دهد!)

نسبت دو پا در گروه M1-D1 همیشه `۴/۱۹ ≈ ۰.۲۱۰۵` است.

---

## ۸. Close in: شمارش تا بستن کندل جاری

```
Close in : 22d 12h 32m 13s
```

از ساعت شروع کندل جاری + طول تایم‌فریم منهای ساعت اکنون؛ واحدهای صفرِ جلویی
حذف می‌شوند (H1 می‌شود `17m 51s`). قرمز، بالای سطر SL/TP.

---

## ۹. فریز پایه per-bar + شواهد (چرا این مدل)

مشاهده سرنوشت‌ساز (XAUUSD/H1، دو شات ۲۷ دقیقه‌ای در یک کندل):
پایه‌ها (SL/TP/SB) کاملاً ثابت ماندند (`303/707/1514/3129` + `673-3196`)
در حالی که Eng/Hunter با نوار زنده M5 حرکت کردند (`114/43 ← 109/41`).
پس پایه‌های کُند هر کندل چارت یک‌بار محاسبه می‌شوند و Eng/Hunter زنده‌اند —
کد همین را می‌کند (`TradePlanComputeLive`).

شواهد ساختاری (فقط نسبت‌ها، بدون ورودی مفروض):
- `TP1/SL = ۷/۳` ،`TP2/SL = ۵` ،`TP3/SL = ۳۱/۳` روی هر دو بلاک واقعی
- `Hunter = round(8×EngTrue/3)` با ورودی گردنشده (نه رُندشده)
- `Width/Base = ۲۰/۹۵ = ۴/۱۹` روی هر دو بلاک (`673/3196` و `228/1085`)
- زنجیره ماکرو: `1.107 × 1066 = 1180.06 ← 1180` و سپس
  `16/3 × 1179.6 = 6291.2 ← 6291` ،`12451 + 2621 = 15072`

---

## ۱۰. نویز زنده، فریز پایه و پمپ رفرش

پایه‌های کُند (SL/TP/SB) هر کندل چارت یک‌بار فریز می‌شوند — مثل استاد که
۲۷ دقیقه ثابت ماند. Eng/Hunter زنده‌اند و لرزش ±۱ آن‌ها طبیعی است.
پمپ زنده (`TradePlanLiveTick`، حداکثر هر ۲ ثانیه، فقط وقتی عددی عوض شده باشد)
بلاک را روی هر تایم‌فریم به‌روز نگه می‌دارد؛ بدون آن بلاک فقط با سوییچ تایم‌فریم
رفرش می‌شد و ثابت به نظر می‌رسید (P-LBL-03).

---

## ۱۱. چه چیزی مانده برای تأیید فوروارد

- **مولت H1 (‏۱.۷۵):** با نوار هم‌کادری `1.75 × 176 = 308` می‌دهد در برابر `303`
  دیده‌شده. اگر فریز per-bar درست باشد، در رول کندل H1 بعدی هر دو باید به یک
  عدد جدید بپرند — همان‌جا معلوم می‌شود مولت درست است یا بازبینی می‌خواهد.
- **پایه‌های W1/MN** (`6291 -- 12451` و `15072 -- 12451`): از زنجیره ماکرو
  می‌آیند ولی هنوز روی چارت W1/MN استاد دیده نشده‌اند — با مشاهده اول تأیید
  یا اصلاح می‌شوند.

---

## ۱۲. نگاشت به کد

| فرمول | کد |
|---|---|
| مولت SL per-TF + سقف ماکرو | `TradePlanSLMult` / `TradePlanBaseMinutes` / `TradePlanSLTrue` |
| SL/TP از SL گردنشده | `TradePlanCompute` |
| تریگر (دو پله پایین، کف M1) | `TradePlanTriggerBack` / `TradePlanTriggerMinutes` |
| Eng از SL تریگر | `TradePlanEngTrue` (ورودی `engTrue`) |
| Hunter از Eng گردنشده | `TradePlanHunterFromEng` |
| StrBond گروهی (Base/Width/W1/MN) | `TradePlanCompute` |
| فریز پایه per-bar + Eng زنده | `TradePlanComputeLive` |
| نگهبان رُندینگ هر سیمبل | `TradePlanSelfCheck` |
| رندر راست‌چین + Close in | `CreateATRTradeLabel` / `DisplayTradePlanTopRows` / `TradePlanCloseInText` در `LabelFunctions.mqh` |
