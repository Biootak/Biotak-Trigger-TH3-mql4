# LEARNING.md — تجربه‌های مهندسی پروژه (برای استفاده در کارهای بعدی)

> خلاصه فارسی: این فایل حافظه «راه‌حل‌های اثبات‌شده» است، نه قوانین پروژه
> (قوانین در `AGENTS.md` و معماری در `ARCHITECTURE.md`).
> هر بار که مسئله‌ای با تحقیقی واقعی حل شد، الگوی آن را همین‌جا ثبت کن تا
> سشن بعدی از همین راه برود و از نو تحقیق نکند. کوتاه بنویس: مسئله → راه → چرا.

---

## §1 — Real-time drag-follow بدون فشار به سیستم (2026-09-07، استرپ بیس‌باکس)

**مسئله:** تولبار شناور باید «همان لحظه‌ای که باکس درگ می‌شود» همراهش حرکت کند،
نه با تأخیر تایمر ۲۵۰ms، و درگ‌استورم (ده‌ها ایونت در ثانیه) نباید CPU را بخوابد.

**راه (به ترتیب اهمیت):**

1. **Event-driven، نه poll سریع‌تر.** روی همان ایونتی سوار شو که دیتا را تکان
   می‌دهد (`CHARTEVENT_OBJECT_DRAG`)، نه اینکه تایمر را تندتر کنی.
   poll فقط fallback می‌ماند (برای تغییراتی که ایونت ندارند: wheel-zoom،
   TF-switch). نمونه: `BkStripFollow` + شاخه `OBJECT_DRAG` در
   `HandleUIChartEvent` (`Biotak/BiotakPanels.mqh`).
2. **Throttle در call-site، حدود 30ms** (≈33 فریم — چشم فرقی با ۶۰ نمی‌بیند،
   هزینه نصف). متغیر `static uint` داخل همان شاخه.
3. **Dead band مکانی (۴px).** حرکت‌های زیر آستانه اصلاً آبجکت را تکان ندهند
   (ضد jitter) — قبل از هر syscall چک شود.
4. **لیست صریح آبجکت‌ها، هرگز اسکن کل چارت.** به‌جای حلقه `ObjectsTotal` روی
   کل چارت (هزاران آبجکت در این اندیکاتور)، فقط آبجکت‌های خودت را با اسم
   جابه‌جا کن (~۱۵ syscall). نمونه: `BkStripMoveBy` + `BkMiniSlotName`
   (single source — اسلات جدید خودکار پوشش داده می‌شود).
5. **ارزان‌ترین چک اول.** مقایسه رشته (`sparam == ...`) قبل از هر
   `ObjectGet*`؛ `ObjectFind` قبل از `ObjectSet`؛ همه change-guarded.
6. **وضعیت موقت همراه حرکت پاک شود.** پاپ‌آور/پالت که مختصات منطقی جدا دارند
   (`g_BkDdX/Y`) با حرکت سوار نمی‌شوند — موقع follow ببندشان، وگرنه hit-rect
   دروغین می‌ماند (`BkDdClose` + `PalClose` در `BkStripFollow`).

**چرا نه راه‌های دیگر:**

- تایمر ۵۰ms به‌جای ایونت → روی چارت بدون تیک همان تأخیر را دارد + همیشه
  هزینه ثابت، حتی وقتی هیچ‌چیز تکان نمی‌خورد.
- `PnlMoveBy` عمومی (اسکن کل چارت) با نرخ درگ → روی چارت شلوغ هر استپ چند
  هزار مقایسه رشته؛ با لیست صریح ~۱۵ syscall است.
- `ChartRedraw` پشت هر استپ لازم نیست حذف شود — حین درگ native ترمینال
  خودش مداوم repaint می‌کند؛ `ObjectMove`های ما قبل از همان repaint
  می‌نشینند (childها frame-perfect دنبال می‌کنند). redraw صریح فقط برای
  پایان ژست/تغییر حالت.

**دوقلوی دامین‌ساید (2026-09-07 بچه‌های باکس حین درگ؛ یکپارچه 2026-09-08، P-BK-07):**
همان الگو، برای `BaseKnotTool.mqh` که نه تایمر دارد نه UI — با یک اصلاح مهم:
دو نویسنده (OBJECT_DRAG→Sync کامل هر استپ + MOUSE_MOVE→دلتای کرسر) با هم
می‌جنگیدند: اولی روی چارت سنگین lag می‌کرد، دومی snap مگنت ترمینال روی BOX را
نمی‌دید — فیل و حاشیه وسط درگ از هم جدا می‌ماندند. حالا نویسنده یکی است
(`BaseKnotFollowDrag`): تا ترمینال انکرها را تکان می‌دهد، از روی live anchor
(دقیق، فقط ObjectMove)، فقط وقتی انکر وسط درگ فریز بود دلتای کرسر از نقطه
press؛ OBJECT_DRAG کرسر معتبر ندارد (فقط anchor-exact — همان قرارداد TH3Tool)،
کرسر از کانال MOUSE_MOVE می‌آید؛ گیت مشترک 30ms تکراری‌ها را جذب می‌کند تا نه
بجنگند نه گرسنه بمانند؛ Sync کامل فقط موقع release. حفره overlap هم با adopt
باکسِ واقعاً درگ‌شده وسط راه پوشیده می‌شود (cursor base همان press می‌ماند).

- مشکل اول: childها (خطوط/لبه‌ها) فقط با `OBJECT_DRAG` سینک می‌شدند — تحویل
  per-step این ایونت و خواندن live anchor حین درگ، behavior بیلدبه‌بیلد فرق
  می‌کند؛ روی بعضی بیلدها همه‌چیز تا release فریز می‌ماند.
- مشکل دوم (2026-09-08): `Sync` کامل هر استپ (ده‌ها style/tooltip syscall) روی
  چارت سنگین lag می‌کرد و دلتای کرسر snap مگنت BOX را نمی‌دید — دو نویسنده،
  دو مقصد.
- راه نهایی: ترک press با `MOUSE_MOVE` در حالت IDLE (همیشه جاری است) + نویسنده
  واحد `BaseKnotFollowDrag` — اول live anchor (دقیق، فقط `ObjectMove`، صفر
  style/color/create؛ هندسه عین `Sync`)، فقط وقتی انکر فریز بود دلتای کرسر از
  نقطه press + یک `BaseKnotSync` مقتدرانه از anchorهای کامیت‌شده موقع release
  («موقع رها شدن درست می‌شود» تضمینی).
- بودجه repaint مشترک (`BaseKnotDragPaint` ‏30ms): پوزیشن هر ایونت سینک
  می‌شود (ارزان)، repaint نه — repaint کاملِ هر استپ روی چارت سنگین، درگ را
  به اسلایدشو تبدیل می‌کند که فقط موقع release settle می‌شود.
- جزئیات: slop درگ/تپ (`BK_DRAG_SLOP` محلی — Lite پنل ندارد)، tap موقع
  release سینک نمی‌خواهد، باکس قفل‌شده کاندید نمی‌شود، حفره overlap با سینک
  نقطه drop هم پوشیده می‌شود. IDLE هیچ‌وقت consume نمی‌کند.

**الحاقیه 2026-09-08 (P-BK-16):** fallback دلتای کرسر باید کل آبجکت را
ببرد، نه فقط بچه‌ها را — وگرنه باکس سرجاش می‌ماند و release-Sync همه را
برمی‌گرداند سرجاش. absolute از نقطه press (هرگز incremental) تا همگرا شود
و double-count نشود؛ لحظه‌ای که ترمینال انکرها را تکان داد، شاخه
anchor-exact دوباره دست می‌گیرد. هر دو واقعیت درگ پوشش داده می‌شود:
native → به باکس دست نزن (P-BK-15)، فریز → خودمان می‌بریمش.

## §2 — حقایق ایونت MT4 که بارها به کار می‌آیند

- **Consumed ≠ hidden.** مقدار برگشتی `true` از `BaseKnotOnChartEvent` فقط از
  `OnChartEventHandler` خارج می‌شود؛ ورودی `.mq4` همه ایونت‌ها را
  **بی‌قیدوشرط** به `HandleUIChartEvent` هم می‌دهد. پس لایه UI می‌تواند روی
  درگ باکسی که دامین consume کرده سوار شود، بدون شکستن لایه‌بندی (Lite).
- `CHARTEVENT_OBJECT_DRAG` حین درگ **مداوم** می‌آید (هر استپ)، نه فقط پایان.
- `CHARTEVENT_CLICK` در برابر `OBJECT_CLICK` برای هر رها کردن **یکی** می‌آید
  (انحصاری) — پس طبقه‌بندی release (جدول زیر) کامل است.
- طبقه‌بندی هر button-up در شاخه `CHARTEVENT_CLICK` (به همین ترتیب):
  1. `fireRel` — رهای پایان hold بازکننده (one-shot، با هر CLICK مصرف شود،
     با press جدید هم پاک شود که stale نماند)؛
  2. `dragRel` — فاصله press-down تا release بیشتر از `BK_CLICK_SLOP`
     (خوانده‌شده از latch **قبل** از `BkHoldOnBoxUp`)؛
  3. راست‌کلیک (`"r"` در sparam — همان قرارداد `BaseKnotTool`)؛
  4. تپ روی آبجکتِ خودِ تولبار (re-hit-test، فقط visible).
  فقط کلیک چپ واقعی روی فضای خالی = dismiss.

## §3 — عددهای throttle پروژه (مرجع، تغییر نده مگر با دلیل)

| مسیر | نرخ | کجا |
|------|-----|-----|
| rubber-band سایزینگ | 30ms | `BaseKnotTool.mqh` |
| درگ هدر پنل | 16ms | `PnlHandleMouseMove` |
| درگ ناب/ترک اسلایدر | 30ms | `PnlHandleMouseMove` |
| strip-follow روی درگ | 30ms + dead-band ‏4px | `HandleUIChartEvent` → `BkStripFollow` |
| strip-follow fallback | per-tick + تایمر ۲۵۰ms | `BkMiniStripHeal` |
| hold-to-open poll | ۲۵۰ms | `BkHoldPoll` |
| پمپ‌های ۵۰۰ms | badges/hint | `RefreshUIPerTick` |
| بودجهٔ heavy-pass در ژست زنده (اسلایدر/میکسر/ناب) | 120ms | `BiotakKit.mqh` → `UIDragBudgetBegin/End` + `ApplyRefreshFlags` |

## §4 — چک‌لیست «فلو بعدی مثل این»

1. ایونتِ محرک دیتا کدام است؟ همان را بگیر، تایمر را تند نکن.
2. throttle + dead band از روز اول بگذار (بعداً اضافه‌کردن سخت‌تر است).
3. آبجکت‌ها را با اسم صریح تکان بده، نه اسکن.
4. وضعیت موقت (پاپ‌آور، درگ، latch) را موقع حرکت/بستن پاک کن.
5. Lite را نشکن: دامین فقط `Object*`، صداکردن UI از دامین ممنوع.
6. الگو را همین‌جا (§جدید) ثبت کن.

## §5 — چارت پشت هر ژست قفل و بی‌حرکت می‌ماند (2026-09-08، رسم/درگ بیس‌باکس)

**مسئله:** موقع هر ژست دستی (سایزینگ باکس، درگ باکس، درگ منو/پنل) چارت پشت
نباید حتی یک پیکسل تکان بخورد؛ وگرنه باکس جای کلیک نمی‌نشیند یا درگ کنسل
می‌شود و برمی‌گردد سرجاش (P-BK-14/15).

**راه (الگوی ثابت — هر ژست جدید عین همین):**

1. **شروع ژست = قفل؛ اولین قفل‌گیرنده save می‌کند، تودرتو فقط re-assert.**
   دامین (Lite-safe): `BaseKnotLockChart(ctxToo)` با `Chart*` خام، بدون
   وابستگی به منو. UI: `CircLockChart()` شمارشی + `ChartScrollReconcile()`
   به‌عنوان watchdog.
2. **حین ژست = re-assert مداوم و read-guarded** (`BaseKnotReassertLock`).
   قفلِ یک‌بار کافی نیست — نویسنده سوم (آنلاک مودال موقع بستن استریپ/کارت
   وسط ژست، watchdog پنل، ریست تمپلیت/ترمینال) وسط ژست بازش می‌کند. روی هر
   استپ throttleشده + پمپِ while-owning صدا بزن؛ steady state فقط read،
   write فقط موقع drift. `ctxToo` مال قفل‌گیرنده اول می‌ماند.
3. **آبجکتِ در حال درگِ native را فقط هندلرهای درگ/ریلیز خودش لمس کنند**
   (نه style/color/tooltip، نه move، نه delete+recreate) — ترمینال درگ را
   کنسل می‌کند و آبجکت برمی‌گردد سرجاش. پمپ‌ها باکسِ `s_bkDragId` را skip
   کنند؛ Sync مقتدرانه فقط موقع release.
4. **پایان ژست = دقیقاً یک Unlock** در همه خروجی‌ها
   (commit/cancel/ESC/release/OnDeinit) + watchdog گیرکردن (button-up +
   ۱.۵ ثانیه سکوت ایونت) برای release گمشده.
5. **tap و drag با slop جدا شوند** (۸px): tap نباید propها را flicker بدهد —
   قفل درگ فقط past-slop (P-BK-08).
6. **Lite را نشکن:** دامین فقط `Chart*`/`Object*` خام؛ صداکردن UI از دامین
   ممنوع (الگو: `BaseKnotLockChart` در `BaseKnotTool.mqh`).

**چرا نه راه‌های دیگر:**

- قفلِ فقط-موقع-شروع → نویسنده سوم وسط ژست بازش می‌کند (P-BK-14).
- Sync کامل هر استپ درگ → هم lag روی چارت سنگین (P-BK-07)، هم کنسل‌شدن
  درگ (P-BK-15)؛ فقط `ObjectMove` حین ژست + یک `Sync` موقع release.
- re-assert بدون read-guard → ده‌ها syscall بیهوده روی هر استپ.

**چک‌لیست «ژست جدید»:** قفل موقع شروع؟ re-assert حین ژست؟ Unlock در همه
خروجی‌ها؟ آبجکت درگ‌شده وسط ژست دست نمی‌خورد؟ slop برای tap؟ Lite
کامپایل می‌شود؟ (مثل چک‌لیست §4.)

## §6 — روش ریشه‌یابی و تحویل فیکس رفتاری (2026-09-08، درس‌های این چت)

1. **همه نویسنده‌ها را بشمر، به اولین تئوری بسنده نکن.** برای «X
   برمی‌گردد / تکان می‌خورد»: تک‌تک `ObjectMove/Set/Create/Delete` روی آن
   آبجکت + همه پمپ‌ها را در برابر تایمینگ ژست چک کن — این چت دو مکانیزم جدا
   داشت (پمپ وسط درگ می‌نوشت: P-BK-15؛ fallback فقط بچه‌ها را می‌برد:
   P-BK-16) و تئوری اول نصف جواب بود.
2. **جهتِ فریزنشده با هیسترزیس دنبال شود، نه با بافر.** stateای که از واقعیت
   زنده عقب می‌ماند (جهت باکس: P-BK-13) را با خودِ آبجکت به‌عنوان باند
   هیسترزیس دنبال کن — بیرون می‌گیرد، داخل نگه می‌دارد؛ نه بافر جدا، نه
   تنظیم جدید، نه فلیکر. `Sync` هرگز recompute نکند؛ refresh قبلش.
3. **Lite پمپ UI ندارد — healهای دامین را از مسیر مشترک تیک بچرخان.**
   (`OnCalculateHandler` با throttle خودش + `#ifdef BUILD_LITE` — P-BK-13.)
4. **هر فیکس رفتاری با اثر انگشت قابل مشاهده + دستور reload.** کاربر بیلد
   قدیمی در حافظه دارد (MT4 هرگز hot-swap نمی‌کند) — بدون fingerprint
   (مثل هینت flip) «درست نشده» یعنی بیلد قدیمی یا باگ واقعی، معلوم نیست.
   اول reload + تأیید fingerprint، بعد تئوری بعدی.
5. **یک مکانیزم در هر کامیت.** حدس تأییدنشده روی هم نریز؛ هر حدس = یک
   کامیت جدا و قابل برگشت.
6. **کاربر کم‌حرف = یک مشاهده تفکیک‌کننده.** به‌جای چند سؤال، همان یک
   جزئیاتی را بخواه که دو مکانیزم را از هم جدا می‌کند (مثل: «خط‌ها به باکس
   چسبیده‌اند یا جدا دنبال موس می‌آیند؟»).

## §7 — پرفورمنس‌فرست: کدی که روی ضعیف‌ترین PC با ده اندیکاتور دیگر هم تند است (2026-09-10، P-PERF-01)

**پیش‌فرض ذهنی:** ترِد چارت MT4 تک‌نخه و مشترک است — هر Print، هر
ChartRedraw، هر iTime اضافه از جیب ده اندیکاتور دیگر می‌رود. بودجه ثابت:
per-tick فقط O(1) compare؛ per-500ms فقط syscall خواندنیِ guardشده؛ repaint
فقط از یک throttle مشترک (۱۰۰ms). هر کد جدید باید از روز اول در همین بودجه
جا شود، نه اینکه بعداً «بهینه» شود.

**قوانین (به ترتیب ارزش):**

1. **امضای تغییر فقط slow-leg.** هیچ‌وقت مقدار تند (countdown ثانیه‌ای،
   spread زنده، قیمت لایو) را در sig نگذار — وگرنه گیت هیچ‌وقت match
   نمی‌شود و مسیر «تغییر» برای همیشه داغ می‌ماند (`TradePlanLiveTick` هر
   ۲ ثانیه rebuild + ۷ لاگ + snapshot هشت‌تایی می‌کرد چون `CloseInText`
   در sig بود). liveها مسیر ارزان جدا دارند (in-place text-set)؛ مسیر
   کامل + لاگ + snapshot فقط پشت تغییر عددی.
2. **delete+recreate ممنوع وقتی upsert کافی است.** الگوی ثابت:
   `ObjectFind<0 → create` بعد `Set*` (مثل `CreateATRTradePiece`)؛
   wipe-then-rebuild فقط flicker + kernel call است. purge لی‌آوت‌های
   منسوخ یک‌بار per-prefix با `static string s_purgedPfx`، نه هر تیک.
3. **repaint یکی، مشترک، ته کار.** وسط حلقه هرگز `ChartRedraw` نه (پمپ BK
   به‌ازای هر باکس می‌زد → فلگ `bkNeedPaint` + یکی ته حلقه). لایه‌ها
   throttle جدا نداشته باشند — همه از `ThrottledChartRedraw` تا repaintها
   روی هم بیفتند؛ raw فقط برای پایان ژست کاربر.
4. **throttle لانه‌ای، ارزان‌ترین چک اول.** گیت زمانی (۲s) بیرون، گیت
   عددی وسط، throttle سنگین (۳s/۱۰s لاگ) داخل؛ invariantهای حلقه (BG،
   edge-time، live-ref مثل `BaseKnotLiveRef`) یک‌بار بیرون حلقه؛
   `StringFormat` فقط بعد از گیت زمانی.
5. **سری‌دیتا (iTime/iATR/...) کمترین تعداد نزدیک‌ترین گیت.** هر صدا
   kernel call است؛ compositeها پشت TTL cache (`CalculateWeightedATR`)؛
   snapshot چندتایی فقط پشت تغییر عددی.
6. **Lite و Full یک بودجه.** healهای دامین از مسیر مشترک با throttle
   خودشان (۵۰۰ms)؛ هیچ مسیر Lite-only بدون throttle نه.

**چک‌لیست «کد جدید»:** sig فقط slow-leg؟ upsert به‌جای wipe؟ repaint ته
کار و مشترک؟ Print پشت گیت عددی؟ syscall تکراری از حلقه بیرون کشیده
شده؟ Lite کامپایل می‌شود؟ (مثل چک‌لیست §4.)

**قانون فیت (2026-09-10، P-ATR):** هیتِ subset/رگرسیون روی تک‌نمونه هیچی را
اثبات نمی‌کند — با ۲۰۴۷ زیرمجموعه، برخورد ۰٫۰٪ تضمینی است (پنج زیرمجموعه
کاملاً متفاوت هم‌زمان «درست» شدند!). فقط بقا در رژیم‌های مختلف (چند نمونه
هم‌دقیقه‌ای در روزهای متفاوت) فرمول را مشخص می‌کند؛ تا آن موقع نزدیک‌ترین
پیکربندیِ اصولی می‌ماند و میکس عددی توی کد نمی‌رود.

**قانون لاگ (2026-09-10):** عدد لاگ فقط از fresh-fetch همان لحظه —
قبل از هر استناد به `[SNAP]`/`[TRADEPLAN]` حتماً
`tools/fetch_tradeplan_log.ps1` را دوباره اجرا کن و `Total log lines` را
چک کن؛ لاگ فریز (خط جدید نیامده) یعنی فید مرده و هر مقایسه‌ای با آن
نامعتبر است. هرگز عدد کش‌شده از پیام‌های قبلی را بازنشر نکن.

## §8 — مالکِ متریک متن باید زیر همهٔ سطوحی باشد که از آن استفاده می‌کنند (2026-09-12، P-UI-34)

**مسئله:** در P-UI-30 مالک متریک (`PnlDpi/PnlPt/PnlTextW/PnlFit`) داخل
`BiotakPanels.mqh` نوشته شد؛ اما در هر entry (`.mq4`) ماژول‌های `BaseKnotTool`،
`BiotakKit` و `BiotakMenu` **قبل از** پنل‌ها include می‌شوند. نتیجه: سطح‌هایی که
تنگ‌ترین جعبه را دارند دقیقاً همان‌هایی بودند که نمی‌توانستند از مالک استفاده
کنند — ۱۹ سایز pt خام، چهار سایتِ حدس عرض `5 * StringLen`، و خط دومِ تولتیپ با
آفست جادویی `y + 26` (که در ۲۰۰٪ به خط اول می‌خورد).

**قالب فیکس (هر متریک/توابع مشترک جدید عین همین):**

1. **مالک را بگذار در اولین ماژولی که همهٔ مصرف‌کننده‌ها include می‌کنند**
   (`UtilityFunctions.mqh`), نه در بالاترین مصرف‌کننده. تست: اگر گیتِ مالک در
   ماژول A باشد ولی مصرف‌کننده‌ها قبل از A include شوند، گیت عملاً بی‌فایده است.
2. **هر متنی که در جعبهٔ ثابت می‌نشیند: pt اسمی + `PnlFit` + عرض اندازه‌گیری‌شده.**
   صفر «حدس عرض» (نه `StringLen*k`, نه عدد جادویی برای y خط دوم — از `PnlLineH`).
3. **یک مالک برای split+clip** اگر همان متن از دو مسیر (رسم و refresh) نوشته
   می‌شود؛ وگرنه مسیر دوم متن خام را برمی‌گرداند و باگ برمی‌گردد.
4. **گیت:** مالک এক‌بار تعریف شده باشد، هیچ `OBJPROP_FONTSIZE` عددی در
   کروم UI نباشد (متغیرهای تنظیمات کاربر و ماژول‌های دامین/بازنشسته با دلیل
   allowlist می‌شوند)، و متن‌ها با **بلندترین حالت** (طولانی‌ترین وضعیت) در
   ۹۶/۱۲۰/۱۴۴dpi اندازه گرفته شوند. `tools/ui-text-audit.py` — و مدل عرض را
   از `tools/panel-mt4-sim.py` import می‌کند تا کپی سوم جدول پیشروی ساخته نشود.

## §9 — چارت کند/لگ‌دار: قانون «هیچ نوشتنی که تغییری نمی‌دهد» (2026-09-12، P-PERF-02)

**مسئله:** به‌محض انداختن اندیکاتور، چارت کند و پر از لگ می‌شود؛ تعویض
تایم‌فریم هم فریز می‌کند.

**ریشه (نه یک باگ، یک خانواده):** در MT4 هر `ObjectSet*` چارت را dirty
می‌کند و هزینهٔ repaint با تعداد آبجکت‌های همین اندیکاتور بزرگ می‌شود
(اینجا ۱۰۰۰ تا ۲۵۰۰). پس هر کار per-frame که O(levels) یا O(objects)
باشد، مستقیماً لگ است:

1. pipeline ماسک `OBJPROP_TIMEFRAMES` را روی هر خط/لیبل/زون در **هر فریم
   سنگین** بازنویسی می‌کرد (~۱۵۰۰ نوشتن بی‌تغییر در فریم)، به‌علاوهٔ tooltip
   خط‌ها و price لیبل‌های فاصله بدون gaurd.
2. `RedrawAllObjects` در هر فریمی که فلگش بالا بود، همهٔ ~۳۰۰ سطح را از نو
   می‌ساخت — حتی وقتی هیچ ورودی تکان نخورده بود (تیک ATR هر ۵ ثانیه کافی
   بود).
3. مسیر تیک تا ۱۰ بار در ثانیه ChartRedraw می‌خواست، حتی برای فریمی که
   هیچ‌چیز نکشیده بود.
4. کندل forming هدف (HTF) هر تیک ۳ آبجکت خودش را بازنویسی می‌کرد.
5. `DeleteAllIndicatorObjects` در تعویض TF با حلقهٔ MQL روی **همهٔ** آبجکت‌های
   چارت و `ObjectDelete` تک‌تک تمیز می‌کرد (~۱۰۰۰ فراخوانی کرنل).
6. `HideAllTHObjects` در حالت مخفی در هر فریم سنگین روی همهٔ آبجکت‌ها
   می‌گشت.
7. `CacheClear` شرط early-exit را بعد از پاک‌کردن فلگ چک می‌کرد؛ هر بار کل
   ۳۲۷۶۸ باکت اسکن می‌شد.

**راه (به ترتیب ارزش):**

1. **یک مالک برای نوشتن، با gaurd مقدار + epoch.** `ApplyTfMaskGuarded` در
   `VisibilityManager.mqh`: اگر همان مقدار/همان epoch قبلاً نوشته شده، صفر
   syscall. هر نویسندهٔ بیرونی فقط باید epoch را bump کند (یا
   `CacheForgetTfMasks(prefix)` بزند) تا gaurd یک بار همه را دوباره بنویسد.
2. **کار per-frame باید تابع یک SIGNATURE باشد، نه حلقه.** `frameSig`
   (topology + base price + range + scaling factor + viewport + visibility +
   draw GENERATION) → فریم بی‌تغییر ~۱۵ مقایسهٔ رشته و صفر syscall.
   `MarkDrawGeneration()` را هر مسیری که آبجکت‌ها را پاک می‌کند صدا بزن،
   وگرنه signature دروغ می‌گوید و رندر انجام نمی‌شود (چارت خالی!).
3. **repaint را بی‌دلیل نخر.** `g_lastRedrawDidWork`؛ raw `ChartRedraw` فقط
   پایان ژست کاربر.
4. **teardown و show/hide باید bulk باشند، نه O(all objects).**
   `ObjectsDeleteAll` به‌ازای هر namespace؛ لایهٔ بیس‌باکس به‌خاطر شکل نام
   (`inpObjectPrefix + _BK_`) هیچ‌وقت زیر یک namespace تایم‌فریمی نمی‌افتد.
5. **آبجکت‌های دیدنی فقط در viewport.** HTF به اندازهٔ viewport + hysteresis
   کشیده می‌شود؛ بودجهٔ نوشتن زنده (`HTF_FORM_MS`) قبل از خواندن سری‌ها چک
   می‌شود تا تیک throttle شده یک iTime باشد و بس.
6. **lookup گران است، نه فقط syscall.** hashِ نام با `StringGetCharacter`
   برای هر کاراکتر حساب می‌شود؛ حتی guard‌شده‌ها per-object پرهزینه‌اند —
   پس قبل از حلقه، در سطح فریم gate بگذار (بند ۲).

**چرا نه راه‌های دیگر:**

- تایمر را کندتر کن → تأخیر را می‌خری، هزینه را نه (تحویل ایونت‌محور: §1).
- «برای اطمینان» هر فریم دوباره بنویس → همیشه بدترین حالت، درست همان چیزی
  که این گیت منعش می‌کند.
- حذف تک‌تک آبجکت‌ها موقع تعویض TF → گران‌ترین راه رسیدن به همان نتیجه.
- اسم water‌ها را تایم‌فریم‌ناوابسته کن تا باکس‌ها reuse شوند → بازنویسی
  بزرگ معماری (hotkeyها/namespace ها)؛ اول گیت‌ها را گذاشتیم.

**گیت:** `python tools/write-budget-audit.py` — مالک یکتا، generation روی هر
پاک‌کردن، gate فریم با generation، teardown بدون حلقهٔ O(all objects)،
بودجهٔ HTF قبل از خواندن سری، و مدل (۵ فریم یکسان روی ۳۰۰ آبجکت = ۳۰۰ نوشتن
به‌جای ۱۵۰۰)؛ `--selftest` هشت خطا می‌کارد و باید همه را بگیرد.


## §10 — «لگ باقی مانده» را از لاگ زندهٔ MT4 پیدا کن، نه با حدس (2026-09-12، P-PERF-03)

**مسئله:** بعد از §9 کاربر گفت «هنوز لگ شدید داره». حدس زدن ممنوع — این بار
ریشه از خود لاگ ترمینال پیدا شد.

**روش (این را تکرار کن):** ترمینال MT4 لاگ‌ها را در
`%APPDATA%\MetaQuotes\Terminal\<HASH>\MQL4\Logs\<YYYYMMDD>.log`
می‌نویسد و `_LOG_GATE_*` همیشه آن‌جاست. یک دستور این را قطعی کرد:

```bash
grep -a "CPU Warning\|CRITICAL CPU" <log> | sed "s/^[0-9]*\t//" \
  | awk "{print substr(\$1,1,5)}" | sort | uniq -c | sort -k2
```

نتیجه خودش جواب بود: **هر** بورت در دقیقهٔ `:00` یا `:30` — یعنی ۴ بار در
هر نیم‌ساعت به‌ازای هر چارت (و ۳۴ رویداد در ساعت ۰۰:۰۰). این «رابطهٔ زمانی»
ارزان‌ترین مدرک دنیاست؛ قبل از هر profiling گرانی اول همین را بگیر.

**ریشه‌ای که لاگ لو داد:**

1. **مرز ۳۰ دقیقه یک «دقیقهٔ کامل» بود، نه یک لبه.**
   `serverMinute == 0 || == 30` یعنی ۱۲۰ ثانیه از هر ۱۸۰۰ ثانیه
   `hasPendingWork=true`، پس کل پاس سنگین با gate ۵۰۰ms (~۲ بار در ثانیه)
   اجرا می‌شد: ~۲۴۰ پاس به‌جای ۱. اندازه‌گیری‌شده: ۶۲ms هر پاس روی این
   دستگاه، روی PC ضعیف چند برابر.
2. **کار خودِ مرز O(ده‌ها هزار) syscall بود:** `FindCurrentSessionStart`
   تا ۱۰٫۰۸۰ کندل را با **دو `iTime()` در هر تکرار** می‌گشت و اگر گپی
   پیدا نمی‌کرد یک بار دیگر کل تاریخچهٔ M1 را از قدیمی‌ترین کندل
   (~۲۹٬۰۰۰) می‌رفت — همان فریز ۱٫۸ تا ۳٫۰ ثانیه‌ای «CRITICAL CPU».
3. `CheckAlerts` هر فریم `~2 x inpMaxLevels` بار `StringFormat` +
   `ObjectFind` + `ObjectGetDouble` می‌زد برای قیمت‌هایی که همان فریم
   همین‌جا داشت.
4. `ClearAllLabels` هر بار ۱۱ بار `ObjectsDeleteAll()` (هر بار یک قدم
   روی کل آبجکت‌های چارت).
5. `WarmupATRMultiTFCache` هشت تایم‌فریم را پشت‌سرهم در `OnInit`
   حساب می‌کرد (هر attach و هر تعویض TF).

**راه‌حل‌ها (هر پنج‌تا همان الگو: کار را از per-frame/per-init بیرون بکش):**

- **مرز = لبه.** مقدار را به‌عنوان *candidate* حساب کن و برچسب بلوک را
  **بعد از همهٔ gateها** بزن؛ اگر قبل از gate بزنی، gate میلی‌ثانیه‌ای همان
  یک فریم را می‌خورد و کار base price اصلاً اجرا نمی‌شود (تله‌ای که یک بار
  در همین کار رخ داد).
- **سری‌ها را bulk بخوان:** یک `CopyTime(symbol, tf, 0, n, arr)` به‌جای
  n بار `iTime()`. ترتیب آرایه در ترمینال‌های مختلف فرق می‌کند، پس اول
  `arr[0] < arr[n-1]` را چک کن و لازم بود معکوس کن — به قرارداد ترمینال
  تکیه نکن. جواب را هم per-day memo کن.
- **لیست قیمت‌ها را در همان پاسی که رسم می‌کنی ثبت کن** (اینجا
  `AlertCacheAdd` داخل `RenderTriggerLines`) و چک را روی آرایه ببر:
  صفر syscall. `g_drawGeneration` را به‌عنوان اعتبار کش نگه دار — کش
  کهنه = سطوح از چارت رفته‌اند، پس skip کن نه اینکه بگردی.
- **Sweep گروهی را به generation گره بزن:** ۹/۱۱ بار پاک‌کردن فقط
  وقتی لازم است که چیزی پاک/عوض شده باشد.
- **Warmup را قرضی کن (amortize):** صف + یک آیتم در هر tick/تایمر.
  این هم `OnInit` و هم تعویض TF را روان می‌کند.
- **خودافشاگری بگذار:** دفتر فاز (base/hist/levels/labels/overlay ms) را
  فقط داخل پیام overrun چاپ کن. دفعهٔ بعد لاگ خودش می‌گوید کدام فاز خورده،
  و لازم نیست کل این تحلیل تکرار شود.

**گیت:** `python tools/write-budget-audit.py` (حالا + مرز لبه‌ای و محل commit،
خواندن bulk سری، کش alert، sweep وابسته به generation، warmup قرضی)؛
`--selftest` چهارده خطا می‌کارد و باید همه را بگیرد.

## §11 — لگی که «حس می‌شود» در مسیرهای تعامل است، نه در تیک (2026-09-12، P-PERF-04)

**مسئله:** بعد از دو دور فیکس، کاربر گفت هنوز موقع **کار با چارت**، **کار با پنل**
و **تعویض تایم‌فریم** لگ می‌زند.

**درس اصلی:** P-PERF-02/03 همه روی مسیر تیک اندازه‌گیری شده بودند (شاهدش
هشدارهای `CRITICAL CPU` در `:00/:30` بود). ولی شکایتِ باقی‌مانده مربوط به
مسیرهای **رویدادی** بود که هیچ‌کس اندازه‌شان نگرفته بود. پس همیشه بپرس:«این
لگ در کدام مسیر است؟ تیک، رویداد، یا reinit؟» و برای همان مسیر شاهد بساز.

**سه ریشهٔ ساختاری که آن‌جا پیدا شد:**

1. **پنجرهٔ viewport هر پیکسلِ pan را دوباره می‌ساخت.** آستانهٔ cache برابر
   «یک point» بود و خروجی همین تابع (`vpTop/vpBottom`) جزء امضای هندسهٔ
   رسم سطوح است → هر جابه‌جایی چارت = یک رندر کامل ~۳۰۰ سطحی. حاشیهٔ کش
   هم ±۵۰٪ محدودهٔ دید بود و برای هر سطح داخلش یک خط + لیبل فاصله +
   **یک مستطیل توپر به عرض کل چارت** ساخته می‌شود.
2. **وقتی چارت محدودهٔ قیمت را نمی‌داد** (استارت، پنجرهٔ مینیمایز، اولین
   فریم بعد از تعویض TF) فالبک «کل high-low تاریخی» بود → یعنی **همهٔ**
   سطوح `inViewport` می‌شدند و در یک فریم کل ست ساخته می‌شد؛ و چون cull
   فقط hide می‌کند، باقی‌ماندهٔ off-screen تا ابد روی چارت می‌ماند و هر
   repaint آن را می‌پیماید.
3. **درگ پنل هر ۱۶ms** یک دسته read+write به‌ازای هر آبجکت پنل می‌زد و
   سر هر تیک ماوس یک `ChartRedraw()` **همگام** می‌خواست (~۶۰ repaint در ثانیه).

**راه‌حل‌ها:**

- **حاشیه/هیسترزیس عدد دارند و قیدشان در گیت است:** `MARGIN 0.25 <`
  قدیم، `HYSTERESIS 0.20`؛ و **الزاماً margin > hysteresis** وگرنه یک pan
  که هرگز پنجره را دوباره نساخته می‌تواند یک سطحِ دیده‌شدنی را نکشد.
- **فالبک viewport محدود شد** (۲٪ حول قیمت) نه کل تاریخچه — این «گاردِ
  انفجار آبجکت» است.
- **آگاهی از پیکسل:** `RenderZones` یک بار مقیاس قیمت→پیکسل را می‌گیرد و هر
  زون نازک‌تر از ۲px را فقط hide می‌کند؛ چون همان حلقه از قبل off-screen ها
  را hide می‌کند، بی‌حالت (stateless) و امن است.
- **پنل**: batch روی ۳۳ms و repaint از `ThrottledChartRedraw()`؛ دلتا در
  `g_PnlMoveLast*` جمع می‌شود پس کارت دقیقاً روی نشانگر می‌ماند.
- **مسیرهای تعاملی اندازه‌گیری شدند:** `P4ReportSlow()` — رویداد چارت >
  `P_P4_EVENT_WARN_MS` (۴۰ms، با تفکیک `indicator=` و `ui=` تا بدانیم کدام
  نیمه) و `OnInit`/`OnDeinit` > `P_P4_INIT_WARN_MS` (۱۵۰ms)، حداکثر یک
  خط در ۲ ثانیه و فقط وقتی بودجه رد شده باشد.

**گیت:** `python tools/write-budget-audit.py` بخش `interaction` را گرفت
(رابطهٔ margin/hysteresis، فالبک محدود، حذف فیلم‌های sub-pixel، batch پنل +
repaint throttle‌شده، و اندازه‌گیری رویداد/init/deinit در **هر دو** entry)؛
`--selftest` بیست خطا می‌کارد و باید همه را بگیرد.

## §12 — بیلد اولیه/تعویض TF را مرحله‌ای (staged) بساز، نه یک‌جا (2026-09-12، P-PERF-06)

**مسئله:** بعد از چهار دور فیکسِ مسیر تیک، تعویض تایم‌فریم/اتچ هنوز چند ثانیه
فریز می‌کرد و درگ پنل/اسلایدر روی PC ضعیف اسلایدشو بود. سه ممیزی موازی نشان
داد مسیر تیک تمیز است؛ هزینه در سه جای دیگر بود: (۱) بیلد هم‌زمان ~۹۰۰ آبجکت
بعد از هر wipe در یک فریم، (۲) اسکن کل چارت در `PnlMoveBy` با نرخ درگ،
(۳) repaint خام با نرخ پوینتر حین درگ.

**راه (به ترتیب ارزش):**

1. **خانواده‌به‌خانواده بساز، نه همه‌چیز یک‌جا.** هر wipe (اتچ، تعویض TF،
   تاگل توپولوژی) `g_buildStage` را از ۱ شروع می‌کند: LINES → ZONES →
   pip-LABELS → labels-BLOCK؛ هر فریم یک خانواده (~۲k به‌جای ~۸k کرنل‌کال).
   امضای هندسه فقط در BLOCK مهر می‌شود؛ `needLabels` تا BLOCK صبر می‌کند؛
   HTF فقط کندل forming را می‌زند تا stage صفر شود؛ تایمر ۲۵۰ms هم پمپ
   می‌کند تا چارت بی‌تیک در ~۱ ثانیه settle شود.
2. **ری‌استارتِ wipe بی‌قیدوشرط.** تاگلی که وسط staging پاک می‌کند باید از
   LINES شروع کند، وگرنه خانواده‌های پاک‌شده هیچ‌وقت برنمی‌گردند. staging
   فقط روی wipe سوار است (نه روی هر رندر)، پس livelock ندارد.
3. **ویوپورتِ مرحله ۱ را فریز کن.** استیج‌های ۲ تا ۴ با همان پنجره cull
   می‌کنند تا pan وسط staging خانواده‌ها را دوپاره نکند؛ کهنگی حداکثر ~۱
   ثانیه است و رندر بعدیِ viewport خودش heal می‌کند.
4. **اسکن را به تایپ محدود کن، نه حذف.** `PnlMoveBy` به‌جای کل چارت، پنج
   تایپ UI را native می‌شمارد (LABEL/BUTTON/BITMAP_LABEL/EDIT/RECTANGLE_LABEL
   — تنها تایپ‌هایی که پنل می‌سازد)؛ رفتار identical، کاندید ~۵ برابر کمتر.
   رجیستریِ دستیِ اسم سریع‌تر بود ولی بدون runtime-test ریسک جاافتادن داشت.
5. **حین درگ، repaint هم throttle می‌خواهد.** شاخه swallow بودجه، ته
   `ApplyRefreshFlags` و `PalUpdateLive` همه `ThrottledChartRedraw` شدند
   (ناب/چیپ هر ۳۰ms تکان می‌خورند، repaint حداکثر ۱۰/s).

**چرا نه راه‌های دیگر:**

- مجازی‌سازی (delete به‌جای hide برای off-screen): اسکرول‌استاپ را پر از
  create/delete می‌کرد درحالی‌که هیسترزیس + گارد همین امروز آن را ارزان
  کرده — خالص منفی.
- ماسک per-TF برای نگه‌داشتن آبجکت‌ها بین TFها: بازنویسی معماری
  (همان که ممیزی صراحتاً defer کرده)؛ wipe می‌ماند، بیلد مرحله‌ای می‌شود.
- کم‌کردن `inpMaxLevels`: انتخاب ظاهری کاربر است؛ دست نزن، در پنل هست.

**چک‌لیست:** state در `GlobalVariables.mqh` (ماژول‌های بعدی ببینند)؛ هر
فریم staging باید pending-work باشد + وارد بلاک شود + skip نکند؛ امضا فقط
برای رندر کامل؛ گیت `staging` در `write-budget-audit.py` (۱۸ چک + مدل
۳۰٪)؛ `--selftest` باید همه را بگیرد (الان ۳۲).


## §13 — گاردِ «نوشتن» گاردِ «بررسی» نیست؛ و هر فیکس باید فازش را در لاگ نام ببرد (2026-09-12، P-PERF-07..10)

**۱) بزرگ‌ترین هزینهٔ باقی‌مانده، خودِ «نوشتن» نبود؛ «پرسیدن» بود.**
P-PERF-02 همهٔ `ObjectSet*`های هر فریم را شرطی کرد، ولی هیچ‌کس شرطی نکرد که آن
گارد چطور می‌فهمد آبجکت هست یا نه. `SetPipelineObjectTimeframesIfExists` برای هر
نامی که کش نمی‌شناسد یک `ObjectFind()` می‌زند — و سطحِ CULL شده هیچ‌وقت ساخته
نمی‌شود، پس هیچ‌وقت وارد کش نمی‌شود، پس هیچ‌وقت پرسیدنش قطع نمی‌شود: ۱ برای خط،
۱ برای لیبل، ۷ برای زون. با `inpMaxLevels=144` یعنی تا ~۲۶۰۰ پرس‌وجوی ترمینالی در
هر فریم سنگین، و چون هر پرس‌وجو لیست آبجکت‌های چارت (هزاران آبجکت) را می‌گردد،
هزینه **O(سطح‌های culled × تعداد کل آبجکت‌ها)** است — یعنی دقیقاً وقتی کاربر
زوم/تعویض TF می‌کند بدتر می‌شود.
قانون: **هر `ObjectFind`/`ObjectGet*` فال‌بکی باید یک حالت «نبودنِ اثبات‌شده» داشته
باشد.** جدول منفی (negative table) با مهر نسل (generation stamp) بساز تا
بی‌اعتبارسازی O(1) باشد و sweep لازم نشود؛ و فقط جایی پاکش کن که آبجکتِ ما
می‌تواند بدون گذر از کش ظاهر شود (`CacheClear` + `OnInit`). نکتهٔ کلیدی: نامی که
بعداً ساخته می‌شود در کشِ اصلی ثبت می‌شود و کشِ اصلی **اول** خوانده می‌شود، پس مهر
منفی خودش سایه می‌شود و نیازی به invalidate دستی ندارد.

**۲) کشی که بعد از syscall خوانده می‌شود، کش نیست.**
`BlendWithBackground()` اول `ChartGetInteger(CHART_COLOR_BACKGROUND)` می‌زد و بعد
کش را می‌دید؛ پس کش هیچ‌وقت یک تماس ترمینال را حذف نمی‌کرد. هر بارِ HTF، سه-چهار
ترکیب رنگ دارد → ۷۰۰-۱۰۰۰ خواندن در هر draw کامل، برای رنگی که فقط با تغییر
پراپرتی چارت عوض می‌شود. قانون: **خواندن گران را به یک مالک ببر و یک‌بار در هر
«پاس» انجام بده**، نه یک‌بار در هر فراخوانی.

**۳) هر فیکس پرفورمنس باید قبل از دیدن اثرش، فاز خودش را نام ببرد.**
`[W][PERF] OnInit (ok) took 3219..4110ms (budget 150ms)` روی همهٔ attach/تعویض‌TF —
درحالی‌که لجر تیک همهٔ فازها را صفر گزارش می‌کرد، چون لجرِ فریم داخل
`RedrawAllObjects` زندگی می‌کند و نمی‌تواند یک «توالی init» را ببندد. پس مسیر init
هم لجر خودش را دارد (`settings/hist/base/atr` + تفکیک `ind=/ui=`). درس P-PERF-05
تکرار شد: **چیزی را که لاگ می‌تواند نام ببرد، دوباره حدس نزن.**

**چک‌لیست این خانواده:** جدول منفی قبل از پروب خوانده شود؛ miss ثبت شود؛
`CacheClear` و `OnInit` مهرها را پاک کنند؛ بی‌اعتبارسازی O(1) (بدون sweep)؛ هر
syscall گران یک مالک و یک نوبت در «پاس» داشته باشد؛ گاردها روی **ورودی‌ها** باشند
نه روی زمان؛ و هر مسیر کند (init/event/تیک) فاز خودش را در لاگ بگوید.
گیت: `python tools/probe-budget-audit.py` (+`--selftest` با ۱۰ خرابی کاشته‌شده).

## §14 — لاگ فاز را نام برد، و آن فاز یک «حلقهٔ خودتخریب» بود (2026-09-12، P-PERF-11..12)

**۱) لجرِ فاز کار کرد؛ پس دوباره حدس نزن، لجر را ریزتر کن.**
خط لاگ دقیقاً همان چیزی را گفت که باید: `[ind=3828 ui=0 settings=15 hist=16
base=3797 atr=0]` — یعنی از ۳۸۲۸ms، فقط ۳۱ms در settings+hist+atr است و **۳۷۹۷ms
تمامش داخل `InitializeBasePriceSystem()`**. یک لجر دوم (file / migrate / rebuild /
cleanup) داخل همان فاز گذاشتم تا قدم بعدی با شاهد برداشته شود، نه با خواندن کد.

**۲) حدسِ محتوایی روی فایلی که خودمان می‌نویسیم، همیشه غلط می‌شود.**
آن فاز تشخیص می‌داد فایل «فرمت قدیم» است اگر **هر** رکوردش ساعت `>= 22:00`
داشته باشد. اما یک سشن کاملِ این برنامه از `00:xx` تا `23:xx` است ⇒ فایلِ روزِ خودش
همیشه یک بلاک `22:xx` دارد ⇒ هر init فایل معتبرِ خودش را پاک می‌کرد، `loadedCount`
صفر می‌شد و کل روز از دادهٔ M30 بازسازی می‌شد؛ و بازسازی همان `22:xx` را دوباره
نوشت ⇒ **دفعهٔ بعد دوباره migrate**. شاهدِ روی دیسک: `base_price_history_XAUUSD_2026_255.txt`
با ۴۸ رکورد `00:28..23:58` و mtime دقیقاً برابر لحظهٔ OnInit. درس: **وضعیتِ یک
فایل را از محتوایش حدس نزن؛ خودش باید خودش را توصیف کند** (اینجا: خط اول
`#FMT=<version>|GMT`، لودر آن را برمی‌دارد و نسخه را گزارش می‌دهد، تصمیم migrate
یک مقایسهٔ نسخه است).

**۳) یک باگ، دو رو.** همان بررسی نشان داد چرا برچسب‌های بلاک ساعت‌ها جلوتر از
بلاک GMT جاری بودند: `FindCurrentSessionStart()` زمان‌های **سرور** را که از
`CopyTime`/`iTime` می‌آید با برشِ `TimeGMT()` مقایسه می‌کرد؛ پس «شروع روز» یک
لحظهٔ سرور بود که به‌عنوان GMT برگردانده می‌شد و پایین‌دست یک‌بار دیگر تبدیل
می‌شد. قانون: **یک تابع هرگز دو فریم زمانی را قاطی نکن؛ یا سرور یا GMT — و اگر
هر دو لازم است، دقیقاً یک‌بار و در یک نقطه تبدیل کن.**

**۴) هر مهر/کش باید خودش بی‌اعتبار شود، نه با یادآوری جای دیگر.**
خرابیِ کامپایل (`CacheAbsentResetAll - function not defined`) نشان داد یک مسیر پاک‌سازی
که فقط با «یادت باشد اینجا صدا بزنی» کار می‌کند، دیر یا زود جا می‌ماند. الان مهرِ
«نبودن» فقط با `g_drawGeneration` زنده سنجیده می‌شود (مهرِ کهنه = اسلاتِ آزاد) و
`CacheAbsentResetAll()` فقط استمپ‌های عددی را صفر می‌کند (صفر هیچ‌وقت نسلِ زنده
نیست) — یعنی درستی از ساختار داده می‌آید، نه از ترتیب صدا زدن‌ها.

**چک‌لیست این خانواده:** هیچ حدسی از محتوای فایل/زمان (خودِ فایل نسخه‌اش را بگوید)؛
رکوردهای متادیتا (`#...`) در لودر حذف شوند و به آرایهٔ داده نرسند؛ مسیر نوشتنِ
جدید هم مهر بزند، وگرنه بار بعد خودش را legacy می‌بیند؛ یک تابع = یک فریم زمانی؛
و اگر فازی چند صد میلی‌ثانیه بود، **قبل از فیکس** آن را چهار تکه کن و در لاگ نام ببر
(تا فیکسِ بعدی هم قابل ابطال باشد). گیت: `python tools/probe-budget-audit.py`
(+`--selftest` با ۱۷ خرابی کاشته‌شده، همه گرفته می‌شوند).

## §15 — روی ضعیف‌ترین کامپیوتر: هر پرسوجو و هر پیمایش چارت باید دلیل داشته باشد (2026-09-12، P-PERF-13..15)

**۱) جدول «نبودن» فقط برای نوشتن نیست؛ برای حذف هم هست.**
`CleanupSurplusPipeline()` در هر فریم **هفت خانوادهٔ نام** را برای «اضافه‌ها» می‌پیماید و هر
پیمایش تا ۶ خطا ادامه می‌دهد؛ و هر خطای یک خانوادهٔ zone **هفت نام** می‌پرسد
(`base/_Top/_Bottom/_B_*`). همهٔ این پرسوجوها با `verify=true` می‌رفتند، یعنی هر کدام
یک `ObjectFind` ترمینالی — روی چارتی که هزاران آبجکت خودمان دارد، یعنی چند صد تماس
بی‌دلیل در هر فریم، برای همیشه. درست همان چیزی که کاربر می‌بیند: «اندیکاتور بی‌دلیل
سطوح را حذف/لمس می‌کند». راه‌حل: `DeleteIndicatorObjectManaged` **قبل از هر چیز**
جدول «نبودن» را می‌خواند، و اگر probe با `verify=true` خطا داد (یعنی ثابت شد آن نام
روی چارت نیست) **miss را ثبت می‌کند**. پس فریمهای بعدی O(1) هستند و اگر آن نام بعداً
ساخته شود، کش اصلی اول خوانده می‌شود و مهر را سایه می‌اندازد — همان invariant P-PERF-07.

**۲) کاری که به «تغییرِ ورودی» وابسته نیست، باید بعد از گاردِ تغییر بنشیند.**
بلوک مهاجرتِ zone (پاک‌کردن `_Top/_Bottom` قدیمی و باقیماندهٔ FILLED/EMPTY) **قبل** از
چک «چیزی عوض نشده؟ return» بود؛ یعنی zoneِ ثابت در هر فریم یک `ObjectFind` می‌پرداخت
(و گاهی ۴ تای دیگر). چنین هزینه‌ای در شمارشِ **نوشتن** دیده نمی‌شود — probe نوشتن نیست.
قانون: **همهٔ کارهای تشخیصی/مهاجرتی را بعد از گارد بگذار**؛ فقط مسیرِ واقعاً «بساز/تغییر بده»
حق دارد probe بزند.

**۳) پیمایش کل چارت برای پیدا کردن چند آبجکت خودمان را با API دست‌ای عوض کن.**
`DeleteHTFCandles()` اول `ObjectsTotal(OBJ_RECTANGLE)` و بعد `ObjectsTotal(OBJ_TREND)`
می‌گرفت و `ObjectName()` را برای هر آبجکت صدا می‌زد تا مالِ خودش را پیدا کند — روی چارتی
که هزاران مستطیلِ zone خودمان دارد، یعنی O(همهٔ آبجکت‌ها)، و دقیقاً روی مسیرِ `OnDeinit`
(هر تعویض TF، ~۳۰۰ms) و روی هر early-out حالت پایدار HTF اجرا می‌شد. جایگزین مستند:
`ObjectsDeleteAll(chart, prefix)` — یک تماس، اسکن داخل ترمینال، همان مجموعهٔ دقیق
(هر دو حلقه در واقع «نام با این پیشوند شروع شود» را تست می‌کردند).
منبع: docs.mql4.com/objects/objectsdeleteall.
نکتهٔ فرعی: MQL4 اعلانِ پیشین (prototype) تمیز ندارد؛ `bool f();` یک warning 46
(«no #import declaration») می‌دهد — پس تابع را **قبل از** مصرف‌کننده‌اش تعریف کن.

**۴) هزینهٔ باقی‌مانده را قبل از فیکس نام ببر.**
دو لکهٔ داغِ باقی‌مانده در لاگ (`OnDeinit=297ms` یک عدد، و `id=1 [indicator=0 ui=485..578]`
که فقط «نیمه UI» را می‌گفت) قبل از هر فیکسی باید تفکیک شوند: ترداون به شش فاز
(`pnl/menu/htf/save/cleanup/handler`) و حرکت ماوس به سه مالک (`ring/panel/hold`).
هر دو فقط وقتی از بودجه بگذرند چاپ می‌شوند.

**چک‌لیست برای کامپیوتر ضعیف:** هیچ probe برای نامی که در این نسل ثابت شده نیست؛
هیچ پیمایشِ کل‌چارت وقتی API پیشوندی وجود دارد؛ هیچ کار تشخیصی قبل از گاردِ تغییر؛
هر مهر/جدول خودش بی‌اعتبار شود؛ و هر مسیرِ گران فاز خودش را در لاگ بگوید.
گیت: `python tools/probe-budget-audit.py` (+`--selftest` با ۲۴ خرابی کاشته‌شده، همه گرفته می‌شوند).

---

## §16 — «یک‌بار در جای درست محاسبه کن»: چیدمان UI، تولتیپ، و هندسهٔ مرحله‌ای (2026-09-12، P-PERF-16..18)

**۱) هر hit-test نباید از ترمینال اندازه بخواند، و نباید برای هر آیتم دوباره fit کند.**
`CircItemAt()` به‌ازای هر آیتمِ رینگ یک‌بار `CircLayout()` را صدا می‌زد، `ToolsItemAt()`
به‌ازای هر تایل `SubPlaceItem()`، و `CircLayout()` خودش `CircFitRadius()` را می‌صدا زد که
روی **همهٔ** آیتم‌ها sin/cos می‌زند. هر دوی این‌ها هم `CHART_WIDTH_IN_PIXELS` و
`CHART_HEIGHT_IN_PIXELS` را مستقیم از ترمینال می‌خواندند. یعنی یک سؤال سادهٔ
«کدام دکمه زیر نشانگر است؟» = **۱۶ تا ۴۰ تماس ترمینالی + O(RING_COUNT²) مثلثات**،
و این در هر حرکت ماوس تکرار می‌شد. قرارداد استاندارد UI toolkitها: یک
**layout cache** با دو نقطهٔ invalidation — رویداد تغییر اندازهٔ چارت و یک
safety refresh در تایمر. بعد از این، یک hit-test صفر تماس ترمینالی دارد و
fit برای کل رینگ یک‌بار حساب می‌شود (memoised روی `(cw,ch,ox,oy)`).
نکتهٔ مهم: قانون «یک مالک برای مستطیل چارت» است، نه «مالکِ فلان ماژول»؛ پس دو
مسیرِ rate-بالای پنل‌ها (`PnlClampSpot`, `BkStripFollow`) هم به **همان** خواننده منتقل شدند.

**۲) «پنهان‌کردن» با حذف آبجکت + `ChartRedraw()` گران‌ترین راه است؛ parking ارزان‌ترین.**
`CircTipHide()` تولتیپ را **حذف** می‌کرد و `ChartRedraw()` می‌زد؛ بعد hover بعدی مجبور
بود همه را از صفر بسازد. و دیده‌شدنش با `ObjectFind(0, CircTipBg())` در هر حرکت ماوس
فهمیده می‌شد. این هزینه در هیچ شمارندهٔ نوشتنی دیده نمی‌شود: یک delete+create در شمارش
خنثی می‌شود و `ChartRedraw()` اصلاً نوشتنِ آبجکت نیست. راه‌حل: تولتیپ در
`CIRC_HIDE_POS` **پارک** می‌شود (همان ایدیومِ Tools fan)، و visibility یک
**متغیر حالت** است (`s_CircTipFeat`) نه probe. ضمناً بعد از نوشتن‌های پراپرتی که خودشان
چارت را dirty می‌کنند، `ChartRedraw()` اضافه است — حذف شد.

**۳) «مرحله‌ای‌کردن» نوشتن‌ها، محاسبهٔ تکراری را چند برابر می‌کند اگر فقط فریم‌ها را بشکنی.**
P-PERF-06 یک rebuild بعد از پاک‌سازی را به چهار فریم (lines/zones/labels/block) شکسته بود
تا سقف تماس‌های هر فریم پایین بیاید — ولی هر فریم دوباره `ExecutePipeline()` را صدا
می‌زد، پس `CalculateLevels → Classify → BuildZonesAndLines` با ورودیِ **بی‌تغییر** چهار
بار اجرا می‌شد. حالا نتیجه زیر یک **کلید** ساخته‌شده از همهٔ ورودی‌هایی که آن سه مرحله
می‌خوانند کش می‌شود (مُد/پیشوند، قیمت مرکز، stepها و مُدشان، classify+lsFirst، سقف‌ها،
پنجرهٔ cull، سوییچ تریگر، base multiplier، همهٔ فیلدهای ظاهری zone/fallback/midpoint،
رنگ/شفافیت/تول‌گلِ زندهٔ خط، پرچم drag قیمت دلخواه، و جدولِ structure-interval).
هر تغییری در این‌ها کلید را عوض می‌کند، پس کش فقط می‌تواند برای هندسهٔ **تغییرنکرده**
جواب بدهد؛ و اگر محاسبه هیچ سطحی تولید نکرد، کش **دراپ** می‌شود نه اینکه خالی ذخیره شود.

**۴) در کلید، «اثبات» کن نه «ادعا».**
ترمِ کلید `ArraySize(g_cachedIntervals)` همیشه ۵ بود: یک عددِ ثابت که هرگز نمی‌تواند
عوض شود، پس هیچ‌وقت چیزی را نمی‌گیرد. جایگزین شد با **خودِ پنج مقدار** جدول. هر ترمی
در یک کلید/گارد باید بتواند در جهانی که ادعای نامزدش عوض شده، **fail** کند — وگرنه
سرگرمی است. (`--selftest` دقیقاً همین را می‌سنجد: اگر بتوانی خرابی را بکاری و گیت
نگیرد، آن ترم تزئینی است.)

**چک‌لیست:** اندازهٔ چارت فقط از یک مالک؛ fit/هندسهٔ مشترک memoise شود؛ پنهان‌کردن =
parking نه حذف؛ visibility = حالت نه probe؛ بعد از نوشتن پراپرتی، repaint اجباری نزن؛
در pipeline مرحله‌ای، محاسبهٔ مشترک را یک‌بار بساز و زیر کلیدِ «همهٔ ورودی‌ها» بگذار؛
و هر ترمِ کلید/گارد باید قابل‌اب‌اطال باشد.
گیت: `python tools/probe-budget-audit.py` (+`--selftest` با ۳۵ خرابی کاشته‌شده، همه گرفته می‌شوند).

---

## §17 — «یک کشِ نگهداشتهشده که کسی صدایش نمیزند» و «قفل بهازای هر خواندن» (2026-09-12، P-PERF-19..20)

**۱) ماکروی متنی، مخفیترین مسیر داغ است.**
هفت متغیر «گلوبال» قدیمی (`g_basePriceCached`, `g_historyCount`, `g_referenceM1Power`,
`g_systemInitialized`, `g_lastHistoryDay`, `g_lastHistoryYear`,
`g_lastProcessedBlockTimestamp`) در واقع **ماکرو** بودند و به `GetSymbolStateIndex()`
باز میشدند. آن تابع یک lookup نیست؛ یک **میوتکس با گلوبالویریبلهای ترمینال** است:
`Symbol()`، دو `GlobalVariableCheck`، `TimeCurrent()`، دو `GlobalVariableSet`، دو
`GlobalVariableTemp`، دو `GlobalVariableDel` و یک حلقهٔ retry — یعنی **حدود ۹ syscall
گلوبالویریبل بهازای هر خواندن**، در ~۱۱۵ محل. و چون ماکرو است، هیچ `grep`ی برای
«تابع گران» آن را نشان نمیدهد: در شرط حلقهها هم نشسته بود
(`for(i = 0; i < g_historyCount; i++)` = یک قفل کامل بهازای هر آیتم).
درس: **قبل از هر بهینهسازی، ببین ماکروها به چه چیزی باز میشوند**؛ یک ماکرو میتواند
یک تابع گران را در صد جا پنهان کند.

**۲) کشی که ساخته و نگهداری میشود ولی صدا زده نمیشود، بدتر از نبودنش است.**
`GetCachedSymbolStateIndex()` وجود داشت، کشش هم درست نگهداری میشد (حتی
invalidateش در LRU وصل بود) — و **هیچ caller ای نداشت**. یعنی کار اضافه میشد و
استفاده نمیشد. این همان کلاس P-PERF-08 است (کشی که *بعد از* syscall چک میشود).
گیت جدید دقیقاً همین را میگیرد: **هر ترمی که مقصدش «un*Cached*» است، خرابی است.**

**۳) عددِ درست در جای اشتباه: زیرفاز را نام ببر.**
`migrate=312ms rebuild=0ms (fileStamp=5)` یعنی «فایل مهرشده، هیچ بازسازیای نبود، ولی
۳۱۲ میلیثانیه رفت». یک مهر برای چهار کار مختلف (dedup / migrate-format / save /
restore) گذاشته بودم؛ پس خودِ مهر بیفایده بود. قاعده: **مهر را جایی بگذار که به یک
سؤال جواب بدهد، نه جایی که مجموع را بپوشاند.** حالا `load=[dedup= fmt= save= restore=]`.

**۴) «تغییر لازم بود؟» را از شمارش استنتاج نکن، از محتوا بپرس.**
`needsSave = (newCount != count || migratedCount > 0)` یک **استنتاج** بود: یک پاس
میتواند «رفع تغییر» گزارش کند ولی خروجی بایتبهبایت یکسان بسازد؛ و بعد فایل روی هر
attach بازنویسی میشود. مسئلهٔ کاربر («چرا بیدلیل آپدیت میکند؟») همین است.
جایگزین: یک مقایسهٔ سادهٔ محتوا (`HistoryContentDiffers`) — ۴۸ رشته در برابر یک flush
دیسک. **هر نوشتنی باید دلیلِ محتوایی داشته باشد، نه دلیلِ شمارشی.**

**۵) کش را با همان شرطی که بار اوّل انجام شد، اعتبارسنجی کن.**
resolver کششده فقط وقتی جواب میدهد که **هم** نام سیمبل عوض نشده باشد **و** اسلات
بهخاطرآوردهشده هنوز داخل بازهٔ زندهٔ آرایه باشد؛ و تنها عملیاتی که آرایه را
جابهجا میکند (LRU eviction) کش را صفر میکند. ضمناً این کش «بهینهسازی» است نه
«دفترداری»: فقط روی مسیر miss شمرده میشود، پس مسیر سریع صفر هزینهٔ اضافی دارد.

**چکلیست:** ماکروها به چه چیزی باز میشوند؟ آیا هر کش caller دارد؟ کدام syscall در هر
خواندن تکرار میشود؟ هر مهر به یک سؤالِ مشخص جواب میدهد؟ هر نوشتن دلیلِ محتوایی
دارد؟ کش با چه شرطی invalid میشود؟
گیت: `python tools/probe-budget-audit.py` (+`--selftest` با ۴۱ خرابی کاشتهشده، همه گرفته میشوند).

---

## §18 — «یک کلید باید فقط خانوادهٔ خودش را عوض کند» — جداسازی خانوادهها (2026-09-12، P-PERF-21..22)

**۱) سوئیچی که یک خانواده دارد، نباید به مسیر «تعمیر کل چارت» وصل شود.**
کلید T (اورلی تریگر) در سه جا به مسیر تعمیرِ Global وصل بود:
- `levelSig` — یعنی **امضای توپولوژی** — شامل `IsTriggerLevelsEnabled()` بود ⇒ هر بار T،
  `shouldClearLevels` میشد و `ClearAllLevels()` **همهٔ** سطوح/زون/لیبل را حذف میکرد؛
- هر سه نقطهٔ تاگل (`T`، حلقهٔ منو، کارت TRIGGER) `g_forceClearOnNextDraw = true` میزدند؛
- `PipelineGeometryKey` هم روی همان فلگ کلید میخورد ⇒ حتی بدون حذف، کل هندسه دور ریخته
  و دوباره محاسبه میشد.

و هیچکدام لازم نبود: آن فلگ **فقط** در `RenderZones` خوانده میشود
(`zones[i].isTrigger && !triggerEnabled`). نه `CalculateLevels`، نه `BuildZonesAndLines`،
و حتی دو helperی که آن را بهعنوان پارامتر میگیرند (`GetPathForLevelOptimized`،
`GetZoneColorForLevel`) هیچوقت نگاهش نمیکنند. **قاعده: امضای «توپولوژی» فقط ورودیهای
هندسه را دارد؛ آنچه فقط تصویر را عوض میکند در امضای «رندر» مینشیند.** نتیجه: T حالا
یک re-render است، نه یک wipe.

**۲) پارامتری که در بدنهٔ تابع استفاده نمیشود، یک رسوبِ خطرناک است.**
`triggerEnabled` در دو helper پارامتر بود و در بدنه هیچجا خوانده نمیشد (کامنتها هم
میگفتند «باید مستقل باشد»). این «پارامترِ مرده» بود که در سه کلید/امضا کپی شده بود و
یک حذف جهانی میساخت. **پارامتر بیاستفاده را حذف کن یا در امضا نگه ندار.**

**۳) در یک کش، «ورودیهایی که نتیجه را میسازند» را باید اثبات کنی، نه به گاردِ دیگری تکیه کنی.**
هندسهٔ کششده **رنگ زون** را هم در خود دارد (`ClassifyLevels` → `zoneColor` از
`GetZoneColorForLevel`/`GetTriggerRenderColor`)، ولی هیچیک از ورودیهای آن رنگ در کلید
نبود. استال شدنش دیده نمیشد چون هر ویرایش رنگ، `levelSig` را عوض میکرد و wipe میزد —
یعنی **درستیِ کش به گاردِ دیگری وابسته بود**. حالا خودِ کلید آن ورودیها را نام میبرد.

**۴) پیمایش کل چارت برای نوشتنِ یک ماسک، غلط است — و راهش از قبل وجود داشت.**
کلید L با `ObjectsTotal(0,-1,-1)` + `ObjectName` + `ObjectGetInteger` برای **هر آبجکت
چارت** (مال ما، مال اندیکاتورهای دیگر، مال خود MT4) یک ماسک مینوشت. تابع درست
(`SetAllLineObjectsVisibility`) در `VisibilityManager` بود، **هیچ caller نداشت**، و
دقیقاً همان کار را روی **کش آبجکت** انجام میداد. این سومین نمونهٔ همین کلاس در این
کدبیس است: helper نوشته و نگهداری شده و بعد نادیده گرفته شده.
**همیشه قبل از نوشتنِ یک walk جدید، بپرس: آیا این مالک از قبل وجود دارد؟**

**۵) استثناهای یک کلید باید با خودش منتقل شوند.** وقتی L را به walk کشی منتقل کردی،
استثناهای خودش (بوردرهای `_B_*` مال کلید F؛ ریهای `_BK_` هرگز خط نیستند) هم باید
منتقل شوند، وگرنه یک خانواده روی دیگری اثر میگذارد — همان چیزی که کاربر گزارش داد.

**چکلیست:** هر تاگل کدام خانواده را دارد؟ آیا در امضای توپولوژی است یا رندر؟ آیا پارامتر
مرده در امضاها هست؟ کش کدام ورودیها را باید اثبات کند؟ برای هر walk جدید، آیا مالکِ
کش/جدولِ از قبل موجود وجود دارد؟ استثناهای کلید همراهش منتقل شدهاند؟
گیت: `python tools/probe-budget-audit.py` (+`--selftest` با ۴۹ خرابی کاشتهشده، همه گرفته میشوند).

## §19 — «چرا روشن/خاموش کردن سطوح اینقدر طول می‌کشد؟»: امضا باید فقط ورودی‌هایی را نام ببرد که مراحلِ خودش می‌خوانند (2026-09-13، P-PERF-23..26)

**۱) یک تاگلِ ظاهری نباید «پاک‌سازی کل چارت» باشد.** چهار ترمِ ظاهری
(`inpShowMidZones`, `inpMidZoneStyle`, `inpMidZoneTransparency`, `inpMidZoneHeightPercent`)
داخل `levelSig` — یعنی امضای **توپولوژی** — بودند. هر تغییرشان `levelTopologyChanged` می‌شد،
`shouldClearLevels` را می‌گرفت، `ClearAllLevels` همهٔ خط‌ها/زون‌ها/لیبل‌ها را حذف می‌کرد و
بازسازی چهارفریمی (با گیت ۲۰۰ms هر فریم) از نو شروع می‌شد — برای سوییچی که فقط تصمیم می‌گیرد
زون‌های میانی **رنگ شوند یا نه**. راه‌حل: توپولوژی و ظاهر را جدا کن (`levelLookSig`)، و ترم‌های
ظاهری را **به امضای رندر منتقل کن، نه حذف** — وگرنه محاسبه می‌شود و هرگز رنگ نمی‌شود.

**۲) امضا فقط ورودی‌هایی را نام ببرد که مراحلِ خودش می‌خوانند.** `g_linesVisible` در کلیدِ هندسه
بود، با این توضیح که «ClassifyLevels ظاهر خط و تاگل L را می‌خواند». **غلط بود**: grep نشان می‌دهد
این فلگ در کل فایل دو مصرف‌کننده دارد — خطِ رنگ‌آمیزی (`lineTf`) و خودِ کلید. یعنی هر تاگل، کل هندسه
را دور می‌ریخت و لیست‌های **بایت‌به‌بایت یکسان** را دوباره می‌ساخت. بدتر: یک چرخهٔ قبلی همین را
به‌عنوان «الزام» در گیت نوشته بود؛ **گیت، باگ را تأیید می‌کرد.** درس: گیت باید قاعده را بسنجد
(«ورودی‌های واقعیِ مرحله»)، نه یک رشتهٔ خاص را.

**۳) چرا عدد‌ها اشتباه خوانده شدند: لاگِ ناخوانا.** دو چرخه، جهش‌های ۴۸۵–۵۷۸ms را «مسیر حرکت ماوس»
ثبت کردند و فیکس‌ها را به hover نشانه گرفتند. id خام چاپ می‌شد. این بار **از کامپایلر پرسیدیم**
(probe با برخوردِ case): `OBJECT_CLICK=1` و `MOUSE_MOVE=10`. پس `id=1` یک **فشردنِ دکمه** است —
همان «تاگل کردن یک سوییچ نیم‌ثانیه طول می‌کشد». حالا لاگ نام می‌نویسد
(`P4EventName`)، و خودِ کلیک به سه مالک شکسته می‌شود (`button/panel/apply`).
**هرگز یک لاگ ننویس که خواننده باید عددها را از حفظ بداند.**

**۴) یک عملِ گسسته باید «همین حالا» رنگ شود.** هر تاگل به `ThrottledChartRedraw()` ختم می‌شد،
که داخل پنجرهٔ ۱۰۰ms **هیچ‌وقت رنگ نمی‌کند** (و در حالت hidden هم همیشه برمی‌گردد). فریمِ خودِ
رویداد قبلاً رنگ کرده بود، پس هیچ کاری معلق نمی‌ماند؛ و فریمِ تیک بعدی در
`if(!hasPendingWork) return;` خارج می‌شود: بازخورد تا تیک بعدیِ بازار معطل می‌ماند.
یک مالک: `RepaintForDiscreteAction()`. اجبارِ رنگ فقط به یک دلیل امن است و گیت همان را اثبات
می‌کند: dispatcher فقط از ماژول‌های UI قابل دسترسی است، هرگز از تیک. رنگِ **درگ** باید throttled بماند.

**۵) ثابت کن کدام ترم عوض شد، حدس نزن.** `frameSig` یک رشتهٔ تخت بود، پس نمی‌شد پرسید «چه چیزی
عوض شد؟». حالا امضا دو تکه است: `frameCore` (هرچه رندر می‌سازد) + ترمِ ماسک. اگر core یکی باشد و
امضای کامل نه، **اثباتاً** فقط ماسک عوض شده ⇒ رندر لازم نیست (پیمایش ماسک قبلاً نوشته، و هر خطِ
تازه‌ساخته‌شده فلگِ زنده را در لحظهٔ ساخت می‌خواند). فریمِ skipشده هم باید **مهر** بزند، وگرنه
فریمِ ثابتِ بعدی دوباره رندر می‌کند. پاک‌سازی و فریم‌های مرحله‌ای از این مسیر مستثنا هستند.

**چک‌لیست:** کدام سوییچ‌ها مالک کدام خانواده‌اند؟ آیا ترمی در امضای توپولوژی مانده که توپولوژی
نیست؟ آیا ترمی از کلیدِ هندسه حذف شده که مرحله‌ای می‌خواندش؟ بازخوردِ عملِ گسسته کجا رنگ می‌شود؟
لاگ نام می‌نویسد یا عدد؟ کدام ترم عوض شد — اثبات شده یا حدس؟
گیت: `python tools/probe-budget-audit.py` (+`--selftest` با ۶۱ خرابی کاشته‌شده) و
`python tools/write-budget-audit.py` (+`--selftest` با ۳۶ خرابی).

## §20 — «با هر تیک عوض نمی‌شود تا تایم‌فریم را عوض کنم»: تفکیکِ «محتوا» از «تصویر» — و کلیدِ سومی که هیچ‌کس نمی‌شمرد (2026-09-13، P-PERF-27..30)

**سؤال کاربر:** «ریل‌تایم توی همون تایم خاموش و روشن نمی‌شه، باید تایم عوض کنم که اعمال بشه.»

سه لایه از هم جدا شد، و هر سه یک چیز را نشان می‌دادند: *ما یک سؤال را به جای دو سؤال جواب می‌دادیم.*

### ۱) لاگ بالاخره مالکِ teardown را گفت — و آن مالک، «هیچ‌کاری» بود

```
[W][PERF] OnDeinit breakdown: pnl=15 menu=0 htf=0 save=78 cleanup=63 handler=16
```
۱۴۱ms از ۱۷۲ms روی چارتی که کسی ویرایشش نکرده بود. `RuntimeSettingsSaveOverrides` هر ۹۷ کلید را می‌نوشت و بعد `GlobalVariablesFlush` می‌زد؛ مستندات رسمی این تابع (`docs.mql4.com/globals/globalvariablesflush`) می‌گوید: «forcibly saves contents of **all** global variables to a disk» — یعنی کلِ جدولِ ترمینال، نه فقط کلیدهای ما. و teardown این کار را **سه بار** می‌کرد.

**درس:** «ذخیره‌ی محتاطانه» بدون *تشخیص تغییر* یعنی پرداختِ هزینه‌ی کامل برای چیزی که همان‌جاست. راه‌حل، یک **سایهٔ نوشتن** (write shadow) است: نوشتن‌ها در این تابع ترتیب ثابت دارند، پس یک شمارنده‌ی slot کلیدِ پایدار می‌دهد و لازم نیست فهرست دومی از نام‌ها بسازیم که با نویسنده از sync خارج شود. مقدارِ برابر ⇒ صفر تماس ترمینال، و flush فقط وقتی که واقعاً چیزی نوشته شده. و نکته‌ای که کل طرح را قابل‌اتکا می‌کند: **هر چیزی که آن کلیدها را حذف می‌کند باید سایه‌ها را باطل کند** (`ClearAllGVs` → `GVShadowsInvalidate`)، وگرنه گارد با کلیدهایی مقایسه می‌کند که وجود ندارند و نوشتنِ بازگرداننده را رد می‌کند.

### ۲) بزرگ‌ترین توقفِ لاگ، یک باگِ «مقدار اولیه‌ی صفر» بود

```c
static double s_lastVisibleMin = 0;   // و مقایسه با 0
if(!sizeChanged && !viewportChanged) return;
```
روی **اولین** رویدادِ هر instance، `MathAbs(visibleMin - 0) > point` بی‌قید‌و‌شرط درست است. پس `viewportChanged` از روی **ساختار** true بود و یک `RedrawAllObjects(false)` کامل داخل رویداد اجرا می‌شد. لاگ خودش این را فریاد می‌زد و کسی نمی‌خواند:

```
initialized 18:53:34.184
[W][PERF] chart event id=9 [indicator=3375 ui=0] took 3375ms   ← ۱۸:۵۳:۳۷.۵۵۵
```
یعنی **مدتِ رویداد = فاصله از init**. ۵۳ بار در یک روز، ۳.۴ تا ۴.۱ ثانیه.

**درس:** snapshot را از «صفر» مقداردهی نکن؛ از **واقعیتِ همان لحظه** پر کن. یک `s_ccPrimed` این را درست کرد.

### ۳) باگِ اصلیِ گزارش کاربر: یک فلگ، چهار نویسنده، یک صاحبِ ماسک

`g_linesVisible` در چهار جا عوض می‌شد و فقط یکی (کلید L) ماسک را می‌نوشت. سه جای دیگر — دو ردیفِ SHOW LINES در کارت‌ها و reset کارخانه — فلگ و mirror و کلیدِ ذخیره‌شده را می‌گذاشتند و می‌رفتند. این تا دو چرخه قبل **دیده نمی‌شد** چون هر فریم کلِ خانواده را دوباره می‌کشید. ولی P-PERF-25 رد‌کردنِ رندر را **قابل‌اثبات** کرد:

> رندر فقط وقتی رد می‌شود که `frameCore == s_lastFrameCore && frameSig != s_lastFrameSig` — یعنی اثبات شده که تنها تفاوت، ترمِ ماسک است — و بعد رندر نمی‌کند، چون **صاحبِ ماسک باید قبلاً نوشته باشدش.**

و کسی ننوشته بود. پس هیچ‌چیز عوض نمی‌شد تا یک تعویضِ تایم‌فریم کلِ چارت را از صفر بسازد. **دقیقاً جمله‌ی کاربر.**

**درس:** وقتی یک بهینه‌سازی را «قابل‌اثبات» می‌کنی، باید ثابت کنی که *پیش‌شرطش* هم برقرار است. P-PERF-25 یک اثبات درست داشت و یک پیش‌فرضِ غلط («صاحبِ ماسک نوشته»). و صاحبِ یک فلگ باید یکی باشد: `SetLinesVisible()` حالا حالت، mirror، کش، کلید و ماسک را با هم می‌نویسد.

### ۴) و همان باگ، یک لایه بالاتر: امضای هندسه نباید رنگ را وتو کند

`frameSig` به سؤالِ درست جواب می‌دهد: «آیا چیزی که هندسه را از آن *مشتق* می‌کنم عوض شده؟» ولی از همان امضا برای وتوی **رنگ** هم استفاده می‌شد — و رنگ، گلوبال‌های زندهٔ بیشتری می‌خواند که `frameSig` نامشان را نمی‌برد (سوییچ‌های structure، style/width/transparency خط، بوردرِ mid-zone، شفافیت تریگر، خط میانی، ظاهر custom price). هر ورودیِ جاافتاده = دکمه‌ای که تا تعویض تایم‌فریم کاری نمی‌کند.

راه‌حل، اضافه‌کردنِ ترم‌های بیشتر نیست (فهرست همیشه ناقص می‌ماند)؛ **جدا کردن دو سؤال** است:
- `frameSig` می‌پرسد «هندسه کهنه است؟»
- `g_renderAllNeeded` می‌گوید «کاربر درخواست رنگ کرد» — و فقط `applyRefreshFlags` (که هرگز از تیک یا درگ صدا زده نمی‌شود) آن را بالا می‌برد.

نتیجه: ویرایش کاربر همیشه رنگ می‌شود، ولی **یک** re-assert — چون خودِ رندرِ کامل‌شده فلگ را مصرف می‌کند. `mustRender` نام گرفت تا ترم‌هایش قابل‌ممیزی باشند، و گیت هر سه ترم را الزام می‌کند.

### ۵) گیت‌ها هم اشتباه نمی‌کردند — شکلِ قدیمی را نگه می‌داشتند

دو گیت روی این تغییر قرمز شدند، و هر دو حق داشتند که قرمز شوند: یکی دنبال `SetAllLineObjectsVisibility` **داخلِ بلاکِ کلید L** می‌گشت (یعنی همان باگ، در قالبِ assert)، و یکی امضا را با یک regex شل می‌سنجید که `!visOnly` را در ترمِ `(g_renderAllNeeded && !visOnly)` هم پیدا می‌کرد. هر دو به **حالتِ درست** بازنویسی شدند، و دو بذرِ کهنه هم خودشان `STALE SEED` دادند — یعنی خودِ ابزار هم می‌گوید «من دیگر نمی‌سنجم چیزی که فکر می‌کنم».

**درس:** گیتِ کهنه، بدتر از نبودِ گیت است: سبز می‌ماند و اعتماد می‌سازد. وقتی شکلِ کد عوض می‌شود، اول assert را به «حالتِ درست» بنویس، بعد ببین بذرها چه می‌گویند.

### نتیجه

Full و Lite: **۰ خطا / ۰ هشدار**. `probe-budget-audit` پاک (گروه‌های جدید `persist-write-shape` و `chart-change-

---

## §21 — «۵۳۱ میلیثانیه داخل رویداد» و «نخ ریسمانی که وجود ندارد» (2026-09-13، P-PERF-34/34b/35)

### سؤال

لاگ امروز — بعد از همهٔ چرخه‌های قبلی — دقیقاً یک مالک را نام برد:

```
[PERF] click breakdown: button=0ms panel=0ms apply=531ms
[PERF] owed frame ... (بعد از این چرخه)
```

سه فاز جدا شده بود تا معلوم شود کجاست. `button` و `panel` **صفر** بودند. یعنی جست‌وجوی دکمهٔ زیر ماوس
رایگان است و **تمام ۵۳۱ms کارِ رفرش** است. سؤال بعدی: چرا کارِ رفرش *داخل رویداد* اجرا می‌شود؟

### ریشه

`applyRefreshFlags` دیسپچرِ **مسیر رویداد** است (کلیک/هات‌کی/پایانِ ژست) و برای هر گروه فلگ سنگین
`RedrawAllObjects(true)` صدا می‌زد. پارامتر `force_redraw` **هر دو گیت** را دور می‌زند، پس کل بدنهٔ فریم
(بیس‌پرایس، ATR، `UpdateHistoricalValues`، `DrawMainLevels`، خانوادهٔ لیبل با `ClearAllLabels` + چهار
`Display*`، `RepositionAllOverlayLabels` و بلوک custom-price) **درون `OnChartEvent`** اجرا می‌شد.
یک بلوک ۵۳۱ میلی‌ثانیه‌ای داخل هندلر رویداد، همان فریزی است که کاربر حس می‌کند — و **همان کلاس نقصی**
است که قبیلتر فریم ۳۳۷۵ میلی‌ثانیه‌ای `CHART_CHANGE` را ساخت: کار به حسابِ رویداد نوشته می‌شد، نه فریم.

### درسِ بزرگ: گیتِ کهنه، خودِ باگ را نگه می‌دارد

دو گیت روی این تغییر **قرمز شدند و درست می‌گفتند**: یکی `g_buildStage != 0` را *داخل `OnTimer`*
می‌خواست — یعنی یکسان‌نگه‌داشتنِ همان شرطی که دو مالک داشت و باید حذف می‌شد (تایمر هم باید می‌فهمید فریم
«بدهکار» است، و دو شرطِ موازی از هم دور می‌افتند). دیگری نگهبانِ فریم داخل تایمر بود. هر دو به شکلِ
**درست** بازنویسی شدند: «مالک پمپ می‌کند، تایمر فقط بدهکار می‌کند». و دو بذرِ کهنه خودشان `STALE SEED`
دادند — دقیقاً همان چیزی که دستگاه باید بدهد.

### دربارهٔ «نخ چندریسمانی»

MQL4 **نخ ندارد** و هیچ ترفندی جایش را نمی‌گیرد: هر برنامه دقیقاً **یک** ریسهٔ سیستمی و یک هسته
The terminal multi-threaded است و *هر برنامه* نه. فقط دو راه خروج هست: **DLL** (که می‌تواند
ریسهٔ سیستمی بسازد) و **OpenCL** (که MQL5-only است، و در مستندات MQL4 جدا فهرست شده).

آنچه *هست* و همان چیزی است که لگ را برمی‌دارد، **هم‌روندی** است نه هم‌زمانی:
- یک صفِ کارِ مستقل که زیر یک بودجهٔ میلی‌ثانیه‌ای تخلیه می‌شود.
- **کارهای ارزان اول**، فریم آخر — تا فریم سنگین نتواند گرسنگیِ بقیه را بسازد.
- کاری که تمام نشد **همچنان بدهکار است**: نه چیزی برای rollback، نه درخواست دوباره.
- **Single-flight**: سه فشار سریع، یک فریم — نه سه.

و صادقانه در کد نوشته شد که این لایه **نمی‌تواند** یک بدنهٔ فریم را بشکند؛ شکستنِ کار
کارِ پایپ‌لاینِ مرحله‌ای است (چهار خانواده، چهار فریم). این لایه فقط **ترتیب و فاصله** می‌دهد.

### نتیجه

Full و Lite: **۰ خطا / ۰ هشدار**. `probe-budget-audit` پاک (**۷۷ بذر، همه گرفته شد**)،
`write-budget-audit` پاک (**۴۱ بذر**) — هر دو گیتِ کهنه به شکلِ درست بازنویسی شدند،
`zorder`/`gesture-budget`/`ui-text` پاک. مصنوعات: `Biotak Trigger TH3.ex4` و `... Lite.ex4`.

---

## §22 — «پاک‌سازی‌ای که با همان فلگی گارد شده که خانواده را خاموش می‌کند» (2026-09-13، P-PERF-36)

### گزارش کاربر
«سطوح با این دکمه خاموش و روشن نمی‌شه ریل تایم» + اسکرین‌شاتِ تولتیپ (پس کنترل مشخص است).

### ریشه — یک زنجیرهٔ کامل و قابل‌اثبات

```
حلقه CIR_ZONES  ->  g_showMidZones   (RuntimeSettings:503  #define inpShowMidZones g_showMidZones)
                ->  GetUnifiedZoneConfig().enabled
                ->  cfg.zonesEnabled
                ->  ساختِ زون‌ها گارد شده روی آن  (LevelPipeline 496/502/566/623/650)
                ->  پس zones[] خالی به RenderZones می‌رسد
                    (و RenderZones هیچ مسیر «حذف کهنه» ندارد؛ فقط لیستی را که گرفته می‌پیماید)
                ->  تنها حذف‌کنندهٔ آبجکت‌های زون: CleanupSurplusPipeline
                ->  و آن می‌خواند:  if(config.zonesEnabled) { CleanupSurplusObjects(..._Zone_...) }
```

**درسِ اصلی:** وظیفهٔ cleanup حذفِ چیزی است که رندرِ فعلی **تولید نمی‌کند** — و «خاموش»
دقیقاً حالتی است که در آن **همه‌چیز** اضافی است. گاردکردنِ حذف با فلگِ خاموش‌کننده یعنی حذف
دقیقاً در همان حالتی که لازم است **اجرا نشود**. نتیجه: سوییچ، *بازساختن* زون‌ها را متوقف
می‌کرد ولی هر آبجکتِ زونِ موجود سرجایش می‌ماند — و روشن‌کردن دوباره هم چیزی را عوض نمی‌کرد،
چون آن‌ها هیچ‌وقت نرفته بودند.

### چرا سوییچ‌های دیگر سالم بودند
فقط خانوادهٔ ZONES این نقص را داشت: cleanup خط‌ها/`_Midpoint_` هیچ‌وقت گارد نشده بود و
سوییچ‌های structure هم اصلاً حذف لازم ندارند (مسیر رنگ‌آمیزی). همین عدم‌تقارن توضیح می‌دهد
چرا کاربر بعد از فیکس شدن L و F، دقیقاً این دکمه را زد — و یک گیت هم به همین دلیل اضافه شد:
**«حذفِ یک خانواده نباید با فلگی که آن خانواده را خاموش می‌کند گارد شود». **

### فیکس
- `zoneCleanupFrom = config.zonesEnabled ? (maxLogicalStep + 1) : 0` — وقتی روشن است از
  بعد از آخرین گام (فقط آنچه کوچک شده)، وقتی خاموش است از **۰** (تمام خانواده اضافی است).
- همان گاردِ معکوس در مسیر بازنشستهٔ `DrawFactorLevelsAligned` (که امروز هیچ caller ندارد)
  هم برداشته شد — گاردِ غلط در کد مرده، مینِ ساعتی برای احیاکننده است.

### نتیجه
Full و Lite: **۰ خطا / ۰ هشدار**؛ دیپلوی تأیید شد. `probe-budget-audit` پاک،
`--selftest` = **۸۰ بذر، همه گرفته شد** (سه بذر جدید: گاردِ دوباره‌پیچیده، پنجره‌ای که فقط
حالت روشن را پوشش می‌دهد، و گاردِ معکوسِ مسیر مرده). `write-budget-audit` ۴۱/۴۱،
`gesture-budget` ۱۸/۱۸، `ui-text` ۹/۹، `zorder` ۹/۹.

---

## §23 — «تعویض تایم‌فریم انگار سطوح را از نو می‌سازد» — و باگی که لاگ قبل از من دید (2026-09-13، P-PERF-35b/37)

### دو عدد از بیلد جدید (فقط شاهد، بدون حدس)
```
[W][PERF] owed frame (chart-event) waited=78ms body=0ms stage=0 clear=0
[W][PERF] coop job=obj-cleanup waited=265ms ran=0ms stillOwed=0
[W][PERF] OnDeinit reason=3 took 157ms ... save=78ms cleanup=47ms handler=16ms
```
۱) `body=0ms` یعنی P-PERF-34 نشست: فشردن دیگر صفر کارِ فریم است؛ آن ۵۳۱ms رفته. ۵۳۱ms «لگ»
به ۷۸ms «تأخیرِ زمان‌بندی» تبدیل شد.

۲) اما `waited=265ms` یک باگِ **تازه‌ی خودم** را لو داد: `COOP_JOB_HEAVY_FRAME` آی‌دی‌اش **۱**
است، و حلقه آی‌دی‌ها را می‌پیمود — پس بازسازی **اول** اجرا می‌شد، کل برش بودجه را می‌خورد و
کارهای خانگی را یک دورهٔ تایمر بدهکار می‌گذاشت. کامنت خودم گفته بود «ارزان‌ها اول» و کد
عکسش را می‌کرد. **هر بار که ترتیبِ کارها را می‌نویسی، آن را قابل‌اثبات کن، نه ضمنی.**

### دربارهٔ «هزینهٔ صفر تعویض تایم‌فریم»
MT4 در تغییر پریودِ چارت **مجبوراً** اندیکاتور را unload و reload می‌کند — و لاگ این را
قطعاً نشان می‌دهد: `uninit reason 3` (یعنی `REASON_CHARTCHANGE`) ده‌ها بار، هر بار یک
teardown + init کامل. پس «صفر» به‌معنای لغوی ناممکن است. آنچه ممکن است
**حذفِ آشفتگیِ دیداری** است، با سه تغییرِ هماهنگ:

1. **نام‌های آبجکت مستقل از تایم‌فریم** — امروز `objectPrefix = inpObjectPrefix + "_" + TF + "_"`،
   پس هر سوییچ یک namespace را یتیم می‌کند و namespace بعدی باید از صفر ساخته شود.
2. **«فرزندخواندگی» در init** — از آبجکت‌های موجودِ روی چارت، کش را پر کن و فقط در جای خودت
   آپدیت کن؛ نه ساختِ دوباره.
3. **حذف‌نکردن در teardown برای `REASON_CHARTCHANGE`** — تا آبجکت‌ها اصلاً از چارت نروند.

→ نتیجه: سوییچ به یک پاسِ آپدیتِ پراپرتی تبدیل می‌شود (همان شکلِ یک رندرِ معمولی) به‌جای
«حذف ~۹۰۰ + ساخت ~۹۰۰ + محاسبهٔ کلِ هندسه». این کار انجام **نشده** و باید به‌صورت یک
تغییرِ منسجم با گیت انجام شود، چون TF داخل نام‌ها بارِ معنایی دارد (پیمایش روح‌گیری
`_LBL_` بین تایم‌فریم‌ها، `ClearAllLevels(s_lastPrefix)`، و suffixهای قدیمی).

### نکتهٔ روش‌شناسی
`[SNAP]` یک دامپِ **درخواستی با کلید X** است (`inpLogDumpKey`، «no background auto-logging»)،
نه لاگِ مسیر init. قبل از هر نتیجه‌گیری، مسیرِ واقعیِ چاپش را بخوان.prime`) با **۷۲ بذر، همه گرفته شد**؛ `write-budget-audit` پاک با **۳۸ بذر، همه گرفته شد**؛ `zorder`/`gesture-budget`/`ui-text` پاک.

## §24 — «تعویض تایم‌فریم = هزینهٔ صفر»: چیزی که باید «از قبل می‌دانست» (2026-09-13، P-PERF-38/38b/38c/38d)

**پرسش کاربر:** «وقتی تایم‌فریم عوض می‌کنم انگار سطوح مجدد محاسبه و رسم می‌شه، نباید اینطوری باشه — بهترین راهکار برای هزینهٔ صفر چیه؟»

پاسخ صادقانه از سه بخش ساخته شد، و بخش سوم جایی است که درس اصلی در آن است.

**۱) نام نباید تایم‌فریم داشته باشد.** پریفیکس آبجکت‌های سطح `prefix + "_" + TF + "_"` بود و در **۱۴** نقطه ساخته می‌شد. یعنی هر تعویض پریود، کلِ خانواده را **تغییر نام** می‌داد: namespace قدیمی باید حذف می‌شد و نamespace جدید از صفر ساخته می‌شد — روی سوییچی که فقط **قیمت‌ها** عوض می‌شوند. حالا یک صاحب دارد (`GetLevelObjectPrefix()`) و بدون TF است. تایم‌فریم هنوز تصویر را عوض می‌کند (`GetTimeframeTH()` → `thValue`)، ولی از راه **امضا** که درجا re-render می‌کند، نه از راه **نام** که تخریب/ساخت را تحمیل می‌کرد.

**۲) teardown نباید خانواده را حذف کند.** MT4 روی تغییر پریود، deinit+init اجباری می‌کند و شعبهٔ `REASON_CHARTCHANGE` کلِ خانواده را حذف می‌کرد. حالا نمی‌کند.

**۳) اما این دو کافی نبودند — و دلیلش ظریف است.** روی تغییر پریود، MT4 اندیکاتور را **reload** می‌کند، پس نمونهٔ جدید با امضای **خالی** شروع می‌شود (`s_lastLevelSig == ""`). یعنی اولین مقایسه **بی‌قیدوشرط true** است → `levelTopologyChanged` → `shouldClearLevels` → یک `ClearAllLevels` کامل روی همان خانواده‌ای که teardown تازه «نگه داشته» بود، و بلافاصله بازسازی مرحله‌ای که دوباره می‌سازدش.

> **درس:** «نگه‌داشتن» یک دارایی بین دو نمونهٔ فرایندی کافی نیست؛ باید **این واقعیت هم منتقل شود که آن دارایی نگه داشته شده**. حالتِ مشترک، خودش باید منتقل شود، نه فقط از حذف‌شدن فرار کند.

و چون مقایسه با **حالتِ فرایندی** انجام می‌شد (staticها)، نه با **حالتِ چارت**، نتیجه همیشه «هیچ‌چیز آنجا نیست» بود. راه‌حل: teardown یک **اثر انگشت** می‌نویسد از ورودی‌هایی که تعیین می‌کنند **کدام نام‌ها مجازند وجود داشته باشند** (شِمهٔ نام‌گذاری، مود، تعداد سطوح، نقطهٔ شروع، LS-first، هارمونیک) — و **نه** قیمت‌ها، که واقعاً هر تایم‌فریم متفاوت‌اند. نمونهٔ جدید آن را مقایسه می‌کند و در صورت تطابق، توپولوژی را **فرزندخوانده** می‌کند: بدون پاک‌سازی، بدون ری‌استارتِ مرحله‌ای، و تعویض تبدیل می‌شود به یک پاسِ درجای پراپرتی.

نکته‌های طراحی که ارزش حفظ دارند:
- **اثر انگشت باید از اعداد کوچک ساخته شود، نه هش رشته.** عدد صحیح دقیق است؛ هش رشته نیاز به استدلال احتمال برخورد دارد و برخورد در جهت غلط (تطابق کاذب) یعنی پاک‌سازیِ لازم انجام نمی‌شود.
- **محافظه‌کارانه به‌طور ساختاری:** نبودِ مهر (اولین attach، بیلد قدیمی، یا چارتی که تازه Remove شده) = عدم فرزندخواندگی = رفتار قبلی. و هر teardown که واقعاً حذف می‌کند، مهر را پاک می‌کند.
- **ترتیب resolve کردن مهم است.** اثر انگشت `g_thStartPointType` را می‌خواند و آن مقدار در جریان restore قیمت سفارشی ست می‌شود — پس resolve در **انتهای** `OnInitHandler` انجام می‌شود، و گیت هم همین را الزام می‌کند.
- گیت جدید (`topology-adoption`، ۶ زیراثبات) هر شش حلقه را می‌گیرد، از جمله «کدام ورودی‌ها در اثر انگشت نام برده شده‌اند» — چون اضافه‌کردن یک ورودیِ تعیین‌کنندهٔ نام بدون افزودنش به اثر انگشت، دقیقاً همان کلاس باگِ قبلی است.

## §25 — «چرا روشن/خاموش کردن سطوح هنوز کند حس می‌شود؟»: کاری که وجود نداشت (2026-09-13، P-PERF-38f/40)

**دو پرسش کاربر:** (۱) هزینهٔ روشن/خاموش کردن همهٔ سطوح را در همه جا نزدیک صفر کن. (۲) تعویض تایم‌فریم نباید سطوح را از نو بسازد/آپدیت کند؛ سطوح یک‌بار حساب شده‌اند.

### بخش ۱ — لاگ، علت را جایی نشان داد که دنبالش نبودیم

روی تغییر هیچ کدی، اول لاگ امروز را شمردم. ۴۵ خط از این جنس:

```
[W][PERF] owed frame (chart-event) waited=46..266ms body=0ms stage=0 clear=0
```

`body=0ms` یعنی **کار صفر**، و `clear=0` یعنی هیچ پاک‌سازی‌ای نبود. آنچه مانده بود `waited` بود: توزیعِ یکنواخت بین ۰ و ۲۶۶ms — یعنی **دقیقاً چرخهٔ ۲۵۰ میلی‌ثانیه‌ای تایمر**. پس:

> **درس:** هر چیزی که «کند» حس می‌شود، لزوماً «سنگین» نیست. یک چرخهٔ زمان‌بندی می‌تواند کندی را بسازد بدون آنکه هیچ محاسبه‌ای گران باشد. اول بپرس «چه‌قدر کار کرد؟» و بعد «چه‌قدر طول کشید؟» — جواب این دو می‌تواند کاملاً متفاوت باشد.

و `body=0ms` یک نکتهٔ بدتر را لو داد: تا وقتی تایمر سر می‌رسید، فریم چیزی برای کشیدن نداشت، چون مسیرِ سوییچ از قبل **تصویر قدیمی** را دوباره کشیده بود. پس کاربر با یک repaint بی‌تغییر تأیید می‌گرفت و تصویر واقعی تا یک‌چهارم ثانیه بعد می‌آمد.

**ریشه:** P-PERF-34 (که یک بار فریم ۵۳۱ms را داخل رویداد اجرا کرد و فریز ساخت) درست بود، ولی فقط «زمان‌بندی» کرد و هیچ‌کس آن بدهی را **در همان رویداد تسویه نکرد**. راه‌حل: تسویه در **نقطهٔ ورود رویداد** (تنها جایی که هم دو هندلر را می‌پوشاند و هم مسیرهای آینده را)، از طریق همان پمپِ مالکِ کار، و با هزینه‌ای که **خودش هم در بودجهٔ رویداد شمرده می‌شود** — چون درینِ پنهان، همان نقص یک‌لایه بالاتر است.

### بخش ۲ — «یک‌بار حساب شده» درست است، ولی تعریف به تایم‌فریم وابسته است

بررسی کردم: `GetTimeframeTH()` → `CalculateTimeframeTH(GetFractalTimeframeForCurrent())` → `ApplyFractalShift(GetBaseFractalTimeframeForCurrent())`، که همه به پریودِ چارت وابسته‌اند. یعنی **فرمول** سطوح به تایم‌فریم وابسته است (`TH% = M1% * sqrt(minutes)`) و قیمت‌ها بنا به تعریف تغییر می‌کنند.

پس «هزینهٔ صفر» نمی‌تواند یعنی «محاسبه نکن»؛ یعنی:
- **صفر تخریب** (P-PERF-38b/38d/38e: خانواده می‌ماند و درجا آپدیت می‌شود)،
- **صفر کارِ تکراریِ بی‌ربط** — و اینجا یک مورد واقعی پیدا شد: `ClearAllLabels` روی **هر** تعویض تایم‌فریم ۱۱ بار `ObjectsDeleteAll` می‌زد تا ghostهای namespaceهای قدیمی را پاک کند؛ در حالی که (الف) با نام‌گذاری بدون تایم‌فریم دیگر ساخته نمی‌شوند و (ب) مهاجرتِ یک‌بارهٔ P-PERF-38c با مجموعهٔ پیشوندی **فراگیرتر** همان‌ها را در لحظهٔ init پاک می‌کند.

> **درس دوم:** پاک‌سازی‌ای که **دفعات بیشتری** از چیزی که پاک می‌کند اجرا شود، به‌سادگی نامرئی می‌ماند و مثل کار ضروری به نظر می‌رسد. علامت تشخیص: بپرس «چند بار در دقیقه اجرا می‌شود؟» و «چند بار در عمر چارت لازم است؟» — اگر جواب‌ها فرق دارند، آن تفاضل بار اضافه است.

هر دو مورد گیت و بذر گرفتند، و هر دو گیتِ قدیمی که **شکل باستان** را قفل کرده بودند (جارو روی draw generation، و ستاره‌شدن روی `s_lastLabelClearGen`) به حالت درست بازنویسی شدند — نه بذرشان به بذر «کهنه» تبدیل شد (`STALE SEED` سخت‌گیر است).

## §26 — «کیبورد و پنل ناهماهنگاند»: مقداری که هیچوقت فرق نمیکرد، ولی **تصویر** می‌کرد (2026-09-13، P-UI-40)

**گزارش کاربر:** «با دکمهٔ کیبورد هماهنگ باشن با پنل، ناهماهنگی وجود نداشته باشه.»

و نکتهٔ مهم این است که ساختنِ این فیکس با **یک بررسی منفی** شروع شد، نه با یک حدس:

> آیا مقادیر از دو منبع متفاوت خوانده می‌شوند؟ **نه.**
> `PnlCurrentSet` مستقیماً گلوبال‌های زنده را برمی‌گرداند — `g_triggerLevelsEnabled`، `g_linesVisible`، `g_atrLabelsVisible`، `g_thLabelsMode`، `g_showLiveCountdown`.

پس **مقدار** هرگز نمی‌توانست اختلاف داشته باشد. اختلاف در **تصویر** بود: هر سوییچ یک `OBJ_BITMAP_LABEL` است که bitmap‌اش فقط با یک پاس رنگ‌آمیزی عوض می‌شود.

**ریشه، و چرا فقط بعضی سوییچ‌ها خراب بودند:** هر نویسندهٔ **داخلِ** فایل پنل، بعد از فشار، ردیف خودش را دوباره رنگ می‌کند. اما فایل پنل **بعد از** `EventHandlers` include می‌شود — یعنی بعد از جایی که **همهٔ** کلیدهای کیبورد و کلیکِ حلقه زندگی می‌کنند. پس:

- کلید را می‌زدی → حالت عوض می‌شد، چارت اطاعت می‌کرد، و کارتِ **باز** تا بسته‌شدنِ بعدی همان موقعیت قبلی را نشان می‌داد.
- و `PnlSyncOpenStepRow` (یک گارد تغییر، هر تیک) **تنها** هم‌زمان‌سازی از این جنس بود که وجود داشت — یعنی دقیقاً همان شکلِ «بعضی سوییچ‌ها کار می‌کنند، بعضی نه» که در گزارش بود.

**راه‌حل — همان الگوی owe/drain که در همان نشست برای فریم ساختیم:** نویسنده‌ای که نمی‌تواند رنگ کند، **درخواست** می‌دهد (`RequestUISync` در `GlobalVariables`، چون هم ترتیبِ include و هم بیلدِ بی‌پنلِ Lite را حل می‌کند)، و لایهٔ UI آن را **درین** می‌کند: اول درخواست را مصرف، بعد **هر دو** لایه را تعمیر کن (حلقه: states + badges؛ کارتِ باز: ردیف‌ها). درین در **دمِ رویداد** و همچنین در `RefreshKitOnBar` اجرا می‌شود، و هزینه‌اش داخل بودجهٔ رویداد شمرده می‌شود.

**دو درسِ طراحی که ارزش حفظ دارند:**

1. **کپی‌کردنِ یک راه‌حلِ نقطه‌ای، راه‌حل نمی‌سازد.** `PnlSyncOpenStepRow` درست بود ولی شکلش (یک مقایسهٔ گارددار به‌ازای هر حالت، هر تیک، تا ابد) برای N حالت مقیاس نمی‌گیرد. تعمیم‌اش یک ثبتِ درخواست است، نه هفت گاردِ مشابه.
2. **یک استثنا وجود دارد و باید صریح نگه داشته شود:** کارتِ Step با تغییر مود **تغییر شکل** می‌دهد (ردیف‌های هر مود زیر سگمنت‌ها زندگی می‌کنند) — رنگ‌کردنِ ردیف برایش کافی نیست، پس مالکِ بازسازیِ خودش را نگه می‌دارد و گیت هم اجازه نمی‌دهد حذف شود.

و یک نکتهٔ ابزارسازی: چکِ «هر شاخه‌ای که حالتِ نمایش‌داده‌شده را **می‌نویسد** درخواست بدهد» اول با جست‌وجوی سادهٔ زیررشته نوشته شد و **به‌درستی شکست خورد**: شاخهٔ کلید F مقدار `g_linesVisible` را به‌عنوان **آرگومان** پاس می‌دهد، که یک *خواندن* است. معیار به انتساب (`x =`) تغییر کرد تا خواندن با نوشتن اشتباه گرفته نشود — گیت باید دقیقاً همان چیزی را بسنجد که ادعا می‌کند.

## §27 — «مثل دکمه f»: یک دکمه، یک سوییچ، و دو لایه‌ای که واقعاً کند بودند

گزارش: «درست خاموش و روشن نمی‌شه مثل دکمه f». اولین خوانشم غلط بود و کاربر
خودش تصحیحش کرد؛ بعد از برگرداندن آن خوانش، دو لایهٔ واقعی ماندند و هر دو یک
شکل داشتند: **یک مالک، نصف تصمیم.**

### لایه ۱ — اشتباهی که خودم کردم و برگرداندم

گزارش: «مثل دکمه f نیست». اولین خوانشِ من این بود: دکمه اسمِ «Zones & Levels» را
می‌برد پس باید هر دو لایه را حرکت دهد. پیاده‌اش کردم
(`ZonesAndLevelsMasterOn() = g_showMidZones && g_linesVisible` و کلیک هر دو را ست
می‌کرد) و کاربر فوراً نتیجه‌اش را دید: **زون روشن + خطوط خاموش → یک فشار → خطوط
دوباره روشن.** یعنی یک کنترل، انتخابِ کنترل دیگری را بی‌صدا بازنویسی کرد.

برگرداندم. و درسِ اصلی همین است: **«حس می‌شود قابل‌اعتماد نیست» را هرگز با حرکت
دادن STATE بیشتر درست نمی‌کنند؛ با درست و قابل‌اعتماد حرکت دادنِ STATE درست
درست می‌کنند.** آنچه واقعاً مشکل بود، دو لایهٔ بعدی است — و آن‌ها هم کلِ فیکس
پرفورمنس این چرخه‌اند. الان حلقه فقط `g_showMidZones` را می‌خواند و می‌نویسد، و
`SetLinesVisible` صریحاً ممنوع است (گیت روی خودِ برنچ، نه روی فایل).

### لایه ۲ — حالت OFF یک «تخریب» بود، نه یک ماسک

`CleanupSurplusPipeline` وقتی `zonesEnabled` غلط بود از ایندکس ۰ شروع می‌کرد،
یعنی OFF = ۷ `ObjectDelete` به ازای هر زون و ON = یک `CreateZone` + ۷ نوشتن
ماسک، برای سوییچی که هیچ قیمت و هیچ هندسه‌ای را عوض نمی‌کند. F و L از اول یک
ماسک به ازای هر آبجکت بودند؛ همین تفاوت است که «کند» را حس می‌کرد.

و یک اشکال ساختاری در همان مسیر خوابیده بود: آن پیمایش **ایندکس** را
می‌شمارد (`prefix + idx`) و بعد از `maxConsecutiveMiss` متوقف می‌شود. یعنی
یک خانوادهٔ نامی که فاصلهٔ ایندکسش از ۶ بیشتر شود، روی چارت می‌ماند. پیمایش
مبتنی بر **کش آبجکت** این کلاس را حذف می‌کند: هر آبجکتی که ما ساخته‌ایم
نام‌بر می‌شود، بدون هیچ فرض ایندکسی و بدون یک `ObjectFind`.

### لایه ۳ — دو مالک، دو سؤال متفاوت

این لایه فقط چون لایهٔ ۲ را درست کردم قابل‌دسترس شد و دقیقاً همان چیزی است
که یک فیکس خوب را به یک باگ تازه تبدیل می‌کند: تا وقتی خانواده را **حذف**
می‌کردیم، نمی‌شد دوباره نشانش داد. حالا که نگهش می‌داریم، دو نویسندهٔ ماسک
باید یک سؤال بپرسند. `VisibilityShowAllCached` (کلید F) هیچ ورودی‌ای برای
سوییچ زون‌ها نداشت و هر آبجکت غیرِخط را `OBJ_ALL_PERIODS` می‌کرد — پس
«زون‌ها خاموش» تا اولین فشار F زنده می‌ماند. و یک اشتباه ریزتر: قاعدهٔ نامِ
`_Zone_` **قبل از** قاعدهٔ `_Above_` بررسی می‌شود، پس خط‌های مرزی
`_Top`/`_Bottom` «خط» حساب نمی‌شدند و F-show آن‌ها را با L خاموش هم برمی‌گرداند.

راه‌حل: یک predicate، `VisibilityZoneMask(nm, zonesVisible, linesVisible)`،
کنارِ بقیهٔ مالک‌های `OBJPROP_TIMEFRAMES` — «سوییچ خانواده → سوییچ خط مرزی →
ماسک F». هر سه مصرف‌کننده از آن می‌پرسند.

### چیزهایی که عمداً انجام نشد (و چرا)

- **حذف trigger band عوض نشد.** وسوسه‌اش زیاد است: «نشان بده نده، پنهانش کن».
  ولی یک band تریگر و یک band ساختاری **هم‌نام** و هم‌شکل‌اند؛ پس `_Zone_`
  هیچ‌وقت نمی‌تواند بگوید کدام است، و پنهان‌کردن یکی، مسیر F-show را مجبور
  می‌کند همه را `ALL_PERIODS` کند — یعنی band برای فاصلهٔ بین فشار F و رندری
  که دوباره پنهانش می‌کند، **یک فریم چشمک می‌زند**. حذف، هزینه‌اش بعد از
  اولین پاک‌سازی ۷ جست‌وجوی «اثبات‌شده‌غایب» است (P-PERF-07/13) و رندر هم فقط
  وقتی اجرا می‌شود که `IsTriggerLevelsEnabled()` عوض شده باشد. یعنی «پنهان
  کن» یک flicker تازه می‌خرید و چیزی نمی‌فروخت.
- **مسیر F دست نخورد.** وسوسه: `ScheduleHeavyFrame` را در F صدا بزنیم تا
  اصلاحِ رندر در همان رویداد بیفتد. اما F همین حالا «کار می‌کند» و معماری
  P-PERF-34/40 دقیق است؛ یک تغییر یک‌خطی در آن، ریسک برگشت فریز را می‌آورد
  برای سودی که لایهٔ ۳ عملاً صفرش کرد (زون‌های خاموش که F-show دوباره
  نشان نمی‌دهد، و bandهای تریگر که خودشان حذف‌اند).

### درسی که ارزش تکرار دارد

۱) وقتی یک کنترل «کند» حس می‌شود، اول بپرس **چه کاری انجام می‌دهد**؛ ممکن
است کار نباشد، تخریب باشد. ۲) و وقتی چیزی را از «حذف» به «نگه‌داشتن» تغییر
می‌دهی، **هر نویسندهٔ دیگری که آن آبجکت‌ها را می‌شناسد** یک سؤال تازه پیدا
می‌کند — این دو تغییر یک طراحی‌اند، نه دو.

## §28 — گاردی که ورودی‌اش ثابت است، گارد نیست؛ قفل است

این چرخه یک باگ **خودم** را پیدا کرد، و ارزشمندترین بخشش همین است: فیکسِ
قبلی (P-PERF-41b) درست بود، ولی یک «بهینه‌سازی» رویش سوار کردم که کنترل را
عملاً مرده کرد.

### شکلِ اشتباه

خواستم پیمایشِ خاموش‌کردنِ خانوادهٔ زون در هر رندر (اسکرول/زوم) اجرا نشود،
پس یک گارد تغییر گذاشتم:

```cpp
if(zonesVisible == s_lastZonesVisible && isHidden == s_lastHidden &&
   linesOn == s_lastLinesOn && epoch == s_lastEpoch)
    return 0;                       // «همه‌چیز همان است که بود»
```

سه سؤال را باید می‌پرسیدم و نپرسیدم:

۱) **آیا `zonesVisible` واقعاً متغیر است؟** نه. تنها caller
`if(!config.zonesEnabled)` است، پس همیشه `false`. یعنی گارد نمی‌تواند
گذاری (transition) را ببیند که ادعا می‌کند تشخیص می‌دهد.

۲) **آیا `epoch` در چرخهٔ off→on→off جابه‌جا می‌شود؟** نه. رندرِ روشن، ماسک‌ها
را از `ApplyTfMaskGuarded` می‌نویسد و آن **عمداً** epoch را بالا نمی‌برد
(وگرنه هر نوشتنِ ماسک گران می‌شد). پس epochِ ذخیره‌شده معتبر می‌ماند.

۳) **آیا هزینهٔ نگرفتنِ گارد واقعاً زیاد است؟** نه. در حالت خاموش، پیمایش
یک prob هش به ازای هر نامِ کش‌شده و **صفر** نوشتنِ ترمینال است؛ در حالی که
رندرِ روشن به ازای هر زون ۸ فراخوانیِ گارددار می‌پردازد. یعنی «صرفه‌جویی»
معکوس بود.

نتیجه، دقیقاً همان چیزی که کاربر دید:

```
فشار ۱ → OFF : زون‌ها مخفی می‌شوند                      ✔
فشار ۲ → ON  : رندر دوباره نشانشان می‌دهد               ✔
فشار ۳ → OFF : کلیدِ گارد عیناً مثل فشار ۱ → return 0   ✗  زون‌ها می‌مانند
... و هر فشار بعدی همین.
```

### شکلِ درست

گارد **سطحِ شیء** را نگه دار (`ApplyTfMaskGuarded`): ماسکی که می‌خواهی را با
ماسکی می‌سنجد که **برای همان نام** نوشتی. این نه می‌تواند گذاری را از دست
بدهد (چون هیچ‌چیز را خلاصه نمی‌کند) و نه گران است. و پارامتر بولی از تابع
برداشته شد تا **نوشتنِ** آن اشتباه ممکن نباشد، نه اینکه فقط با گیت گرفته شود.

### قاعدهٔ عمومی که این‌بار با خونِ خودم نوشته شد

**هر گاردِ «هزینه‌ای که حذف کردم را ثابت کن» باید یک ورودی متغیر داشته باشد
که گذار در آن دیده شود.** اگر همهٔ ورودی‌ها در لحظهٔ گذار ثابت بمانند، آن
گارد فقط کار را برای همیشه خاموش می‌کند. و اگر درست‌کردنش هزینهٔ محسوسی
ندارد (اینجا: یک prob هش)، گارد را نگه ندار.

نکتهٔ دوم این چرخه: **قبل از هر تغییری یک فرضیهٔ رقیب را بررسی کردم و رد
شد.** شک کردم که `OBJPROP_SELECTABLE=false` روی آیتم‌های حلقه باعث می‌شود
`CHARTEVENT_OBJECT_CLICK` هرگز نرسد. مرجعِ خواهرِ پروژه (`../ichimoku`) با
همان هندسهٔ حلقه هم `false` می‌گذارد، و منوی Tools در اسکرین‌شات کاربر باز
بود — یعنی کلیک می‌رسد. اگر آن فرضیه را «منطقی به‌نظر می‌رسد» می‌گرفتم،
کل مسیر کلیک را بازنویسی می‌کردم و باگِ واقعی سرجایش می‌ماند.

## §29 — کنترلی که وضعیتی را می‌نویسد که **هیچ‌کس نمی‌خواند** (2026-09-13، P-UI-46/47)

کاربر گفت: «همهٔ پنل‌ها را از دید کاربر نگاه کن، ببین کجا کار نمی‌کند و باگ دارد و
همه را رفع کن.» جواب این سؤال با چشم دادن نیست؛ با **یک سؤال** گرفتن است که به کد
می‌شود داد:

> **این ردیف، کدام global را می‌نویسد — و چه کسی آن را می‌خواند؟**

### چهار ردیفی که «کار نمی‌کردند»

| کارت / ردیف | متغیری که می‌نوشت | چرا چیزی عوض نمی‌شد |
|---|---|---|
| Zones & Levels / `MIDPOINT` | `g_showMidpointLine` | خطِ میدپوینت بازنشسته شده: پایپ‌لاین هر رندر `_Midpoint_` را پاک می‌کند («legacy cleanup») |
| Custom Price / `MAGNET` | `g_enableMagnet` | مگنت با **تصمیم خودِ کاربر** بازنشسته شده (`BKMAGNET-OFF`)، هیچ مصرف‌کننده‌ای ندارد |
| Custom Price / `MAGNET SENS` | `g_magnetSensitivityPips` | همان |
| ATR Labels / `ROW GAP` | `g_atrLabelRowGap` | چیدمانِ لیبل‌ها `inpLabelRowGap` (ورودیِ خودِ اندیکاتور) را می‌خواند، نه این کپیِ ران‌تایم |

و دو ردیفِ دیگر از همان کارتِ TH Labels (`FRACTAL THs` / `STANDARD THs`) همان باگِ
یک‌طرفهٔ P-UI-44 را داشتند: مود را از همان آینه‌هایی **بازخوانی** می‌کردند که داشتند
می‌نوشتند، پس آخرین منبعِ روشن هرگز خاموش نمی‌شد.

### چرا با چشم پیدا نشد، و با این سؤال پیدا شد

هر چهار کنترل **درست رندر می‌شدند**: کلید می‌لغزید، چراغ روشن/خاموش می‌شد،
`Reset` هم مقدار را برمی‌گرداند. تنها چیزی که غایب بود اثرِ آن‌ها بود. تفاوتِ
«کار می‌کند» و «کار نمی‌کند» در ظاهر صفر است — یعنی برای این کلاس از باگ،
بازبینیِ بصری بی‌فایده است و باید مصرف‌کنندهٔ وضعیت را در **کلِ مخزن** جست.

نکتهٔ فنی که این جست‌وجو را قابل‌اعتماد می‌کند: مصرف‌کننده همیشه با نامِ `g_...`
نوشته نمی‌شود، چون `RuntimeSettings` هر ورودی را با `#define inpX gX` به کپیِ
ران‌تایم وصل می‌کند. پس باید **هر دو نام** جست شود، و **خطوطِ لوله‌کشی** خودِ
تنظیمات (`static`، `#define`، `g_x = inp_x;`، `RSSetNext(...)`،
`GlobalVariableCheck(p + ...)`، `g_factoryDefaults[...] = inpX;`) از «خواندن» کنار
گذاشته شوند. آخرین مورد همان دامی است که اول گیتی که نوشتم را فریب داد:
تیترِ enum، `FF_ENABLE_MAGNET,   // inpEnableMagnet`، یک «خواندن» ساختگی می‌ساخت —
یعنی **کامنتِ انتهایی هم باید حذف شود**، نه فقط خطِ کامنتِ کامل.

### قاعده‌ها

۱. **هر ردیفی که رندر می‌شود باید اثری بیرون از لایهٔ پنل داشته باشد.** «صرفاً
   `RequestUISync`/`Save` صدا زدن» و «کمکیِ مقداری (`ClampInt(MathRound(v))`)»
   اثر نیستند؛ یا باید وضعیت جایی **خوانده** شود، یا همان وضعیت به یک مالکِ بیرونی
   **داده** شود (`SetLinesVisible(g_showLines, true)`).
۲. **حالتِ محلیِ خودِ UI استثناست، ولی استثنا باید نام‌دار و کوچک باشد**
   (`UI_LOCAL = ("g_BkTab",)`): مصرف‌کنندهٔ تبِ کارت، خودِ کارت است که بازسازی می‌شود.
   یک لیستِ نام‌دار و کامنت‌دار با یک عضو، بهتر از یک قاعدهٔ مبهم است.
۳. **حذفِ ردیف، بدون دست‌زدن به شمارشِ ردیف‌ها.** ردیفِ مرده از **spec** حذف شد
   (`PnlSpecAdd`) و ایندکسِ setting سرِ جایش ماند؛ پس هیچ چیز شماره‌گذاری نمی‌شود و
   مقدارهای ذخیره‌شدهٔ کاربر بی‌تغییر می‌مانند. این همان کاری است که پروژه برای
   `VIEWLOCK-OFF` و `TH3TOOL-OFF` کرده بود: بازنشستگی را در `PnlSetDef` کامنت کن.
۴. **حذفِ وضعیتِ مشتق از خودش.** الگوی `x = f(mirrors); if(master && x==0) x=1;`
   در هر دو ردیف ۱ و ۲ (و در ردیف ۰ پیش‌تر) یک باگ بود: مقدارِ «خاموش» باید همان
   چیزی باشد که بقیهٔ کنترل‌ها برای خاموش می‌نویسند (مود ۰)، نه یک «پیش‌فرض» که
   رویش بازنویسی می‌شود.

### گیت

`probe-budget-audit` گروهِ جدید **`live-control`** دارد: برای هر settingِ **قابلِ‌رندر**
(از spec، پس ردیف‌های بازنشسته اندازه‌گیری نمی‌شوند) شاخهٔ `PnlApplySet` را با
**معناشناسیِ MQL** می‌سازد (زنجیرهٔ `if/else if/else`، شرطِ چندردیفی مثل
`row==1 || row==2`، و `else` که ردیف‌های نام‌نبرده را مالک است)، و آینه‌ای از
نام‌های «خوانده‌شده» در همهٔ فایل‌ها می‌سازد تا جست‌وجو در سلف‌تستِ ۱۳۸ seedی ارزان بماند.

---

## §30 — «حرکت نمی‌کنه، باید مثل قدیم حرکت کنه»: قابلیت را برای رفع تداخل حذف نکن

گزارش کاربر: «خط کاستوم پرایس چرا مثل قدیم حرکت نمی‌کنه؟ باید مثل قدیم حرکت بکنه ولی
مشکلاتش رفع بشه.» پیام کوتاه است ولی یک قاعدهٔ عمومی را می‌گوید.

۱. **تداخل از «انتخابِ باقی‌مانده» بود، نه از «قابلِ‌انتخاب‌بودن».** P-UI-45 گزارش
   «خط با پنل‌ها و آبجکت‌ها می‌جنگد» را به «پس خط نباید SELECTABLE باشد» ترجمه کرد. اما
   مکانیزمِ واقعی این بود که `OBJPROP_SELECTED` **ماندگار** بود: MT4 آبجکتِ SELECTED را
   در **هر درگِ بعدی در هر جای چارت** جابه‌جا می‌کند، و کدِ قدیمی همین پرچم را در لحظهٔ
   ساخت `true` می‌نوشت. یعنی خط **پیش از هر گِسچرِ خودش** در آن وضعیت بود. پرچمی که
   قابلیت را می‌دهد (SELECTABLE) و پرچمی که وضعیت را نگه می‌دارد (SELECTED) دو چیزند؛
   رفعِ باگ دومی را نکن، اولی را دست نزن.

۲. **قاعدهٔ کلی:** وقتی چیزی «مثل قبل کار نمی‌کنه» پس از یک رفعِ باگ، سؤال این نیست که
   «کدام خط را برگردانم؟» بلکه «آن رفع، کدام قابلیت را با کدام عارضه قربانی کرد؟» اگر
   می‌شود عارضه را جدا کشت، قابلیت برمی‌گردد و «بی‌تغییر در رفتار» معنا پیدا می‌کند.

۳. **هر گِسچر باید یک نقطهٔ پایانِ تضمین‌شده داشته باشد.** مالکِ واحدِ «پاک‌کردنِ انتخاب»
   چهار تریگر دارد، چون هیچ‌کدام تنها کافی نیست: (a) ماوس‌مُو با دکمهٔ بالا (در رهاشدنِ
   بی‌حرکت **هرگز نمی‌آید** — تلهٔ P-BK-03)؛ (b) فاینالایزرِ OBJECT_CLICK/CLICK؛
   (c) فشاری که **مالکِ دیگری** آن را claim کرده (`g_DragOwner != DRAG_NONE` — پنل یا
   رینگ)؛ (d) `CHARTEVENT_OBJECT_DRAG` برای **هر آبجکتی که خودِ خط نیست**. مورد (d)
   مهم‌ترین است چون نامرئی‌ترین بود: باکس‌های Base/Knot هم درگِ بومی دارند (SELECTABLE
   همان هندلِ باکس است) و MT4 **هر** آبجکتِ SELECTED را حرکت می‌دهد — پس باکسِ درگ‌شده
   خط را هم جابه‌جا می‌کرد. استثنای خودِ خط با **نام** انجام می‌شود، چون همان انتخاب
   موتورِ live-update است. چهار تریگر، **یک** مالکِ نوشتن.

۴. **مالکِ تکرارشده، خودش باگ است.** مجموعهٔ پراپرتی‌های خط **پنج** نسخهٔ دست‌نویس داشت
   (کلید C، کلیک چارت، بازگردانی TF Lock، دکمهٔ PIN رینگ، و هلپر). هر نسخه یک فرصت برای
   جاگذاشتنِ یک پرچم بود — و دقیقاً همین‌طور بود که آن پرچم یک رفعِ قبلی را جان سالم به
   در برد. همهٔ مسیرها الان از یک سازنده رد می‌شوند و گیت می‌شمارد: `SELECTABLE` فقط
   **یک** بار نوشته می‌شود و `SELECTED, false` فقط **دو** بار.

۵. **متنِ تولتیپ هم قرارداد است.** اگر خط در همهٔ حالت‌ها کشیدنی است، «PIN button to
   move» دروغ است؛ تولتیپ بعد از این همیشه «Drag to adjust, Double-click to confirm»
   است. یک کنترل نباید کاربر را به مسیری بفرستد که دیگر لازم نیست.

---

## §31 — «نمی‌شود درگش کرد»: گِسچری که با نوشتن روی آبجکتِ در حالِ درگ کشته می‌شود (2026-09-13، P-UI-49)

گزارش کاربر: «خط کاستوم پرایس رو نمیشه درگش کرد مشکل چیه».

۱. **پرچم درست بود، گِسچر می‌مرد.** P-UI-48 حرکت را از «انتخابِ دائمی» به «درگِ بومیِ هر
   آبجکت» منتقل کرد. آن درگ یک گِسچرِ **زنده** است که MT4 مالکش است — و قانونِ همان گِسچر
   از قبل در همین مخزن ثبت شده بود: **MT4 درگِ در جریان را وقتی آبجکتِ درگ‌شده به‌صورت
   برنامه‌ای بازنویسی شود لغو می‌کند** (P-BK-15، روی باکسِ Base/Knot: «هر Sync/heal وسط درگ،
   باکس را به نقطهٔ شروعِ درگ برمی‌گرداند»؛ و همان قانون در followِ خانوادهٔ سطوح: «هرگز Sync
   کامل در هر استپ — بازنویسیِ style/tooltip بچه‌ها را از باکسِ بومی عقب می‌انداخت»).

۲. **ما خودمان ناقضِ آن قانون بودیم.** هندلرِ `CHARTEVENT_OBJECT_DRAG` خط — که **در هر
   استپِ** درگ می‌آید — تولتیپِ خط را در هر استپ می‌نوشت (متن شامل قیمت است، پس همیشه
   متفاوت ⇒ نوشتنِ واقعی). تا وقتی خط پیش‌انتخاب بود، حرکت از **انتخاب** می‌آمد (MT4 هر
   آبجکتِ SELECTED را در هر mouse-move جابه‌جا می‌کند) و آن نوشتن دیده نمی‌شد؛ لحظه‌ای که
   حرکت به درگِ per-object منتقل شد، همان نوشتن گِسچرِ خودش را کشت. پس «حرکت نمی‌کند» یک
   باگِ **دوم** بود، نه بازگشتِ باگِ اول.

۳. **قاعدهٔ گیت‌شدنی: «تزئینِ یک گِسچر در حینِ آن گِسچر ممنوع است.»** هر نوشتنِ پراپرتی روی
   آبجکتی که کاربر دارد می‌کشد (tooltip / style / heal / sync) باید به لحظهٔ **رها شدن**
   منتقل شود. هزینهٔ این قاعده منفی است: یک نوشتن در هر گِسچر به‌جای یک نوشتن در هر استپ —
   یعنی «بی‌تغییر در پرفورمنس» به‌سادگی با *کم‌کردنِ* کار به دست می‌آید.

۴. **درسِ فرآیندی:** P-UI-45 و P-UI-48 یک گزارش را با دو مدلِ ذهنیِ متفاوت از MT4 رفع
   کردند و هر دو مدل «منطقی» به نظر می‌رسید. تفاوتِ این چرخه این بود که **قانونِ MT4 قبلاً
   در همین مخزن اندازه‌گیری شده بود** (P-BK-15). قبل از ساختنِ مدلِ جدید از رفتار ترمینال،
   سراغ قوانینِ ثبت‌شدهٔ پروژه برو و بپرس: کدام‌شان را داریم نقض می‌کنیم؟

۵. **گیت:** `probe-budget-audit custom-price-mode` — بدنهٔ درگِ خط نباید هیچ `ObjectSet*`
   روی خط داشته باشد، تولتیپ باید **یک** مالک داشته باشد، و آن مالک باید از مسیرِ **رها شدن**
   صدا زده شود؛ ۲ seed جدید ⇒ **۱۴۵/۱۴۵**. تأیید: Full / Installed / Lite صفر خطا صفر
   هشدار؛ `write` / `gesture` / `ui-text` / `zorder` تمیز.

۶. **دو نیمهٔ یک گِسچر، در همان یک رویداد، خلافِ هم تصمیم گرفتند (P-UI-49b).** گزارش بعدی
   «حالت درگ خیلی سریع قطع میشه» بود: یعنی درگ **شروع** میشد و در همان گِسچر می‌مرد.
   هندلرِ کلیکِ دامنه برای همین خطر «پاک‌کردن را موکول» کرده بود (`g_customPriceNativeDrag` را
   arm می‌کند و پراپرتی نمی‌نویسد)، ولی فاینالایزرِ `ChartPointerFinalizeOnUps` — که یک
   چرخهٔ بعد، در همان رفع، اضافه شد — همان پاک‌کردن را **inline** نوشت. پس در **همان
   رویدادی** که یک لایه موکول کرد، لایهٔ دیگر انجامش داد؛ MT4 انتخابش را از دست داد و درگی
   که همان فشار شروع کرده بود، همان‌جا مرد.
   **قاعده:** اگر یک رویداد را نمی‌توان قطعی به press یا release نسبت داد، راه‌حل **موکول‌کردن**
   است: در دنیای release فقط چند میلی‌ثانیه دیرتر پاک می‌شود، و در دنیای press درگ را نمی‌کشد.
   هزینهٔ موکول‌کردن (یک bool) از هزینهٔ انجام‌دادن (خواندنِ گارد‌دار + نوشتنِ پراپرتی) کمتر
   است — یعنی «بی‌تغییر در پرفورمنس» دوباره با *کم‌کردنِ* کار به دست آمد.

۷. **گیت:** فاینالایزر نباید `ClearCustomPriceSelection()` داشته باشد و باید latch را arm کند
   (۲ seed ⇒ **۱۴۶/۱۴۶**).

---

## §32 — «یک پرچم، دو نویسنده»: وقتی رویدادِ پیوسته، پرچمِ لبهٔ فشار را صفر می‌کند (2026-09-13، P-UI-51)

گزارش کاربر: «حالت درگ خط کاستوم پرایس خیلی سریع قطع میشه نمیشه درگ کرد قابل سلکت بودنش» +
«طوری درستش کن که به پروفورمنس و رفتار اندیکاتور تغییر نیاد».

۱. **هر گِسچر یک مالکِ رویدادی دارد.** `g_customPriceLineDragging` در **لبهٔ فشار** set می‌شد و
   در `CHARTEVENT_OBJECT_DRAG` — رویدادی که MT4 در **هر استپ** می‌فرستد — صفر. آن پرچم یعنی
   «تصمیمِ گِسچر گرفته شده، این گِسچر مالِ من است»، پس هر منطقی که به آن تکیه کرده بود در هر
   استپ از صفر شروع می‌کرد: بلوکِ grab دوباره اجرا می‌شد و مرجعِ تستِ یخ‌زدهٔ carry
   (`s_ownGrabPrice`) به قیمتِ **کنونیِ** خط re-arm می‌شد ⇒ تست خودش را با خودش مقایسه می‌کرد،
   همیشه می‌گذشت، و carry وسطِ درگِ خودِ MT4 می‌نوشت ⇒ P-BK-15 ⇒ درگ لغو و خط به نقطهٔ شروع
   برگشت. **قاعده:** یک پرچمِ گِسچر فقط دو نویسنده دارد: لبهٔ شروع و لبهٔ پایان.

۲. **«نوشتنِ محتاطانه»ای که گاردش را از حالتِ ترمینال می‌خواند، وسطِ گِسچر خطر است.**
   `ClearCustomPriceSelection` فقط وقتی می‌نویسد که `OBJPROP_SELECTED` true باشد — یعنی دقیقاً
   وقتی ترمینال خط را در دست دارد. پس «پاک‌کردنِ انتخاب در فشارِ ناشناخته» در همان لبهٔ فشار،
   خودِ درگی را می‌کشت که ترمینال شروع کرده بود. هیِت‌تستِ لبهٔ فشار روی نخستین MOVE اجرا
   می‌شود (چند پیکسل دورتر، و در درگِ سریع قطعاً miss می‌دهد) — پس «نمی‌فهمم مالِ من است یا
   نه» باید **موکول** کند، نه اقدام.

۳. **لبهٔ فشار همان رویدادی است که تستِ یخ‌زده در آن بی‌معنی است** (قیمت به‌تعریف با قیمتِ
   لحظهٔ grab برابر است، چه ترمینال در حالِ گرفتن باشد چه هرگز نگیرد). fallback باید یک رویداد
   دیرتر شروع شود؛ چون absolute از کرسر است، همان قیمت را می‌دهد و رفتار عوض نمی‌شود.

۴. **معافیتِ پرفورمنس هم باید یک مالک داشته باشد.** با روشن‌ماندنِ پرچم در کلِ درگ، فریمِ
   اجباریِ هندلرِ درگ از حالتِ deferred به **inline** می‌رود (معافیتِ P-PERF-34)؛ همان بودجهٔ
   ۵۰ms مسیرِ ماوس‌مُوی شریک شد تا کادنس عوض نشود — و کارِ هر استپ **کمتر** شد.

۵. **گیت:** `probe-budget-audit custom-price-mode` — فشارِ ناشناخته نباید
   `ClearCustomPriceSelection()` داشته باشد و باید latch را arm کند؛ carry باید گاردِ
   `!pressEdge` را با خود داشته باشد؛ هندلرِ درگِ خط نباید پرچم را صفر کند؛ ۴ seed جدید ⇒
   **۱۵۳/۱۵۳**. Full / Installed / Lite: ۰ خطا / ۰ هشدار.

---

## §33 — «منبع رسم قیمت»: مقداری که دو جا حل می‌شود، دو اولویت دارد (2026-09-13، P-UI-56)

گزارش کاربر: «چرا سطوح سرجای خودشون نیستن، هر دفعه یک جایی دیگه میره … حتماً نگاه کن
منبع رسم قیمت چطوریه».

۱. **سؤالِ درست این بود: «کلِ لدر از کدام مقدار ساخته می‌شود؟»** جواب یک خط بود:
   `GetMidpointPrice(g_thStartPointType)` → `g_customTHStartPrice` → `CalculateCommonStepData`
   → `data.midpointPrice` → `ExecutePipeline`. پس هر باگ در این مقدار، باگِ «سطوح سرجایشان
   نیست» است — و برای پیدا کردنش لازم نیست پنل را باز کرد؛ باید مالک‌های نوشتنِ آن را شمرد.

۲. **دو کپی از یک زنجیرهٔ سه‌شاخه = دو اولویت.** همان زنجیره در `OnInitHandler` و در مسیرِ
   هر فریم نوشته شده بود. قاعده: اگر مقداری بیش از یک نویسندهٔ **تصمیم** داشته باشد، یک روز
   دو شاخه با هم اختلاف پیدا می‌کنند؛ راه‌حل «همگام نگه داشتن» نیست، «یک تابعِ حل» است که هر دو
   جا صدا بزنند (این‌جا `CustomPriceResolveSource`).

۳. **کلیدِ وضعیت باید هم‌دامنهٔ چیزی باشد که توصیف می‌کند.** قیمتِ سفارشی زیر نامِ **نماد**
   ذخیره می‌شد (`Biotak_CustomPrice_<SYM>`) در حالی که هر وضعیتِ در-چارتِ دیگرِ همین پروژه زیر
   **شناسهٔ چارت** است. نتیجه: دو چارت از یک نماد، یک قیمت — حرکت یکی، حرکتِ لدرِ آن یکی.
   درسِ گیت‌شدنی: «کلیدِ مخصوصِ یک چارت، شناسهٔ چارت را در نام دارد؛ اگر نماد را دارد،
   دو چارت یک وضعیت را قرض می‌گیرند.» مهاجرت هم لازم است (قیمتِ چارتِ قدیمی گم نشود) و
   مهاجرت باید «جای‌گذاری» را از «بذرِ اینپوت» تشخیص دهد — با شاهدِ خودِ نویسنده‌ها، نه با حدس.

۴. **یک شاخهٔ «پیش‌فرض» نباید کلیدِ وضعیتِ کاربر را بنویسد.** شاخهٔ اینپوت در هر فریم
   `GlobalVariableSet(priceKey, inpCustomTHStartPrice)` می‌کرد؛ یعنی مقداری که کاربر درگ کرده
   بود با عددِ اینپوت جایگزین می‌شد و بعد «درگِ جدید» از روی همان اینپوت ادامه می‌داد.

۵. **تستِ بازه، تستِ اعتبار نیست (§33b، P-UI-57).** `x < min || x > max` و `x <= 0` هر دو
   برای NaN **نادرست** می‌شوند، پس NaN از همهٔ گاردهای مخزن رد می‌شد. `MathIsValidNumber`
   تنها تستی است که آن را می‌بیند، و جایش **مالکِ عدد** است نه مصرف‌کننده‌ها
   (`CalculateLevels`, `CalculateCommonStepData`, `GetCachedPipSize`). مخرجِ «پیپ» هم از
   `Point` می‌آید که روی نمادِ لود‌نشده صفر است: `x / 0.0` در MQL4 خطا نیست، `inf` است.

۶. **باندِ مشتق‌شده نباید به هندسه‌ای که توصیفش می‌کند برسد (§33c، P-UI-58).** زون روی سطح
   وسط‌چین است و خط روی **میانهٔ** دو سطح؛ «فاصله» فقط تا وقتی هست که نیم‌ارتفاعِ باند کمتر از
   نصفِ فاصله باشد — و کنترل تا ۱۰۰٪ باز است، یعنی دقیقاً مرز. گارد را در **سازندهٔ باند**
   بگذارید و از **همان فاصله‌ای** بخوانید که باند در آن نشسته (نه از step ثابتِ SS/LS).
   با نسبتِ گپِ ۲۰٪، هر تنظیمی ≤ ۸۰٪ پیکسل‌به‌پیکسل بی‌تغییر می‌ماند.

## §34 — «یک فاصله، دو طرف دارد» و «مالکِ پاک‌سازی باید از مسیرِ حذف صدا زده شود» (2026-09-13، P-UI-59/60)

**الف) گاردی که فقط یک طرفِ یک فاصله را می‌سنجد، نیمی از دفعات کار نمی‌کند.**

P-UI-58 نیم‌ارتفاعِ باند را با «فاصلهٔ همان اینتروالی که باند در آن ساخته شد» کلمپ کرد. اما
هر باند **دو** همسایه دارد و هر همسایه خطِ میانهٔ خودش را دارد؛ در حالت SS/LS دو اینتروال
دو طرف **برابر نیستند** (قدم‌ها یکی‌درمیان کوتاه/بلند می‌شوند، `CalculateLinearLevels`). پس:

- نیم‌ارتفاع باند = `fixedZoneStepSize * hp / 2 = min(ss, ls) * hp / 2` → در ۱۰۰٪ می‌شود `ss/2`.
- خطِ اینتروالِ `ss` دقیقاً `ss/2` از همان سطح فاصله دارد → **روی لبهٔ باند** می‌نشیند.
- باندی که با طرفِ بلند (`ls`) کلمپ شده باشد، **اصلاً کلمپ نشده** (سقفش `0.4*ls` است).

درسِ گیت‌شدنی: **اینواریانت به خط تعلق دارد، نه به باند.** برای هر خط، زون‌های دو طرفش باید
هر دو حداقل گپ را نگه دارند ⇒ کلمپ با **نزدیک‌ترین** دو اینتروال:

```mql4
double ClampZoneHalfHeightBoth(const double wantedHalfHeight,
                               const double stepBelow, const double stepAbove)
{
    double bound = 0.0;
    if(MathIsValidNumber(stepBelow) && stepBelow > 0.0) bound = stepBelow;
    if(MathIsValidNumber(stepAbove) && stepAbove > 0.0 &&
       (bound <= 0.0 || stepAbove < bound)) bound = stepAbove;
    return ClampZoneHalfHeight(wantedHalfHeight, bound);   // bound==0 ⇒ بدونِ قید
}
```

و برای اینکه «همسایهٔ سطح i» واقعاً `i±1` باشد، آرایه باید **مونوتونِ اکید** باشد: سطحی که
توالی را جلو نمی‌برد (تکراری/بی‌ترتیب) باید در همان پاسِ جمع‌آوری **حذف** شود، نه با یک
`continue` داخل حلقه‌های ساخت. آن `continue` مقدارِ `prevPrice` را روی سطحِ رد‌شده جا می‌گذاشت
و «سطحِ بعدیِ همین یکی» را بدونِ یک پویشِ رو‌به‌جلو ناشناخته می‌کرد؛ ضمناً هر حلقه یک شرط
اضافه هم داشت. ساختِ فعلی مونوتون است، پس این حذف روی چارتِ سالم **هیچ اثری** ندارد.

**ب) پاک‌سازی یعنی «از مسیرِ حذف صدا زده می‌شود»، نه «تابعش وجود دارد».**

سیاههٔ همهٔ خانوده‌های `Biotak_*` در برابر متنِ مسیرِ `REASON_REMOVE` فقط **یک** خانوادهٔ
بی‌مالک پیدا کرد: مُهرِ مهاجرتِ نام‌گذاری (`NameSchemeStampName()`، P-PERF-38c). مُهر
**به‌ازای هر شناسهٔ چارت** است و شناسهٔ چارتی که بسته شده دیگر هرگز برنمی‌گردد ⇒ هر چارتی که
ترمینال در عمرش نشان داده، تا آخرِ عمرِ ترمینال یک `Biotak_NameScheme_<id>` در جدولِ
گلوبال‌وریبل‌ها می‌گذارد. این تزئینی نیست: جدولِ MT4 متناهی است و جدولِ پر، `GlobalVariableSet`
را رد می‌کند — یعنی ذخیره‌سازیِ وضعیت **بی‌صدا** از کار می‌افتد.

حالا مسیرِ `REASON_REMOVE` مُهر را با همان آبجکت‌هایی که توصیفشان می‌کند پاک می‌کند — همان
قاعده‌ای که `ClearTopologyAdoptionStamp()` بغلش از قبل داشت. هزینه‌اش: پویشِ ۱۱-پیشوندیِ
یک‌باره فقط در **اتصالِ مجدد به همان چارت** (سوئیچِ تایم‌فریم `REASON_CHARTCHANGE` است و مُهر
را نگه می‌دارد، پس وعدهٔ P-PERF-38c دست‌نخورده است).

۱۶۶ → **۱۷۰** تستِ خودآزمون؛ گیتِ جدید `teardown-census` روی ۲۵ خانواده، **نتیجه: clean**.

## §35 — «وضعیت را با کار قفل نکن»: مقداری که کل نردبان از آن مشتق می‌شود، داخلِ ترمز رندر جا ندارد (2026-09-13، P-UI-61)

گزارش: «درگ خط کاستوم پرایس روان نیست و لگ داره، و وقتی خط را رها میکنم سطوح از یک جای
دیگه رسم میشن، همون سطوح نیستن». یک باگ، دو نشانه.

**۱. لگ = وضعیت و تصویر با هم ترمز شده بودند.** کل نردبان از یک مقدار مشتق می‌شود
(`g_customTHStartPrice` → `GetMidpointPrice` → `midpointPrice`) و درگ آن را **داخلِ دریچهٔ**
رندر می‌نوشت:

```mql4
if(nowMs - g_lastDragRedrawTime > DRAG_REDRAW_THROTTLE_MS)
{
    g_customTHStartPrice = currentLinePrice;   // ← لنگر، ترمز شده
    RedrawAllObjects(true);
    g_lastDragRedrawTime = nowMs;
}
```

نتیجه: **آبجکتِ خط** با کرسر هم‌قدم بود (MT4 خودش می‌بردش، carry ما می‌نوشتش) ولی **لنگر** با
نرخِ دریچه جلو می‌رفت؛ بقیهٔ رویدادهای داخلِ دریچه **دور ریخته** می‌شدند (نه «بدهی»). پس
نردبان به اندازه‌ای عقب می‌ماند که به این بستگی داشت که رهاکردن کجای دریچه بیفتد.

**۲. جهش در لحظهٔ رهاکردن = سه نویسنده برای یک مقدار.** `RedrawAllObjects` قیمت را از
**کلید ذخیره‌شده** حل می‌کند (P-UI-56)، اما کانالِ carry هیچ‌وقت ذخیره نمی‌کرد (فقط کانالِ
درگِ نیتیو ذخیره می‌کرد). پس کلید قیمتی از **قبل از گِسچر** را داشت، شاخهٔ `priceMoved` لنگر را
با آن **بازنویسی** می‌کرد، و کل نردبان جایی ساخته می‌شد که کرسر هرگز آنجا نبود. حقیقتِ گِسچر
(قیمتِ خودِ آبجکت) هیچ‌کدامِ این سه نبود.

**درسِ گیت‌شدنی:** «وضعیت» را با «کار» در یک گارد نگذارید. اگر مقداری کل تصویر را تعیین
می‌کند، روی **هر** رویداد به‌روز می‌شود؛ فقط **کار** (پاسِ سنگین) بودجه دارد، و کاری که رد شد
**بدهی** است نه دورریز.

```mql4
bool CustomPriceDragAnchorSet(const double price)   // تنها نویسنده در طول گِسچر
{
    if(!MathIsValidNumber(price) || price <= 0.0) return false;
    if(MathAbs(price - g_customTHStartPrice) <= _Point * 0.5) return false;  // بی‌حرکت: یک مقایسه
    g_customTHStartPrice = price;
    g_thStartPointType = TH_START_POINT_CUSTOM_PRICE;
    g_customPriceKeyboardOverride = true;
    CustomPricePersistPlacement(price);   // کلید هرگز عقب‌تر از لنگر نمی‌ماند
    g_redrawTHLevelsNeeded = true;
    return true;
}

void CustomPriceDragFrame(const bool force)          // تنها بده‌کارگیرِ پاسِ سنگین
{
    s_cpDragFrameOwed = true;
    if(!force && GetTickCount() - g_lastDragRedrawTime <= DRAG_REDRAW_THROTTLE_MS) return;
    CustomPriceDragReassertLock();
    RedrawAllObjects(true);
    ThrottledChartRedraw();
    g_lastDragRedrawTime = GetTickCount();
    s_cpDragFrameOwed = false;
}
```

و رهاکردن، از **آبجکت** تسویه می‌کند و فریم را **مجبور** می‌کند:
`double settledPrice = ObjectGetDouble(0, g_customPriceHorizontalLineName, OBJPROP_PRICE, 0);`
⇒ `if(movedByGesture) { CustomPriceDragAnchorSet(settledPrice); CustomPriceDragFrame(true); }`
⇒ یک کلیکِ بی‌حرکت هنوز **صفر** فریم هزینه دارد (P-UI-54 دست‌نخورده).

**هزینه (خواستهٔ صریحِ کاربر: کم و فقط جای لازم):** حرکتِ بی‌تغییر = یک `MathAbs`؛ حرکتِ
واقعی = حداکثر یک پاسِ سنگین در هر ۵۰ms که **دو کانال در آن شریک‌اند** (قبلاً هر کانال دریچهٔ
خودش را روی همان استمپ داشت)؛ رهاکردن = دقیقاً یک فریم؛ بدونِ wipe و بدونِ بازسازیِ مرحله‌ای.
`g_lastDragRedrawTime` و `CustomPriceDragReassertLock` حالا **هر کدام یک مصرف‌کننده** دارند.

۱۷۰ → **۱۷۵** تستِ خودآزمون؛ گروهِ جدیدِ `drag-anchor` (۳ invariant + ۴ seed) و invariant
بازتنظیم‌شدهٔ `custom-price-mode`. کامپایل Full + Lite: ۰ خطا / ۰ وارنینگ.

## §36 — «یک مقدار، دو معنا» و «اشغالبودنِ کش، اجازهٔ دستزدن نیست» (2026-09-13، P-UI-62)

**۱. یک بیتِ بولین که دو سؤال را جواب میدهد، همیشه یک روز میترکد.**
`ENUM_ZONE_STYLE` سه اسلات داشت: Filled، Empty، Hidden. اسلاتِ سوم «آیا زونها
رسم شوند؟» را جواب میداد — همان سؤالی که سوئیچِ MID ZONES یک ردیف بالاتر جوابش را
میداد. این فقط تکرارِ UI نبود؛ یعنی کارت دو کنترل داشت که میتوانستند با هم
مخالف باشند، و کاربر دقیقاً همین را دید: «هیدن از همون دکمه بالا استفاده میشه دیگه».

قاعده: **محورِ ظاهر (چه چیزی رسم شود) و محورِ دیدهشدن (آیا رسم شود) دو محورِ جدا
هستند و نباید یک enum مشترک داشته باشند.** حالا هر سه اسلات «تصویر»اند:
FILLED = باند، EMPTY = لبه، OUTLINED = هر دو.

**۲. یک بیت برای «تصویر»، دو نیمهٔ مستقل را غیرقابلبیان میکند.**
تصویرِ زون یک `filled` بود و لبه دقیقاً `!filled`. پس `false` باید همزمان دو معنا
میداد و حالتِ سوم (باند + لبه) قابلِبیان نبود. نتیجهٔ جانبیاش این بود که ردیفهای
BORDER و BORDER WIDTH روی هیچی اثر نمیکردند: `OBJ_RECTANGLE` استایل/عرض را
نادیده میگیرد (دلیلِ وجودِ سه سگمنتِ لبه)، پس در حالتِ FILLED اصلاً خطی نبود که
آن ردیفها آن را استایل کنند.

قاعده: **هر نیمهای که میتواند مستقل حرکت کند، بیتِ خودش را دارد** — و اگر «تصویر»
را در کش نگه میداری، هر دو بیت را نگه دار، وگرنه «فقط یک نیمه عوض شد» قابلِتشخیص
نیست و لبه هرگز نه ساخته میشود نه پاک.

**۳. برگشتِ زودهنگام، کدِ پایینتر را از مسیرِ خطِا بیرون میبرد — نه از اجرا.**
مسیرِ EMPTY پیش از خواندنِ کش `return` میکرد و همان `return` علتِ باگِ گزارششده
بود: مهاجرتی که مستطیلِ جامانده را پاک میکرد، *پایینتر* از return بود، یعنی دقیقاً
در همان تصویری که به آن نیاز داشت غیرقابلدسترس. ما همان کار را آنجا گذاشتیم که
«تصویر» یکبار حل شود و **همهٔ** مسیرهای بعدی نتیجهٔ آن باشند.

**۴. در MT4، با ZORDER برابر، ترتیبِ نقاشی ترتیبِ ساخت است.**
باند و لبهاش عمداً یک ZORDER دارند (هر دو برای بقیهٔ لایهها یک چیزند)، پس هرکدام
که **دیرتر ساخته شود روی دیگری** را میپوشاند. لبه هرگز نباید زیرِ باند گم شود، پس
باند اول ساخته میشود و لبه بعد. این یک جزئیاتِ دیداری نیست که با تست کردن فهمیده
شود؛ یک ترتیبِ منبع است و گِیت همان ترتیب را assert میکند.

**۵. «در کش هست» با «روی چارت هست» یکی نیست.**
تا امروز هر اسلاتِ اشغال‌شده یک شیءِ زنده بود؛ P-UI-62 اجازه داد یک اسلات تصویری را
توصیف کند که هیچ شیئی زیرِ آن نام ندارد (تصویرِ لبهتنها). این یک نکتهٔ پنهان را
زنده کرد: همهٔ walk‌هایی که روی **هر** اسلاتِ اشغال‌شده یک write میزنند، برای نامِ
غیرموجود یک تماسِ ترمینالِ بیاثر میپرداختند (و `CacheValidate` هم از قبل
`exists = false` میگذاشت، یعنی این ناسازگاری همانجا هم کمین کرده بود). حالا یک سؤال
واحد وجود دارد: **`CacheSlotIsLive(i)`** — اشغالبودن سقفِ پیمایش است، زندهبودن
اجازهٔ دستزدن.

**۶. علامتِ منفی باید با ساختِ نام پاک شود، و cacheِ اصلی باید بر علامت مقدم باشد.**
`DeleteIndicatorObjectManaged` در توضیحش ادعا میکرد «نامی که ساخته شود در کشِ اصلی
میرود و علامت را سایه میکند»، ولی کد **اول** علامت را میخواند. یعنی میشد نامی که
واقعاً روی چارت است، بهخاطر علامتی که در تصویرِ بیلبه اثبات شده بود، حذف **نشود**.
حالا ترتیب همان است که توضیح میگفت (یک lookup، یکبار)، و سمتِ ساخت هم
`CacheForgetAbsent` را صدا میزند — کمربند و بند.

**۷. مقدارِ یک enum که بازنشسته میشود، ممکن است بعداً دوباره قانونی شود.**
`MZ = 2` قبلاً HIDDEN بود و حالا OUTLINED است؛ دو معنای متفاوت برای یک عددِ ذخیرهشده.
بدونِ مهاجرت، کاربری که Hiden داشت زونهایش روشن میشد؛ با مهاجرتِ بدونِ مهر، کاربری
که خودش OUTLINED را انتخاب میکرد در استارتِ بعدی مهاجرت میخورد. هر دو غلط است، پس
مهاجرت **یکبار با مهرِ اختصاصی** انجام میشود و مقدارِ اصلاحشده **همانجا روی دیسک
نوشته میشود**، نه به امیدِ save بعدی.

## §37 — «دو نیمهٔ یک تصویر، دو مالکِ شفافیت» و «خطی که زیر باند گم میشود» (2026-09-13، P-UI-63/64)

**۱. یک عدد برای دو نیمه = ناتوانیِ دائمی در تنظیم مستقل.**
`borderColor = finalColor` یعنی باند و لبه با یک شفافیت ترکیب میشدند. کاربر
«شفافیت خط و زون جدا از هم» را خواست. **بهترین راهکارِ بدونِ شلوغی، کارتِ نو نیست:**
لبه از قبل دو ردیف داشت (BORDER / BORDER WIDTH) در بخشِ GEOMETRY؛ پس شفافیتش
ردیفِ **سومِ همان سهگانه** است، در همان بخشی که مالکِ لبه است. یک ردیف، هیچ بخش و
زیرکارتِ تازه، و صفر جابهجایی در بقیهٔ کارت.

**۲. مقداری که دو جا یکی بود، باگش دیده نمیشود.**
تا پیش از این `finalColor` و `borderColor` همیشه یک مقدار بودند (چون یک ورودی
داشتند)، پس اگر جای اشتباه استفاده میشدند هیچکس نمیفهمید. وقتی آنها را از هم جدا
کردیم، **هر مصرفکننده باید صریح شود**: باند با `finalColor` رنگ و کش میشود، لبه با
`borderColor`؛ و کشِ رنگِ ورودیِ باند با `finalColor` مقایسه میشود، وگرنه باند با
حرکتِ ردیفِ خودش هرگز repaint نمیشود.

**۳. فیلدی که روی stack ساخته میشود، «پیشفرض» ندارد.**
`SZoneCreationRequest` روی stack ساخته میشود و اعضایش صفر نیستند (به همین دلیل هم
همهجا `startTime = 0` را صریح مینویسند). پس برای «شفافیتِ لبه» بهجای sentinelِ
مثبت/منفی به یک **گاردِ دامنه** تکیه کردیم: هر مقدار بیرونِ ۰..۱۰۰ یعنی «از باند
پیروی کن». نتیجه: تولیدکنندهای که مقدار را فراموش کند، ظاهرِ قبل از تغییر را
میگیرد؛ نه یک لبهٔ تصادفی.

**۴. با ZORDER برابر، خطِ نازک زیرِ باند گم میشود.**
لبه بعد از باند ساخته میشود تا **روی** آن بیفتد، ولی با عرض ۱px نیمی از خط زیرِ
باند است. پس تصویری که لبه میکشد، اگر عرضش هنوز پیشفرضِ تصویرِ بیلبه (۱) باشد،
به عرضِ دیدنی (۵) ارتقا پیدا میکند. قانون یکجا و دیدنی است (ردیفِ BORDER WIDTH همان
عدد را نشان میدهد) و **هرگز** مقداری که کاربر خودش انتخاب کرده را بازنویسی نمیکند.

**۵. «همون لبهٔ زون» یک ادعای هندسی است، نه سلیقه.**
اگر لبه با آفست/inset رسم شود، تبدیل به **خطِ دوم** میشود؛ پس سه سگمنتِ لبه با
همان `startTime`/`endTime` و `topPrice`/`bottomPrice` باند رسم میشوند و دو سگمنتِ
افقی هم مثل خودِ مستطیل `rayRight=true` دارند. گِیت همین آرگومانهای دقیق را
assert میکند، چون یک آفستِ کوچک فقط با چشمِ خیلی دقیق دیده میشود.

## §38 — «ادعای کلیک» باید به گِسچر تعلق داشته باشد، نه به ساعت (2026-09-13، P-UI-65)

گزارشِ کاربر یک جمله بود («بعضی وقتا عالی کار می‌کنه، بعضی وقتا قاطی می‌کنه؛ مثلاً دکمهٔ بستن
کار نمی‌کنه یا سطوح تریگر روشن/خاموش نمی‌شه») و هیچ‌کدام از آن‌ها یک باگِ ثابت نیستند — یعنی
دنبال چیزی بودیم که *زمان* در آن دخیل است. پیدا شد: `UISuppressNextClick()`.

معنای این تابع دقیقاً یک چیز است: «button-up ای که این گِسچر را تمام می‌کند، خرج شده —
release نباید دوباره عمل کند.» اما هویتش یک مهلتِ ساعتِ دیواری بود:
`g_UIClickSuppressUntil = GetTickCount() + 350`. هر دو طرفش غلط بود:

1. **نشت (نقطهٔ اصلی).** این ادعا در **زمانِ press** مسلح می‌شود (تقریباً همهٔ ۲۴ محلِ
   مسلح‌سازی) و release همان رویدادی است که باید خورده شود. اگر کاربر دکمه را بیشتر از
   پنجره نگه دارد (press → بخوان → release)، ادعا قبل از release خودش مُرده است: آن‌وقت
   release بدون هیچ محافظی به `HandleButtonClick` / `PnlHandleClick` می‌رسد، روی کنترلی
   که press اش **قبلاً** عمل کرده. طولِ press انتخابِ کاربر است ⇒ خرابی ذاتاً تصادفی است.
2. **زیاده‌خوری.** `UIShouldSuppressClick()` هرگز مهلت را پاک نمی‌کرد، پس یک release
   آزمونِ زمان را مصرف می‌کرد و **باقیِ پنجره** کلیکِ واقعیِ بعدی را می‌خورد: «X را می‌زنم،
   هیچی نمی‌شه، دوباره می‌زنم کار می‌کنه» — و بستگی دارد که release قبلی کجای پنجره افتاده
   باشد. یعنی همان شکلِ P-UI-01؛ P-UI-40c این قالب را برای latch لانگ‌پرس درست کرده بود و
   پنجرهٔ اطرافش جا مانده بود.

**قاعدهٔ درست، همان قاعدهٔ همیشگیِ پروژه: latch هویتِ گِسچرِ خودش را حمل می‌کند و دقیقاً یک
بار توسط همان مصرف می‌شود.**

- `MousePressStart` روی **لبهٔ بالا** شمارندهٔ press را جلو می‌برد: `g_UIPressSeq++`
  (تست روی `pressStart`، نه روی `leftDown` — هر بار که می‌خواهی «لبه» را تشخیص بدهی، این
  تفاوت همان جایی است که باگ پیدا می‌شود).
- `UISuppressNextClick()` حالا سه چیز را ثبت می‌کند: `s_uiClickClaim = true`،
  `s_uiClickClaimDown = g_MouseWasDown` (ادعا زیرِ pressِ زنده مسلح شد؟) و
  `s_uiClickClaimSeq = g_UIPressSeq` (زیرِ کدام press). برای فرمِ «روی release مسلح شد»
  (انتهای درگِ ارب/پنل/میکسر، یا کارتی که خودِ release بازش کرده) که pressی برای بستن به
  آن وجود ندارد، یک TTL کوتاه نگه داشته می‌شود — تنها جای باقی‌ماندهٔ ساعت، و آن هم لحظهٔ
  release مسلح می‌شود نه لحظهٔ press.
- `UIShouldSuppressClick()` **مصرف می‌کند**: claim را پاک می‌کند و بعد
  `UI_RELEASE_ECHO_MS = 80` اکو می‌دهد، چون MT4 یک release فیزیکی را می‌تواند هم به‌صورت
  `CHARTEVENT_CLICK` و هم `CHARTEVENT_OBJECT_CLICK` تحویل بدهد و هر دو مالِ همان گِسچرند.
  یک pressِ جدید ادعای مصرف‌نشده را بازنشسته می‌کند (`s_uiClickClaimSeq != g_UIPressSeq`)،
  پس ادعا هرگز از گِسچرش زنده‌تر نمی‌ماند.
- `UIReleaseClaimReset()` از `CreateMenu()` صدا زده می‌شود — تنها نقطهٔ ورودِ UI که **هر**
  attach (و هر تعویض تایم‌فریم) اجرا می‌کند — با دو نرده: زیرِ pressِ زنده نه
  (`g_MouseWasDown`)، و وسطِ release نه (پنجرهٔ اکو)، چون `CreateMenu` از داخلِ
  `HandleButtonClick` هم صدا زده می‌شود، یعنی داخلِ همان dispatch.

**هزینه:** حالت پایدار (بدونِ ادعا) دو خواندنِ bool است؛ مسیرهای مسلح‌سازی فقط یک store
اضافه گرفتند. در عوض هیچ ساعتی — و در نتیجه هیچ drift با بارِ تایمر/تیک — در تصمیم دخیل نیست.

**درسِ عمومی:** هر جا لچی با «مهلتِ زمانی» شناسایی می‌شود، دو خرابیِ متضاد هم‌زمان وجود
دارد: گِسچرهای کندتر از پنجره نشت می‌کنند و گِسچرهای سریع‌تر، کلیکِ بعدی را می‌خورند. راه‌حل
نه «بلندتر کردن پنجره» است و نه «کوتاه‌تر کردن»: **باید هویت را از ساعت به گِسچر منتقل کرد.**

**وابستگی‌ها (خواستهٔ خودِ کاربر — کامل بررسی شد):** ۲۴ محلِ مسلح‌سازی در
`BiotakMenu`/`BiotakPanels` همه از یک کلاس‌اند («release این گِسچر خرج شده») و هیچ‌کدام
معنایی را که نشود با press-seq بیان کرد ندارند؛ تنها ۲ مصرف‌کننده وجود دارد (هر دو
`if(UIShouldSuppressClick()) { UILongPressLatchClear(); return; }` در `BiotakPanels`) و
اسم/امضا عوض نشد، پس گیتِ `longpress-latch` که متنِ همین دو خط را قفل می‌کند دست‌نخورده
معتبر است. `Biotak_PanelFlow_Test.mq4` (PART 5 = «پنجرهٔ کهنه نباید X را بخورد») با
API جدید **کامپایل می‌شود: ۰ خطا/۰ وارنینگ**، چون آن تست مسیرِ press را می‌زند و مسیرِ press
هرگز claim را نگاه نمی‌کند. `Biotak Trigger TH3 Lite.mq4` هیچ‌کدام از
`BiotakMenu/BiotakPanels/BiotakKit` را include نمی‌کند (بدونِ UI ⇒ بدونِ claim)، پس
`UIReleaseClaimReset` اصلاً در آن بیلد وجود ندارد و نیازی به فراخوانی ندارد؛ `Biotak Sync
Compare.mq4` هم این API را لمس نمی‌کند (گرپ کلِ مخزن: فقط ۳ فایل). `DragCanGrab` (مالکِ
داوریِ درگ) بررسی شد و **دست‌نخورده ماند**: پاک‌کردنِ زودهنگامِ `g_DragOwner` روی لبهٔ پایین،
مسیرِ جبرانِ `ChartPointerFinalizeOnUps` را از کار می‌انداخت و `PnlCommitMove` (ذخیرهٔ
جایِ پنلِ درگ‌شده‌ای که release اش گم شده) را می‌خورد؛ خودِ finalizer روی هر button-up مالک
را پاک می‌کند و release هم X را می‌بندد، پس آن مسیر نیازی به تغییر ندارد.

**گیت:** گروهِ تازهٔ `click-claim` در `probe-budget-audit` (۵ اینواریانت + ۹ seed) ⇒
سلف‌تست **۲۰۶/۲۰۶**؛ probe/write/gesture/ui-text/zorder تمیز؛ سیم در ۹۶/۱۲۰/۱۴۴ تمیز؛
Full + Lite **۰ خطا / ۰ وارنینگ**.

---

## §24 — «شدو ها باید اینطوری باشه»: شکلِ کندلِ HTF یک تنظیم است، و یک مالک دارد (2026-09-14، P-UI-68)

**گزارش کاربر:** یک اسکرین‌شات از یک کندلِ HTF. سایه یک **باکسِ پُر و کوچک** است که
دقیقاً وسطِ یک بدنهٔ پهن نشسته، و کندل‌ها به هم نمی‌چسبند. چیزی که شیپ می‌شد: سایه یک
خطِ نازکِ ۱ پیکسلی بود (در وسطِ **تقویمی**)، بدنه تمامِ پریود را می‌گرفت، و تنها اسلایدری
که باید ضخیمش می‌کرد (`WICK WIDTH`) عملاً فقط عرضِ یک خط را عوض می‌کرد.

**چهار علت، یک شکل:**
1. **شکل، تنظیم نبود.** هیچ‌جای موتور نمی‌دانست بدنه چند درصدِ پریود را پر می‌کند یا سایه
   چقدر عرض دارد؛ هر دو «هر چه کدِ رسم حساب می‌کرد» بودند. (⇒ دو تنظیمِ تازه: `SHADOW GAP`
   ۰..۴۰٪، `SHADOW WIDTH` ۶..۱۰۰٪؛ ردیف‌ها **APPEND** شدند تا هیچ آدرسی جابه‌جا نشود.)
2. **وسط و هر سهم در زمانِ تقویمی اندازه‌گیری می‌شد.** `ot + (nt - ot) / 2`: وقتی بازار
   داخلِ یک کندل تعطیل است (W1 دوشنبه→دوشنبه، آخرین D1 هفته، MN روی تعطیلات)، تقویم
   ≈۷۰٪ می‌افتد بیرون. سهمِ تقویمی هم همان سهمِ **رسم‌شده** نیست؛ یعنی دقیقاً تایم‌فریم‌های
   بالا بد شکل می‌شدند. (⇒ همهٔ لبه‌ها در فضای **ایندکسِ چارت**، جایی که فاصلهٔ ایندکس =
   فاصلهٔ پیکسل.)
3. **عرض، write-once بود.** upsert رنگ و هندسهٔ سایه را مقایسه می‌کرد و **عرض** را نه؛ پس
   اسلایدر تکان می‌خورد، `REFRESH_HTF` همه‌چیز را از نو می‌کشید، و سایهٔ زنده همان ۱ پیکسلِ
   تولدش می‌ماند (همان درسِ P-UI-66، این‌بار روی شیءِ دیگر).
4. **رنگِ «تنظیم‌نشده» سیاه رنگ می‌شد.** یک هدفِ رنگی که عمداً `clrNONE` است («از کندلِ
   خودش پیروی کن») در نوارِ رنگِ کارت با `clrNONE` پِینت می‌شد — و `clrNONE` در MT4 یعنی
   مشکی؛ پس «دنبالِ کندل» از «مشکی» قابل تشخیص نبود.

**فیکس:** یک مالکِ هندسه (`HTFCandleGeometry(ot, nt)`) که هر چهار لبه را برمی‌گرداند؛
سایه از همان upsert گارددارِ بدنه به‌صورت **مستطیلِ پُر** کشیده می‌شود (`OBJ_TREND` بازنشسته،
شاخهٔ نامِ ناشناس یک **TYPE FENCE** گرفت تا ویکِ trend-lined که با تمپلیت می‌آید دوباره
ساخته شود نه بازنویسی)؛ `WICK WIDTH` → `SHADOW MIN` شد (کفِ پیکسلیِ سایه: `%` روی چارتِ
زوم‌اوت به صفر می‌رسد) و زوم یک‌بار در هر پاس خوانده می‌شود؛ کلیدهای تازه ذخیره/کلَمپ/پاک
می‌شوند و `PnlCsetCellColor` یک هدفِ بی‌رنگ را با چهرهٔ AUTO می‌کشد نه مشکی.

**درسِ عمومی:** اگر کاربر چیزی را «نشان می‌دهد» و می‌گوید «باید این‌طوری باشه»، آن شکل
باید به **تنظیم** تبدیل شود، نه به چند عددِ پخش‌شده در کدِ رسم. و هر عددی که «سهم از یک
فاصله» است باید در همان فضایی اندازه‌گیری شود که **کشیده می‌شود** (ایندکسِ بار)، وگرنه
همان تنظیم روی تایم‌فریم‌های بالا معنای دیگری پیدا می‌کند.

**گیت:** گروهِ تازهٔ `htf-geometry` در `write-budget-audit` (۷ اینواریانت + ۷ seed، از جمله
«رنجِ اسلایدرِ کارت == کلَمپِ موتور») ⇒ سلف‌تست **۴۹/۴۹**؛ probe/write/gesture/ui-text/zorder
و `panel-mt4-sim --audit` تمیز؛ Full + Lite **۰ خطا / ۰ وارنینگ**.

---

## §39 — «این کلش بیاد اینجا»: یک کارتِ بصری هرگز دو لنگره نیست (2026-09-14، P-LBL-06/07)

**مسئله:** کاربر یک بلوک را با فلش نشان داد و گفت «بیارش پایین»، بعد گفت «بیار اینجا».
دو بار رفت‌وبرگشت، دو ادیت، دو کامپایل — و هر بار فقط **یک نیمه** جابه‌جا شد یا فقط
جای اسپیس عوض شد، چون در کد همان کارتِ بصری دو لنگر مستقل داشت: برند وHunter از
`inpLabelsMarginTop` بالا-راست می‌آمدند و سطر `#SL/TP` از `inpLabelsMarginBottom`
پایین-راست. «بلوک» فقط در چشم کاربر یکی بود.

**راه:**

1. **تصویر را به یک لنگر بچسبان.** هر کاری که کاربر «یک کارت» می‌بیند، باید یک مالک
   هندسه داشته باشد که همهٔ ردیف‌هایش از آن مشتق شود (`TRexStampBottomY`): سطر تریدپلن
   پایین است، Hunter یک ردیف بالاتر، برند یک ردیف بالاتر از آن — رشد به **بالا** رخ می‌دهد،
   پس هم‌پوشانی ریاضی غیرممکن می‌شود، نه «به‌احتمال کم».
2. **جهت لنگر یک تصمیم است.** همان لحظه‌ای که بلوک از گوشهٔ بالا به پایین می‌رود،
   گوشه/انکرِ تک‌تک اجزا (`CORNER_RIGHT_LOWER`/`ANCHOR_RIGHT_LOWER`) و معنای «بالا»
   عوض می‌شود (`yBrand - 6` قدیم ⇒ `yBrand + brandRow - 6`). یک شیءِ بالا-لنگر جامانده
   در یک استک پایین-لنگر، دقیقاً همان «کارت دو تیکه» است.
3. **پیچ افقی/عمودی باید اندازه‌گیری شود، نه حدس.** gام ردیف = `PnlRawLineH(inpFontSize)`
   (مالک تکِ em خام، همان که `PnlRawTextW` با آن اندازه می‌گیرد) — نه `fontSize + 6`.
4. **ورودی کاربر باید کلمپ داشته باشد — دو تایی.** کلمپ اول `0..20` (خواستهٔ او)، کلمپ
   دوم **ظرفیت خود چارت**: `maxRows = (chartH - pad - em - pitch - base) / pitch` از
   `GetCachedChartHeight()` (کش ۱۰۰ms، همان الگوی width). بدون این یکی، اینپوت ۲۰ روی
   چارت ۴۰۰ پیکسلی کارت را از سقف می‌بُرد.

**درس عمومی (دور اول):** «فاصله تا لبهٔ چارت» یک عددِ بین دو **گوشه** است؛ اگر یک کارت
دو گوشه داشته باشد، آن یک عدد دو معنا می‌شود و هر تنظیم جدید فقط نیمی از کارت را
حرکت می‌دهد. و هر عددی که کاربر می‌تواند تایپ کند باید قبل از رسم به **ظرفیت واقعی**
کلَمپ شود، وگرنه اندیکاتور با یک اینپوت بی‌گناه چارت را ترک می‌کند.

**دور دوم (همان روز، اسکرین‌شات بعدی — P-LBL-08):** «چرا این‌قدر از هم فاصله دارند،
TRex وسط باشه، و TRex چرا به هم چسبیده». سه حدس، سه باگ، همه در همان کد تازه:

1. **درزها ناهمگون بودند** چون Y از **لنگر** سطر پایینی شمرده می‌شد: سطرها `pitch` فاصله
   داشتند ولی چون ارتفاع متن (`em`) داخلش حساب می‌شد، یک درز `gap` و درز دیگر `em+gap`
   می‌شد. قاعده: درز = فاصلهٔ **بین جعبهٔ متن‌ها**، نه بین لنگرها — `seam = y_next − (y_prev + em)`.
2. **همه راست‌چین بودند، پس محور مشترک نداشتند.** با لنگر راست، مرکز هر سطر = `x + w/2`؛
   برای هم‌مرکز شدن باید `x = rightEdge + (cardW − w)/2` با **یک `cardW` مشترک** (عریض‌ترین
   سطر) — و `cardW` را روی همهٔ سطرها حساب کن، نه فقط سطرهای روشن، وگرنه با هر کلید سطرها می‌لغزند.
3. **فاصلهٔ `TR` و `ex` حدس پیکسلی بود** (`2.0 * brandSize * 0.7`): عرض `ex` در Arial Bold
   ۱۱۱۲/۱۰۰۰em است ⇒ ۲۱px در ۱۴pt/96DPI ولی ۳۱px در 144DPI و ۴۱px در 192DPI. یعنی حدس
   در 96/120/144/192DPI به‌ترتیب ۱.۴ و ۶.۰ و ۱۱.۴ و ۲۱.۴px **کم** بود و حروف بالای
   96DPI روی هم می‌افتادند.
   این همان تلهٔ P-UI-30/P-UI-42 است که این‌بار روی خودِ چارت (و به‌خاطر canvas/خارج از پنل)
   از دید گیت‌های موجود پنهان مانده بود.

**مالک واقعی:** `STrexCardLayout` + `TRexTradeCardLayout()` — هر سه سطر (Y **و** X)،
عرض‌های اندازه‌گیری‌شده، محور مرکزی و فاصلهٔ جفت `TR`/`ex` از همین یک تابع می‌آید.
متن سطرها هم مالک تک دارد (`TradePlanTPRowText` / `TradePlanHunterText`)، چون «رشته‌ای
که اندازه می‌گیری» و «رشته‌ای که می‌کشی» باید یکی باشند.

**گیت:** `python tools/chart-label-audit.py` (تازه): پنج چک سورسی (مالک‌تک Y و X، یک
خانوادهٔ گوشه/انکر، **عدم حدس عرض** — الگوی `brandSize * 0.7` و فاصلهٔ دو-فاصله‌ای متنی،
مالک‌تک em خام، پیش‌فرض‌ها و مرزهای کلمپ از **خود سورس**) + مدل **۳۲٫۴۰۰ ترکیب** با ۷ ناوردا
(کلمپ، درزهای برابر، هوای درخواستی کاربر، یک محور مرکزی، جفت اندازه‌گیری‌شده، سقف چارت،
یک‌نوایی) + جا‌شدن پیش‌فرض‌های شیپ‌شده در چارت ۳۲۰px؛ `--model` خطای حدس بازنشسته را در
۴ DPI چاپ می‌کند (۱.۴ / ۱۱.۴ / ۲۱.۴px روی‌هم‌افتادگی) و `--selftest` **۱۶/۱۶** فالت را
می‌گیرد؛ شش گیت + Full + Lite تمیز.

---

## §40 — «وقتی یکیش خاموش می‌شه بقیه جاشو بگیره»: اسلاتِ ثابت = حفره، فاصلهٔ مشترک = کارتِ کشیده (2026-09-14، P-LBL-09/10)

**مسئله:** سه ایراد بصری از یک اسکرین‌شات: (الف) با خاموش‌کردن یک سطر (مثلاً TP ROW)
بقیه سر جایشان می‌ماندند و **حفره** می‌افتاد؛ (ب) فاصلهٔ سطرها این‌قدر زیاد بود که «ظاهرش
به‌هم می‌ریزد»؛ (ج) خواستهٔ صریح: «با بار صفر». هیچ‌کدام یک تصمیم سلیقه‌ای نبود — هر سه
یک **قرارداد چیدمان** غلط بودند.

**درس‌ها:**

1. **سطرِ خاموش نباید اسلات رزرو کند.** هر چیدمانی که «جای هر ردیف از قبل مشخص است»
   به‌محض خاموش‌شدن یک ردیف تبدیل به حفره می‌شود. درستش **pack** است: از کف شروع کن و
   فقط برای هر سطرِ **کشیده‌شده** یک `pitch` بالا برو (`if(show) { y_i = y; y += pitch; }`).
   نتیجهٔ مستقیم: `drawn = 1 + showA + showB` هم باید داخل فرمول کلمپِ ارتفاع برود، وگرنه
   سطرِ خاموش یک واحد از ظرفیت چارت را بی‌دلیل می‌خورد.
2. **یک ورودیِ مشترک بین دو خانوادهٔ بصری، هیچ‌وقت برای هیچ‌کدام درست نیست.** کارتِ
   گوشه‌ای سه‌سطره و گریدِ ستون‌های لیبل هر دو `inpLabelRowGap` را می‌خواندند: عدد درست برای
   گرید (۱۸px) روی متنِ ۱۱پیکسلیِ کارت، درزی بزرگ‌تر از ارتفاع خودِ متن می‌ساخت. ورودیِ
   درست (`inpATRTradeLabelRowGap`، «compact gap between ATR trade rows»، پیش‌فرض ۱۰) وجود
   داشت ولی **هیچ ماژولی نمی‌خواندش** — همان P-UI-47، این‌بار در نقشِ «تنظیم درست، مصرف‌کنندهٔ غلط».
   هر خانواده = ورودی خودش، و گیت باید ورودِ ورودیِ مشترک را **ممنوع** کند، نه فقط ورودیِ درست را بخواهد.
3. **محور مرکزی روی سطرهای روشن است، نه روی همهٔ سطرها.** قاعدهٔ دور قبل («`cardW` روی همهٔ
   سطرها») جلوی لرزیدن را می‌گرفت ولی با خاموش‌کردن عریض‌ترین سطر، محور روی چیزی می‌ماند
   که هیچ‌کس اشغال نکرده. محور = عرضِ **عریض‌ترین سطرِ کشیده‌شده**.
4. **حضور = کلید (presence == the switch).** پاک‌کردنِ سطرِ خاموش را نگذار پایِ **call site**:
   در این کد مسیر relayout اول پیشوند `LBL_` را پاک می‌کند ولی پامپِ ۲ثانیه‌ایِ زنده نه — پس
   سطری که وسط کار خاموش می‌شد می‌توانست روی `Y`ای بماند که استکِ پک‌شده دیگر آن را رزرو
   نکرده. حالا **نویسندهٔ همان سطر مالک وجودش هم هست** (خودش `ObjectDelete` می‌کند) و دو
   مسیرِ رندر هر سه نویسنده را **بی‌قید** صدا می‌زنند. دو مالک برای delete = هم روح، هم دوباره‌کاری.
5. **«مالکِ تک» را در سطح pass تست کن، نه در سطح تابع.** «هر نویسنده مالک را صدا بزند» یک
   قاعدهٔ بی‌خاصیت بود (همان چیزی که cause اصلی بود)؛ قاعدهٔ درست: در هر تابعی که رسم می‌کند،
   `TRexTradeCardLayout(` باید **دقیقاً یک بار** و **قبل از اولین رسم** بیاید. این هم بار را
   قفل می‌کند (نه per-row، نه per-tick) و هم شکلِ غلط را غیرممکن می‌سازد.
6. **«بار صفر» یعنی صفر فراخوانی کرنل، نه صفر کد.** چیدمان = ۴ اندازه‌گیری متن + ۲ جعبهٔ em +
   یک ارتفاعِ کش‌شده؛ همه حسابِ محض روی جدولِ عرضِ گلیف، بدون `ObjectGet`/`TextGetSize` و
   بدون ساخت آبجکت. یک بار در هر pass (نه در هر تیک و نه در هر ردیف) ⇒ از دید CPU عملاً صفر.

**گیت:** `chart-label-audit.py` به شش چک سورسی رسید (مالک/انکر/اندازه‌گیری/em خام/**حضور**/
ورودی‌ها-از-سورس)، به I8 «سطرها پک‌شده‌اند» و به خواندنِ `TREX_CARD_MAX_ROW_GAP` از سورس +
صحتِ کلمپِ درز؛ `--selftest` حالا **۲۳/۲۳** فالت را می‌گیرد (سه فالتِ تازه دقیقاً همین سه
درس را می‌سنجند: نویسنده‌ای که خودش چیدمان می‌کند، مالکی که در یک pass دوبار صدا زده می‌شود،
و مالکی که ورودیِ مشترکِ گرید را می‌خواند). شش گیت + Full + Lite تمیز، و پیش‌فرضِ کارت
از سقفِ ۱۰۲px به **۸۶px** از کفِ چارتِ ۳۲۰px رسید (درزها ۱۸px ⇒ ۱۰px).

---

## §41 — «از پایین زیاد فاصله داره و شخصی‌سازی بشه»: نامِ ورودی، مالکیت را ثابت نمی‌کند (2026-09-14، P-LBL-11)

**مسئله:** کارت ۲۵ پیکسل بالای کف چارت نشسته بود و «شخصی‌سازی» نداشت. وسوسهٔ اول این است که
مقدار پیش‌فرض را عوض کنی؛ سؤالِ درست این است: **این عدد را واقعاً کی می‌خواند؟**

**راه:**

1. **قبل از تغییر، مصرف‌کننده‌ها را بشمار.** `inpLabelsMarginTop/Bottom/Left` و
   `inpLabelRowGap/FontSize` مثل یک خانواده به‌نظر می‌رسند، ولی جواب برای هر کدام فرق داشت:
   `inpLabelsMarginBottom` فقط یک خواننده داشت (همین کارت؛ پشتهٔ TH از
   `inpTHLabelsMarginBottom` استفاده می‌کند) ولی Top/Left/RowGap/FontSize هنوز با **ستون‌های**
   لیبل مشترکند. پس کم‌کردن پیش‌فرض Bottom فقط کارت را حرکت می‌دهد (امن)، ولی همان کار روی Top
   یا Left ستون‌ها را هم جابه‌جا می‌کند — و کامنت‌های گروه ۱۳ حالا دقیقاً همین را می‌گویند.
2. **پیش‌فرض جدید + کلمپ دستی.** کارت از ۲۵px به **۸px** از کف آمد (سقفِ کارت روی چارتِ ۳۲۰px:
   ۱۰۲ ⇒ **۶۹**) و ورودی دستی کلمپ می‌شود (`0..TREX_CARD_MAX_MARGIN_BOTTOM`) چون در init
   خام اعمال می‌شود: ۰ یا −۵ نباید سطرها را از کف بزند و ۵۰۰۰ نباید کارت را وسط صفحه پارک کند.
3. **ترجمهٔ «شخصی‌سازی» به یک فهرست مشخص.** برای کاربر نهایی، «قابل تنظیم» یعنی بداند کدام عدد
   کدام شيء را حرکت می‌دهد. سه ورودیِ کارت (`inpLabelsMarginBottom`، `inpATRTradeLabelRowGap`،
   `inpTrexStampGapRows`) در مستندات و در کامنت‌های گروه با همین نقش نام برده شدند و بقیه
   صریحاً «مشترک با ستون‌ها» علامت خوردند.
4. **دام نام‌گذاری را کامنت کن، نه اینکه اسم را عوض کنی.** برای کارت راست-لنگر،
   `inpLabelsMarginLeft` یعنی فاصله از لبهٔ **راست** (برای ستون‌ها: چپ). تغییر نام = شکستن
   فایل‌های `.set` ذخیره‌شدهٔ کاربر؛ درست‌ترین کار، کامنتِ صریح در محل تعریف است.

**درس عمومی:** «فلان ورودی را عوض کن» را با **شمارش خواننده‌ها** شروع کن، نه با ویرایش عدد.
یک ورودی مشترک بین دو سطحِ بصری، هر تغییری را دو-معنایی می‌کند؛ و «شخصی‌سازی‌پذیر» یعنی
مستند شود که هر عدد کدام شيء را حرکت می‌دهد.

---

## §42 — «رنگ ها کار نمی‌کنه»: کنترلی که رنگش با پس‌زمینهٔ خودش یکی است، یک کنترلِ خالی است (2026-09-14، P-UI-69)

**شکایت:** «رنگ ها کار نمی‌کنه» + اسکرین‌شاتِ ردیفِ `COUNT COLOR`: هشت خانه که آخری‌اش
مثل یک **جای خالی** دیده می‌شد و با زدنش رنگی اعمال می‌شد که روی چارت ناپدید بود.

**دو علتِ مستقل، هر دو در همان کنترل‌ها:**

1. **رنگی که رنگِ پس‌زمینهٔ خودش است، حفره است.** هر سواچ یک حاشیهٔ نازکِ مشترک
   (`PNL_CLR_LINE`) داشت. رنگِ خانهٔ هشتم `#141414` روی صورتِ کارت `#1A2029`:
   پر‌کردن **1.16:1** و حاشیه **1.08:1**. ماتریس ۱۹۰‌رنگهٔ پالت روی صورتِ خودش
   (`#20262F`) هم یک خانهٔ `(33,33,33)` با **1.05:1** داشت. از ۱۹۸ رنگی که پنل
   می‌تواند به‌عنوان سواچ بکشد، **یازده** رنگ بین 1.05:1 تا 1.68:1 بودند و اولین
   رنگِ واقعاً دیده‌شدنی 1.72:1 بود. یعنی کاربر هشت خانه را «هفت رنگ + یک جعبهٔ
   خالی» می‌خواند — و حق داشت.
2. **چیپِ `+` به‌عنوان سواچِ ۰ پارس می‌شد.** شرط، **پیشوندی** بود:
   `StringLen(kind)==2 && StringGetCharacter(kind,0)=='Q'`. شناسهٔ چیپِ پیکر `"QA"`
   از این شرط رد می‌شد و `StringToInteger("A")` می‌شد **۰** — یعنی چیپی که
   تولتیپش «پیکرِ کامل رنگ» را وعده می‌دهد، در واقع رنگِ **اولین** سواچ را اعمال
   می‌کرد. در مسیر معمول، تستِ هندسیِ `PnlColorAddHit` جلویش را می‌گرفت؛ ولی هر
   کلیکی که به روترِ آبجکت می‌رسید، اشتباه بود. «یک شرطِ پیشوندی» = «یک شناسهٔ
   دیگر که فردا اضافه می‌شود هم از همان شرط رد می‌شود».

**راه‌حل:**

1. **یک مالک برای «آیا این سواچ روی پس‌زمینهٔ خودش دیده می‌شود؟»** —
   `PnlSwatchBorder(fill,backdrop)`، با `PnlLum`/`PnlContrast` دقیقاً بر اساس
   WCAG (لومینانسِ نسبی روی کانال‌های خطی‌شده). هر **هفت** خانوادهٔ سواچ از همین
   مالک حاشیه می‌گیرند. `color` در MQL4 **بی‌علامت** است، پس گاردِ `clrNONE` باید
   اول به `int` کست بخورد (`c < 0` = وارنینگ ۶۵ و همیشه غلط).
2. **آستانه اندازه‌گیری شد، انتخاب نشد.** روی ۱۹۸ رنگِ قابل‌رسم، مرزِ طبیعی بین
   1.68:1 و 1.72:1 افتاد؛ پس `PNL_SWATCH_MIN_CONTRAST 1.7` دقیقاً روی همان
   سواچ‌هایی آتش می‌کند که به‌صورت «فضای خالی» خوانده می‌شوند و روی هیچ‌کدام دیگر.
3. **زیرِ آستانه، رنگِ سواچ عوض نمی‌شود** — کاربر رنگِ مشکی خواسته، پس راه‌حل
   «حاشیهٔ دیده‌شدنی» است نه «جانشین‌کردنِ رنگ». حلقهٔ انتخاب (accent) هنوز کارِ
   صدازننده است.
4. **شناسه‌ها دقیق پارس می‌شوند:** `StringLen==2` + `'Q'` + یک **رقم** + کرانِ
   `PNL_QSW_N`.
5. **پاسِ رفرش هم یک «داور» است** — و اولین نسخهٔ همین فیکس این را جا انداخت:
   فقط پاسِ ساخت عوض شد، در حالی که `PnlUpdateRow` هنوز برای سواچ‌های انتخاب‌نشده
   حاشیهٔ تختِ `PNL_CLR_LINE` را می‌نوشت (یک داورِ دوم) و پیش‌نمایش/سلول‌های رنگ را
   اصلاً حاشیه نمی‌داد. یعنی به‌محض عوض‌کردن رنگ — یا هر پمپِ ۲ثانیه‌ای که ردیف را
   دوباره می‌کشد — حفرهٔ ناپیدا **بی هیچ rebuildی** برمی‌گشت. همان شکلِ «فیکسِ فقط-در-
   ساخت» (P-UI-66) یک لایه بالاتر. گیت حالا پاسِ رفرش را هم به‌عنوان یک سطحِ
   پوشش‌داده‌شده می‌خواند و دو سیدِ منفیِ جدا برایش دارد (۱۵/۱۵ فالت).
   **قاعده: «مالک تک» یعنی شمردنِ همهٔ نویسنده‌ها — نه فقط نویسنده‌ای که اول پیدا کردی.**
6. **مسیرهای زندهٔ خودِ پاپ‌اور هم داور هستند.** `PalDraw` حاشیهٔ بلوکِ
   «رنگ فعلی» را **یکبار در لحظهٔ باز شدن** می‌نوشت؛ پس کشیدنِ میکسر تا نزدیکِ مشکی،
   همان جعبه‌ای که خودِ پالت می‌گوید «انتخاب شد» را دوباره خالی نشان می‌داد. حالا
   `PalUpdateLive` هم آن را از مالک می‌گیرد.
7. **نوار RECENT یک نقاش دارد، نه یک بلوکِ درون‌خطی.** `PaletteApplyColor` در هر
   اپلای لیست را جلو می‌برد (در درگِ میکسر، ۳۰ بار در ثانیه)، و نوار فقط داخلِ `PalDraw`
   کشیده می‌شد — یعنی کاربر رنگ می‌گرفت و می‌دید فهرستِ رنگی که خودش ساخته بود برابر
   نیست. حالا `PalPaintRecents` **تنها** جایی است که خانه‌های `r{i}` ساخته می‌شوند،
   و هر دو مسیر (بازکردن و اپلای) از آن رد می‌شوند؛ فقط **درگِ زنده** با پنجرهٔ
   `PAL_RECENTS_MS` (۱۵۰ms) هم‌آمیخته می‌شود و دُمِ آن در لحظهٔ رهاکردن فلاش می‌شود —
   پس نه یک رنگ عقب می‌ماند و نه به هر تیکِ درگ یک رفرشِ پاپ‌اور اضافه می‌شود.

**درس عمومی:** «کار نمی‌کند» همیشه یعنی «منطق غلط است»؛ گاهی یعنی **کنترل
دیده نمی‌شود**. قبل از بازکردن موتورِ رنگ، کنتراستِ واقعیِ هر کنترل با پس‌زمینه‌اش را
با یک عدد بسنج (اینجا 1.05:1) — و آستانه را از خود داده بیرون بکش، نه از سلیقه.
و هر شناسه‌ای که با **پیشوند** تطبیق داده می‌شود، فردا یک خواهرِ ناخواسته پیدا می‌کند.

---

## §43 — «پنل ها رو نمیشه درگ کردش»: فالبکِ درگ روی کل کارت، کنترل‌ها را می‌خورد (2026-09-14، P-UI-70)

**شکایت‌ها:** (۱) «تنظیمات شخصی‌سازی این trex sl , tp ها چرا در پنل نیستش».
(۲) «پنل ها رو نمیشه درگ کردش». (۳) «مشکلات ui خیلی زیاد … همه رو پیدا کن».

**چهار علت، و یکیشان در گیتِ خودم:**

1. **درگ فقط از هدر کار می‌کرد.** تنها راهِ جابه‌جایی، نوارِ ۵۶ پیکسلیِ بالا بود؛
   کلیکِ بدنهٔ کارت هیچ کاری نمی‌کرد. پس بدنه باید **دستگیره** باشد — ولی این
   «دستگیرهٔ فالبک» خطرناک است: اگر **بعد از** تستِ کنترل‌ها نباشد، خودش کلیکِ
   دکمه‌ها را می‌بلعد و به‌جای عمل‌کردنِ دکمه، **کل کارت را زیر انگشتِ کاربر
   می‌کشد**. پس ترتیبِ تستِ جای‌ها یک ناوردایِ گیت است — برای **هر ده** کنترل
   (بستن، دراپ‌داون، انکر، نوب، ترک، سوییچ، cset، dual، رنگ‌افزا، بند)، نه فقط
   چهارتایی که اول به ذهنم رسید.
2. **کلیمِ درگ هیچ بازیابی نداشت.** رها‌کردنِ کلیم فقط از مالکِ هم‌نامش قبول
   می‌شد. اگر MT4 رویدادِ رها‌کردن را نمی‌رساند (از دست رفتن فوکوس، Esc، چارتِ
   دیگر)، `g_DragOwner` **برای همیشه** اشغال می‌ماند و همهٔ کنترل‌های مختصاتیِ
   چارت می‌مردند — «بعضی وقتا قاطی می‌کنه» با هر بار یک علامتِ متفاوت. حالا
   `PnlPressAllowed()` خودش را درمان می‌کند: کلیمی که دستِ دیگری است تا وقتی
   `TERMINAL_KEYSTATE_LEFT < 0` (یعنی دکمه **پایین** است) محترم می‌ماند و
   **لحظه‌ای** که نیست، پس گرفته می‌شود.
3. **ردیفِ بدونِ تنظیم.** شخصی‌سازیِ کارتِ TRex (فونتِ ردیف، درز، ردیف‌های اضافه،
   فاصلهٔ کف، پنج رنگش) فقط در دیالوگِ گروه ۱۳ بود: پنج تنظیم که در پنل هیچ
   کنترلی نداشتند. نُه ردیف به کارتِ برچسب اضافه شد و هرکدام **همان** تنظیمی را
   می‌نویسد که ورودیِ دیالوگ می‌نویسد — پس ردیفِ پنل هرگز با چارت اختلاف پیدا
   نمی‌کند. `PIP LABELS` هم که «چیه اصلاً»: معلوم شد **زنده** است (چهار ماژول
   می‌خوانندش) و نامش مشکل بود نه خودش — به `H/L PIP LABELS` تغییر کرد تا بگوید
   روی کدام خط‌ها اثر می‌گذارد.
4. **ردیفی که نوشتنش جایی نمی‌رسید.** ردیف‌های کارتِ برچسب که **هندسه** را
   عوض می‌کنند، مقدار را می‌نوشتند و تمام: هندسه در پاسِ برچسب خوانده می‌شود، پس
   چیپِ پنل رنگ می‌گرفت، عدد عوض می‌شد و چارت تا یک رفرشِ بی‌ربط تکان نمی‌خورد.
   حالا قاعدهٔ گیت این است: هر ردیفی که در همان arm یکی از متغیرهایی را بنویسد
   که `LabelFunctions` می‌خواند، **باید** در همان arm `g_labelsRelayoutNeeded`
   هم بگذارد.
5. **و گیتِ خودم خالی بود.** `body()` تابع را با **اولین اشارهٔ متنی** به نامش
   پیدا می‌کرد؛ یک کامنتِ توضیحی که فقط نامِ لایه را می‌برد، **جسمِ تابعِ بعدی** را
   برمی‌گرداند — و سه گروهِ کامل از چک‌ها برای دلیلِ غلط پاس می‌شدند. (یک `void `
   اشتباه هم باعث می‌شد هر سه لایه «گم‌شده» خوانده شوند، چون `PnlDefValSet` نوعش
   `double` است.) حالا `body()` روی **تعریفِ** سرخط لنگر می‌اندازد و پیشوندش باید
   نوعِ بازگشتی باشد. و مهم‌تر: **سیدی که لنگرش دیگر در سورس نیست، حالا گزارش
   می‌شود** — در همین گیت و در دو گیتِ خواهرش. کنترلِ منفیِ خالی، همان خالی‌بودنِ
   یک سطح بالاتر است.

**درس عمومی:** «مالکِ تک» یعنی **ترتیب و تعدادِ همهٔ مصرف‌کننده‌ها** — یک فالبک
که جای درستش نیست، نه فقط بی‌اثر است، بلکه عملِ کنترلی را که باید انجام شود
**به عملِ دیگری تبدیل می‌کند**. و هر کلیمی که بازیابی ندارد، یک قفلِ موقت است که
با اولین رویدادِ گم‌شده دائمی می‌شود. و پیش از آن‌که به گیت اعتماد کنی، به گیتی که
خودش را نمی‌سنجد اعتماد نکن: اگر لنگرِ یک سید پیدا نشود، آن سید پاس نمی‌شود،
**شکست می‌خورد**.

## §44 — «دکمه رنگ‌ها کار نمی‌کنه و می‌شه جداش کرد»: اسمی که ایندکسِ نمایش را حمل می‌کند، شبح‌ساز است (2026-09-14، P-UI-71/72)

دو تا شکایت که «یک باگ» بودند، به‌اضافهٔ یک قاعدهٔ غایب. هیچ‌کدام با خواندن کد
پیدا نشد؛ **از اسکرین‌شات اندازه‌گیری شد**.

1. **یک ردیف، دو بار نقاشی شده — یک بار بیرون از بدنهٔ کارت.** نامِ آبجکتِ هر ردیف
   ایندکسِ **نمایشی**‌اش را حمل می‌کند (`Pnl2_18_CS3`) و `PnlEnsureSpec` با جمع‌شدنِ
   یک سکشن، اعضا را حذف می‌کند ⇒ ردیف‌ها **renumber** می‌شوند. ردیف زیر ایندکسِ
   **جدید** کشیده می‌شد و خانوادهٔ قدیمی سرِ خطِ **قدیمی** روی چارت می‌ماند.
   اندازه‌گیری: نوارِ `CARD COLORS` دو بار وجود داشت، با همان X، **۸۵ پیکسل**
   فاصله — بالای سلول‌ها y=697 (خطِ ۱۰، ردیفِ زندهٔ داخل کارت) و y=782 (خطِ ۱۲ =
   همان ردیف وقتی «TRADE CARD» باز است، یعنی **زیرِ** لبهٔ پایینِ کارت در y≈769).
   کپیِ کهنه در لیستِ hit هیچ‌کس نیست (`PnlCsetHit` فقط ردیف‌های *فعلی* را
   می‌گردد) ⇒ «کلیک می‌کنم هیچ کاری نمی‌کنه»، و جای‌گیری‌اش دلیلِ «می‌شه جداش کرد»:
   نوار مثل یک تکهٔ کنده‌شده از پنل دیده می‌شد.

2. **حلِ یک‌جا: پاک‌سازی با پیشوند، نه لیستِ ایندکسی.** `PnlDestroy` کلِ پیشوندِ
   `Pnl<item>_` را با **یک** `ObjectsDeleteAll` می‌گیرد، از همان عبارتی که
   `PnlName` می‌سازد — پس هرگز از سازنده‌ها جدا نمی‌افتد و هیچ سقفِ ایندکسی
   نمی‌تواند از یک کارتِ رشدکرده کوچک‌تر بماند (لیستِ دستی روی ۱۶ می‌ایستاد و
   کارتِ ۲ نوزده ردیف و BASE BOX بیست‌وچهار ردیف دارد). هر مسیرِ بازسازی
   (`PnlOpen`، سوئیچِ تب/دراپ‌داون، جمع‌کردنِ باند، Reset) از
   `PnlCloseAll → PnlDestroy` رد می‌شود.

3. **قاعدهٔ غایب: کارتِ مودال، صاحبِ تکِ پرس است.** hit-boxهای رینگ **هندسی**
   هستند (`CircItemAt`/`ToolsItemAt`/مستطیلِ orb)، کارت **جابه‌جاشدنی** است و orb
   معمولاً همان‌جایی است که کارت می‌نشیند ⇒ پرس روی یک کنترلِ کارت که روی آیتمِ
   رینگ افتاده، **قبل از** `PnlHandleMouseMove` صاحبش را `DRAG_MENU` می‌کرد.
   `PnlPressAllowed` عمداً تسلیمِ ادعای دیگری می‌شود، پس کنترل **مرده** خوانده
   می‌شد و orb از زیرِ کارت با کرسر راه می‌افتاد. حالا `CircHandleMouseMove` در
   حالتِ `g_UIPanelOpen` **شروعِ** گرفتنِ تازه را رد می‌کند — ولی درگِ زندهٔ orb و
   long-pressی که **قبل** از بازشدنِ کارت مسلح شده بود تا رها شدن ادامه می‌یابند.
   (فلگ در `GlobalVariables.mqh` است، که **قبل** از منو و پنل include می‌شود؛ یک
   گارد روی فلگی که ماژول نمی‌بیند، یا کامپایل نمی‌شود یا گارد نیست.)

**درس عمومی:** **نام، آدرس است.** اگر داخلِ یک شناسه یک مختصات بگذاری، هر عملیاتی
که آن مختصات را جابه‌جا کند، شناسهٔ قدیمی را به یک شبح تبدیل می‌کند که «هیچ‌کس صاحبش
نیست» و کاربر آن را «کنترلی که درست کار نمی‌کند» می‌بیند. حلِ درست، پاک‌کردنِ
**کلِ خانواده با یک عبارتِ مشترک** است، نه شمردنِ اعضا. و «حالتِ مودال» باید یک
**پرچمِ خواندنی برای همهٔ موتورها** باشد: هر موتوری که با پرس شروع می‌شود، تا وقتی
کارت باز است نباید شروع کند. قاعده را در یک جا نوشتیم، نه در پنج موتور.

---

## §45 — «پشت پس زمینه نداره»: هنرِ مشتق (sliced) یک بیتِ جدید است — و بیتِ جدید تا اعلان نشود جاسازی نمی‌شود (2026-09-14، P-UI-71c)

**مسئله:** بدنهٔ کارت‌های WIDE (مثل ATR Labels با ۱۹ ردیف) به‌صورت «کاپِ سر +
یک نوار به‌ازای هر خط + کاپِ پا + واشِ `.fade`» از یک اسکینِ بیک‌شده برش می‌خورد
(`tools/slice-card-skins.py`) — و روی چارت هیچ‌کدام دیده نمی‌شد: کارت بدون هیچ
پس‌زمینه‌ای، فقط ویجت‌ها (لیبل/اسلایدر/سوئیچ) روی کندل‌ها. هر گیتِ موجود هم سبز بود.

**چرا:** MetaEditor یک بیت‌نقشه را فقط وقتی **جاسازی** می‌کند که یک `#resource`
صریح نامش را ببرد. نبودِ اعلان ⇒ کامپایلِ ۰ error / ۰ warning (لاگِ کامپایل هم
عمداً چیزی نمی‌گوید) و `OBJPROP_BMPFILE` روی نامی که جاسازی نشده ⇒ هیچ. پس دقیقاً
همان نیمه‌ای که خودِ کارت بود گم شد و کنترل‌های داخلش سالم ماندند. این همان کلاسِ
**P-PANELUI-01** است، دوباره: دفعهٔ قبل «فراموشیِ اعلان»، این‌بار «هنرِ تازه‌ای که
اسکریپتِ اعلان‌کننده برایش اجرا نشد». گیتِ `[body]` هم نمی‌توانست بگیرد: آن فقط
وجودِ فایل‌ها روی دیسک و درستیِ حسابِ چیدمان را ثابت می‌کرد، و هیچ‌کدام نمی‌گوید
ترمینال آن‌ها را **می‌بیند**.

**راه (به ترتیب اهمیت):**

1. **اعلان + تأیید در خودِ محصول.** `python tools/add-panel-resources.py`
   (idempotent) چهار اعلان را اضافه می‌کند. تأیید **در باینری**: MT4 نامِ منابع را
   UTF-16 ذخیره می‌کند، پس نام باید داخل بایت‌های `.ex4` باشد (قبل: نبود).
   «سبز بودنِ کامپایل» هرگز تأییدِ یک منبع نیست.
2. **هنرِ مشتق باید در همان فلو بازتولید شود.** `tools/deploy.ps1` مرحلهٔ **1b**
   گرفت: برشِ مجددِ قطعات + اجرای اعلان‌کننده در هر دیپلوی، **قبل** از کامپایلی که
   `Files/Icons` را در همهٔ ترمینال‌ها سینک می‌کند.
3. **سیم باید همان چیز را بکشد که ترمینال می‌کشد.** `panel-mt4-sim.py` بدنهٔ WIDE
   را از همان چهار قطعه و همان ثابت‌ها می‌سازد؛ قبلاً `pnl_cardW{n}` می‌کشید، یعنی
   اثباتِ هنر کارتی را نشان می‌داد که ترمینال دیگر نمی‌کشد — و همین است که توضیح
   می‌دهد چرا اثبات تمیز بود و چارت بدون پس‌زمینه.
4. **مرجعِ مرده را نگذار بماند.** نویسنده‌های `badge.bmp` (فایلی که هیچ‌جا وجود
   ندارد و هیچ‌وقت اعلان نشده) با مارکر حذف شدند؛ یک منبعِ ناموجود فقط می‌تواند
   بی‌صدا شکست بخورد و در هر repaint دوباره تلاش کند.

**گیتِ جدید `[chrome]`** در `tools/panel-wiring-audit.py`: گرافِ واقعیِ `#include`
را از ورودی می‌پیماید (پس `TH3/` بازنشسته و `Tests/` سؤال نمی‌شوند)، همهٔ
`#resource`ها و همهٔ ارجاع‌های زمان‌اجرای `::Files\Icons\*.bmp` را جمع می‌کند —
**حتی ارجاع‌های الحاقی**، که در آن‌ها لیترال یک پیشوند است و هر بیت‌نقشهٔ موجودِ
شروع‌شونده با آن پیشوند یک کاندیدای واقعی است — و اگر یکی بلااعلان یا ناموجود باشد
می‌شکند.

**درسِ عمومی:** «فایلی که تولید/برش خورد» = «فایلی که ترمینال دارد» نیست. هر آرتِ
مشتق باید (الف) در همان فلو بازتولید شود و (ب) صریحاً اعلان شود؛ و اثباتش در
**باینریِ خروجی** است، نه در سورس و نه در پیش‌نمایش.

---

## §46 — «پنل درگ نمیشه»: یک ویژگی با دو گویش، و ژستی که فقط با رها‌کردن تمام می‌شود (2026-09-14، P-UI-73)

**مسئله:** پرس روی کارت بعضی وقت‌ها هیچ کاری نمی‌کرد — نه درگ، نه کنترل — و
وابسته به این بود که زیر کرسر چه چیزی افتاده باشد.

**راه (سه نیمه، هر سه لازم):**

1. **یک مالک برای دکمهٔ فیزیکی.** `TERMINAL_KEYSTATE_LEFT` در MQL4 دو گویش دارد
   (`<0` = فشرده، یا بیتِ ۰ = فشرده) و **هر دو گویش در یک باینری** بودند
   (`PnlPressAllowed`/`BkHoldPoll` یکی را می‌پرسیدند و `EventHandlers`/`BaseKnot`
   دیگری را). نتیجه: روی هر ترمینال نیمی از شبکهٔ ایمنی تصادفی جواب می‌داد — یا
   بازیابیِ ادعای کهنه هرگز شلیک نمی‌کرد (کارت بعد از **یک** رهاشدنِ گمشده تا
   اتصالِ بعدی قفل می‌مانْد) یا یک watchdog وسطِ درگِ زنده «آزاد» تشخیص می‌داد.
   حالا `UILeftButtonDown()` (اجتماعِ دو گویش، محافظه‌کار در جهتِ «فشرده») و
   `UILeftButtonUp()` (اشتراکِ دو گویش: فقط وقتی هر دو موافق «آزاد» باشند) در
   `UtilityFunctions.mqh` — زیرِ همهٔ سطوحی که می‌پرسند.
2. **تخریبِ ژست فقط با رها‌کردن.** `ChartPointerFinalizeOnUps()` از هر دو رویدادِ
   CLICK/OBJECT_CLICK صدا زده می‌شود و P-UI-49b ثبت کرده بود که یکی از آن دو
   **روی خودِ پرس** می‌آید؛ اجرای «پیایانِ ژست» در آن لحظه، ادعای درگ را در همان
   اینست که ساخته شده پاک می‌کرد. حالا گاردِ `UILeftButtonUp()` **اول** است — و
   چون `g_MouseWasDown` قبلش ریست می‌شود، اکوی نادیده‌گرفته‌شده لبهٔ پرس را باقی
   می‌گذارد و حرکتِ بعدیِ همان پرس، ادعا را **از نو** می‌گیرد (خودترمیم، و درگ
   چسبنده هم نمی‌سازد: شاخهٔ حرکت فقط وقتی پیروی می‌کند که خودِ ایونت بگوید دکمه
   پایین است).
3. **بازشدنِ کارت، پوینتر را از رینگ می‌گیرد.** `CircAbortRingGesture()` از
   `PnlOpen` صدا زده می‌شود و لچِ long-press، ادعای `DRAG_MENU` و قفلِ چارت را
   برمی‌گرداند — و `g_LongPressFired` را دست نمی‌زند، چون آن ادعا باید همان
   رها‌کردن را بخورد و در CLICK خودش را پاک می‌کند.

**درسِ عمومی:** وضعیتِ جهانیِ سیستم (دکمهٔ ماوس، فوکوس، کلید، زمان) اگر در دو جای
مختلف با دو گویش خوانده شود، حتماً نصفِ گاردها مرده است. یک مالک بساز و هر گارد را
وادار کن از او بپرسد. و «پایانِ ژست» را به رویدادی گره بزن که **واقعاً** پایان است،
نه به رویدادی که «معمولاً» پایان است.

---

## §47 — «از پنل پالت درستش هست، ولی از اینجا نه»: کنترلی که اشتباه نیست، **دستنیافتنی** است (2026-09-14، P-UI-74)

**مسئله:** کلیک روی نوار رنگِ کارت ATR (CARD COLORS) هیچ کاری نمی‌کرد، درحالی‌که
پاپ‌آپ پالت همان رنگ‌ها را درست اعمال می‌کرد. سومین گزارش با همین شکل
(§42 / §44)، پس این بار قبل از دست‌زدن به کد، کنترل **اندازه‌گیری** شد.

**اندازه‌گیری (اسکرین‌شاتِ کاربر، پیکسل‌به‌پیکسل، روی همان باینریِ نصب‌شده):**
نوار رنگ در x = 617/661/705/749/793 (پیچِ `PNL_CSET_W 38` + `PNL_CSET_GAP 6`،
وسط‌چین در ستون ۰ کارتِ ۶۲۴ پیکسلی) و بالا/پایینِ سلول‌ها y = 794..813 کشیده
می‌شود؛ `PnlCsetHit` همان‌ها را در `bx + (PNL_WEL-total)/2` = 616 و بازهٔ
`ry+13 .. ry+41` = 790..818 می‌جوید. خط‌کشیِ ردیف‌ها (`PNL_HEAD_H 56 + line*42`)،
هیرلاینِ باندها، فوتر روی خطِ دوازدهم، و ترکِ اسلایدرها در **هر دو** ستون
پیکسل‌به‌پیکسل درست بودند و همهٔ گیت‌ها هم سبز. یعنی هندسه و منطقِ اِپلای ایراد
نداشتند: ایراد **مسیرِ تحویلِ رویداد** بود.

**ریشه:** یک کنترلِ کارت فقط روی **یک** کانال زندگی می‌کند:
* کانال **اسم‌محور** (`PnlHandleClick` از `CHARTEVENT_OBJECT_CLICK`): دکمه‌های
  ناوبری/سگمنت، بلوک پیش‌نمایشِ رنگ، X/Done و سواچ‌های سریع؛
* کانال **مختصات‌محور** (یک `MOUSE_MOVE` تازه با دکمهٔ پایین): سوییچ، سلول‌های
  cset، چیپ `+`، باندها و اسلایدرها — و **فقط** همین کانال.

کانال دوم می‌تواند توسط ادعای یک موتور دیگر خورده شود (§43/§44)، یا توسط
رهایی‌ای که MT4 گزارش نکرده، یا اصلاً **تولید نشود**: بالاترین آبجکتِ هر سلولِ
رنگ، بیت‌مپِ «شیشه» است و MT4 برای `OBJ_BITMAP_LABEL` هیچ `OBJECT_CLICK`ی
نمی‌فرستد. نتیجه: یک کنترل می‌میرد در حالی که همسایه‌هایش کار می‌کنند.
دومین ایرادِ همان پاس: `PalHandleClick` شناسهٔ سواچ‌های خودِ پالت را با تستِ
**پیشوندی** `StringFind(id,"s")==0` می‌خواند (همان شکلِ §42)، پس راهنمای حالتِ
خالی (`rempty`) به‌عنوان recent صفر پارس می‌شد و رنگی اعمال می‌کرد که کاربر
انتخاب نکرده بود.

**راه (برای همیشه، بدون کپیِ دومِ لیست):** کانال کلیک، **همان** دیسپچ را
دوباره وارد می‌شود (`PnlHandleMouseMove(cx,cy,false,true)`) — لیستِ کنترل‌ها،
ترتیبشان (قانونِ §43) و هر arm یک مالک دارند و نمی‌توانند از هم دور شوند.
لچ‌ها هر دو ترتیبِ تحویل را پوشش می‌دهند: `UIPressAct()` هویتِ فشار
(`g_UIPressSeq`) را لچ می‌کند تا رویدادِ دوقلوی یک رها‌کردن **دوبار** عمل نکند،
و `s_PnlClickActed` برای ترتیبِ برعکس (§46 / P-UI-49b) است. روی کانال کلیک
**هیچ ادعای رهایی نباید مسلح شود** (رهایی خودِ رویداد است؛ مسلح‌کردن = خوردنِ
کلیکِ *بعدی*) و گرابِ بدنهٔ کارت باید رد شود (دکمهٔ رهاشده نباید درگ شروع کند).

**درسِ عمومی:** وقتی کاربر می‌گوید «این کنترل کار نمی‌کند ولی همان کار از راهِ
دیگر انجام می‌شود»، اول **بسنج** که کنترل در جای درست کشیده شده — و اگر شد،
دیگر سراغِ هندسه و منطق نرو: سراغِ **مسیرِ رسیدنِ رویداد** برو. هر کنترل باید از
هر دو کانالِ موجود پاسخ بدهد، ولی هر ژست باید دقیقاً یک مالک داشته باشد.

---

## §48 — «پنل تنظیمات درگ نمی‌شود، ثابت است»: ژستی که یک کانالِ تحویل داشت و فریمی که کسی نمی‌پرداخت (2026-09-14، P-UI-75)

**مسئله:** کارت تنظیمات اصلاً جابه‌جا نمی‌شد؛ و در جاهایی که جابه‌جا می‌شد، دنبالِ
کرسر نمی‌آمد. چهارمین گزارش با همین شکل، ولی این بار **دو** نقصِ جدا بود با دو
درسِ جدا.

**ریشه (a) — یک ژست، یک کانالِ تحویل.** گرابِ کارت فقط از لبهٔ تازهٔ پرسِ
`CHARTEVENT_MOUSE_MOVE` (`leftDown && !g_MouseWasDown`) قابلِ دسترس بود — و تاریخِ
همین پروژه ثبت کرده که آن رویداد برای «پرس» قابلِ اعتماد نیست: MT4 برای پرسی که
حرکت نمی‌کند **هیچ** `MOUSE_MOVE` نمی‌فرستد (P-BK-03، در کامنتِ خودِ
`UILeftButtonDown`)، و بیتِ دکمه در `sparam` هم شاهدی است که می‌تواند با دکمهٔ
فیزیکی اختلاف داشته باشد (P-UI-73a). هر ژستِ دیگرِ این فایل برای همین یک سایهٔ
پول‌شده (polled shadow) دارد — `BkHoldPoll` برای هولدِ باکس، `CustomPriceDragHealStale`
برای خط، `ChartScrollReconcile` برای قفل — و درگ، یعنی پرگزارش‌ترین ژستِ کاربر،
هیچ نداشت.

**ریشه (b) — درگ فریمِ خودش را نمی‌پرداخت.** P-PERF-04/06 حرکت را با
`PNL_MOVE_COALESCE_MS` (۳۰Hz) جمع می‌کرد و از `ThrottledChartRedraw()` (ترمزِ
۱۰۰ms مسیرِ تیک) رنگ می‌گرفت؛ این دو گیت با هم compose نمی‌شدند، پس مختصات
۳۰ بار در ثانیه می‌نشست و تصویر ~۱۰ بار در ثانیه کشیده می‌شد ⇒ کارت پشتِ کرسر
می‌پرد. هیچ نرخِ ثابتی هم درست نیست، چون نرخِ درست یک **اندازه‌گیری** است: یک درگ
به‌ازای هر بچِ اعمال‌شده **یک** فریم می‌دهد، و اینکه یک ماشین چند بچ را می‌تواند
پرداخت کند، خودِ هزینهٔ بچ است.

**راه:**
1. **همان قرارداد، دو ورودی.** `PnlDragPoll()` کنارِ `BkHoldPoll` از تیک/تایمر
   پمپ می‌شود و از **همان** `PnlTryGrabMove` گراب می‌گیرد، از **همان**
   `PnlDragStep` قدم می‌زند و با **همان** `PnlDragFinish` تمام می‌شود؛ نه لیستِ
   دومِ affordance، نه مالکِ دوم. فقط وقتی مسلح می‌شود که دکمهٔ **فیزیکی** پایین
   باشد و لچِ رویداد آن پرس را ندیده باشد (`!g_MouseWasDown`) — کانالِ رویدادِ
   سالم همیشه ژستِ خودش را نگه می‌دارد — و مسیرِ بازیابی فقط وقتی نقطه را
   **پین** می‌کند که بتواند ثابت کند کارت واقعاً حرکت کرده.
2. **پنجرهٔ وفقی به‌جای نرخِ ثابت.** درگاهِ `PNL_MOVE_FRAME_*` است: گراب با
   `PNL_MOVE_FRAME_MIN_MS` شروع می‌شود (چسبیده به کرسر) و هر بچ یک‌چهارمِ راه را
   به‌سوی هزینهٔ **اندازه‌گیری‌شدهٔ** همان بچ + `PNL_MOVE_SLACK_MS` می‌رود، با سقفِ
   `PNL_MOVE_FRAME_MAX_MS` (EWMA 3:1، بدونِ نوسان). ماشینِ ضعیف به نرخی که
   واقعاً می‌تواند نگه دارد همگرا می‌شود، ماشینِ قوی کارتِ چسبیده به کرسر می‌گیرد.
   و چون دلتا **انباشته** می‌شود، کارت هنوز دقیقاً مسیرِ کرسر را می‌رود (وعدهٔ
   P-PERF-04 حفظ شد).
3. **فریمِ خودِ درگ یک مالک دارد.** `DragFrameRedraw()` در `UtilityFunctions.mqh`
   رنگ می‌کند و `g_lastChartRedrawTime` را هم به‌روز می‌کند تا تیک نتواند بی‌درنگ
   همان تصویر را دوباره بکشد (قانونِ P-PERF-06). هر بچ **یک** فریم می‌دهد و
   هزینهٔ حالتِ بی‌کار صفر است: بدونِ درگِ زنده، این تابع هیچ صداکننده‌ای ندارد.

**هزینهٔ حالتِ بی‌کار:** پول دو خواندنِ ارزان اول را قبل از هر hit test و قبل از
هر API می‌گذراند (`g_PnlMoveItem`، بعد `g_PnlOpen`) — یعنی با کارتِ بسته **یک**
مقایسهٔ int و با کارتِ باز چند خواندنِ وضعیت. گیت این ترتیب را قفل می‌کند.

**درسِ عمومی:** ژستی که فقط روی **یک** کانالِ تحویل زندگی می‌کند، همان روزی
می‌میرد که آن کانال یک روزِ بد داشته باشد؛ و ژستی که فریمِ خودش را نمی‌پردازد،
همیشه از کرسر عقب می‌ماند. برای هر ژست: یک مالک برای گرفتن/قدم/پایان، دو ورودی
(رویداد + پول)، و یک مالکِ فریم که **اندازه‌گیری** را به نرخ تبدیل می‌کند — نه یک
عددِ ثابت.

---

## §49 — «پنل ثابت است»: پایانِ ژست مالِ کانالی است که آن را شروع کرد (2026-09-14، P-UI-77)

**مسئله:** کارتِ تنظیمات اصلاً درگ نمی‌شد — نه کُند، نه پرشی؛ ثابت. در حالی که
گراب (`PnlTryGrabMove`)، سایهٔ پول (`PnlDragPoll`)، پنجرهٔ وفقی و فریمِ خودِ درگ
همه سبزِ گیت بودند و همهٔ گیت‌ها هم سبز. پنجمین گزارش با همین شکل، ولی این بار
نه گراب مشکل داشت، نه قدم، نه فریم: مشکل **پایان** بود.

**ریشه:** بلوکِ حرکت روی یک شاهدِ تنها تمام می‌کرد: `if(!leftDown)` روی بیتِ
دکمهٔ `sparam` — که به گواهیِ خودِ همین پروژه (P-UI-73a) فقط **یکی از دو** شاهدِ
دکمه است و می‌تواند با دکمهٔ فیزیکی اختلاف داشته باشد. سناریوی مرگ دقیقاً این
بود: پرسی که بیتِ رویداد هیچ‌وقت نشانش نداد (همان تلهٔ P-BK-03) توسط
`PnlDragPoll` مسلح می‌شد — و **اولین** `MOUSE_MOVE`ای که می‌رسید (با بیتِ
خاموش، چون آن پرس روی آن بیت نبود) همان درگ را در قدمِ اول تمام می‌کرد:
`PnlDragFinish(true, true)` بدونِ حتی یک قدم، و ادعای رهایی هم کلیکِ همان ژست
را می‌خورد. هر پرس عیناً همین‌طور می‌مرد — برای همین کارت «ثابت» خوانده می‌شد،
نه «کُند». ترمینالِ سالم هیچ‌وقت این را نمی‌دید (لبهٔ رویداد همیشه مسابقهٔ مسلح
را می‌برد)، پس باگ فقط روی ترمینالی بود که بیتش دروغ می‌گفت — و دقیقاً همان‌جا
که پول برای نجات ساخته شده بود، پایانِ تک‌شاهده آن را می‌کشت.

**راه (یک پرچم، نه یک مالکِ دوم):** گراب کانالش را ضبط می‌کند
(`PnlTryGrabMove(mx,my,byPoll)` → ‏`s_PnlMoveByPoll`؛ زنجیرهٔ پرس `false`، پول
`true`) و بلوکِ حرکت فقط برای درگِ پول‌مسلح توافقِ شاهدِ دوم را می‌خواهد:
`if(!leftDown && (!s_PnlMoveByPoll || UILeftButtonUp()))`. درگِ رویدادمسلح
بایت‌به‌بایت همان رفتارِ قبل (بیت تمامش می‌کند)؛ درگِ پول‌مسلح را فقط رهاییِ
موردِ توافقِ هر دو شاهد تمام می‌کند. هر دو پایان (`PnlDragStep` از زنجیره و
`PnlDragFinish` از پول، و `ChartPointerFinalizeOnUps` که از بیرون پاک می‌کند)
پرچم را با خودِ ژست می‌اندازند. شکلِ مشابه حسابرسی شد و دست نخورد: رهاییِ
ناب/ترکِ اسلایدر و میکسرِ پالت فقط رویدادمسلح‌اند (پول‌مسلحی ندارند)، پس قتلی
از کانالِ دیگر آنجا ممکن نیست.

**هزینه:** روی مسیرِ سالم **صفر** — `||` قبل از هر خواندنِ ترمینال اتصالِ کوتاه
می‌کند (`!s_PnlMoveByPoll` درست است، پس `UILeftButtonUp()` اصلاً صدا زده
نمی‌شود)؛ روی مسیرِ پول‌مسلح یک خواندنِ `TerminalInfoInteger` در هر رویدادِ
حرکت، یعنی همان مرتبه‌ای که همان مسیر امروز می‌پرداخت. هیچ رفتارِ دیگری عوض
نشد: نه ترتیبِ affordanceها، نه پنجره، نه فریم، نه هیچ armای در بودجهٔ ژست
(گیتِ gesture همان یک خطایِ شناخته‌شدهٔ P-UI-76 را دارد، نه بیشتر).

**درسِ عمومی:** هر ژستی که دو ورودی دارد، دو شاهدِ پایان هم دارد — و شاهدی که
آن ژست را **نمسلح** کرده، حق ندارد آن را تمام کند. پایان را به کانالِ مسلح‌کننده
بده (رویدادمسلح با بیت، پول‌مسلح با توافق)، وگرنه ورودیِ دوم دقیقاً همان ژستی
را می‌کشد که برای نجاتش ساخته شده. و مثل همیشه: اول **بسنج** کدام شاهد دروغ
می‌گوید (اینجا: درگی که پول مسلحش کرده و اولین رویداد تمامش می‌کند)، بعد قانون
را بنویس — نه برعکس.

---

## §50 — «هنوز ثابت است»: ژستی که مرگش را نام نبرد، بی‌نام می‌میرد (2026-09-14، P-UI-78)

**مسئله:** کارت روی بیلدِ P-UI-77 هم ثابت ماند — و این بار با سند: لاگِ زندهٔ
ترمینال نشان داد کاربر **بعد** از کامپایلِ فیکس ری‌اتچ کرده (`T2 FULL` ساعت
12:29)؛ تئوریِ ex4 بیات مرد. در عین حال همهٔ گیت‌ها سبز بودند و کلِ زنجیرهٔ
گراب/قدم/پایان روی کاغذ درست خوانده می‌شد. تنها مشاهدهٔ عینی: **هیچ** — مسیرِ
رویدادِ درگ صفر خطِ لاگ داشت (عامدانه: «مسیرِ حرکت چیزی چاپ نکند»)، پس نه مسلح
شدنِ رویدادی قابلِ تأیید بود، نه هیچ ردی، نه پایانی. خطِ غایبِ مسلح‌شدنِ پول
فقط ثابت می‌کرد پول مسلح نکرده — و دربارهٔ باقیِ مسیر هیچ نمی‌گفت.

**دو کار، یک کامیت:**

**(الف) ژست خودش را دفتر می‌کند (ledger)، با قلمروِ ژست.** مسلح شدن (کدام کانال
+ کدام کارت + کجا)، رد شدن (کدام کدِ دلیل در تنها مالکِ `PnlGrabRefused`:
M/X/P/C/H — و چاپ فقط از کانالِ رویداد، چون پول موقعِ نگه‌داشتن هر تیک صدا
می‌زند و سکوتش عمدی است تا لاگ سیل نشود)، تمام شدن (حرکت کرد؟ با کدام قانون؟).
سقف: سه خط در هر ژست؛ و مسیرِ بچِ هر حرکت **هیچ** چاپ نمی‌کند (گیت‌شده — وگرنه
خودِ تشخیص تبدیل به همان لگی می‌شود که برای یافتنش ساخته شده).

**(ب) پایانِ پول دو خواندنِ پیاپی می‌خواهد.** یک `UILeftButtonUp` شایعه است:
پروبِ KEYSTATE وسطِ ژست سوسو می‌زند (P-BK-05) و همان یک توافق، درگ‌های زنده را
وسطِ پرس می‌کشت. `s_PnlPollUpArmed` صافیِ شایعه است (در هر دو پایان ریست
می‌شود)؛ رهاییِ واقعی چندین پاس طول می‌کشد، و مسیرِ رویداد + نهایی‌سازِ CLICK
معمولاً زودتر تمام می‌کنند — پس هزینه‌اش برای درگِ زنده صفر و برای رهاییِ
واقعی حداکثر یک پریودِ پول است.

**هزینه:** ~۳ خط لاگ در هر ژست، صفر در هر فریم؛ رفتارِ مسیرِ سالم بایت‌به‌بایت
همان (اتصالِ کوتاهِ `||` قبل از هر خواندنِ ترمینال). هیچ armای به بودجهٔ ژست
اضافه نشد (گیتِ gesture همان یک خطایِ شناخته‌شدهٔ P-UI-76).

**درسِ عمومی (قانونِ اشکال‌زدایی):** برای هر «نمی‌توانم X» اول دفترِ ژست را
بساز — بعد حدس بزن. لاگی که سکوتش «عامدانه» است، همان‌جایی است که باگِ بعدی
پنهان می‌شود (درسِ P-PERF-05/10، یک لایه بالاتر). و یک خواندنِ تنها از هر شاهدی
شایعه است: پایانِ ژستِ زنده دو شاهدِ پیاپی می‌خواهد، نه یکی.

---

## §51 — «نقاشی، سند است وقتی مختصاتِ یادداشت‌شده کهنه شده» (2026-09-14، P-UI-79)

**مسئله:** اولین صیدِ دفترِ P-UI-78 سه پرس روی کارتِ ۱ بود که هر سه با کدِ
`(H)` رد شدند — (540,80)، (577,69)، (639,72). همهٔ گیت‌ها سبز، کلِ زنجیره روی
کاغذ درست، ولی پرس هیچ‌وقت به گراب نرسید. و این بار برخلافِ همیشه، سؤال دقیق
بود (نه «چرا کار نمی‌کند»، بلکه «چرا این سه نقطه بیرون‌اند») چون دفتر، کدِ
دلیل را نوشته بود.

**دو نیمه، یک کامیت:**

**(الف) «بیرون» بدونِ مختصاتِ اعلام‌شده جواب نیست.** خطِ رد حالا خودِ
مستطیلِ یادداشت‌شده را هم می‌نویسد (`rect=px,py,pw,ph`) — همان چهار عددی که
گراب با آن قضاوت می‌کند. از این به بعد هر `(H)` در برابرِ یک مستطیلِ اعلام‌شده
سنجیده می‌شود، نه در برابرِ یک حس. هزینه: همان یک خطِ لاگ، چند عدد بیشتر.

**(ب) قبل از رد کردن، از خودِ نقاشی بپرس.** `PnlSkinHit` پوست‌های بدنهٔ کارت
(`card`/`cardm*`/`cardb` — همان نام‌هایی که `PnlCreate` می‌سازد) را با
`PnlCtrlRectHit` برمی‌گرداند، یعنی با همان آموزهٔ §44 (هندسه را از روی آبجکتِ
کشیده‌شده بخوان، نه از روی ثابت‌های نقاشی). پرسی که روی پیکسلِ کشیده‌شدهٔ کارت
است، حتی اگر مستطیلِ یادداشت‌شده جابه‌جا شده باشد، گراب می‌گیرد — و چون در دفتر
`via=skin` ثبت می‌شود، خودِ واگرایی هم دیده می‌ماند (پنهان نمی‌شود). فقط روی
مسیرِ «رد» اجرا می‌شود (خواندنِ آبجکت، بدونِ نوشتن)؛ `cardf` (واشِ روی فوتر)
هیچ‌وقت داور نیست.

**درسِ عمومی:** وقتی حافظه (مستطیلِ یادداشت‌شده) و واقعیت (پیکسلِ کشیده‌شده)
می‌توانند از هم جدا شوند، تصمیم را با واقعیت بگیر و حافظه را فقط برای دفتر
نگه دار. و هر «نه»ای که به کاربر می‌گویی، باید مدرکش همراهش باشد (اینجا:
مستطیل) — «نه»ِ بی‌مدرک اشکال‌زدایی‌ناپذیر است.

---

## §52 — «مثل همون بشه»: تپ، درگ نیست — به آن ناحیهٔ مردهٔ منو را بده (2026-09-14، P-UI-80)

**مسئله:** «منو که کار می‌کنه ولی درگ پنل نه». دفترِ کامل چیزِ عجیبی نشان داد:
مکانیزم سالم بود (۹ `moved=1` روی سه کارت در همان روز) و همهٔ شکست‌ها پرسِ
لبه/بیرون بودند؛ و درگ‌های «موفقِ» ۱۶۰ تا ۳۲۰ میلی‌ثانیه‌ای، تپ‌لرزش‌هایی بودند
که نقطهٔ میکروسکوپی را **کامیت** می‌کردند — پس کارت زیرِ کرسر می‌خزید (تعقیبِ
12:44: دو فلیک، چهار پرس در جایِ قبلی، حذف از روی عصبانیت). پنل ناحیهٔ مرده
نداشت و بی‌قیدوشرط ساپرس/کامیت می‌کرد؛ منو هر دو را دارد.

**راه (برابری با منو، نه هیچ چیزِ دیگر):** `PNL_DRAG_THRESHOLD_PX` (٣، همان کران
و همان شکلِ `>` در `ORB_DRAG_THRESHOLD`) بچ را گیت می‌کند (`s_PnlMoveGrabX/Y`
در لحظهٔ گراب لنگر می‌اندازد؛ زیرِ آستانه فقط دنبال می‌کند — بدونِ حرکت، بدونِ
نقاشی، بدونِ پین، و بدونِ پرش موقعِ عبور چون last جلو می‌رود)، و مسیرِ پرس با
`PnlDragFinish(s_PnlMoveMoved, s_PnlMoveMoved)` تمام می‌کند — همان قانونِ خودِ
پول که حالا مشترک است. `s_PnlMoveMoved` خودش لچِ «کشیده شد» است (همانِ
`g_OrbWasDragged`). درگِ واقعیِ آن‌طرفِ آستانه بایت‌به‌بایت مثلِ قبل است (اولین
بچِ دور بلافاصله اعمال می‌شود).

**درسِ عمومی:** وقتی کاربر می‌گوید «مثلِ آن یکی بشه»، دو موتور را خط‌به‌خط diff
کن و تفاوتِ ساختاری را پیدا کن — نه اینکه موتورِ خراب را بیشتر حدس بزنی. اینجا
تفاوت فقط ناحیهٔ مرده + ساپرسِ مشروط بود؛ بقیه (دو ورودی، پنجرهٔ وفقی، دفتر)
سرِ جایش ماند. و «موفق» در لاگ همیشه به معنیِ «درست» نیست: `moved=1`ای که در
۱۶۰ میلی‌ثانیه تمام می‌شود، تپ است نه درگ — مدتِ ژست را هم بخوان، نه فقط
پرچمش را.

---

## §53 — «جابجا میشه ولی دیگه قفل میشه»: لچی که هر سه راهِ خروجش به شاهدی
وابسته است که ترمینال می‌تواند از آن دریغ کند (2026-09-14، P-UI-81)

**مسئله:** پنجمین گزارشِ همان شکل، و این بار نیمهٔ دوم را هم نام می‌برد: کارت
**حرکت می‌کند** و از همان لحظه کلِ پنل **مرده** است تا اندیکاتور را بردارند و
دوباره اضافه کنند. چهار دورِ قبلی (گراب، کانال، پنجره، فریم، ناحیهٔ مرده) سبز
بودند و پنل هنوز قفل می‌شد — چون ایراد در **درگ** نبود، در **لچ** بود.

**ریشه:** تا وقتی `g_PnlMoveItem >= 0` است، زنجیرهٔ حرکت **قبل از** بلوکِ
کنترل‌ها `return` می‌کند؛ لچِ اسلایدر (`g_PnlDragItem`) و میکسر (`g_PalMixDrag`)
هم همان بلوک را همان‌طور رد می‌کنند. پس **یک** لچِ جامانده، **هر** کنترلِ **هر**
کارت را خاموش می‌کند — و گراب را هم، که دقیقاً همان «پرس بعدی کارت را حرکت
می‌دهد و هیچ‌چیز کلیک نمی‌شود» است. و هر سه راهِ خروجِ آن لچ به شاهدی وابسته
است که ترمینال آزاد است هیچ‌وقت تحویلش ندهد:

1. یک `CHARTEVENT_MOUSE_MOVE` بعدی که بیتِ رها را حمل کند — MT4 برای رهایی که
   جابجا نمی‌شود **هیچ** رویدادِ حرکتی نمی‌فرستد (P-BK-03)؛
2. کاوشگرِ فیزیکیِ `UILeftButtonUp()` — یک هیوریستیک با دو گویشِ تاریخیِ
   KEYSTATE (P-UI-73a)؛
3. فاینالایزرِ بالا‌آمدنِ دکمه — که **خودش** با همان کاوشگر گیت شده است.

اگر هر سه در یک ژست از دست بروند (رهایی بدونِ حرکت، دزدیده‌شدن فوکوس توسّط
دیالوگِ خودِ ترمینال، کاوشگری که مخالفت می‌کند) لچ **همیشه‌گی** می‌شود.

**راه (به سمتِ جلو شفا بده، منتظرِ شاهدِ غیرقابل‌اطمینان نمان):** شاهدی که
نمی‌شود از آن دریغ کرد، **لبهٔ پرسِ بعدی** است — لبهٔ پرس یعنی دکمه بالا رفته و
دوباره پایین آمده، و هیچ چیزِ دیگری نمی‌تواند لبه بسازد. پس
`PnlReapStaleGestures(commitMove)` در **ابتدا**ی `PnlHandleMouseMove` و به شرطِ
`pressStart && !s_PnlClickChannel` اجرا می‌شود و هر لچِ پنل را از **مسیرِ
پایانِ خودش** می‌بندد: برای حرکت `PnlDragFinish(s_PnlMoveMoved, false)` (بدونِ
`suppressClick` — آن کلیک مالِ پرسِ **جدید** است)، و برای اسلایدر و میکسر
`DragReleaseIf(DRAG_PANEL_KNOB)` + `UIDragBudgetEnd()` + `CircUnlockChart()`
(میکسر `PalRefreshRecents(true)` هم می‌زند). نه ساعت، نه کاوشگر، نه رویدادِ
اضافه، نه stateِ جدید. ژست‌های سالم بایت‌به‌بایت دست‌نخورده‌اند: در هر پرسِ
واقعی فقط **یک** لبهٔ پرس وجود دارد و همان لبه چیزی است که ژست را **مسلح**
می‌کند؛ تنها شکلی که لبهٔ دوم تولید می‌کند اکویِ تحویلِ P-UI-49b است، که در آن
همان پرس در خطِ بعد دوباره گراب می‌گیرد (لنگر = محلِ فعلیِ کرسر) — پس بدترین
حالت یک فریم است، نه پنلِ قفل‌شده.

**مکملِ همان موضوع:** فاینالایزرِ بالا‌آمدنِ دکمه هم آخرین مسیرش بسته شد —
`if(s_PnlMoveMoved) PnlCommitMove(...)`: تپ نباید نقطه‌ای را پین کند که هرگز به
آن نرسیده (برابری با P-UI-80 روی مسیرِ رهاییِ ازدست‌رفته).

**درسِ عمومی (و درسِ ابزارِ خودمان):** لچی که **هر** راهِ خروجش به رویدادِ بعدی
یا به کاوشگرِ ترمینال وابسته است، در عمل یک قفلِ دائمی است؛ برای هر لچِ ژست،
قوی‌ترین شاهدِ ممکن را پیدا کن — و شاهدِ «پرسِ جدید» از همه قوی‌تر است، چون
خودِ وجودش اثباتِ رهاییِ قبلی است. و درسِ دوم: `gesture-budget-audit` در همین
حال **سرخ** بود، چون تعدادِ **شکل** (چهار سایتِ مسلح‌سازی) را می‌شمرد و P-UI-76
دو سایتِ ناب/ترک را به‌عمد در یک شرطِ `||` ادغام کرده بود — گیتی که شکل می‌شمارد
نه وعده، گیت نیست؛ حالا شمارش روی ادغامِ به‌عمدی کلید می‌خورد و **هر دو شکلِ
اسلایدر** را طلب می‌کند.

---

و در همین دور یک گیتِ سوم هم لو رفت (پیامدِ همان کار): `panel-colour-audit`
فلاشِ دُمِ درگ را در **کلِ فایل** می‌جست (`"PalRefreshRecents(true)" not in text`)؛
تا وقتی یک تماس‌کننده بیشتر نبود این کار می‌کرد، ولی همین که ریپر یک تماس‌کنندهٔ
دوم ساخت، کنترلِ منفی **بی‌اثر** شد (سید شاخهٔ میکسر را می‌برداشت و نسخهٔ دوم
همچنان جواب می‌داد). حالا خودِ شاخهٔ رهایشِ میکسر خوانده می‌شود. **قانون: گیت باید
سایت را بخواند، نه فایل را.** هر تماس‌کنندهٔ جدید روی یک تابعِ تحتِ گیت، هر گیتی
که «در کل فایل هست/نیست» می‌پرسد را بالقوه بی‌اثر می‌کند — سیدِ منفی‌اش را همان
لحظه بازبینی کن.

---

## §54 — «جابه جا میشه ولی ریل تایم نیست»: هزینهٔ اسکنِ هر بچ، و پارکی که به
رهایی گره خورده بود (2026-09-14، P-UI-82)

**مسئله:** «جابه جا میشه ولی به صورت ریل تایم نیستش یعنی وقتی باز و بسته کنیم
پنل رو موقعیتش عوض میشه» — دو نیمهٔ یک جمله، دو ایراد جدا.

**(الف) ریل تایم نبود، چون هر بچ کلِ چارت را اسکن می‌کرد.** `PnlMoveBy` در هر
بچ پنج نوعِ UI را می‌گشت (۵ تا `ObjectsTotal` + یک `ObjectName` + یک
`StringFind` به‌ازای هر کاندید، شصت بار در ثانیه) تا فهرستی را از نو بسازد که
**نمی‌تواند** در طولِ یک پرس عوض شود — کارت تا وقتی دکمه پایین است منجمد است،
چون زنجیرهٔ حرکت قبل از هر کنترلی `return` می‌کند. همان اسکن، همان هزینه‌ای است
که پنجرهٔ وفقیِ P-UI-75b به سمتش همگرا می‌شود؛ پس پنجره روی سقفش (۵۰ms) می‌نشست
و کارت به‌جای چسبیدن به کرسر، «پله‌پله» می‌رفت. حالا فهرست **یک بار در هر ژست**
(موقعِ گراب) ساخته می‌شود و بچ دقیقاً N نوشتن است؛ پالت هم با همان جملهٔ خودش
سوار می‌شود (مالکِ یگانه‌ی قاعده: `PnlPalFollows`).

**همراهِ همان، یک صرفه‌جویی رایگان:** `PnlMoveOne` محوری را که دلتای صفر دارد
**نمی‌نویسد** (قانونِ خودِ R-PERF: هر پراپرتی‌ای که تغییرش نمی‌دهی را ننویس).
درگِ افقی/عمودی حالا نصفِ بچ را می‌دهد.

**(ب) موقعیت ریل تایم پارک نمی‌شد.** `PnlCommitMove` فقط در **پایانِ** ژست اجرا
می‌شد؛ پس از اولین بچِ اعمال‌شده تا رهایی، «جای منطقی» — همان چیزی که باز شدنِ
بعدی می‌خوانَد (`g_PnlManualPos` + `PnlComputePosition`) — هنوز **جای قبلی** بود.
هر ژستی که بدونِ رهایی تمام شود (هات‌کی‌ای که کارت را می‌بندد، سوییچ تایم‌فریم،
رهایی‌ای که MT4 گزارش نکرد) کارت را سرِ جای قبلی باز می‌کرد. حالا پارک **همان
لحظه‌ای** نوشته می‌شود که درگ اثبات می‌شود (اولین بچِ بعد از ناحیهٔ مرده: دقیقاً
همان تعریفی که P-UI-80 از پارک دارد) — یک `GlobalVariableSet` در هر ژست، نه در
هر بچ — و رهایی همان مقدار را با پیکسلِ نهایی تازه می‌کند.

**درسِ عمومی:** (۱) هر هزینهٔ تکرارشونده را بپرس «آیا ورودی‌اش می‌توانست در این
فاصله عوض شود؟» — اگر نه، آن را یک بار در «مرزِ ژست/رخداد» بساز، نه در هر تیک.
(۲) هر وضعیتی که «در آینده خوانده می‌شود» (موقعیت، حالت، تنظیم) را همان لحظه‌ای
بنویس که **اثبات** می‌شود، نه در پایانِ یک سلسله‌رویداد که ممکن است هرگز نرسد؛
گره‌زدنِ وضعیت به «رویدادِ بعدی» همان لچِ P-UI-81 است، فقط یک لایه بالاتر.
(۳) و مثلِ همیشه: وقتی گیت، یک **سایت** را می‌خواند و تو آن سایت را جابه‌جا
می‌کنی، گیت را با همان تعهد (نه با همان رشته) به محلِ جدید ببر.

---

## §55 — «هنوز لایو نیست، باید مثل منوی اصلی بشه»: ژستی که شاهدانِ منو را نمی‌پرسید (2026-09-14، P-UI-83)

**مسئله:** ششمین گزارش با همان شکل، و این بار مترِ کاربر صریح است: منوی حلقه
(orb) لایو جابه‌جا می‌شود، کارت نه. این بار به‌جای تئوری از **دفترِ ژستِ خودِ
ترمینال** (ابزارِ P-UI-78) پرسیدیم: ۳۵۸ خط در یک روز، ۱۵۰ مسلح‌شدن که
**صفر**تایشان از پول بود، ۵۵ پایانِ `via=finalizer` در ۱۲۵-۱۷۶ میلی‌ثانیه و ۷۶
پایانِ `moved=1` در ۲۵۳-۵۰۰ میلی‌ثانیه. یعنی **هیچ درگی در کلِ آن روز نیم‌ثانیه
زنده نماند** در حالی‌که کاربر دکمه را نگه داشته بود. گراب، پنجرهٔ وفقی، ناحیهٔ
مرده و فریم همه سالم بودند: ژست را **شاهدِ اشتباه** اجرا می‌کرد.

**ریشه — درگِ منو یک شاهد دارد، درگِ کارت سه تا.** منو فقط بیتِ رویداد را
می‌پرسد (`if(!leftDown)`)؛ کارت دو پایانِ اضافه داشت که هر دو به کاوشگرِ فیزیکی
`UILeftButtonUp()` گره خورده‌اند: قانونِ رهاییِ `PnlDragPoll` (پاسِ دومش = یک تیک
یا تایمرِ ۲۵۰ms ⇒ هر پرس ۲۵۰-۵۰۰ms درگ) و گیتِ تخریبِ
`ChartPointerFinalizeOnUps` (کاوشگر می‌گوید «بالا» ⇒ اکویِ تحویلِ P-UI-49b —
همان `OBJECT_CLICK` پرسی که آبجکت را گرفته — درگِ زنده را در ۱۲۵-۱۷۶ms تمام
می‌کند). روی ترمینالی که کاوشگرش وقتی دکمه پایین است «آزاد» می‌خواند (گویش‌های
P-UI-73a؛ و کاوشگری که هیچ‌وقت «پایین» نمی‌خواند هیچ‌وقت هم نمی‌تواند پولی را مسلح
کند — همان چیزی که صفر بودنِ مسلح‌شدن‌های پول در دفتر نشانش می‌دهد) هر دو یک
درگِ زنده را تمام می‌کنند.

**راه — یک شاهدِ جدید که آن دو را از هم جدا می‌کند: تازگیِ بیتِ رویداد.**
`s_PnlDownAt` جایی مُهر می‌شود که خودِ بیت خوانده می‌شود و `PnlPointerQuiet()`
می‌گوید «آیا از آخرین خواندنِ `leftDown=1` به اندازهٔ `PNL_DOWN_RECENT_MS`
(۲۵۰ms) گذشته؟». تا وقتی ترمینال خواندن‌های DOWN را تحویل می‌دهد، پرس **زنده**
است و کلیکی که در آن پنجره می‌رسد اکویِ همان پرس است نه رهایش؛ در مقابل، رهایی‌ای
که کرسر در آن تکان نمی‌خورد **هیچ** رویدادِ حرکتی نمی‌فرستد (P-BK-03) پس ذاتاً
«آرام» است — و همان حالتی است که فاینالایزر برای آن وجود دارد. یک شاهد، دو
درگاه: `if(UILeftButtonUp() && PnlPointerQuiet())` در پول و
`if(!UILeftButtonUp() || !PnlPointerQuiet()) return;` در فاینالایزر. روی
ترمینالِ سالم (کاوشگرِ صادق) رفتار بایت‌به‌بایت همان قبل است.

**نیمهٔ دوم — همان diff، یک لایه پایین‌تر: بچ باید محاسبه کند، نه بخواند.**
`SubChromeMove` (موتورِ منو) `px + offset` را می‌نویسد و هیچ‌وقت از آبجکت
نمی‌پرسد کجاست؛ کارت هر آبجکت را در هر دو محور get-then-set می‌کرد، یعنی
**نصفِ هر بچ** خواندنِ مقداری بود که در طولِ یک پرسِ نگه‌داشته‌شده نمی‌تواند
عوض شود. حالا offsetها **یک بار در هر ژست** (در سازندهٔ فهرست) خوانده می‌شوند
(`s_PnlMoveX0/Y0` در قالبِ مبدأِ کارت `s_PnlMoveOx/Oy`) و
`PnlMoveOne(i,nx,ny)` می‌نویسد `x0 + (target - origin)`: فقط SET، بدونِ خواندن،
و افتادنِ یک فریم هم دیگر نمی‌تواند کارت را دریفت کند (هر بچ مکانِ **مطلقِ**
خواستهٔ کرسر را می‌نویسد، در همان قالبی که offsetها خوانده شده‌اند).

**به‌علاوه، ژست خودش را می‌سنجد:** خطِ پایانِ درگ حالا `frames=` و `worst=` را
چاپ می‌کند (تعدادِ بچ‌های واقعاً اعمال‌شده و هزینهٔ بدترین بچ). «لایو نیست»
دفعهٔ بعد از لاگ جواب می‌گیرد، نه از بازتحقیق — و اگر frames نزدیکِ صفر بود،
مشکل ریتم است نه هزینه.

**درس‌های عمومی:**
۱. وقتی کاربر می‌گوید «مثلِ آن یکی بشه»، دو موتور را نه بر اساس **نرخ** بلکه
بر اساس **شاهدها** diff کن: چه کسی حق دارد ژست را تمام کند؟ هر شاهدِ اضافه‌ای
که می‌تواند دروغ بگوید، یک قاتلِ ژستِ زنده است.
۲. اول بسنج، بعد قانون بنویس (اینجا: توزیعِ **مدتِ ژست** در دفتر، نه فقط پرچمِ
`moved=1` — `moved=1`ی که در ۱۵۰ms تمام می‌شود درگ نیست).
۳. و درسِ همیشگیِ خودِ ابزار: گیت باید **سایت** را بخواند نه فایل را — متنِ همین
بند (`via=finalizer`) باعث شد یک کنترلِ منفیِ رشته‌ای در `panel-wiring-audit`
با حذفِ خودِ Print هم سبز بماند؛ حالا خودِ دُمِ Print خوانده می‌شود.

## §56 — «این trex sl , tp جدا از atr خاموش و روشن بشه»: کنترلی که دو ارباب داشت (2026-09-14، P-UI-84)

**مسئله:** کاربر به کارتِ پایین-راستِ تریدپلن اشاره کرد (برندِ `TR`/`ex`، ردیفِ
سرخِ `Hunter SL / Eng.SL`، ردیفِ آبیِ `#SL/TP`) و گفت باید **جدا از ATR**
خاموش/روشن شود، چون «الان atr روش تاثیر میزاره».

**ریشه — کارت دو ارباب داشت.** کارت در پنل سوئیچِ خودش را دارد
(`inpShowATRTradeLabels` و دو فلگِ ردیفش، نوارِ TRADE PLAN ROWS)، ولی هر
دروازهٔ تصمیم‌گیرِ کارت آن را با `g_atrLabelsVisible` هم AND می‌کرد — یعنی
سوئیچِ **نمای کلیِ ATR** (کاشیِ `ATR` در منوی حلقه، ردیفِ `ATR LABELS` کارتِ
ATR، کلیدِ A). پس یک پرس روی هر کدام از آن سه، کلِ تریدپلن را همراهِ ستون‌های
ATR از چارت می‌برد و کارت فرزندِ بلوکِ ATR خوانده می‌شد. چهار چهرهٔ این گره:
گیتِ پاک‌سازیِ `DisplayATRTradeLabels`، گیتِ پمپِ ۲ ثانیه‌ای `TradePlanLiveTick`،
نویسندهٔ ماسکِ `SetATRLabelsVisibility` (`tradeTF = (shouldShow && ...)`) و دو گاردِ
`if(g_atrLabelsVisible) DisplayATRTradeLabels(...)` در `EventHandlers` (پاسِ
بازچینش و `RedrawLabelsOnly`).

**راه — کارت یک تریدپلن است، نه بخشی از بلوکِ ATR.** تنها ورودی‌اش
`inpShowATRTradeLabels` (و دو فلگِ ردیفِ خودش، از طریق مالکِ چیدمان) است.
`IsIndicatorHidden()` تنها ترمی است که با بلوکِ ATR مشترک می‌ماند، چون آن
پنهان‌سازیِ کلِ اندیکاتور است نه انتخابِ یک لایه. گیتِ ATR همان‌جاست که بود:
داخلِ `DisplayATRLabels`، روی ستون‌هایی که واقعاً مالکشان است.

**درس‌های عمومی:**
۱. «جدا خاموش/روشن بشه» یعنی **هر کنترل یک مالک داشته باشد**؛ فلگی که در چند
دروازه AND می‌شود در عمل چند ارباب دارد و کاربر از چند جا فرمانِ یکسان می‌گیرد.
۲. این یک باگِ **مالکیت** بود نه محاسبه — پس گیتِ درست هم هندسه/عدد نیست،
**مالکیتِ خواندن** است: تنها خوانندهٔ مجازِ فلگ، مالکِ همان لایه است
(`[decoupled]` در `chart-label-audit`).
۳. و یک هشدارِ همیشگی: کامنتِ کهنه در `DisplayATRLabels` از قبل ادعا می‌کرد
«trade-plan labels are independent from the optional ATR overview» در حالی‌که
کد این را نمی‌گفت — جملهٔ درست بدونِ گیت، یک قصدِ جامانده است؛ گیت را روی همان
جمله بگذار تا دوباره گم نشود.

## §57 — «کندل لایو اندازه‌ی کندل مثل بقیه باشه» + لایهٔ متن: دو چیزی که کد درست می‌گفت و ترمینال جور دیگری تصمیم می‌گرفت (2026-09-14، P-UI-85)

**مسئله (الف):** کاربر خواست کندل لایوِ HTF «اندازه‌ی کندل مثل بقیه» شود تا معلوم
باشد کجای دورهٔ جاری هستیم. یعنی همان برچسبی که تا الان فکر می‌کردیم به کل اسلات
رسم می‌شود، به اندازهٔ «بازهٔ سپری‌شده» رسم می‌شد و از هر کندل بستهٔ بغل‌دستی
باریک‌تر بود.

**ریشه:** حق با کاربر بود، و نه به‌خاطر هندسه بلکه به‌خاطر **دیکشنریِ ترمینال**.
لبهٔ راستِ کندلِ درحال‌تشکیل `HTFBarCloseTime(ot, tf)` است — یعنی یک زمانِ
**آینده** — و `HTFChartIndexAt` همه‌چیز را از `iBarShift(sym, tf, t, false)`
می‌پرسید. آن تابع وقتی زمان دقیقاً سرِ یک کندل نباشد «نزدیک‌ترین کندل» را
برمی‌گرداند، و نزدیک‌ترین کندل به یک زمانِ نیامده **کندلِ ۰** است. پس
`iR = 0` می‌شد، `span = iL - iR` فقط کندل‌های سپری‌شده را می‌شمرد و بدنه/سایه
روی همان بخش حساب می‌شد. استثنای آینده (extrapolate با گامِ آخرین اسلات) در
کد بود، ولی **فقط داخل شاخهٔ `i < 0`** — یعنی اجرای آن به این بستگی داشت که
ترمینال برای یک زمان آینده `-1` بگوید یا `0`.

**راه:** پرسش از ترمینال حذف شد، نه جوابش. حالا `if(i <= 0)` — که هم اسلاتِ
خودِ کندل ۰ و هم هر زمانِ جدیدتر از آن را پوشش می‌دهد و `iBarShift` بین این دو
نمی‌تواند تفکیک کند — از **گامِ کندل** حل می‌شود:
`idx = -(t - t0) / (t0 - t1)` که دقیقاً همان چیزی است که خودِ ترمینال برای یک
زمانِ آینده چیدمان می‌کند. مسیر تاریخی (`i > 0`) بایت‌به‌بایت دست‌نخورده ماند و
مسیر لایو **ارزان‌تر** شد (دو `iTime` + یک تقسیم، به‌جای یک `iBarShift` + همان
دو `iTime`) — چون همان مسیر، داغ‌ترین مسیرِ هر تیک است.

**مسئله (ب): لایهٔ متن مرتبه‌اش را نمی‌نوشت.** نردبانِ Z (P-UI-31) از همیشه
می‌گوید `Z_CHART_LABEL 100` مالِ «لیبل‌های قیمت/سطح، انکر ویو و تگ شمارش»
است، و **همهٔ** متن‌های چارتی (ستون‌های ATR، ستون‌های TH، کارتِ تریدپلن، تگ
شمارش) از یک قیف رد می‌شوند: `InitATRChartLabel()`. آن قیف گوشه/انکر/قابل‌انتخاب/
مخفی را می‌نوشت و **مرتبه را نه** — پس کلِ لایهٔ متن روی `ZORDER 0` پیش‌فرضِ
`ObjectCreate` می‌نشست، یعنی هم‌مرتبه با خودِ گرافیک چارت (زون‌ها ۰، خطوط ۱،
بدنه/لبهٔ جعبه‌ها ۵۰-۶۱)، و برنده را «ترتیبِ ساخت» به‌علاوهٔ رفتار همان بیلدِ
ترمینال با `OBJPROP_BACK` تعیین می‌کرد (پروژه خودش مستند کرده که بعضی بیلدها
مستطیلی که `FILL=false` است را هم توپر می‌کنند). نتیجه: یک کندل HTF یا لبهٔ یک
زون می‌توانست روی متنِ ATR بیفتد — همان چیزی که در اسکرین‌شاتِ کاربر دیده
می‌شود.

**راه (ب):** یک خط، در همان قیف: `OBJPROP_ZORDER, Z_CHART_LABEL`. فقط روی مسیر
ساخت (هر بار که لیبل‌ها پس از `ClearAllLabels` از نو ساخته می‌شوند)، پس هیچ
فریمِ پایدار هزینه‌ای نمی‌دهد. حالا ترتیب **قابل اثبات** است نه اتفاقی: متن ۱۰۰
بالای هر گرافیکِ چارتی و هر پیلِ جعبه (<= ۶۱) و همچنان بسیار زیر منوی
حلقه (۱۰۰۴+) و کارت‌ها (۱۴۸۰+).

**درس‌های عمومی:**
۱. وقتی یک تابع کتابخانه‌ای (این‌جا `iBarShift` با `exact=false`) معنیِ
«نزدیک‌ترین» را دارد، هیچ‌وقت آن را برای مقادیری که در دامنهٔ مسئله نیستند
نپرس — برای زمانِ آینده خودت از گام محاسبه کن.
۲. «مرتبه» یک مقدارِ پیش‌فرضِ پنهان است: اگر نردبان جای لیبل را اعلام کرده ولی
کد نمی‌نویسدش، نردبان سند است نه واقعیت. گیت را روی همان قیف بگذار
(`check_text_rung`) تا سند و واقعیت یکی بمانند.
۳. و مثل همیشه: گیت باید **سایت** را بخواند — هر دو گیتِ امروز، خودِ پل و خودِ
قیف را می‌خوانند، نه فایل را.









