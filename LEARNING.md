# LEARNING.md — تجربه‌های مهندسی پروژه (برای استفاده در کارهای بعدی)

> خلاصه فارسی: این فایل حافظه «راه‌حل‌های اثبات‌شده» است، نه قوانین پروژه
> (قوانین در `AGENTS.md` و معماری در `ARCHITECTURE.md`).
> هر بار که مسئله‌ای با تحقیقی واقعی حل شد، الگوی آن را همین‌جا ثبت کن تا
> سشن بعدی از همین راه برود و از نو تحقیق نکند. کوتاه بنویس: مسئله → راه → چرا.

---

## §1 — Real-time drag-follow بدون فشار به سیستم (2026-09-07، استرپ بیس‌باکس)

**مسئله:** تولبار شناور باید «همان لحظه‌ای که باکس درگ می‌شود» همراهش حرکت کند،
نه با تأخیر تایمر ۲۵۰ms، و درگ‌استورم (ده‌ها ایونت در ثانیه) نباید CPU را بخوابد.

**راه (به ترتیب اهمیت):**

1. **Event-driven، نه poll سریع‌تر.** روی همان ایونتی سوار شو که دیتا را تکان
   می‌دهد (`CHARTEVENT_OBJECT_DRAG`)، نه اینکه تایمر را تندتر کنی.
   poll فقط fallback می‌ماند (برای تغییراتی که ایونت ندارند: wheel-zoom،
   TF-switch). نمونه: `BkStripFollow` + شاخه `OBJECT_DRAG` در
   `HandleUIChartEvent` (`Biotak/BiotakPanels.mqh`).
2. **Throttle در call-site، حدود 30ms** (≈33 فریم — چشم فرقی با ۶۰ نمی‌بیند،
   هزینه نصف). متغیر `static uint` داخل همان شاخه.
3. **Dead band مکانی (۴px).** حرکت‌های زیر آستانه اصلاً آبجکت را تکان ندهند
   (ضد jitter) — قبل از هر syscall چک شود.
4. **لیست صریح آبجکت‌ها، هرگز اسکن کل چارت.** به‌جای حلقه `ObjectsTotal` روی
   کل چارت (هزاران آبجکت در این اندیکاتور)، فقط آبجکت‌های خودت را با اسم
   جابه‌جا کن (~۱۵ syscall). نمونه: `BkStripMoveBy` + `BkMiniSlotName`
   (single source — اسلات جدید خودکار پوشش داده می‌شود).
5. **ارزان‌ترین چک اول.** مقایسه رشته (`sparam == ...`) قبل از هر
   `ObjectGet*`؛ `ObjectFind` قبل از `ObjectSet`؛ همه change-guarded.
6. **وضعیت موقت همراه حرکت پاک شود.** پاپ‌آور/پالت که مختصات منطقی جدا دارند
   (`g_BkDdX/Y`) با حرکت سوار نمی‌شوند — موقع follow ببندشان، وگرنه hit-rect
   دروغین می‌ماند (`BkDdClose` + `PalClose` در `BkStripFollow`).

**چرا نه راه‌های دیگر:**

- تایمر ۵۰ms به‌جای ایونت → روی چارت بدون تیک همان تأخیر را دارد + همیشه
  هزینه ثابت، حتی وقتی هیچ‌چیز تکان نمی‌خورد.
- `PnlMoveBy` عمومی (اسکن کل چارت) با نرخ درگ → روی چارت شلوغ هر استپ چند
  هزار مقایسه رشته؛ با لیست صریح ~۱۵ syscall است.
- `ChartRedraw` پشت هر استپ لازم نیست حذف شود — حین درگ native ترمینال
  خودش مداوم repaint می‌کند؛ `ObjectMove`های ما قبل از همان repaint
  می‌نشینند (childها frame-perfect دنبال می‌کنند). redraw صریح فقط برای
  پایان ژست/تغییر حالت.

**دوقلوی دامین‌ساید (2026-09-07 بچه‌های باکس حین درگ؛ یکپارچه 2026-09-08، P-BK-07):**
همان الگو، برای `BaseKnotTool.mqh` که نه تایمر دارد نه UI — با یک اصلاح مهم:
دو نویسنده (OBJECT_DRAG→Sync کامل هر استپ + MOUSE_MOVE→دلتای کرسر) با هم
می‌جنگیدند: اولی روی چارت سنگین lag می‌کرد، دومی snap مگنت ترمینال روی BOX را
نمی‌دید — فیل و حاشیه وسط درگ از هم جدا می‌ماندند. حالا نویسنده یکی است
(`BaseKnotFollowDrag`): تا ترمینال انکرها را تکان می‌دهد، از روی live anchor
(دقیق، فقط ObjectMove)، فقط وقتی انکر وسط درگ فریز بود دلتای کرسر از نقطه
press؛ OBJECT_DRAG کرسر معتبر ندارد (فقط anchor-exact — همان قرارداد TH3Tool)،
کرسر از کانال MOUSE_MOVE می‌آید؛ گیت مشترک 30ms تکراری‌ها را جذب می‌کند تا نه
بجنگند نه گرسنه بمانند؛ Sync کامل فقط موقع release. حفره overlap هم با adopt
باکسِ واقعاً درگ‌شده وسط راه پوشیده می‌شود (cursor base همان press می‌ماند).

- مشکل اول: childها (خطوط/لبه‌ها) فقط با `OBJECT_DRAG` سینک می‌شدند — تحویل
  per-step این ایونت و خواندن live anchor حین درگ، behavior بیلدبه‌بیلد فرق
  می‌کند؛ روی بعضی بیلدها همه‌چیز تا release فریز می‌ماند.
- مشکل دوم (2026-09-08): `Sync` کامل هر استپ (ده‌ها style/tooltip syscall) روی
  چارت سنگین lag می‌کرد و دلتای کرسر snap مگنت BOX را نمی‌دید — دو نویسنده،
  دو مقصد.
- راه نهایی: ترک press با `MOUSE_MOVE` در حالت IDLE (همیشه جاری است) + نویسنده
  واحد `BaseKnotFollowDrag` — اول live anchor (دقیق، فقط `ObjectMove`، صفر
  style/color/create؛ هندسه عین `Sync`)، فقط وقتی انکر فریز بود دلتای کرسر از
  نقطه press + یک `BaseKnotSync` مقتدرانه از anchorهای کامیت‌شده موقع release
  («موقع رها شدن درست می‌شود» تضمینی).
- بودجه repaint مشترک (`BaseKnotDragPaint` ‏30ms): پوزیشن هر ایونت سینک
  می‌شود (ارزان)، repaint نه — repaint کاملِ هر استپ روی چارت سنگین، درگ را
  به اسلایدشو تبدیل می‌کند که فقط موقع release settle می‌شود.
- جزئیات: slop درگ/تپ (`BK_DRAG_SLOP` محلی — Lite پنل ندارد)، tap موقع
  release سینک نمی‌خواهد، باکس قفل‌شده کاندید نمی‌شود، حفره overlap با سینک
  نقطه drop هم پوشیده می‌شود. IDLE هیچ‌وقت consume نمی‌کند.

**الحاقیه 2026-09-08 (P-BK-16):** fallback دلتای کرسر باید کل آبجکت را
ببرد، نه فقط بچه‌ها را — وگرنه باکس سرجاش می‌ماند و release-Sync همه را
برمی‌گرداند سرجاش. absolute از نقطه press (هرگز incremental) تا همگرا شود
و double-count نشود؛ لحظه‌ای که ترمینال انکرها را تکان داد، شاخه
anchor-exact دوباره دست می‌گیرد. هر دو واقعیت درگ پوشش داده می‌شود:
native → به باکس دست نزن (P-BK-15)، فریز → خودمان می‌بریمش.

## §2 — حقایق ایونت MT4 که بارها به کار می‌آیند

- **Consumed ≠ hidden.** مقدار برگشتی `true` از `BaseKnotOnChartEvent` فقط از
  `OnChartEventHandler` خارج می‌شود؛ ورودی `.mq4` همه ایونت‌ها را
  **بی‌قیدوشرط** به `HandleUIChartEvent` هم می‌دهد. پس لایه UI می‌تواند روی
  درگ باکسی که دامین consume کرده سوار شود، بدون شکستن لایه‌بندی (Lite).
- `CHARTEVENT_OBJECT_DRAG` حین درگ **مداوم** می‌آید (هر استپ)، نه فقط پایان.
- `CHARTEVENT_CLICK` در برابر `OBJECT_CLICK` برای هر رها کردن **یکی** می‌آید
  (انحصاری) — پس طبقه‌بندی release (جدول زیر) کامل است.
- طبقه‌بندی هر button-up در شاخه `CHARTEVENT_CLICK` (به همین ترتیب):
  1. `fireRel` — رهای پایان hold بازکننده (one-shot، با هر CLICK مصرف شود،
     با press جدید هم پاک شود که stale نماند)؛
  2. `dragRel` — فاصله press-down تا release بیشتر از `BK_CLICK_SLOP`
     (خوانده‌شده از latch **قبل** از `BkHoldOnBoxUp`)؛
  3. راست‌کلیک (`"r"` در sparam — همان قرارداد `BaseKnotTool`)؛
  4. تپ روی آبجکتِ خودِ تولبار (re-hit-test، فقط visible).
  فقط کلیک چپ واقعی روی فضای خالی = dismiss.

## §3 — عددهای throttle پروژه (مرجع، تغییر نده مگر با دلیل)

| مسیر | نرخ | کجا |
|------|-----|-----|
| rubber-band سایزینگ | 30ms | `BaseKnotTool.mqh` |
| درگ هدر پنل | 16ms | `PnlHandleMouseMove` |
| درگ ناب/ترک اسلایدر | 30ms | `PnlHandleMouseMove` |
| strip-follow روی درگ | 30ms + dead-band ‏4px | `HandleUIChartEvent` → `BkStripFollow` |
| strip-follow fallback | per-tick + تایمر ۲۵۰ms | `BkMiniStripHeal` |
| hold-to-open poll | ۲۵۰ms | `BkHoldPoll` |
| پمپ‌های ۵۰۰ms | badges/hint | `RefreshUIPerTick` |
| بودجهٔ heavy-pass در ژست زنده (اسلایدر/میکسر/ناب) | 120ms | `BiotakKit.mqh` → `UIDragBudgetBegin/End` + `ApplyRefreshFlags` |

## §4 — چک‌لیست «فلو بعدی مثل این»

1. ایونتِ محرک دیتا کدام است؟ همان را بگیر، تایمر را تند نکن.
2. throttle + dead band از روز اول بگذار (بعداً اضافه‌کردن سخت‌تر است).
3. آبجکت‌ها را با اسم صریح تکان بده، نه اسکن.
4. وضعیت موقت (پاپ‌آور، درگ، latch) را موقع حرکت/بستن پاک کن.
5. Lite را نشکن: دامین فقط `Object*`، صداکردن UI از دامین ممنوع.
6. الگو را همین‌جا (§جدید) ثبت کن.

## §5 — چارت پشت هر ژست قفل و بی‌حرکت می‌ماند (2026-09-08، رسم/درگ بیس‌باکس)

**مسئله:** موقع هر ژست دستی (سایزینگ باکس، درگ باکس، درگ منو/پنل) چارت پشت
نباید حتی یک پیکسل تکان بخورد؛ وگرنه باکس جای کلیک نمی‌نشیند یا درگ کنسل
می‌شود و برمی‌گردد سرجاش (P-BK-14/15).

**راه (الگوی ثابت — هر ژست جدید عین همین):**

1. **شروع ژست = قفل؛ اولین قفل‌گیرنده save می‌کند، تودرتو فقط re-assert.**
   دامین (Lite-safe): `BaseKnotLockChart(ctxToo)` با `Chart*` خام، بدون
   وابستگی به منو. UI: `CircLockChart()` شمارشی + `ChartScrollReconcile()`
   به‌عنوان watchdog.
2. **حین ژست = re-assert مداوم و read-guarded** (`BaseKnotReassertLock`).
   قفلِ یک‌بار کافی نیست — نویسنده سوم (آنلاک مودال موقع بستن استریپ/کارت
   وسط ژست، watchdog پنل، ریست تمپلیت/ترمینال) وسط ژست بازش می‌کند. روی هر
   استپ throttleشده + پمپِ while-owning صدا بزن؛ steady state فقط read،
   write فقط موقع drift. `ctxToo` مال قفل‌گیرنده اول می‌ماند.
3. **آبجکتِ در حال درگِ native را فقط هندلرهای درگ/ریلیز خودش لمس کنند**
   (نه style/color/tooltip، نه move، نه delete+recreate) — ترمینال درگ را
   کنسل می‌کند و آبجکت برمی‌گردد سرجاش. پمپ‌ها باکسِ `s_bkDragId` را skip
   کنند؛ Sync مقتدرانه فقط موقع release.
4. **پایان ژست = دقیقاً یک Unlock** در همه خروجی‌ها
   (commit/cancel/ESC/release/OnDeinit) + watchdog گیرکردن (button-up +
   ۱.۵ ثانیه سکوت ایونت) برای release گمشده.
5. **tap و drag با slop جدا شوند** (۸px): tap نباید propها را flicker بدهد —
   قفل درگ فقط past-slop (P-BK-08).
6. **Lite را نشکن:** دامین فقط `Chart*`/`Object*` خام؛ صداکردن UI از دامین
   ممنوع (الگو: `BaseKnotLockChart` در `BaseKnotTool.mqh`).

**چرا نه راه‌های دیگر:**

- قفلِ فقط-موقع-شروع → نویسنده سوم وسط ژست بازش می‌کند (P-BK-14).
- Sync کامل هر استپ درگ → هم lag روی چارت سنگین (P-BK-07)، هم کنسل‌شدن
  درگ (P-BK-15)؛ فقط `ObjectMove` حین ژست + یک `Sync` موقع release.
- re-assert بدون read-guard → ده‌ها syscall بیهوده روی هر استپ.

**چک‌لیست «ژست جدید»:** قفل موقع شروع؟ re-assert حین ژست؟ Unlock در همه
خروجی‌ها؟ آبجکت درگ‌شده وسط ژست دست نمی‌خورد؟ slop برای tap؟ Lite
کامپایل می‌شود؟ (مثل چک‌لیست §4.)

## §6 — روش ریشه‌یابی و تحویل فیکس رفتاری (2026-09-08، درس‌های این چت)

1. **همه نویسنده‌ها را بشمر، به اولین تئوری بسنده نکن.** برای «X
   برمی‌گردد / تکان می‌خورد»: تک‌تک `ObjectMove/Set/Create/Delete` روی آن
   آبجکت + همه پمپ‌ها را در برابر تایمینگ ژست چک کن — این چت دو مکانیزم جدا
   داشت (پمپ وسط درگ می‌نوشت: P-BK-15؛ fallback فقط بچه‌ها را می‌برد:
   P-BK-16) و تئوری اول نصف جواب بود.
2. **جهتِ فریزنشده با هیسترزیس دنبال شود، نه با بافر.** stateای که از واقعیت
   زنده عقب می‌ماند (جهت باکس: P-BK-13) را با خودِ آبجکت به‌عنوان باند
   هیسترزیس دنبال کن — بیرون می‌گیرد، داخل نگه می‌دارد؛ نه بافر جدا، نه
   تنظیم جدید، نه فلیکر. `Sync` هرگز recompute نکند؛ refresh قبلش.
3. **Lite پمپ UI ندارد — healهای دامین را از مسیر مشترک تیک بچرخان.**
   (`OnCalculateHandler` با throttle خودش + `#ifdef BUILD_LITE` — P-BK-13.)
4. **هر فیکس رفتاری با اثر انگشت قابل مشاهده + دستور reload.** کاربر بیلد
   قدیمی در حافظه دارد (MT4 هرگز hot-swap نمی‌کند) — بدون fingerprint
   (مثل هینت flip) «درست نشده» یعنی بیلد قدیمی یا باگ واقعی، معلوم نیست.
   اول reload + تأیید fingerprint، بعد تئوری بعدی.
5. **یک مکانیزم در هر کامیت.** حدس تأییدنشده روی هم نریز؛ هر حدس = یک
   کامیت جدا و قابل برگشت.
6. **کاربر کم‌حرف = یک مشاهده تفکیک‌کننده.** به‌جای چند سؤال، همان یک
   جزئیاتی را بخواه که دو مکانیزم را از هم جدا می‌کند (مثل: «خط‌ها به باکس
   چسبیده‌اند یا جدا دنبال موس می‌آیند؟»).

## §7 — پرفورمنس‌فرست: کدی که روی ضعیف‌ترین PC با ده اندیکاتور دیگر هم تند است (2026-09-10، P-PERF-01)

**پیش‌فرض ذهنی:** ترِد چارت MT4 تک‌نخه و مشترک است — هر Print، هر
ChartRedraw، هر iTime اضافه از جیب ده اندیکاتور دیگر می‌رود. بودجه ثابت:
per-tick فقط O(1) compare؛ per-500ms فقط syscall خواندنیِ guardشده؛ repaint
فقط از یک throttle مشترک (۱۰۰ms). هر کد جدید باید از روز اول در همین بودجه
جا شود، نه اینکه بعداً «بهینه» شود.

**قوانین (به ترتیب ارزش):**

1. **امضای تغییر فقط slow-leg.** هیچ‌وقت مقدار تند (countdown ثانیه‌ای،
   spread زنده، قیمت لایو) را در sig نگذار — وگرنه گیت هیچ‌وقت match
   نمی‌شود و مسیر «تغییر» برای همیشه داغ می‌ماند (`TradePlanLiveTick` هر
   ۲ ثانیه rebuild + ۷ لاگ + snapshot هشت‌تایی می‌کرد چون `CloseInText`
   در sig بود). liveها مسیر ارزان جدا دارند (in-place text-set)؛ مسیر
   کامل + لاگ + snapshot فقط پشت تغییر عددی.
2. **delete+recreate ممنوع وقتی upsert کافی است.** الگوی ثابت:
   `ObjectFind<0 → create` بعد `Set*` (مثل `CreateATRTradePiece`)؛
   wipe-then-rebuild فقط flicker + kernel call است. purge لی‌آوت‌های
   منسوخ یک‌بار per-prefix با `static string s_purgedPfx`، نه هر تیک.
3. **repaint یکی، مشترک، ته کار.** وسط حلقه هرگز `ChartRedraw` نه (پمپ BK
   به‌ازای هر باکس می‌زد → فلگ `bkNeedPaint` + یکی ته حلقه). لایه‌ها
   throttle جدا نداشته باشند — همه از `ThrottledChartRedraw` تا repaintها
   روی هم بیفتند؛ raw فقط برای پایان ژست کاربر.
4. **throttle لانه‌ای، ارزان‌ترین چک اول.** گیت زمانی (۲s) بیرون، گیت
   عددی وسط، throttle سنگین (۳s/۱۰s لاگ) داخل؛ invariantهای حلقه (BG،
   edge-time، live-ref مثل `BaseKnotLiveRef`) یک‌بار بیرون حلقه؛
   `StringFormat` فقط بعد از گیت زمانی.
5. **سری‌دیتا (iTime/iATR/...) کمترین تعداد نزدیک‌ترین گیت.** هر صدا
   kernel call است؛ compositeها پشت TTL cache (`CalculateWeightedATR`)؛
   snapshot چندتایی فقط پشت تغییر عددی.
6. **Lite و Full یک بودجه.** healهای دامین از مسیر مشترک با throttle
   خودشان (۵۰۰ms)؛ هیچ مسیر Lite-only بدون throttle نه.

**چک‌لیست «کد جدید»:** sig فقط slow-leg؟ upsert به‌جای wipe؟ repaint ته
کار و مشترک؟ Print پشت گیت عددی؟ syscall تکراری از حلقه بیرون کشیده
شده؟ Lite کامپایل می‌شود؟ (مثل چک‌لیست §4.)

**قانون فیت (2026-09-10، P-ATR):** هیتِ subset/رگرسیون روی تک‌نمونه هیچی را
اثبات نمی‌کند — با ۲۰۴۷ زیرمجموعه، برخورد ۰٫۰٪ تضمینی است (پنج زیرمجموعه
کاملاً متفاوت هم‌زمان «درست» شدند!). فقط بقا در رژیم‌های مختلف (چند نمونه
هم‌دقیقه‌ای در روزهای متفاوت) فرمول را مشخص می‌کند؛ تا آن موقع نزدیک‌ترین
پیکربندیِ اصولی می‌ماند و میکس عددی توی کد نمی‌رود.

**قانون لاگ (2026-09-10):** عدد لاگ فقط از fresh-fetch همان لحظه —
قبل از هر استناد به `[SNAP]`/`[TRADEPLAN]` حتماً
`tools/fetch_tradeplan_log.ps1` را دوباره اجرا کن و `Total log lines` را
چک کن؛ لاگ فریز (خط جدید نیامده) یعنی فید مرده و هر مقایسه‌ای با آن
نامعتبر است. هرگز عدد کش‌شده از پیام‌های قبلی را بازنشر نکن.

## §8 — مالکِ متریک متن باید زیر همهٔ سطوحی باشد که از آن استفاده می‌کنند (2026-09-12، P-UI-34)

**مسئله:** در P-UI-30 مالک متریک (`PnlDpi/PnlPt/PnlTextW/PnlFit`) داخل
`BiotakPanels.mqh` نوشته شد؛ اما در هر entry (`.mq4`) ماژول‌های `BaseKnotTool`،
`BiotakKit` و `BiotakMenu` **قبل از** پنل‌ها include می‌شوند. نتیجه: سطح‌هایی که
تنگ‌ترین جعبه را دارند دقیقاً همان‌هایی بودند که نمی‌توانستند از مالک استفاده
کنند — ۱۹ سایز pt خام، چهار سایتِ حدس عرض `5 * StringLen`، و خط دومِ تولتیپ با
آفست جادویی `y + 26` (که در ۲۰۰٪ به خط اول می‌خورد).

**قالب فیکس (هر متریک/توابع مشترک جدید عین همین):**

1. **مالک را بگذار در اولین ماژولی که همهٔ مصرف‌کننده‌ها include می‌کنند**
   (`UtilityFunctions.mqh`), نه در بالاترین مصرف‌کننده. تست: اگر گیتِ مالک در
   ماژول A باشد ولی مصرف‌کننده‌ها قبل از A include شوند، گیت عملاً بی‌فایده است.
2. **هر متنی که در جعبهٔ ثابت می‌نشیند: pt اسمی + `PnlFit` + عرض اندازه‌گیری‌شده.**
   صفر «حدس عرض» (نه `StringLen*k`, نه عدد جادویی برای y خط دوم — از `PnlLineH`).
3. **یک مالک برای split+clip** اگر همان متن از دو مسیر (رسم و refresh) نوشته
   می‌شود؛ وگرنه مسیر دوم متن خام را برمی‌گرداند و باگ برمی‌گردد.
4. **گیت:** مالک এক‌بار تعریف شده باشد، هیچ `OBJPROP_FONTSIZE` عددی در
   کروم UI نباشد (متغیرهای تنظیمات کاربر و ماژول‌های دامین/بازنشسته با دلیل
   allowlist می‌شوند)، و متن‌ها با **بلندترین حالت** (طولانی‌ترین وضعیت) در
   ۹۶/۱۲۰/۱۴۴dpi اندازه گرفته شوند. `tools/ui-text-audit.py` — و مدل عرض را
   از `tools/panel-mt4-sim.py` import می‌کند تا کپی سوم جدول پیشروی ساخته نشود.

## §9 — چارت کند/لگ‌دار: قانون «هیچ نوشتنی که تغییری نمی‌دهد» (2026-09-12، P-PERF-02)

**مسئله:** به‌محض انداختن اندیکاتور، چارت کند و پر از لگ می‌شود؛ تعویض
تایم‌فریم هم فریز می‌کند.

**ریشه (نه یک باگ، یک خانواده):** در MT4 هر `ObjectSet*` چارت را dirty
می‌کند و هزینهٔ repaint با تعداد آبجکت‌های همین اندیکاتور بزرگ می‌شود
(اینجا ۱۰۰۰ تا ۲۵۰۰). پس هر کار per-frame که O(levels) یا O(objects)
باشد، مستقیماً لگ است:

1. pipeline ماسک `OBJPROP_TIMEFRAMES` را روی هر خط/لیبل/زون در **هر فریم
   سنگین** بازنویسی می‌کرد (~۱۵۰۰ نوشتن بی‌تغییر در فریم)، به‌علاوهٔ tooltip
   خط‌ها و price لیبل‌های فاصله بدون gaurd.
2. `RedrawAllObjects` در هر فریمی که فلگش بالا بود، همهٔ ~۳۰۰ سطح را از نو
   می‌ساخت — حتی وقتی هیچ ورودی تکان نخورده بود (تیک ATR هر ۵ ثانیه کافی
   بود).
3. مسیر تیک تا ۱۰ بار در ثانیه ChartRedraw می‌خواست، حتی برای فریمی که
   هیچ‌چیز نکشیده بود.
4. کندل forming هدف (HTF) هر تیک ۳ آبجکت خودش را بازنویسی می‌کرد.
5. `DeleteAllIndicatorObjects` در تعویض TF با حلقهٔ MQL روی **همهٔ** آبجکت‌های
   چارت و `ObjectDelete` تک‌تک تمیز می‌کرد (~۱۰۰۰ فراخوانی کرنل).
6. `HideAllTHObjects` در حالت مخفی در هر فریم سنگین روی همهٔ آبجکت‌ها
   می‌گشت.
7. `CacheClear` شرط early-exit را بعد از پاک‌کردن فلگ چک می‌کرد؛ هر بار کل
   ۳۲۷۶۸ باکت اسکن می‌شد.

**راه (به ترتیب ارزش):**

1. **یک مالک برای نوشتن، با gaurd مقدار + epoch.** `ApplyTfMaskGuarded` در
   `VisibilityManager.mqh`: اگر همان مقدار/همان epoch قبلاً نوشته شده، صفر
   syscall. هر نویسندهٔ بیرونی فقط باید epoch را bump کند (یا
   `CacheForgetTfMasks(prefix)` بزند) تا gaurd یک بار همه را دوباره بنویسد.
2. **کار per-frame باید تابع یک SIGNATURE باشد، نه حلقه.** `frameSig`
   (topology + base price + range + scaling factor + viewport + visibility +
   draw GENERATION) → فریم بی‌تغییر ~۱۵ مقایسهٔ رشته و صفر syscall.
   `MarkDrawGeneration()` را هر مسیری که آبجکت‌ها را پاک می‌کند صدا بزن،
   وگرنه signature دروغ می‌گوید و رندر انجام نمی‌شود (چارت خالی!).
3. **repaint را بی‌دلیل نخر.** `g_lastRedrawDidWork`؛ raw `ChartRedraw` فقط
   پایان ژست کاربر.
4. **teardown و show/hide باید bulk باشند، نه O(all objects).**
   `ObjectsDeleteAll` به‌ازای هر namespace؛ لایهٔ بیس‌باکس به‌خاطر شکل نام
   (`inpObjectPrefix + _BK_`) هیچ‌وقت زیر یک namespace تایم‌فریمی نمی‌افتد.
5. **آبجکت‌های دیدنی فقط در viewport.** HTF به اندازهٔ viewport + hysteresis
   کشیده می‌شود؛ بودجهٔ نوشتن زنده (`HTF_FORM_MS`) قبل از خواندن سری‌ها چک
   می‌شود تا تیک throttle شده یک iTime باشد و بس.
6. **lookup گران است، نه فقط syscall.** hashِ نام با `StringGetCharacter`
   برای هر کاراکتر حساب می‌شود؛ حتی guard‌شده‌ها per-object پرهزینه‌اند —
   پس قبل از حلقه، در سطح فریم gate بگذار (بند ۲).

**چرا نه راه‌های دیگر:**

- تایمر را کندتر کن → تأخیر را می‌خری، هزینه را نه (تحویل ایونت‌محور: §1).
- «برای اطمینان» هر فریم دوباره بنویس → همیشه بدترین حالت، درست همان چیزی
  که این گیت منعش می‌کند.
- حذف تک‌تک آبجکت‌ها موقع تعویض TF → گران‌ترین راه رسیدن به همان نتیجه.
- اسم water‌ها را تایم‌فریم‌ناوابسته کن تا باکس‌ها reuse شوند → بازنویسی
  بزرگ معماری (hotkeyها/namespace ها)؛ اول گیت‌ها را گذاشتیم.

**گیت:** `python tools/write-budget-audit.py` — مالک یکتا، generation روی هر
پاک‌کردن، gate فریم با generation، teardown بدون حلقهٔ O(all objects)،
بودجهٔ HTF قبل از خواندن سری، و مدل (۵ فریم یکسان روی ۳۰۰ آبجکت = ۳۰۰ نوشتن
به‌جای ۱۵۰۰)؛ `--selftest` هشت خطا می‌کارد و باید همه را بگیرد.


## §10 — «لگ باقی مانده» را از لاگ زندهٔ MT4 پیدا کن، نه با حدس (2026-09-12، P-PERF-03)

**مسئله:** بعد از §9 کاربر گفت «هنوز لگ شدید داره». حدس زدن ممنوع — این بار
ریشه از خود لاگ ترمینال پیدا شد.

**روش (این را تکرار کن):** ترمینال MT4 لاگ‌ها را در
`%APPDATA%\MetaQuotes\Terminal\<HASH>\MQL4\Logs\<YYYYMMDD>.log`
می‌نویسد و `_LOG_GATE_*` همیشه آن‌جاست. یک دستور این را قطعی کرد:

```bash
grep -a "CPU Warning\|CRITICAL CPU" <log> | sed "s/^[0-9]*\t//" \
  | awk "{print substr(\$1,1,5)}" | sort | uniq -c | sort -k2
```

نتیجه خودش جواب بود: **هر** بورت در دقیقهٔ `:00` یا `:30` — یعنی ۴ بار در
هر نیم‌ساعت به‌ازای هر چارت (و ۳۴ رویداد در ساعت ۰۰:۰۰). این «رابطهٔ زمانی»
ارزان‌ترین مدرک دنیاست؛ قبل از هر profiling گرانی اول همین را بگیر.

**ریشه‌ای که لاگ لو داد:**

1. **مرز ۳۰ دقیقه یک «دقیقهٔ کامل» بود، نه یک لبه.**
   `serverMinute == 0 || == 30` یعنی ۱۲۰ ثانیه از هر ۱۸۰۰ ثانیه
   `hasPendingWork=true`، پس کل پاس سنگین با gate ۵۰۰ms (~۲ بار در ثانیه)
   اجرا می‌شد: ~۲۴۰ پاس به‌جای ۱. اندازه‌گیری‌شده: ۶۲ms هر پاس روی این
   دستگاه، روی PC ضعیف چند برابر.
2. **کار خودِ مرز O(ده‌ها هزار) syscall بود:** `FindCurrentSessionStart`
   تا ۱۰٫۰۸۰ کندل را با **دو `iTime()` در هر تکرار** می‌گشت و اگر گپی
   پیدا نمی‌کرد یک بار دیگر کل تاریخچهٔ M1 را از قدیمی‌ترین کندل
   (~۲۹٬۰۰۰) می‌رفت — همان فریز ۱٫۸ تا ۳٫۰ ثانیه‌ای «CRITICAL CPU».
3. `CheckAlerts` هر فریم `~2 x inpMaxLevels` بار `StringFormat` +
   `ObjectFind` + `ObjectGetDouble` می‌زد برای قیمت‌هایی که همان فریم
   همین‌جا داشت.
4. `ClearAllLabels` هر بار ۱۱ بار `ObjectsDeleteAll()` (هر بار یک قدم
   روی کل آبجکت‌های چارت).
5. `WarmupATRMultiTFCache` هشت تایم‌فریم را پشت‌سرهم در `OnInit`
   حساب می‌کرد (هر attach و هر تعویض TF).

**راه‌حل‌ها (هر پنج‌تا همان الگو: کار را از per-frame/per-init بیرون بکش):**

- **مرز = لبه.** مقدار را به‌عنوان *candidate* حساب کن و برچسب بلوک را
  **بعد از همهٔ gateها** بزن؛ اگر قبل از gate بزنی، gate میلی‌ثانیه‌ای همان
  یک فریم را می‌خورد و کار base price اصلاً اجرا نمی‌شود (تله‌ای که یک بار
  در همین کار رخ داد).
- **سری‌ها را bulk بخوان:** یک `CopyTime(symbol, tf, 0, n, arr)` به‌جای
  n بار `iTime()`. ترتیب آرایه در ترمینال‌های مختلف فرق می‌کند، پس اول
  `arr[0] < arr[n-1]` را چک کن و لازم بود معکوس کن — به قرارداد ترمینال
  تکیه نکن. جواب را هم per-day memo کن.
- **لیست قیمت‌ها را در همان پاسی که رسم می‌کنی ثبت کن** (اینجا
  `AlertCacheAdd` داخل `RenderTriggerLines`) و چک را روی آرایه ببر:
  صفر syscall. `g_drawGeneration` را به‌عنوان اعتبار کش نگه دار — کش
  کهنه = سطوح از چارت رفته‌اند، پس skip کن نه اینکه بگردی.
- **Sweep گروهی را به generation گره بزن:** ۹/۱۱ بار پاک‌کردن فقط
  وقتی لازم است که چیزی پاک/عوض شده باشد.
- **Warmup را قرضی کن (amortize):** صف + یک آیتم در هر tick/تایمر.
  این هم `OnInit` و هم تعویض TF را روان می‌کند.
- **خودافشاگری بگذار:** دفتر فاز (base/hist/levels/labels/overlay ms) را
  فقط داخل پیام overrun چاپ کن. دفعهٔ بعد لاگ خودش می‌گوید کدام فاز خورده،
  و لازم نیست کل این تحلیل تکرار شود.

**گیت:** `python tools/write-budget-audit.py` (حالا + مرز لبه‌ای و محل commit،
خواندن bulk سری، کش alert، sweep وابسته به generation، warmup قرضی)؛
`--selftest` چهارده خطا می‌کارد و باید همه را بگیرد.

## §11 — لگی که «حس می‌شود» در مسیرهای تعامل است، نه در تیک (2026-09-12، P-PERF-04)

**مسئله:** بعد از دو دور فیکس، کاربر گفت هنوز موقع **کار با چارت**، **کار با پنل**
و **تعویض تایم‌فریم** لگ می‌زند.

**درس اصلی:** P-PERF-02/03 همه روی مسیر تیک اندازه‌گیری شده بودند (شاهدش
هشدارهای `CRITICAL CPU` در `:00/:30` بود). ولی شکایتِ باقی‌مانده مربوط به
مسیرهای **رویدادی** بود که هیچ‌کس اندازه‌شان نگرفته بود. پس همیشه بپرس:«این
لگ در کدام مسیر است؟ تیک، رویداد، یا reinit؟» و برای همان مسیر شاهد بساز.

**سه ریشهٔ ساختاری که آن‌جا پیدا شد:**

1. **پنجرهٔ viewport هر پیکسلِ pan را دوباره می‌ساخت.** آستانهٔ cache برابر
   «یک point» بود و خروجی همین تابع (`vpTop/vpBottom`) جزء امضای هندسهٔ
   رسم سطوح است → هر جابه‌جایی چارت = یک رندر کامل ~۳۰۰ سطحی. حاشیهٔ کش
   هم ±۵۰٪ محدودهٔ دید بود و برای هر سطح داخلش یک خط + لیبل فاصله +
   **یک مستطیل توپر به عرض کل چارت** ساخته می‌شود.
2. **وقتی چارت محدودهٔ قیمت را نمی‌داد** (استارت، پنجرهٔ مینیمایز، اولین
   فریم بعد از تعویض TF) فالبک «کل high-low تاریخی» بود → یعنی **همهٔ**
   سطوح `inViewport` می‌شدند و در یک فریم کل ست ساخته می‌شد؛ و چون cull
   فقط hide می‌کند، باقی‌ماندهٔ off-screen تا ابد روی چارت می‌ماند و هر
   repaint آن را می‌پیماید.
3. **درگ پنل هر ۱۶ms** یک دسته read+write به‌ازای هر آبجکت پنل می‌زد و
   سر هر تیک ماوس یک `ChartRedraw()` **همگام** می‌خواست (~۶۰ repaint در ثانیه).

**راه‌حل‌ها:**

- **حاشیه/هیسترزیس عدد دارند و قیدشان در گیت است:** `MARGIN 0.25 <`
  قدیم، `HYSTERESIS 0.20`؛ و **الزاماً margin > hysteresis** وگرنه یک pan
  که هرگز پنجره را دوباره نساخته می‌تواند یک سطحِ دیده‌شدنی را نکشد.
- **فالبک viewport محدود شد** (۲٪ حول قیمت) نه کل تاریخچه — این «گاردِ
  انفجار آبجکت» است.
- **آگاهی از پیکسل:** `RenderZones` یک بار مقیاس قیمت→پیکسل را می‌گیرد و هر
  زون نازک‌تر از ۲px را فقط hide می‌کند؛ چون همان حلقه از قبل off-screen ها
  را hide می‌کند، بی‌حالت (stateless) و امن است.
- **پنل**: batch روی ۳۳ms و repaint از `ThrottledChartRedraw()`؛ دلتا در
  `g_PnlMoveLast*` جمع می‌شود پس کارت دقیقاً روی نشانگر می‌ماند.
- **مسیرهای تعاملی اندازه‌گیری شدند:** `P4ReportSlow()` — رویداد چارت >
  `P_P4_EVENT_WARN_MS` (۴۰ms، با تفکیک `indicator=` و `ui=` تا بدانیم کدام
  نیمه) و `OnInit`/`OnDeinit` > `P_P4_INIT_WARN_MS` (۱۵۰ms)، حداکثر یک
  خط در ۲ ثانیه و فقط وقتی بودجه رد شده باشد.

**گیت:** `python tools/write-budget-audit.py` بخش `interaction` را گرفت
(رابطهٔ margin/hysteresis، فالبک محدود، حذف فیلم‌های sub-pixel، batch پنل +
repaint throttle‌شده، و اندازه‌گیری رویداد/init/deinit در **هر دو** entry)؛
`--selftest` بیست خطا می‌کارد و باید همه را بگیرد.

## §12 — بیلد اولیه/تعویض TF را مرحله‌ای (staged) بساز، نه یک‌جا (2026-09-12، P-PERF-06)

**مسئله:** بعد از چهار دور فیکسِ مسیر تیک، تعویض تایم‌فریم/اتچ هنوز چند ثانیه
فریز می‌کرد و درگ پنل/اسلایدر روی PC ضعیف اسلایدشو بود. سه ممیزی موازی نشان
داد مسیر تیک تمیز است؛ هزینه در سه جای دیگر بود: (۱) بیلد هم‌زمان ~۹۰۰ آبجکت
بعد از هر wipe در یک فریم، (۲) اسکن کل چارت در `PnlMoveBy` با نرخ درگ،
(۳) repaint خام با نرخ پوینتر حین درگ.

**راه (به ترتیب ارزش):**

1. **خانواده‌به‌خانواده بساز، نه همه‌چیز یک‌جا.** هر wipe (اتچ، تعویض TF،
   تاگل توپولوژی) `g_buildStage` را از ۱ شروع می‌کند: LINES → ZONES →
   pip-LABELS → labels-BLOCK؛ هر فریم یک خانواده (~۲k به‌جای ~۸k کرنل‌کال).
   امضای هندسه فقط در BLOCK مهر می‌شود؛ `needLabels` تا BLOCK صبر می‌کند؛
   HTF فقط کندل forming را می‌زند تا stage صفر شود؛ تایمر ۲۵۰ms هم پمپ
   می‌کند تا چارت بی‌تیک در ~۱ ثانیه settle شود.
2. **ری‌استارتِ wipe بی‌قیدوشرط.** تاگلی که وسط staging پاک می‌کند باید از
   LINES شروع کند، وگرنه خانواده‌های پاک‌شده هیچ‌وقت برنمی‌گردند. staging
   فقط روی wipe سوار است (نه روی هر رندر)، پس livelock ندارد.
3. **ویوپورتِ مرحله ۱ را فریز کن.** استیج‌های ۲ تا ۴ با همان پنجره cull
   می‌کنند تا pan وسط staging خانواده‌ها را دوپاره نکند؛ کهنگی حداکثر ~۱
   ثانیه است و رندر بعدیِ viewport خودش heal می‌کند.
4. **اسکن را به تایپ محدود کن، نه حذف.** `PnlMoveBy` به‌جای کل چارت، پنج
   تایپ UI را native می‌شمارد (LABEL/BUTTON/BITMAP_LABEL/EDIT/RECTANGLE_LABEL
   — تنها تایپ‌هایی که پنل می‌سازد)؛ رفتار identical، کاندید ~۵ برابر کمتر.
   رجیستریِ دستیِ اسم سریع‌تر بود ولی بدون runtime-test ریسک جاافتادن داشت.
5. **حین درگ، repaint هم throttle می‌خواهد.** شاخه swallow بودجه، ته
   `ApplyRefreshFlags` و `PalUpdateLive` همه `ThrottledChartRedraw` شدند
   (ناب/چیپ هر ۳۰ms تکان می‌خورند، repaint حداکثر ۱۰/s).

**چرا نه راه‌های دیگر:**

- مجازی‌سازی (delete به‌جای hide برای off-screen): اسکرول‌استاپ را پر از
  create/delete می‌کرد درحالی‌که هیسترزیس + گارد همین امروز آن را ارزان
  کرده — خالص منفی.
- ماسک per-TF برای نگه‌داشتن آبجکت‌ها بین TFها: بازنویسی معماری
  (همان که ممیزی صراحتاً defer کرده)؛ wipe می‌ماند، بیلد مرحله‌ای می‌شود.
- کم‌کردن `inpMaxLevels`: انتخاب ظاهری کاربر است؛ دست نزن، در پنل هست.

**چک‌لیست:** state در `GlobalVariables.mqh` (ماژول‌های بعدی ببینند)؛ هر
فریم staging باید pending-work باشد + وارد بلاک شود + skip نکند؛ امضا فقط
برای رندر کامل؛ گیت `staging` در `write-budget-audit.py` (۱۸ چک + مدل
۳۰٪)؛ `--selftest` باید همه را بگیرد (الان ۳۲).


## §13 — گاردِ «نوشتن» گاردِ «بررسی» نیست؛ و هر فیکس باید فازش را در لاگ نام ببرد (2026-09-12، P-PERF-07..10)

**۱) بزرگ‌ترین هزینهٔ باقی‌مانده، خودِ «نوشتن» نبود؛ «پرسیدن» بود.**
P-PERF-02 همهٔ `ObjectSet*`های هر فریم را شرطی کرد، ولی هیچ‌کس شرطی نکرد که آن
گارد چطور می‌فهمد آبجکت هست یا نه. `SetPipelineObjectTimeframesIfExists` برای هر
نامی که کش نمی‌شناسد یک `ObjectFind()` می‌زند — و سطحِ CULL شده هیچ‌وقت ساخته
نمی‌شود، پس هیچ‌وقت وارد کش نمی‌شود، پس هیچ‌وقت پرسیدنش قطع نمی‌شود: ۱ برای خط،
۱ برای لیبل، ۷ برای زون. با `inpMaxLevels=144` یعنی تا ~۲۶۰۰ پرس‌وجوی ترمینالی در
هر فریم سنگین، و چون هر پرس‌وجو لیست آبجکت‌های چارت (هزاران آبجکت) را می‌گردد،
هزینه **O(سطح‌های culled × تعداد کل آبجکت‌ها)** است — یعنی دقیقاً وقتی کاربر
زوم/تعویض TF می‌کند بدتر می‌شود.
قانون: **هر `ObjectFind`/`ObjectGet*` فال‌بکی باید یک حالت «نبودنِ اثبات‌شده» داشته
باشد.** جدول منفی (negative table) با مهر نسل (generation stamp) بساز تا
بی‌اعتبارسازی O(1) باشد و sweep لازم نشود؛ و فقط جایی پاکش کن که آبجکتِ ما
می‌تواند بدون گذر از کش ظاهر شود (`CacheClear` + `OnInit`). نکتهٔ کلیدی: نامی که
بعداً ساخته می‌شود در کشِ اصلی ثبت می‌شود و کشِ اصلی **اول** خوانده می‌شود، پس مهر
منفی خودش سایه می‌شود و نیازی به invalidate دستی ندارد.

**۲) کشی که بعد از syscall خوانده می‌شود، کش نیست.**
`BlendWithBackground()` اول `ChartGetInteger(CHART_COLOR_BACKGROUND)` می‌زد و بعد
کش را می‌دید؛ پس کش هیچ‌وقت یک تماس ترمینال را حذف نمی‌کرد. هر بارِ HTF، سه-چهار
ترکیب رنگ دارد → ۷۰۰-۱۰۰۰ خواندن در هر draw کامل، برای رنگی که فقط با تغییر
پراپرتی چارت عوض می‌شود. قانون: **خواندن گران را به یک مالک ببر و یک‌بار در هر
«پاس» انجام بده**، نه یک‌بار در هر فراخوانی.

**۳) هر فیکس پرفورمنس باید قبل از دیدن اثرش، فاز خودش را نام ببرد.**
`[W][PERF] OnInit (ok) took 3219..4110ms (budget 150ms)` روی همهٔ attach/تعویض‌TF —
درحالی‌که لجر تیک همهٔ فازها را صفر گزارش می‌کرد، چون لجرِ فریم داخل
`RedrawAllObjects` زندگی می‌کند و نمی‌تواند یک «توالی init» را ببندد. پس مسیر init
هم لجر خودش را دارد (`settings/hist/base/atr` + تفکیک `ind=/ui=`). درس P-PERF-05
تکرار شد: **چیزی را که لاگ می‌تواند نام ببرد، دوباره حدس نزن.**

**چک‌لیست این خانواده:** جدول منفی قبل از پروب خوانده شود؛ miss ثبت شود؛
`CacheClear` و `OnInit` مهرها را پاک کنند؛ بی‌اعتبارسازی O(1) (بدون sweep)؛ هر
syscall گران یک مالک و یک نوبت در «پاس» داشته باشد؛ گاردها روی **ورودی‌ها** باشند
نه روی زمان؛ و هر مسیر کند (init/event/تیک) فاز خودش را در لاگ بگوید.
گیت: `python tools/probe-budget-audit.py` (+`--selftest` با ۱۰ خرابی کاشته‌شده).

## §14 — لاگ فاز را نام برد، و آن فاز یک «حلقهٔ خودتخریب» بود (2026-09-12، P-PERF-11..12)

**۱) لجرِ فاز کار کرد؛ پس دوباره حدس نزن، لجر را ریزتر کن.**
خط لاگ دقیقاً همان چیزی را گفت که باید: `[ind=3828 ui=0 settings=15 hist=16
base=3797 atr=0]` — یعنی از ۳۸۲۸ms، فقط ۳۱ms در settings+hist+atr است و **۳۷۹۷ms
تمامش داخل `InitializeBasePriceSystem()`**. یک لجر دوم (file / migrate / rebuild /
cleanup) داخل همان فاز گذاشتم تا قدم بعدی با شاهد برداشته شود، نه با خواندن کد.

**۲) حدسِ محتوایی روی فایلی که خودمان می‌نویسیم، همیشه غلط می‌شود.**
آن فاز تشخیص می‌داد فایل «فرمت قدیم» است اگر **هر** رکوردش ساعت `>= 22:00`
داشته باشد. اما یک سشن کاملِ این برنامه از `00:xx` تا `23:xx` است ⇒ فایلِ روزِ خودش
همیشه یک بلاک `22:xx` دارد ⇒ هر init فایل معتبرِ خودش را پاک می‌کرد، `loadedCount`
صفر می‌شد و کل روز از دادهٔ M30 بازسازی می‌شد؛ و بازسازی همان `22:xx` را دوباره
نوشت ⇒ **دفعهٔ بعد دوباره migrate**. شاهدِ روی دیسک: `base_price_history_XAUUSD_2026_255.txt`
با ۴۸ رکورد `00:28..23:58` و mtime دقیقاً برابر لحظهٔ OnInit. درس: **وضعیتِ یک
فایل را از محتوایش حدس نزن؛ خودش باید خودش را توصیف کند** (اینجا: خط اول
`#FMT=<version>|GMT`، لودر آن را برمی‌دارد و نسخه را گزارش می‌دهد، تصمیم migrate
یک مقایسهٔ نسخه است).

**۳) یک باگ، دو رو.** همان بررسی نشان داد چرا برچسب‌های بلاک ساعت‌ها جلوتر از
بلاک GMT جاری بودند: `FindCurrentSessionStart()` زمان‌های **سرور** را که از
`CopyTime`/`iTime` می‌آید با برشِ `TimeGMT()` مقایسه می‌کرد؛ پس «شروع روز» یک
لحظهٔ سرور بود که به‌عنوان GMT برگردانده می‌شد و پایین‌دست یک‌بار دیگر تبدیل
می‌شد. قانون: **یک تابع هرگز دو فریم زمانی را قاطی نکن؛ یا سرور یا GMT — و اگر
هر دو لازم است، دقیقاً یک‌بار و در یک نقطه تبدیل کن.**

**۴) هر مهر/کش باید خودش بی‌اعتبار شود، نه با یادآوری جای دیگر.**
خرابیِ کامپایل (`CacheAbsentResetAll - function not defined`) نشان داد یک مسیر پاک‌سازی
که فقط با «یادت باشد اینجا صدا بزنی» کار می‌کند، دیر یا زود جا می‌ماند. الان مهرِ
«نبودن» فقط با `g_drawGeneration` زنده سنجیده می‌شود (مهرِ کهنه = اسلاتِ آزاد) و
`CacheAbsentResetAll()` فقط استمپ‌های عددی را صفر می‌کند (صفر هیچ‌وقت نسلِ زنده
نیست) — یعنی درستی از ساختار داده می‌آید، نه از ترتیب صدا زدن‌ها.

**چک‌لیست این خانواده:** هیچ حدسی از محتوای فایل/زمان (خودِ فایل نسخه‌اش را بگوید)؛
رکوردهای متادیتا (`#...`) در لودر حذف شوند و به آرایهٔ داده نرسند؛ مسیر نوشتنِ
جدید هم مهر بزند، وگرنه بار بعد خودش را legacy می‌بیند؛ یک تابع = یک فریم زمانی؛
و اگر فازی چند صد میلی‌ثانیه بود، **قبل از فیکس** آن را چهار تکه کن و در لاگ نام ببر
(تا فیکسِ بعدی هم قابل ابطال باشد). گیت: `python tools/probe-budget-audit.py`
(+`--selftest` با ۱۷ خرابی کاشته‌شده، همه گرفته می‌شوند).

## §15 — روی ضعیف‌ترین کامپیوتر: هر پرسوجو و هر پیمایش چارت باید دلیل داشته باشد (2026-09-12، P-PERF-13..15)

**۱) جدول «نبودن» فقط برای نوشتن نیست؛ برای حذف هم هست.**
`CleanupSurplusPipeline()` در هر فریم **هفت خانوادهٔ نام** را برای «اضافه‌ها» می‌پیماید و هر
پیمایش تا ۶ خطا ادامه می‌دهد؛ و هر خطای یک خانوادهٔ zone **هفت نام** می‌پرسد
(`base/_Top/_Bottom/_B_*`). همهٔ این پرسوجوها با `verify=true` می‌رفتند، یعنی هر کدام
یک `ObjectFind` ترمینالی — روی چارتی که هزاران آبجکت خودمان دارد، یعنی چند صد تماس
بی‌دلیل در هر فریم، برای همیشه. درست همان چیزی که کاربر می‌بیند: «اندیکاتور بی‌دلیل
سطوح را حذف/لمس می‌کند». راه‌حل: `DeleteIndicatorObjectManaged` **قبل از هر چیز**
جدول «نبودن» را می‌خواند، و اگر probe با `verify=true` خطا داد (یعنی ثابت شد آن نام
روی چارت نیست) **miss را ثبت می‌کند**. پس فریمهای بعدی O(1) هستند و اگر آن نام بعداً
ساخته شود، کش اصلی اول خوانده می‌شود و مهر را سایه می‌اندازد — همان invariant P-PERF-07.

**۲) کاری که به «تغییرِ ورودی» وابسته نیست، باید بعد از گاردِ تغییر بنشیند.**
بلوک مهاجرتِ zone (پاک‌کردن `_Top/_Bottom` قدیمی و باقیماندهٔ FILLED/EMPTY) **قبل** از
چک «چیزی عوض نشده؟ return» بود؛ یعنی zoneِ ثابت در هر فریم یک `ObjectFind` می‌پرداخت
(و گاهی ۴ تای دیگر). چنین هزینه‌ای در شمارشِ **نوشتن** دیده نمی‌شود — probe نوشتن نیست.
قانون: **همهٔ کارهای تشخیصی/مهاجرتی را بعد از گارد بگذار**؛ فقط مسیرِ واقعاً «بساز/تغییر بده»
حق دارد probe بزند.

**۳) پیمایش کل چارت برای پیدا کردن چند آبجکت خودمان را با API دست‌ای عوض کن.**
`DeleteHTFCandles()` اول `ObjectsTotal(OBJ_RECTANGLE)` و بعد `ObjectsTotal(OBJ_TREND)`
می‌گرفت و `ObjectName()` را برای هر آبجکت صدا می‌زد تا مالِ خودش را پیدا کند — روی چارتی
که هزاران مستطیلِ zone خودمان دارد، یعنی O(همهٔ آبجکت‌ها)، و دقیقاً روی مسیرِ `OnDeinit`
(هر تعویض TF، ~۳۰۰ms) و روی هر early-out حالت پایدار HTF اجرا می‌شد. جایگزین مستند:
`ObjectsDeleteAll(chart, prefix)` — یک تماس، اسکن داخل ترمینال، همان مجموعهٔ دقیق
(هر دو حلقه در واقع «نام با این پیشوند شروع شود» را تست می‌کردند).
منبع: docs.mql4.com/objects/objectsdeleteall.
نکتهٔ فرعی: MQL4 اعلانِ پیشین (prototype) تمیز ندارد؛ `bool f();` یک warning 46
(«no #import declaration») می‌دهد — پس تابع را **قبل از** مصرف‌کننده‌اش تعریف کن.

**۴) هزینهٔ باقی‌مانده را قبل از فیکس نام ببر.**
دو لکهٔ داغِ باقی‌مانده در لاگ (`OnDeinit=297ms` یک عدد، و `id=1 [indicator=0 ui=485..578]`
که فقط «نیمه UI» را می‌گفت) قبل از هر فیکسی باید تفکیک شوند: ترداون به شش فاز
(`pnl/menu/htf/save/cleanup/handler`) و حرکت ماوس به سه مالک (`ring/panel/hold`).
هر دو فقط وقتی از بودجه بگذرند چاپ می‌شوند.

**چک‌لیست برای کامپیوتر ضعیف:** هیچ probe برای نامی که در این نسل ثابت شده نیست؛
هیچ پیمایشِ کل‌چارت وقتی API پیشوندی وجود دارد؛ هیچ کار تشخیصی قبل از گاردِ تغییر؛
هر مهر/جدول خودش بی‌اعتبار شود؛ و هر مسیرِ گران فاز خودش را در لاگ بگوید.
گیت: `python tools/probe-budget-audit.py` (+`--selftest` با ۲۴ خرابی کاشته‌شده، همه گرفته می‌شوند).

---

## §16 — «یک‌بار در جای درست محاسبه کن»: چیدمان UI، تولتیپ، و هندسهٔ مرحله‌ای (2026-09-12، P-PERF-16..18)

**۱) هر hit-test نباید از ترمینال اندازه بخواند، و نباید برای هر آیتم دوباره fit کند.**
`CircItemAt()` به‌ازای هر آیتمِ رینگ یک‌بار `CircLayout()` را صدا می‌زد، `ToolsItemAt()`
به‌ازای هر تایل `SubPlaceItem()`، و `CircLayout()` خودش `CircFitRadius()` را می‌صدا زد که
روی **همهٔ** آیتم‌ها sin/cos می‌زند. هر دوی این‌ها هم `CHART_WIDTH_IN_PIXELS` و
`CHART_HEIGHT_IN_PIXELS` را مستقیم از ترمینال می‌خواندند. یعنی یک سؤال سادهٔ
«کدام دکمه زیر نشانگر است؟» = **۱۶ تا ۴۰ تماس ترمینالی + O(RING_COUNT²) مثلثات**،
و این در هر حرکت ماوس تکرار می‌شد. قرارداد استاندارد UI toolkitها: یک
**layout cache** با دو نقطهٔ invalidation — رویداد تغییر اندازهٔ چارت و یک
safety refresh در تایمر. بعد از این، یک hit-test صفر تماس ترمینالی دارد و
fit برای کل رینگ یک‌بار حساب می‌شود (memoised روی `(cw,ch,ox,oy)`).
نکتهٔ مهم: قانون «یک مالک برای مستطیل چارت» است، نه «مالکِ فلان ماژول»؛ پس دو
مسیرِ rate-بالای پنل‌ها (`PnlClampSpot`, `BkStripFollow`) هم به **همان** خواننده منتقل شدند.

**۲) «پنهان‌کردن» با حذف آبجکت + `ChartRedraw()` گران‌ترین راه است؛ parking ارزان‌ترین.**
`CircTipHide()` تولتیپ را **حذف** می‌کرد و `ChartRedraw()` می‌زد؛ بعد hover بعدی مجبور
بود همه را از صفر بسازد. و دیده‌شدنش با `ObjectFind(0, CircTipBg())` در هر حرکت ماوس
فهمیده می‌شد. این هزینه در هیچ شمارندهٔ نوشتنی دیده نمی‌شود: یک delete+create در شمارش
خنثی می‌شود و `ChartRedraw()` اصلاً نوشتنِ آبجکت نیست. راه‌حل: تولتیپ در
`CIRC_HIDE_POS` **پارک** می‌شود (همان ایدیومِ Tools fan)، و visibility یک
**متغیر حالت** است (`s_CircTipFeat`) نه probe. ضمناً بعد از نوشتن‌های پراپرتی که خودشان
چارت را dirty می‌کنند، `ChartRedraw()` اضافه است — حذف شد.

**۳) «مرحله‌ای‌کردن» نوشتن‌ها، محاسبهٔ تکراری را چند برابر می‌کند اگر فقط فریم‌ها را بشکنی.**
P-PERF-06 یک rebuild بعد از پاک‌سازی را به چهار فریم (lines/zones/labels/block) شکسته بود
تا سقف تماس‌های هر فریم پایین بیاید — ولی هر فریم دوباره `ExecutePipeline()` را صدا
می‌زد، پس `CalculateLevels → Classify → BuildZonesAndLines` با ورودیِ **بی‌تغییر** چهار
بار اجرا می‌شد. حالا نتیجه زیر یک **کلید** ساخته‌شده از همهٔ ورودی‌هایی که آن سه مرحله
می‌خوانند کش می‌شود (مُد/پیشوند، قیمت مرکز، stepها و مُدشان، classify+lsFirst، سقف‌ها،
پنجرهٔ cull، سوییچ تریگر، base multiplier، همهٔ فیلدهای ظاهری zone/fallback/midpoint،
رنگ/شفافیت/تول‌گلِ زندهٔ خط، پرچم drag قیمت دلخواه، و جدولِ structure-interval).
هر تغییری در این‌ها کلید را عوض می‌کند، پس کش فقط می‌تواند برای هندسهٔ **تغییرنکرده**
جواب بدهد؛ و اگر محاسبه هیچ سطحی تولید نکرد، کش **دراپ** می‌شود نه اینکه خالی ذخیره شود.

**۴) در کلید، «اثبات» کن نه «ادعا».**
ترمِ کلید `ArraySize(g_cachedIntervals)` همیشه ۵ بود: یک عددِ ثابت که هرگز نمی‌تواند
عوض شود، پس هیچ‌وقت چیزی را نمی‌گیرد. جایگزین شد با **خودِ پنج مقدار** جدول. هر ترمی
در یک کلید/گارد باید بتواند در جهانی که ادعای نامزدش عوض شده، **fail** کند — وگرنه
سرگرمی است. (`--selftest` دقیقاً همین را می‌سنجد: اگر بتوانی خرابی را بکاری و گیت
نگیرد، آن ترم تزئینی است.)

**چک‌لیست:** اندازهٔ چارت فقط از یک مالک؛ fit/هندسهٔ مشترک memoise شود؛ پنهان‌کردن =
parking نه حذف؛ visibility = حالت نه probe؛ بعد از نوشتن پراپرتی، repaint اجباری نزن؛
در pipeline مرحله‌ای، محاسبهٔ مشترک را یک‌بار بساز و زیر کلیدِ «همهٔ ورودی‌ها» بگذار؛
و هر ترمِ کلید/گارد باید قابل‌اب‌اطال باشد.
گیت: `python tools/probe-budget-audit.py` (+`--selftest` با ۳۵ خرابی کاشته‌شده، همه گرفته می‌شوند).

---

## §17 — «یک کشِ نگهداشتهشده که کسی صدایش نمیزند» و «قفل بهازای هر خواندن» (2026-09-12، P-PERF-19..20)

**۱) ماکروی متنی، مخفیترین مسیر داغ است.**
هفت متغیر «گلوبال» قدیمی (`g_basePriceCached`, `g_historyCount`, `g_referenceM1Power`,
`g_systemInitialized`, `g_lastHistoryDay`, `g_lastHistoryYear`,
`g_lastProcessedBlockTimestamp`) در واقع **ماکرو** بودند و به `GetSymbolStateIndex()`
باز میشدند. آن تابع یک lookup نیست؛ یک **میوتکس با گلوبالویریبلهای ترمینال** است:
`Symbol()`، دو `GlobalVariableCheck`، `TimeCurrent()`، دو `GlobalVariableSet`، دو
`GlobalVariableTemp`، دو `GlobalVariableDel` و یک حلقهٔ retry — یعنی **حدود ۹ syscall
گلوبالویریبل بهازای هر خواندن**، در ~۱۱۵ محل. و چون ماکرو است، هیچ `grep`ی برای
«تابع گران» آن را نشان نمیدهد: در شرط حلقهها هم نشسته بود
(`for(i = 0; i < g_historyCount; i++)` = یک قفل کامل بهازای هر آیتم).
درس: **قبل از هر بهینهسازی، ببین ماکروها به چه چیزی باز میشوند**؛ یک ماکرو میتواند
یک تابع گران را در صد جا پنهان کند.

**۲) کشی که ساخته و نگهداری میشود ولی صدا زده نمیشود، بدتر از نبودنش است.**
`GetCachedSymbolStateIndex()` وجود داشت، کشش هم درست نگهداری میشد (حتی
invalidateش در LRU وصل بود) — و **هیچ caller ای نداشت**. یعنی کار اضافه میشد و
استفاده نمیشد. این همان کلاس P-PERF-08 است (کشی که *بعد از* syscall چک میشود).
گیت جدید دقیقاً همین را میگیرد: **هر ترمی که مقصدش «un*Cached*» است، خرابی است.**

**۳) عددِ درست در جای اشتباه: زیرفاز را نام ببر.**
`migrate=312ms rebuild=0ms (fileStamp=5)` یعنی «فایل مهرشده، هیچ بازسازیای نبود، ولی
۳۱۲ میلیثانیه رفت». یک مهر برای چهار کار مختلف (dedup / migrate-format / save /
restore) گذاشته بودم؛ پس خودِ مهر بیفایده بود. قاعده: **مهر را جایی بگذار که به یک
سؤال جواب بدهد، نه جایی که مجموع را بپوشاند.** حالا `load=[dedup= fmt= save= restore=]`.

**۴) «تغییر لازم بود؟» را از شمارش استنتاج نکن، از محتوا بپرس.**
`needsSave = (newCount != count || migratedCount > 0)` یک **استنتاج** بود: یک پاس
میتواند «رفع تغییر» گزارش کند ولی خروجی بایتبهبایت یکسان بسازد؛ و بعد فایل روی هر
attach بازنویسی میشود. مسئلهٔ کاربر («چرا بیدلیل آپدیت میکند؟») همین است.
جایگزین: یک مقایسهٔ سادهٔ محتوا (`HistoryContentDiffers`) — ۴۸ رشته در برابر یک flush
دیسک. **هر نوشتنی باید دلیلِ محتوایی داشته باشد، نه دلیلِ شمارشی.**

**۵) کش را با همان شرطی که بار اوّل انجام شد، اعتبارسنجی کن.**
resolver کششده فقط وقتی جواب میدهد که **هم** نام سیمبل عوض نشده باشد **و** اسلات
بهخاطرآوردهشده هنوز داخل بازهٔ زندهٔ آرایه باشد؛ و تنها عملیاتی که آرایه را
جابهجا میکند (LRU eviction) کش را صفر میکند. ضمناً این کش «بهینهسازی» است نه
«دفترداری»: فقط روی مسیر miss شمرده میشود، پس مسیر سریع صفر هزینهٔ اضافی دارد.

**چکلیست:** ماکروها به چه چیزی باز میشوند؟ آیا هر کش caller دارد؟ کدام syscall در هر
خواندن تکرار میشود؟ هر مهر به یک سؤالِ مشخص جواب میدهد؟ هر نوشتن دلیلِ محتوایی
دارد؟ کش با چه شرطی invalid میشود؟
گیت: `python tools/probe-budget-audit.py` (+`--selftest` با ۴۱ خرابی کاشتهشده، همه گرفته میشوند).

---

## §18 — «یک کلید باید فقط خانوادهٔ خودش را عوض کند» — جداسازی خانوادهها (2026-09-12، P-PERF-21..22)

**۱) سوئیچی که یک خانواده دارد، نباید به مسیر «تعمیر کل چارت» وصل شود.**
کلید T (اورلی تریگر) در سه جا به مسیر تعمیرِ Global وصل بود:
- `levelSig` — یعنی **امضای توپولوژی** — شامل `IsTriggerLevelsEnabled()` بود ⇒ هر بار T،
  `shouldClearLevels` میشد و `ClearAllLevels()` **همهٔ** سطوح/زون/لیبل را حذف میکرد؛
- هر سه نقطهٔ تاگل (`T`، حلقهٔ منو، کارت TRIGGER) `g_forceClearOnNextDraw = true` میزدند؛
- `PipelineGeometryKey` هم روی همان فلگ کلید میخورد ⇒ حتی بدون حذف، کل هندسه دور ریخته
  و دوباره محاسبه میشد.

و هیچکدام لازم نبود: آن فلگ **فقط** در `RenderZones` خوانده میشود
(`zones[i].isTrigger && !triggerEnabled`). نه `CalculateLevels`، نه `BuildZonesAndLines`،
و حتی دو helperی که آن را بهعنوان پارامتر میگیرند (`GetPathForLevelOptimized`،
`GetZoneColorForLevel`) هیچوقت نگاهش نمیکنند. **قاعده: امضای «توپولوژی» فقط ورودیهای
هندسه را دارد؛ آنچه فقط تصویر را عوض میکند در امضای «رندر» مینشیند.** نتیجه: T حالا
یک re-render است، نه یک wipe.

**۲) پارامتری که در بدنهٔ تابع استفاده نمیشود، یک رسوبِ خطرناک است.**
`triggerEnabled` در دو helper پارامتر بود و در بدنه هیچجا خوانده نمیشد (کامنتها هم
میگفتند «باید مستقل باشد»). این «پارامترِ مرده» بود که در سه کلید/امضا کپی شده بود و
یک حذف جهانی میساخت. **پارامتر بیاستفاده را حذف کن یا در امضا نگه ندار.**

**۳) در یک کش، «ورودیهایی که نتیجه را میسازند» را باید اثبات کنی، نه به گاردِ دیگری تکیه کنی.**
هندسهٔ کششده **رنگ زون** را هم در خود دارد (`ClassifyLevels` → `zoneColor` از
`GetZoneColorForLevel`/`GetTriggerRenderColor`)، ولی هیچیک از ورودیهای آن رنگ در کلید
نبود. استال شدنش دیده نمیشد چون هر ویرایش رنگ، `levelSig` را عوض میکرد و wipe میزد —
یعنی **درستیِ کش به گاردِ دیگری وابسته بود**. حالا خودِ کلید آن ورودیها را نام میبرد.

**۴) پیمایش کل چارت برای نوشتنِ یک ماسک، غلط است — و راهش از قبل وجود داشت.**
کلید L با `ObjectsTotal(0,-1,-1)` + `ObjectName` + `ObjectGetInteger` برای **هر آبجکت
چارت** (مال ما، مال اندیکاتورهای دیگر، مال خود MT4) یک ماسک مینوشت. تابع درست
(`SetAllLineObjectsVisibility`) در `VisibilityManager` بود، **هیچ caller نداشت**، و
دقیقاً همان کار را روی **کش آبجکت** انجام میداد. این سومین نمونهٔ همین کلاس در این
کدبیس است: helper نوشته و نگهداری شده و بعد نادیده گرفته شده.
**همیشه قبل از نوشتنِ یک walk جدید، بپرس: آیا این مالک از قبل وجود دارد؟**

**۵) استثناهای یک کلید باید با خودش منتقل شوند.** وقتی L را به walk کشی منتقل کردی،
استثناهای خودش (بوردرهای `_B_*` مال کلید F؛ ریهای `_BK_` هرگز خط نیستند) هم باید
منتقل شوند، وگرنه یک خانواده روی دیگری اثر میگذارد — همان چیزی که کاربر گزارش داد.

**چکلیست:** هر تاگل کدام خانواده را دارد؟ آیا در امضای توپولوژی است یا رندر؟ آیا پارامتر
مرده در امضاها هست؟ کش کدام ورودیها را باید اثبات کند؟ برای هر walk جدید، آیا مالکِ
کش/جدولِ از قبل موجود وجود دارد؟ استثناهای کلید همراهش منتقل شدهاند؟
گیت: `python tools/probe-budget-audit.py` (+`--selftest` با ۴۹ خرابی کاشتهشده، همه گرفته میشوند).

## §19 — «چرا روشن/خاموش کردن سطوح اینقدر طول می‌کشد؟»: امضا باید فقط ورودی‌هایی را نام ببرد که مراحلِ خودش می‌خوانند (2026-09-13، P-PERF-23..26)

**۱) یک تاگلِ ظاهری نباید «پاک‌سازی کل چارت» باشد.** چهار ترمِ ظاهری
(`inpShowMidZones`, `inpMidZoneStyle`, `inpMidZoneTransparency`, `inpMidZoneHeightPercent`)
داخل `levelSig` — یعنی امضای **توپولوژی** — بودند. هر تغییرشان `levelTopologyChanged` می‌شد،
`shouldClearLevels` را می‌گرفت، `ClearAllLevels` همهٔ خط‌ها/زون‌ها/لیبل‌ها را حذف می‌کرد و
بازسازی چهارفریمی (با گیت ۲۰۰ms هر فریم) از نو شروع می‌شد — برای سوییچی که فقط تصمیم می‌گیرد
زون‌های میانی **رنگ شوند یا نه**. راه‌حل: توپولوژی و ظاهر را جدا کن (`levelLookSig`)، و ترم‌های
ظاهری را **به امضای رندر منتقل کن، نه حذف** — وگرنه محاسبه می‌شود و هرگز رنگ نمی‌شود.

**۲) امضا فقط ورودی‌هایی را نام ببرد که مراحلِ خودش می‌خوانند.** `g_linesVisible` در کلیدِ هندسه
بود، با این توضیح که «ClassifyLevels ظاهر خط و تاگل L را می‌خواند». **غلط بود**: grep نشان می‌دهد
این فلگ در کل فایل دو مصرف‌کننده دارد — خطِ رنگ‌آمیزی (`lineTf`) و خودِ کلید. یعنی هر تاگل، کل هندسه
را دور می‌ریخت و لیست‌های **بایت‌به‌بایت یکسان** را دوباره می‌ساخت. بدتر: یک چرخهٔ قبلی همین را
به‌عنوان «الزام» در گیت نوشته بود؛ **گیت، باگ را تأیید می‌کرد.** درس: گیت باید قاعده را بسنجد
(«ورودی‌های واقعیِ مرحله»)، نه یک رشتهٔ خاص را.

**۳) چرا عدد‌ها اشتباه خوانده شدند: لاگِ ناخوانا.** دو چرخه، جهش‌های ۴۸۵–۵۷۸ms را «مسیر حرکت ماوس»
ثبت کردند و فیکس‌ها را به hover نشانه گرفتند. id خام چاپ می‌شد. این بار **از کامپایلر پرسیدیم**
(probe با برخوردِ case): `OBJECT_CLICK=1` و `MOUSE_MOVE=10`. پس `id=1` یک **فشردنِ دکمه** است —
همان «تاگل کردن یک سوییچ نیم‌ثانیه طول می‌کشد». حالا لاگ نام می‌نویسد
(`P4EventName`)، و خودِ کلیک به سه مالک شکسته می‌شود (`button/panel/apply`).
**هرگز یک لاگ ننویس که خواننده باید عددها را از حفظ بداند.**

**۴) یک عملِ گسسته باید «همین حالا» رنگ شود.** هر تاگل به `ThrottledChartRedraw()` ختم می‌شد،
که داخل پنجرهٔ ۱۰۰ms **هیچ‌وقت رنگ نمی‌کند** (و در حالت hidden هم همیشه برمی‌گردد). فریمِ خودِ
رویداد قبلاً رنگ کرده بود، پس هیچ کاری معلق نمی‌ماند؛ و فریمِ تیک بعدی در
`if(!hasPendingWork) return;` خارج می‌شود: بازخورد تا تیک بعدیِ بازار معطل می‌ماند.
یک مالک: `RepaintForDiscreteAction()`. اجبارِ رنگ فقط به یک دلیل امن است و گیت همان را اثبات
می‌کند: dispatcher فقط از ماژول‌های UI قابل دسترسی است، هرگز از تیک. رنگِ **درگ** باید throttled بماند.

**۵) ثابت کن کدام ترم عوض شد، حدس نزن.** `frameSig` یک رشتهٔ تخت بود، پس نمی‌شد پرسید «چه چیزی
عوض شد؟». حالا امضا دو تکه است: `frameCore` (هرچه رندر می‌سازد) + ترمِ ماسک. اگر core یکی باشد و
امضای کامل نه، **اثباتاً** فقط ماسک عوض شده ⇒ رندر لازم نیست (پیمایش ماسک قبلاً نوشته، و هر خطِ
تازه‌ساخته‌شده فلگِ زنده را در لحظهٔ ساخت می‌خواند). فریمِ skipشده هم باید **مهر** بزند، وگرنه
فریمِ ثابتِ بعدی دوباره رندر می‌کند. پاک‌سازی و فریم‌های مرحله‌ای از این مسیر مستثنا هستند.

**چک‌لیست:** کدام سوییچ‌ها مالک کدام خانواده‌اند؟ آیا ترمی در امضای توپولوژی مانده که توپولوژی
نیست؟ آیا ترمی از کلیدِ هندسه حذف شده که مرحله‌ای می‌خواندش؟ بازخوردِ عملِ گسسته کجا رنگ می‌شود؟
لاگ نام می‌نویسد یا عدد؟ کدام ترم عوض شد — اثبات شده یا حدس؟
گیت: `python tools/probe-budget-audit.py` (+`--selftest` با ۶۱ خرابی کاشته‌شده) و
`python tools/write-budget-audit.py` (+`--selftest` با ۳۶ خرابی).

## §20 — «با هر تیک عوض نمی‌شود تا تایم‌فریم را عوض کنم»: تفکیکِ «محتوا» از «تصویر» — و کلیدِ سومی که هیچ‌کس نمی‌شمرد (2026-09-13، P-PERF-27..30)

**سؤال کاربر:** «ریل‌تایم توی همون تایم خاموش و روشن نمی‌شه، باید تایم عوض کنم که اعمال بشه.»

سه لایه از هم جدا شد، و هر سه یک چیز را نشان می‌دادند: *ما یک سؤال را به جای دو سؤال جواب می‌دادیم.*

### ۱) لاگ بالاخره مالکِ teardown را گفت — و آن مالک، «هیچ‌کاری» بود

```
[W][PERF] OnDeinit breakdown: pnl=15 menu=0 htf=0 save=78 cleanup=63 handler=16
```
۱۴۱ms از ۱۷۲ms روی چارتی که کسی ویرایشش نکرده بود. `RuntimeSettingsSaveOverrides` هر ۹۷ کلید را می‌نوشت و بعد `GlobalVariablesFlush` می‌زد؛ مستندات رسمی این تابع (`docs.mql4.com/globals/globalvariablesflush`) می‌گوید: «forcibly saves contents of **all** global variables to a disk» — یعنی کلِ جدولِ ترمینال، نه فقط کلیدهای ما. و teardown این کار را **سه بار** می‌کرد.

**درس:** «ذخیره‌ی محتاطانه» بدون *تشخیص تغییر* یعنی پرداختِ هزینه‌ی کامل برای چیزی که همان‌جاست. راه‌حل، یک **سایهٔ نوشتن** (write shadow) است: نوشتن‌ها در این تابع ترتیب ثابت دارند، پس یک شمارنده‌ی slot کلیدِ پایدار می‌دهد و لازم نیست فهرست دومی از نام‌ها بسازیم که با نویسنده از sync خارج شود. مقدارِ برابر ⇒ صفر تماس ترمینال، و flush فقط وقتی که واقعاً چیزی نوشته شده. و نکته‌ای که کل طرح را قابل‌اتکا می‌کند: **هر چیزی که آن کلیدها را حذف می‌کند باید سایه‌ها را باطل کند** (`ClearAllGVs` → `GVShadowsInvalidate`)، وگرنه گارد با کلیدهایی مقایسه می‌کند که وجود ندارند و نوشتنِ بازگرداننده را رد می‌کند.

### ۲) بزرگ‌ترین توقفِ لاگ، یک باگِ «مقدار اولیه‌ی صفر» بود

```c
static double s_lastVisibleMin = 0;   // و مقایسه با 0
if(!sizeChanged && !viewportChanged) return;
```
روی **اولین** رویدادِ هر instance، `MathAbs(visibleMin - 0) > point` بی‌قید‌و‌شرط درست است. پس `viewportChanged` از روی **ساختار** true بود و یک `RedrawAllObjects(false)` کامل داخل رویداد اجرا می‌شد. لاگ خودش این را فریاد می‌زد و کسی نمی‌خواند:

```
initialized 18:53:34.184
[W][PERF] chart event id=9 [indicator=3375 ui=0] took 3375ms   ← ۱۸:۵۳:۳۷.۵۵۵
```
یعنی **مدتِ رویداد = فاصله از init**. ۵۳ بار در یک روز، ۳.۴ تا ۴.۱ ثانیه.

**درس:** snapshot را از «صفر» مقداردهی نکن؛ از **واقعیتِ همان لحظه** پر کن. یک `s_ccPrimed` این را درست کرد.

### ۳) باگِ اصلیِ گزارش کاربر: یک فلگ، چهار نویسنده، یک صاحبِ ماسک

`g_linesVisible` در چهار جا عوض می‌شد و فقط یکی (کلید L) ماسک را می‌نوشت. سه جای دیگر — دو ردیفِ SHOW LINES در کارت‌ها و reset کارخانه — فلگ و mirror و کلیدِ ذخیره‌شده را می‌گذاشتند و می‌رفتند. این تا دو چرخه قبل **دیده نمی‌شد** چون هر فریم کلِ خانواده را دوباره می‌کشید. ولی P-PERF-25 رد‌کردنِ رندر را **قابل‌اثبات** کرد:

> رندر فقط وقتی رد می‌شود که `frameCore == s_lastFrameCore && frameSig != s_lastFrameSig` — یعنی اثبات شده که تنها تفاوت، ترمِ ماسک است — و بعد رندر نمی‌کند، چون **صاحبِ ماسک باید قبلاً نوشته باشدش.**

و کسی ننوشته بود. پس هیچ‌چیز عوض نمی‌شد تا یک تعویضِ تایم‌فریم کلِ چارت را از صفر بسازد. **دقیقاً جمله‌ی کاربر.**

**درس:** وقتی یک بهینه‌سازی را «قابل‌اثبات» می‌کنی، باید ثابت کنی که *پیش‌شرطش* هم برقرار است. P-PERF-25 یک اثبات درست داشت و یک پیش‌فرضِ غلط («صاحبِ ماسک نوشته»). و صاحبِ یک فلگ باید یکی باشد: `SetLinesVisible()` حالا حالت، mirror، کش، کلید و ماسک را با هم می‌نویسد.

### ۴) و همان باگ، یک لایه بالاتر: امضای هندسه نباید رنگ را وتو کند

`frameSig` به سؤالِ درست جواب می‌دهد: «آیا چیزی که هندسه را از آن *مشتق* می‌کنم عوض شده؟» ولی از همان امضا برای وتوی **رنگ** هم استفاده می‌شد — و رنگ، گلوبال‌های زندهٔ بیشتری می‌خواند که `frameSig` نامشان را نمی‌برد (سوییچ‌های structure، style/width/transparency خط، بوردرِ mid-zone، شفافیت تریگر، خط میانی، ظاهر custom price). هر ورودیِ جاافتاده = دکمه‌ای که تا تعویض تایم‌فریم کاری نمی‌کند.

راه‌حل، اضافه‌کردنِ ترم‌های بیشتر نیست (فهرست همیشه ناقص می‌ماند)؛ **جدا کردن دو سؤال** است:
- `frameSig` می‌پرسد «هندسه کهنه است؟»
- `g_renderAllNeeded` می‌گوید «کاربر درخواست رنگ کرد» — و فقط `applyRefreshFlags` (که هرگز از تیک یا درگ صدا زده نمی‌شود) آن را بالا می‌برد.

نتیجه: ویرایش کاربر همیشه رنگ می‌شود، ولی **یک** re-assert — چون خودِ رندرِ کامل‌شده فلگ را مصرف می‌کند. `mustRender` نام گرفت تا ترم‌هایش قابل‌ممیزی باشند، و گیت هر سه ترم را الزام می‌کند.

### ۵) گیت‌ها هم اشتباه نمی‌کردند — شکلِ قدیمی را نگه می‌داشتند

دو گیت روی این تغییر قرمز شدند، و هر دو حق داشتند که قرمز شوند: یکی دنبال `SetAllLineObjectsVisibility` **داخلِ بلاکِ کلید L** می‌گشت (یعنی همان باگ، در قالبِ assert)، و یکی امضا را با یک regex شل می‌سنجید که `!visOnly` را در ترمِ `(g_renderAllNeeded && !visOnly)` هم پیدا می‌کرد. هر دو به **حالتِ درست** بازنویسی شدند، و دو بذرِ کهنه هم خودشان `STALE SEED` دادند — یعنی خودِ ابزار هم می‌گوید «من دیگر نمی‌سنجم چیزی که فکر می‌کنم».

**درس:** گیتِ کهنه، بدتر از نبودِ گیت است: سبز می‌ماند و اعتماد می‌سازد. وقتی شکلِ کد عوض می‌شود، اول assert را به «حالتِ درست» بنویس، بعد ببین بذرها چه می‌گویند.

### نتیجه

Full و Lite: **۰ خطا / ۰ هشدار**. `probe-budget-audit` پاک (گروه‌های جدید `persist-write-shape` و `chart-change-

---

## §21 — «۵۳۱ میلیثانیه داخل رویداد» و «نخ ریسمانی که وجود ندارد» (2026-09-13، P-PERF-34/34b/35)

### سؤال

لاگ امروز — بعد از همهٔ چرخه‌های قبلی — دقیقاً یک مالک را نام برد:

```
[PERF] click breakdown: button=0ms panel=0ms apply=531ms
[PERF] owed frame ... (بعد از این چرخه)
```

سه فاز جدا شده بود تا معلوم شود کجاست. `button` و `panel` **صفر** بودند. یعنی جست‌وجوی دکمهٔ زیر ماوس
رایگان است و **تمام ۵۳۱ms کارِ رفرش** است. سؤال بعدی: چرا کارِ رفرش *داخل رویداد* اجرا می‌شود؟

### ریشه

`applyRefreshFlags` دیسپچرِ **مسیر رویداد** است (کلیک/هات‌کی/پایانِ ژست) و برای هر گروه فلگ سنگین
`RedrawAllObjects(true)` صدا می‌زد. پارامتر `force_redraw` **هر دو گیت** را دور می‌زند، پس کل بدنهٔ فریم
(بیس‌پرایس، ATR، `UpdateHistoricalValues`، `DrawMainLevels`، خانوادهٔ لیبل با `ClearAllLabels` + چهار
`Display*`، `RepositionAllOverlayLabels` و بلوک custom-price) **درون `OnChartEvent`** اجرا می‌شد.
یک بلوک ۵۳۱ میلی‌ثانیه‌ای داخل هندلر رویداد، همان فریزی است که کاربر حس می‌کند — و **همان کلاس نقصی**
است که قبیلتر فریم ۳۳۷۵ میلی‌ثانیه‌ای `CHART_CHANGE` را ساخت: کار به حسابِ رویداد نوشته می‌شد، نه فریم.

### درسِ بزرگ: گیتِ کهنه، خودِ باگ را نگه می‌دارد

دو گیت روی این تغییر **قرمز شدند و درست می‌گفتند**: یکی `g_buildStage != 0` را *داخل `OnTimer`*
می‌خواست — یعنی یکسان‌نگه‌داشتنِ همان شرطی که دو مالک داشت و باید حذف می‌شد (تایمر هم باید می‌فهمید فریم
«بدهکار» است، و دو شرطِ موازی از هم دور می‌افتند). دیگری نگهبانِ فریم داخل تایمر بود. هر دو به شکلِ
**درست** بازنویسی شدند: «مالک پمپ می‌کند، تایمر فقط بدهکار می‌کند». و دو بذرِ کهنه خودشان `STALE SEED`
دادند — دقیقاً همان چیزی که دستگاه باید بدهد.

### دربارهٔ «نخ چندریسمانی»

MQL4 **نخ ندارد** و هیچ ترفندی جایش را نمی‌گیرد: هر برنامه دقیقاً **یک** ریسهٔ سیستمی و یک هسته
The terminal multi-threaded است و *هر برنامه* نه. فقط دو راه خروج هست: **DLL** (که می‌تواند
ریسهٔ سیستمی بسازد) و **OpenCL** (که MQL5-only است، و در مستندات MQL4 جدا فهرست شده).

آنچه *هست* و همان چیزی است که لگ را برمی‌دارد، **هم‌روندی** است نه هم‌زمانی:
- یک صفِ کارِ مستقل که زیر یک بودجهٔ میلی‌ثانیه‌ای تخلیه می‌شود.
- **کارهای ارزان اول**، فریم آخر — تا فریم سنگین نتواند گرسنگیِ بقیه را بسازد.
- کاری که تمام نشد **همچنان بدهکار است**: نه چیزی برای rollback، نه درخواست دوباره.
- **Single-flight**: سه فشار سریع، یک فریم — نه سه.

و صادقانه در کد نوشته شد که این لایه **نمی‌تواند** یک بدنهٔ فریم را بشکند؛ شکستنِ کار
کارِ پایپ‌لاینِ مرحله‌ای است (چهار خانواده، چهار فریم). این لایه فقط **ترتیب و فاصله** می‌دهد.

### نتیجه

Full و Lite: **۰ خطا / ۰ هشدار**. `probe-budget-audit` پاک (**۷۷ بذر، همه گرفته شد**)،
`write-budget-audit` پاک (**۴۱ بذر**) — هر دو گیتِ کهنه به شکلِ درست بازنویسی شدند،
`zorder`/`gesture-budget`/`ui-text` پاک. مصنوعات: `Biotak Trigger TH3.ex4` و `... Lite.ex4`.

---

## §22 — «پاک‌سازی‌ای که با همان فلگی گارد شده که خانواده را خاموش می‌کند» (2026-09-13، P-PERF-36)

### گزارش کاربر
«سطوح با این دکمه خاموش و روشن نمی‌شه ریل تایم» + اسکرین‌شاتِ تولتیپ (پس کنترل مشخص است).

### ریشه — یک زنجیرهٔ کامل و قابل‌اثبات

```
حلقه CIR_ZONES  ->  g_showMidZones   (RuntimeSettings:503  #define inpShowMidZones g_showMidZones)
                ->  GetUnifiedZoneConfig().enabled
                ->  cfg.zonesEnabled
                ->  ساختِ زون‌ها گارد شده روی آن  (LevelPipeline 496/502/566/623/650)
                ->  پس zones[] خالی به RenderZones می‌رسد
                    (و RenderZones هیچ مسیر «حذف کهنه» ندارد؛ فقط لیستی را که گرفته می‌پیماید)
                ->  تنها حذف‌کنندهٔ آبجکت‌های زون: CleanupSurplusPipeline
                ->  و آن می‌خواند:  if(config.zonesEnabled) { CleanupSurplusObjects(..._Zone_...) }
```

**درسِ اصلی:** وظیفهٔ cleanup حذفِ چیزی است که رندرِ فعلی **تولید نمی‌کند** — و «خاموش»
دقیقاً حالتی است که در آن **همه‌چیز** اضافی است. گاردکردنِ حذف با فلگِ خاموش‌کننده یعنی حذف
دقیقاً در همان حالتی که لازم است **اجرا نشود**. نتیجه: سوییچ، *بازساختن* زون‌ها را متوقف
می‌کرد ولی هر آبجکتِ زونِ موجود سرجایش می‌ماند — و روشن‌کردن دوباره هم چیزی را عوض نمی‌کرد،
چون آن‌ها هیچ‌وقت نرفته بودند.

### چرا سوییچ‌های دیگر سالم بودند
فقط خانوادهٔ ZONES این نقص را داشت: cleanup خط‌ها/`_Midpoint_` هیچ‌وقت گارد نشده بود و
سوییچ‌های structure هم اصلاً حذف لازم ندارند (مسیر رنگ‌آمیزی). همین عدم‌تقارن توضیح می‌دهد
چرا کاربر بعد از فیکس شدن L و F، دقیقاً این دکمه را زد — و یک گیت هم به همین دلیل اضافه شد:
**«حذفِ یک خانواده نباید با فلگی که آن خانواده را خاموش می‌کند گارد شود». **

### فیکس
- `zoneCleanupFrom = config.zonesEnabled ? (maxLogicalStep + 1) : 0` — وقتی روشن است از
  بعد از آخرین گام (فقط آنچه کوچک شده)، وقتی خاموش است از **۰** (تمام خانواده اضافی است).
- همان گاردِ معکوس در مسیر بازنشستهٔ `DrawFactorLevelsAligned` (که امروز هیچ caller ندارد)
  هم برداشته شد — گاردِ غلط در کد مرده، مینِ ساعتی برای احیاکننده است.

### نتیجه
Full و Lite: **۰ خطا / ۰ هشدار**؛ دیپلوی تأیید شد. `probe-budget-audit` پاک،
`--selftest` = **۸۰ بذر، همه گرفته شد** (سه بذر جدید: گاردِ دوباره‌پیچیده، پنجره‌ای که فقط
حالت روشن را پوشش می‌دهد، و گاردِ معکوسِ مسیر مرده). `write-budget-audit` ۴۱/۴۱،
`gesture-budget` ۱۸/۱۸، `ui-text` ۹/۹، `zorder` ۹/۹.

---

## §23 — «تعویض تایم‌فریم انگار سطوح را از نو می‌سازد» — و باگی که لاگ قبل از من دید (2026-09-13، P-PERF-35b/37)

### دو عدد از بیلد جدید (فقط شاهد، بدون حدس)
```
[W][PERF] owed frame (chart-event) waited=78ms body=0ms stage=0 clear=0
[W][PERF] coop job=obj-cleanup waited=265ms ran=0ms stillOwed=0
[W][PERF] OnDeinit reason=3 took 157ms ... save=78ms cleanup=47ms handler=16ms
```
۱) `body=0ms` یعنی P-PERF-34 نشست: فشردن دیگر صفر کارِ فریم است؛ آن ۵۳۱ms رفته. ۵۳۱ms «لگ»
به ۷۸ms «تأخیرِ زمان‌بندی» تبدیل شد.

۲) اما `waited=265ms` یک باگِ **تازه‌ی خودم** را لو داد: `COOP_JOB_HEAVY_FRAME` آی‌دی‌اش **۱**
است، و حلقه آی‌دی‌ها را می‌پیمود — پس بازسازی **اول** اجرا می‌شد، کل برش بودجه را می‌خورد و
کارهای خانگی را یک دورهٔ تایمر بدهکار می‌گذاشت. کامنت خودم گفته بود «ارزان‌ها اول» و کد
عکسش را می‌کرد. **هر بار که ترتیبِ کارها را می‌نویسی، آن را قابل‌اثبات کن، نه ضمنی.**

### دربارهٔ «هزینهٔ صفر تعویض تایم‌فریم»
MT4 در تغییر پریودِ چارت **مجبوراً** اندیکاتور را unload و reload می‌کند — و لاگ این را
قطعاً نشان می‌دهد: `uninit reason 3` (یعنی `REASON_CHARTCHANGE`) ده‌ها بار، هر بار یک
teardown + init کامل. پس «صفر» به‌معنای لغوی ناممکن است. آنچه ممکن است
**حذفِ آشفتگیِ دیداری** است، با سه تغییرِ هماهنگ:

1. **نام‌های آبجکت مستقل از تایم‌فریم** — امروز `objectPrefix = inpObjectPrefix + "_" + TF + "_"`،
   پس هر سوییچ یک namespace را یتیم می‌کند و namespace بعدی باید از صفر ساخته شود.
2. **«فرزندخواندگی» در init** — از آبجکت‌های موجودِ روی چارت، کش را پر کن و فقط در جای خودت
   آپدیت کن؛ نه ساختِ دوباره.
3. **حذف‌نکردن در teardown برای `REASON_CHARTCHANGE`** — تا آبجکت‌ها اصلاً از چارت نروند.

→ نتیجه: سوییچ به یک پاسِ آپدیتِ پراپرتی تبدیل می‌شود (همان شکلِ یک رندرِ معمولی) به‌جای
«حذف ~۹۰۰ + ساخت ~۹۰۰ + محاسبهٔ کلِ هندسه». این کار انجام **نشده** و باید به‌صورت یک
تغییرِ منسجم با گیت انجام شود، چون TF داخل نام‌ها بارِ معنایی دارد (پیمایش روح‌گیری
`_LBL_` بین تایم‌فریم‌ها، `ClearAllLevels(s_lastPrefix)`، و suffixهای قدیمی).

### نکتهٔ روش‌شناسی
`[SNAP]` یک دامپِ **درخواستی با کلید X** است (`inpLogDumpKey`، «no background auto-logging»)،
نه لاگِ مسیر init. قبل از هر نتیجه‌گیری، مسیرِ واقعیِ چاپش را بخوان.prime`) با **۷۲ بذر، همه گرفته شد**؛ `write-budget-audit` پاک با **۳۸ بذر، همه گرفته شد**؛ `zorder`/`gesture-budget`/`ui-text` پاک.

## §24 — «تعویض تایم‌فریم = هزینهٔ صفر»: چیزی که باید «از قبل می‌دانست» (2026-09-13، P-PERF-38/38b/38c/38d)

**پرسش کاربر:** «وقتی تایم‌فریم عوض می‌کنم انگار سطوح مجدد محاسبه و رسم می‌شه، نباید اینطوری باشه — بهترین راهکار برای هزینهٔ صفر چیه؟»

پاسخ صادقانه از سه بخش ساخته شد، و بخش سوم جایی است که درس اصلی در آن است.

**۱) نام نباید تایم‌فریم داشته باشد.** پریفیکس آبجکت‌های سطح `prefix + "_" + TF + "_"` بود و در **۱۴** نقطه ساخته می‌شد. یعنی هر تعویض پریود، کلِ خانواده را **تغییر نام** می‌داد: namespace قدیمی باید حذف می‌شد و نamespace جدید از صفر ساخته می‌شد — روی سوییچی که فقط **قیمت‌ها** عوض می‌شوند. حالا یک صاحب دارد (`GetLevelObjectPrefix()`) و بدون TF است. تایم‌فریم هنوز تصویر را عوض می‌کند (`GetTimeframeTH()` → `thValue`)، ولی از راه **امضا** که درجا re-render می‌کند، نه از راه **نام** که تخریب/ساخت را تحمیل می‌کرد.

**۲) teardown نباید خانواده را حذف کند.** MT4 روی تغییر پریود، deinit+init اجباری می‌کند و شعبهٔ `REASON_CHARTCHANGE` کلِ خانواده را حذف می‌کرد. حالا نمی‌کند.

**۳) اما این دو کافی نبودند — و دلیلش ظریف است.** روی تغییر پریود، MT4 اندیکاتور را **reload** می‌کند، پس نمونهٔ جدید با امضای **خالی** شروع می‌شود (`s_lastLevelSig == ""`). یعنی اولین مقایسه **بی‌قیدوشرط true** است → `levelTopologyChanged` → `shouldClearLevels` → یک `ClearAllLevels` کامل روی همان خانواده‌ای که teardown تازه «نگه داشته» بود، و بلافاصله بازسازی مرحله‌ای که دوباره می‌سازدش.

> **درس:** «نگه‌داشتن» یک دارایی بین دو نمونهٔ فرایندی کافی نیست؛ باید **این واقعیت هم منتقل شود که آن دارایی نگه داشته شده**. حالتِ مشترک، خودش باید منتقل شود، نه فقط از حذف‌شدن فرار کند.

و چون مقایسه با **حالتِ فرایندی** انجام می‌شد (staticها)، نه با **حالتِ چارت**، نتیجه همیشه «هیچ‌چیز آنجا نیست» بود. راه‌حل: teardown یک **اثر انگشت** می‌نویسد از ورودی‌هایی که تعیین می‌کنند **کدام نام‌ها مجازند وجود داشته باشند** (شِمهٔ نام‌گذاری، مود، تعداد سطوح، نقطهٔ شروع، LS-first، هارمونیک) — و **نه** قیمت‌ها، که واقعاً هر تایم‌فریم متفاوت‌اند. نمونهٔ جدید آن را مقایسه می‌کند و در صورت تطابق، توپولوژی را **فرزندخوانده** می‌کند: بدون پاک‌سازی، بدون ری‌استارتِ مرحله‌ای، و تعویض تبدیل می‌شود به یک پاسِ درجای پراپرتی.

نکته‌های طراحی که ارزش حفظ دارند:
- **اثر انگشت باید از اعداد کوچک ساخته شود، نه هش رشته.** عدد صحیح دقیق است؛ هش رشته نیاز به استدلال احتمال برخورد دارد و برخورد در جهت غلط (تطابق کاذب) یعنی پاک‌سازیِ لازم انجام نمی‌شود.
- **محافظه‌کارانه به‌طور ساختاری:** نبودِ مهر (اولین attach، بیلد قدیمی، یا چارتی که تازه Remove شده) = عدم فرزندخواندگی = رفتار قبلی. و هر teardown که واقعاً حذف می‌کند، مهر را پاک می‌کند.
- **ترتیب resolve کردن مهم است.** اثر انگشت `g_thStartPointType` را می‌خواند و آن مقدار در جریان restore قیمت سفارشی ست می‌شود — پس resolve در **انتهای** `OnInitHandler` انجام می‌شود، و گیت هم همین را الزام می‌کند.
- گیت جدید (`topology-adoption`، ۶ زیراثبات) هر شش حلقه را می‌گیرد، از جمله «کدام ورودی‌ها در اثر انگشت نام برده شده‌اند» — چون اضافه‌کردن یک ورودیِ تعیین‌کنندهٔ نام بدون افزودنش به اثر انگشت، دقیقاً همان کلاس باگِ قبلی است.

## §25 — «چرا روشن/خاموش کردن سطوح هنوز کند حس می‌شود؟»: کاری که وجود نداشت (2026-09-13، P-PERF-38f/40)

**دو پرسش کاربر:** (۱) هزینهٔ روشن/خاموش کردن همهٔ سطوح را در همه جا نزدیک صفر کن. (۲) تعویض تایم‌فریم نباید سطوح را از نو بسازد/آپدیت کند؛ سطوح یک‌بار حساب شده‌اند.

### بخش ۱ — لاگ، علت را جایی نشان داد که دنبالش نبودیم

روی تغییر هیچ کدی، اول لاگ امروز را شمردم. ۴۵ خط از این جنس:

```
[W][PERF] owed frame (chart-event) waited=46..266ms body=0ms stage=0 clear=0
```

`body=0ms` یعنی **کار صفر**، و `clear=0` یعنی هیچ پاک‌سازی‌ای نبود. آنچه مانده بود `waited` بود: توزیعِ یکنواخت بین ۰ و ۲۶۶ms — یعنی **دقیقاً چرخهٔ ۲۵۰ میلی‌ثانیه‌ای تایمر**. پس:

> **درس:** هر چیزی که «کند» حس می‌شود، لزوماً «سنگین» نیست. یک چرخهٔ زمان‌بندی می‌تواند کندی را بسازد بدون آنکه هیچ محاسبه‌ای گران باشد. اول بپرس «چه‌قدر کار کرد؟» و بعد «چه‌قدر طول کشید؟» — جواب این دو می‌تواند کاملاً متفاوت باشد.

و `body=0ms` یک نکتهٔ بدتر را لو داد: تا وقتی تایمر سر می‌رسید، فریم چیزی برای کشیدن نداشت، چون مسیرِ سوییچ از قبل **تصویر قدیمی** را دوباره کشیده بود. پس کاربر با یک repaint بی‌تغییر تأیید می‌گرفت و تصویر واقعی تا یک‌چهارم ثانیه بعد می‌آمد.

**ریشه:** P-PERF-34 (که یک بار فریم ۵۳۱ms را داخل رویداد اجرا کرد و فریز ساخت) درست بود، ولی فقط «زمان‌بندی» کرد و هیچ‌کس آن بدهی را **در همان رویداد تسویه نکرد**. راه‌حل: تسویه در **نقطهٔ ورود رویداد** (تنها جایی که هم دو هندلر را می‌پوشاند و هم مسیرهای آینده را)، از طریق همان پمپِ مالکِ کار، و با هزینه‌ای که **خودش هم در بودجهٔ رویداد شمرده می‌شود** — چون درینِ پنهان، همان نقص یک‌لایه بالاتر است.

### بخش ۲ — «یک‌بار حساب شده» درست است، ولی تعریف به تایم‌فریم وابسته است

بررسی کردم: `GetTimeframeTH()` → `CalculateTimeframeTH(GetFractalTimeframeForCurrent())` → `ApplyFractalShift(GetBaseFractalTimeframeForCurrent())`، که همه به پریودِ چارت وابسته‌اند. یعنی **فرمول** سطوح به تایم‌فریم وابسته است (`TH% = M1% * sqrt(minutes)`) و قیمت‌ها بنا به تعریف تغییر می‌کنند.

پس «هزینهٔ صفر» نمی‌تواند یعنی «محاسبه نکن»؛ یعنی:
- **صفر تخریب** (P-PERF-38b/38d/38e: خانواده می‌ماند و درجا آپدیت می‌شود)،
- **صفر کارِ تکراریِ بی‌ربط** — و اینجا یک مورد واقعی پیدا شد: `ClearAllLabels` روی **هر** تعویض تایم‌فریم ۱۱ بار `ObjectsDeleteAll` می‌زد تا ghostهای namespaceهای قدیمی را پاک کند؛ در حالی که (الف) با نام‌گذاری بدون تایم‌فریم دیگر ساخته نمی‌شوند و (ب) مهاجرتِ یک‌بارهٔ P-PERF-38c با مجموعهٔ پیشوندی **فراگیرتر** همان‌ها را در لحظهٔ init پاک می‌کند.

> **درس دوم:** پاک‌سازی‌ای که **دفعات بیشتری** از چیزی که پاک می‌کند اجرا شود، به‌سادگی نامرئی می‌ماند و مثل کار ضروری به نظر می‌رسد. علامت تشخیص: بپرس «چند بار در دقیقه اجرا می‌شود؟» و «چند بار در عمر چارت لازم است؟» — اگر جواب‌ها فرق دارند، آن تفاضل بار اضافه است.

هر دو مورد گیت و بذر گرفتند، و هر دو گیتِ قدیمی که **شکل باستان** را قفل کرده بودند (جارو روی draw generation، و ستاره‌شدن روی `s_lastLabelClearGen`) به حالت درست بازنویسی شدند — نه بذرشان به بذر «کهنه» تبدیل شد (`STALE SEED` سخت‌گیر است).

## §26 — «کیبورد و پنل ناهماهنگاند»: مقداری که هیچوقت فرق نمیکرد، ولی **تصویر** می‌کرد (2026-09-13، P-UI-40)

**گزارش کاربر:** «با دکمهٔ کیبورد هماهنگ باشن با پنل، ناهماهنگی وجود نداشته باشه.»

و نکتهٔ مهم این است که ساختنِ این فیکس با **یک بررسی منفی** شروع شد، نه با یک حدس:

> آیا مقادیر از دو منبع متفاوت خوانده می‌شوند؟ **نه.**
> `PnlCurrentSet` مستقیماً گلوبال‌های زنده را برمی‌گرداند — `g_triggerLevelsEnabled`، `g_linesVisible`، `g_atrLabelsVisible`، `g_thLabelsMode`، `g_showLiveCountdown`.

پس **مقدار** هرگز نمی‌توانست اختلاف داشته باشد. اختلاف در **تصویر** بود: هر سوییچ یک `OBJ_BITMAP_LABEL` است که bitmap‌اش فقط با یک پاس رنگ‌آمیزی عوض می‌شود.

**ریشه، و چرا فقط بعضی سوییچ‌ها خراب بودند:** هر نویسندهٔ **داخلِ** فایل پنل، بعد از فشار، ردیف خودش را دوباره رنگ می‌کند. اما فایل پنل **بعد از** `EventHandlers` include می‌شود — یعنی بعد از جایی که **همهٔ** کلیدهای کیبورد و کلیکِ حلقه زندگی می‌کنند. پس:

- کلید را می‌زدی → حالت عوض می‌شد، چارت اطاعت می‌کرد، و کارتِ **باز** تا بسته‌شدنِ بعدی همان موقعیت قبلی را نشان می‌داد.
- و `PnlSyncOpenStepRow` (یک گارد تغییر، هر تیک) **تنها** هم‌زمان‌سازی از این جنس بود که وجود داشت — یعنی دقیقاً همان شکلِ «بعضی سوییچ‌ها کار می‌کنند، بعضی نه» که در گزارش بود.

**راه‌حل — همان الگوی owe/drain که در همان نشست برای فریم ساختیم:** نویسنده‌ای که نمی‌تواند رنگ کند، **درخواست** می‌دهد (`RequestUISync` در `GlobalVariables`، چون هم ترتیبِ include و هم بیلدِ بی‌پنلِ Lite را حل می‌کند)، و لایهٔ UI آن را **درین** می‌کند: اول درخواست را مصرف، بعد **هر دو** لایه را تعمیر کن (حلقه: states + badges؛ کارتِ باز: ردیف‌ها). درین در **دمِ رویداد** و همچنین در `RefreshKitOnBar` اجرا می‌شود، و هزینه‌اش داخل بودجهٔ رویداد شمرده می‌شود.

**دو درسِ طراحی که ارزش حفظ دارند:**

1. **کپی‌کردنِ یک راه‌حلِ نقطه‌ای، راه‌حل نمی‌سازد.** `PnlSyncOpenStepRow` درست بود ولی شکلش (یک مقایسهٔ گارددار به‌ازای هر حالت، هر تیک، تا ابد) برای N حالت مقیاس نمی‌گیرد. تعمیم‌اش یک ثبتِ درخواست است، نه هفت گاردِ مشابه.
2. **یک استثنا وجود دارد و باید صریح نگه داشته شود:** کارتِ Step با تغییر مود **تغییر شکل** می‌دهد (ردیف‌های هر مود زیر سگمنت‌ها زندگی می‌کنند) — رنگ‌کردنِ ردیف برایش کافی نیست، پس مالکِ بازسازیِ خودش را نگه می‌دارد و گیت هم اجازه نمی‌دهد حذف شود.

و یک نکتهٔ ابزارسازی: چکِ «هر شاخه‌ای که حالتِ نمایش‌داده‌شده را **می‌نویسد** درخواست بدهد» اول با جست‌وجوی سادهٔ زیررشته نوشته شد و **به‌درستی شکست خورد**: شاخهٔ کلید F مقدار `g_linesVisible` را به‌عنوان **آرگومان** پاس می‌دهد، که یک *خواندن* است. معیار به انتساب (`x =`) تغییر کرد تا خواندن با نوشتن اشتباه گرفته نشود — گیت باید دقیقاً همان چیزی را بسنجد که ادعا می‌کند.

## §27 — «مثل دکمه f»: یک دکمه، یک سوییچ، و دو لایه‌ای که واقعاً کند بودند

گزارش: «درست خاموش و روشن نمی‌شه مثل دکمه f». اولین خوانشم غلط بود و کاربر
خودش تصحیحش کرد؛ بعد از برگرداندن آن خوانش، دو لایهٔ واقعی ماندند و هر دو یک
شکل داشتند: **یک مالک، نصف تصمیم.**

### لایه ۱ — اشتباهی که خودم کردم و برگرداندم

گزارش: «مثل دکمه f نیست». اولین خوانشِ من این بود: دکمه اسمِ «Zones & Levels» را
می‌برد پس باید هر دو لایه را حرکت دهد. پیاده‌اش کردم
(`ZonesAndLevelsMasterOn() = g_showMidZones && g_linesVisible` و کلیک هر دو را ست
می‌کرد) و کاربر فوراً نتیجه‌اش را دید: **زون روشن + خطوط خاموش → یک فشار → خطوط
دوباره روشن.** یعنی یک کنترل، انتخابِ کنترل دیگری را بی‌صدا بازنویسی کرد.

برگرداندم. و درسِ اصلی همین است: **«حس می‌شود قابل‌اعتماد نیست» را هرگز با حرکت
دادن STATE بیشتر درست نمی‌کنند؛ با درست و قابل‌اعتماد حرکت دادنِ STATE درست
درست می‌کنند.** آنچه واقعاً مشکل بود، دو لایهٔ بعدی است — و آن‌ها هم کلِ فیکس
پرفورمنس این چرخه‌اند. الان حلقه فقط `g_showMidZones` را می‌خواند و می‌نویسد، و
`SetLinesVisible` صریحاً ممنوع است (گیت روی خودِ برنچ، نه روی فایل).

### لایه ۲ — حالت OFF یک «تخریب» بود، نه یک ماسک

`CleanupSurplusPipeline` وقتی `zonesEnabled` غلط بود از ایندکس ۰ شروع می‌کرد،
یعنی OFF = ۷ `ObjectDelete` به ازای هر زون و ON = یک `CreateZone` + ۷ نوشتن
ماسک، برای سوییچی که هیچ قیمت و هیچ هندسه‌ای را عوض نمی‌کند. F و L از اول یک
ماسک به ازای هر آبجکت بودند؛ همین تفاوت است که «کند» را حس می‌کرد.

و یک اشکال ساختاری در همان مسیر خوابیده بود: آن پیمایش **ایندکس** را
می‌شمارد (`prefix + idx`) و بعد از `maxConsecutiveMiss` متوقف می‌شود. یعنی
یک خانوادهٔ نامی که فاصلهٔ ایندکسش از ۶ بیشتر شود، روی چارت می‌ماند. پیمایش
مبتنی بر **کش آبجکت** این کلاس را حذف می‌کند: هر آبجکتی که ما ساخته‌ایم
نام‌بر می‌شود، بدون هیچ فرض ایندکسی و بدون یک `ObjectFind`.

### لایه ۳ — دو مالک، دو سؤال متفاوت

این لایه فقط چون لایهٔ ۲ را درست کردم قابل‌دسترس شد و دقیقاً همان چیزی است
که یک فیکس خوب را به یک باگ تازه تبدیل می‌کند: تا وقتی خانواده را **حذف**
می‌کردیم، نمی‌شد دوباره نشانش داد. حالا که نگهش می‌داریم، دو نویسندهٔ ماسک
باید یک سؤال بپرسند. `VisibilityShowAllCached` (کلید F) هیچ ورودی‌ای برای
سوییچ زون‌ها نداشت و هر آبجکت غیرِخط را `OBJ_ALL_PERIODS` می‌کرد — پس
«زون‌ها خاموش» تا اولین فشار F زنده می‌ماند. و یک اشتباه ریزتر: قاعدهٔ نامِ
`_Zone_` **قبل از** قاعدهٔ `_Above_` بررسی می‌شود، پس خط‌های مرزی
`_Top`/`_Bottom` «خط» حساب نمی‌شدند و F-show آن‌ها را با L خاموش هم برمی‌گرداند.

راه‌حل: یک predicate، `VisibilityZoneMask(nm, zonesVisible, linesVisible)`،
کنارِ بقیهٔ مالک‌های `OBJPROP_TIMEFRAMES` — «سوییچ خانواده → سوییچ خط مرزی →
ماسک F». هر سه مصرف‌کننده از آن می‌پرسند.

### چیزهایی که عمداً انجام نشد (و چرا)

- **حذف trigger band عوض نشد.** وسوسه‌اش زیاد است: «نشان بده نده، پنهانش کن».
  ولی یک band تریگر و یک band ساختاری **هم‌نام** و هم‌شکل‌اند؛ پس `_Zone_`
  هیچ‌وقت نمی‌تواند بگوید کدام است، و پنهان‌کردن یکی، مسیر F-show را مجبور
  می‌کند همه را `ALL_PERIODS` کند — یعنی band برای فاصلهٔ بین فشار F و رندری
  که دوباره پنهانش می‌کند، **یک فریم چشمک می‌زند**. حذف، هزینه‌اش بعد از
  اولین پاک‌سازی ۷ جست‌وجوی «اثبات‌شده‌غایب» است (P-PERF-07/13) و رندر هم فقط
  وقتی اجرا می‌شود که `IsTriggerLevelsEnabled()` عوض شده باشد. یعنی «پنهان
  کن» یک flicker تازه می‌خرید و چیزی نمی‌فروخت.
- **مسیر F دست نخورد.** وسوسه: `ScheduleHeavyFrame` را در F صدا بزنیم تا
  اصلاحِ رندر در همان رویداد بیفتد. اما F همین حالا «کار می‌کند» و معماری
  P-PERF-34/40 دقیق است؛ یک تغییر یک‌خطی در آن، ریسک برگشت فریز را می‌آورد
  برای سودی که لایهٔ ۳ عملاً صفرش کرد (زون‌های خاموش که F-show دوباره
  نشان نمی‌دهد، و bandهای تریگر که خودشان حذف‌اند).

### درسی که ارزش تکرار دارد

۱) وقتی یک کنترل «کند» حس می‌شود، اول بپرس **چه کاری انجام می‌دهد**؛ ممکن
است کار نباشد، تخریب باشد. ۲) و وقتی چیزی را از «حذف» به «نگه‌داشتن» تغییر
می‌دهی، **هر نویسندهٔ دیگری که آن آبجکت‌ها را می‌شناسد** یک سؤال تازه پیدا
می‌کند — این دو تغییر یک طراحی‌اند، نه دو.

## §28 — گاردی که ورودی‌اش ثابت است، گارد نیست؛ قفل است

این چرخه یک باگ **خودم** را پیدا کرد، و ارزشمندترین بخشش همین است: فیکسِ
قبلی (P-PERF-41b) درست بود، ولی یک «بهینه‌سازی» رویش سوار کردم که کنترل را
عملاً مرده کرد.

### شکلِ اشتباه

خواستم پیمایشِ خاموش‌کردنِ خانوادهٔ زون در هر رندر (اسکرول/زوم) اجرا نشود،
پس یک گارد تغییر گذاشتم:

```cpp
if(zonesVisible == s_lastZonesVisible && isHidden == s_lastHidden &&
   linesOn == s_lastLinesOn && epoch == s_lastEpoch)
    return 0;                       // «همه‌چیز همان است که بود»
```

سه سؤال را باید می‌پرسیدم و نپرسیدم:

۱) **آیا `zonesVisible` واقعاً متغیر است؟** نه. تنها caller
`if(!config.zonesEnabled)` است، پس همیشه `false`. یعنی گارد نمی‌تواند
گذاری (transition) را ببیند که ادعا می‌کند تشخیص می‌دهد.

۲) **آیا `epoch` در چرخهٔ off→on→off جابه‌جا می‌شود؟** نه. رندرِ روشن، ماسک‌ها
را از `ApplyTfMaskGuarded` می‌نویسد و آن **عمداً** epoch را بالا نمی‌برد
(وگرنه هر نوشتنِ ماسک گران می‌شد). پس epochِ ذخیره‌شده معتبر می‌ماند.

۳) **آیا هزینهٔ نگرفتنِ گارد واقعاً زیاد است؟** نه. در حالت خاموش، پیمایش
یک prob هش به ازای هر نامِ کش‌شده و **صفر** نوشتنِ ترمینال است؛ در حالی که
رندرِ روشن به ازای هر زون ۸ فراخوانیِ گارددار می‌پردازد. یعنی «صرفه‌جویی»
معکوس بود.

نتیجه، دقیقاً همان چیزی که کاربر دید:

```
فشار ۱ → OFF : زون‌ها مخفی می‌شوند                      ✔
فشار ۲ → ON  : رندر دوباره نشانشان می‌دهد               ✔
فشار ۳ → OFF : کلیدِ گارد عیناً مثل فشار ۱ → return 0   ✗  زون‌ها می‌مانند
... و هر فشار بعدی همین.
```

### شکلِ درست

گارد **سطحِ شیء** را نگه دار (`ApplyTfMaskGuarded`): ماسکی که می‌خواهی را با
ماسکی می‌سنجد که **برای همان نام** نوشتی. این نه می‌تواند گذاری را از دست
بدهد (چون هیچ‌چیز را خلاصه نمی‌کند) و نه گران است. و پارامتر بولی از تابع
برداشته شد تا **نوشتنِ** آن اشتباه ممکن نباشد، نه اینکه فقط با گیت گرفته شود.

### قاعدهٔ عمومی که این‌بار با خونِ خودم نوشته شد

**هر گاردِ «هزینه‌ای که حذف کردم را ثابت کن» باید یک ورودی متغیر داشته باشد
که گذار در آن دیده شود.** اگر همهٔ ورودی‌ها در لحظهٔ گذار ثابت بمانند، آن
گارد فقط کار را برای همیشه خاموش می‌کند. و اگر درست‌کردنش هزینهٔ محسوسی
ندارد (اینجا: یک prob هش)، گارد را نگه ندار.

نکتهٔ دوم این چرخه: **قبل از هر تغییری یک فرضیهٔ رقیب را بررسی کردم و رد
شد.** شک کردم که `OBJPROP_SELECTABLE=false` روی آیتم‌های حلقه باعث می‌شود
`CHARTEVENT_OBJECT_CLICK` هرگز نرسد. مرجعِ خواهرِ پروژه (`../ichimoku`) با
همان هندسهٔ حلقه هم `false` می‌گذارد، و منوی Tools در اسکرین‌شات کاربر باز
بود — یعنی کلیک می‌رسد. اگر آن فرضیه را «منطقی به‌نظر می‌رسد» می‌گرفتم،
کل مسیر کلیک را بازنویسی می‌کردم و باگِ واقعی سرجایش می‌ماند.
