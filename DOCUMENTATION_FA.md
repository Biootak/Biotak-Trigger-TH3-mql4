#               Biotak Trigger TH3

## مقدمه

**Biotak Trigger TH3** یک سیستم تحلیل تکنیکال پیشرفته برای پلتفرم MetaTrader 4 است که بر پایه تئوری TH (Time Harmonic) و محاسبات فراکتال طراحی شده است.

**نسخه کنونی:** 3.10 - GOLD (Post-Audit - All Critical Issues Fixed)

## سامان‌دهی تنظیمات ورودی

ورودی‌های اندیکاتور به ترتیب استفاده‌ی کاربر در گروه‌های زیر قرار گرفته‌اند:

| گروه | کاربرد |
|---|---|
| 01) Calculation / Mode & Core | حالت محاسبه، تعداد سطوح، قیمت پایه و Adaptive |
| 02) Base Price / Anchor | نوع نقطه شروع، Custom Price و Magnet |
| 03) Display / Visibility | نمایش خطوط، سطوح، برچسب‌ها و Targetها |
| 04) SS/LS Step | تنظیمات گام کوتاه و بلند |
| 05) Combo Step | Preset، حالت Advanced و مؤلفه‌های Combo |
| 06) Factor Step | حالت Factor، مبنا، مقدار و ظاهر آن |
| 07) Zones / Mid | فعال‌سازی و تراکم زون‌ها |
| 08) Level Style / Lines | رنگ، استایل و ضخامت همه‌ی خطوط (پایه، Structure L1-L5، Trigger و High/Low) |
| 09) Visibility - Structure & Trigger | نمایش سطوح Structure و Trigger و رنگ برچسب‌ها |
| 10) High/Low Tooltips | Tooltip سقف/کف تاریخی |
| 11) Text & Font | فونت متن‌های اندیکاتور |
| 12) Mode Label | برچسب وضعیت حالت فعال |
| 13) Label Layout | فاصله‌ها و چیدمان برچسب‌ها |
| 14) TH3 Tool / 15) AB=CD | حذف شده (ابزار TH3 بازنشسته شد) |
| 16) Alerts / 17) Hotkeys | هشدارها و کلیدهای میانبر |
| 18) Advanced / Objects & 19) History & Log | تنظیمات فنی و لاگ‌ها |

`Max Levels` تنها ورودی تعداد سطوح است و تعداد سطوح را به‌صورت یکسان برای سمت بالا و پایین، در همه‌ی حالت‌های محاسبه، کنترل می‌کند. ورودی‌های قدیمیِ جداگانه‌ی `Above`، `Below` و `Custom Price Max Levels` دیگر در تنظیمات نمایش داده نمی‌شوند.

نام متغیرهای کاربردی و مقدارهای پیش‌فرض محاسباتی برای حفظ رفتار قبلی تغییر نکرده‌اند؛ فقط ترتیب نمایش ورودی‌ها و عنوان بخش‌ها سامان‌دهی شده است.

---

## ساختار پروژه (Project Structure)

### سازماندهی فایل‌ها

تمامی فایل‌های هسته سیستم در دایرکتوری `Biotak/` قرار دارند:

```
    Biotak Trigger TH3.mq4          # فایل اصلی اندیکاتور
    Biotak/                         # دایرکتوری هسته سیستم
        THCalculations.mqh          # محاسبات پایه TH
        ATRCalculations.mqh         # محاسبات مبتنی بر ATR
        BasePriceManager.mqh        # مدیریت قیمت پایه
        LevelPipeline.mqh           # خط لوله پردازش لول‌ها و زون‌ها
        ModeDefinitions.mqh         # تعاریف مودهای مختلف (TH, SSLS, Factor, Combo)
        UnifiedZoneSystem.mqh       # سیستم یکپارچه رسم زون
        FractalTimeframes.mqh       # مدیریت تایم‌فریم‌های فراکتال
        ConstantsAndEnums.mqh       # ثابت‌ها و انوم‌ها
        UtilityFunctions.mqh        # توابع کمکی عمومی
        EventHandlers.mqh           # هندلرهای رویداد متاتریدر
```

---

## محاسبات فنی (Core Calculations)

### ۱. محاسبه پایه TH (Time Harmonic)

فرمول اصلی محاسبه گام‌های TH به شرح زیر است:

```
TH = (BasePrice * Percentage) / 100
```

**پارامترها:**
- **BasePrice:** قیمت پایه (معمولاً قیمت بسته شدن ۳۰ دقیقه‌ای در مرزهای ساعت).
- **Percentage:** درصد مربوط به تایم‌فریم فراکتال.

---

### ۲. تایم‌فریم‌های فراکتال و درصدها

سیستم از تایم‌فریم‌های معادل فراکتال برای دقت بالاتر استفاده می‌کند:

| تایم‌فریم جاری | تایم‌فریم فراکتال | درصد (%) | نام کوتاه |
|----------|-------------|----------|----------|
| M1 | M1 | 2.08 | M1 |
| M5 | M4 | 4.17 | M4 |
| M15/M30 | M16 | 8.33 | M16 |
| H1 | H1+M4 | 16.66 | H1.4 |
| H4 | H4+M16 | 33.33 | H4.16 |
| D1 | H17+M4 | 66.66 | H17.4 |
| W1 | D11+H9+M4 | 266.64 | D11.9.4 |
| MN1 | D45+H12+M16 | 533.28 | D45.12.16 |

---

### ۳. برچسب‌های معاملاتی ATR و تعمیم تایم‌فریم‌ها

ضرایب تصویر برای H1 به‌عنوان **نسبتِ فاصله به ATR همان تایم‌فریم** پیاده‌سازی شده‌اند، نه به‌صورت تعداد پیپ ثابت:

```text
SL     = 0.62 × ATR(tf)
TP1    = 1.53 × ATR(tf)
TP2    = 3.30 × ATR(tf)
TP3    = 6.81 × ATR(tf)
HuntSL = 0.63 × ATR(tf)
EngSL  = 0.27 × ATR(tf)
```

بنابراین روی H1، ATR_H1 استفاده می‌شود؛ روی M15، ATR_M15 و روی H4، ATR_H4. این روش با تغییر نوسان و مقیاس هر بازار سازگار است و از انتقال اشتباه اعداد پیپی H1 به تایم‌فریم‌های دیگر جلوگیری می‌کند. در هر لحظه فقط بلوک تایم‌فریم فعال/قفل‌شده نمایش داده می‌شود. این بلوک به‌صورت کامپکت در گوشه‌ی پایین راست چارت قرار دارد تا با لیبل‌های TH و سایر اطلاعات پایین چپ تداخل نداشته باشد. در تنظیمات می‌توان `inpShowATRTradeSLLabels` و `inpShowATRTradeTPLabels` را جداگانه خاموش/روشن کرد؛ `inpShowATRTradeLabels` کل بلوک را کنترل می‌کند.

#### چرا این اعداد انتخاب شده‌اند؟

از روی قطعه‌کد ارائه‌شده می‌توان با قطعیت گفت این‌ها **ضرایب تجربی/کالیبره‌شده‌ی استاد نسبت به ATR_H1** هستند؛ در خود قطعه‌کد دلیل آماری، نمونه‌داده یا آزمون مستقلی برای استخراج دقیق آن‌ها وجود ندارد. برداشت عملی آن‌ها چنین است: `EngSL=0.27` حد ضرر بسیار نزدیک برای ورود سریع، `SL=0.62` فاصله‌ی کنترل/ریسک اولیه، `HuntSL=0.63` تقریباً هم‌اندازه‌ی S1 برای شکار نقدینگی، و TPهای `1.53، 3.30، 6.81` اهداف افزایشی با فاصله‌ی بیشتر و نسبت ریسک‌به‌پاداش بالاترند. این تفسیر باید با بک‌تست و داده‌ی سازنده تأیید شود؛ نباید آن را اثبات نظری قطعی فرض کرد.

### ۴. سلسله مراتب لول‌ها (Hierarchy)

هر سطح قیمتی بر اساس اهمیت به دسته‌های زیر تقسیم می‌شود:
- **Structure (S):** لول اصلی TH (۱۰۰٪ گام).
- **Pattern (P):** معادل ۵۰٪ لول استراکچر.
- **Trigger (T):** معادل ۲۵٪ لول استراکچر.

---

### ۵. گام‌های فرعی (Step Multipliers)

برای رسم لول‌های پیشرفته از ضرایب زیر استفاده می‌شود:

#### Short Step (SS) & Long Step (LS)
```
SS = Structure * 1.5
LS = Structure * 2.0
```

در مود SS/LS فاصله‌ی مرکز تا مرکز به‌صورت یک‌درمیان SS و LS ساخته می‌شود. گزینه‌ی شروع با ورودی `inpLSFirst` تعیین می‌شود؛ همچنین با دوبارکلیک روی خط `Custom Price` یک منوی سریع باز می‌شود تا `LS first` یا `SS first` را انتخاب کنید. این انتخاب برای همان چارت ذخیره می‌شود و با کلید `Q` به مقدار ورودی برمی‌گردد. اندازه‌ی تمام زون‌های این مود همیشه از گام کوتاه گرفته می‌شود، حتی وقتی فاصله‌ی مرکز تا مرکز مربوط به LS باشد.

#### Control (C)
```
Control = (SS + LS) / 2 = Structure * 1.75
```

#### preset کوتاه SS در حالت Combo
برای اینکه در حالت `Combo` و `Preset` فاصله‌ی مرکز تا مرکز سطوح دقیقاً برابر گام کوتاه باشد، مقدار `Combo Preset` را روی `COMBO_PRESET_SS` بگذارید. این preset از یک مؤلفه‌ی `Pattern / SS` استفاده می‌کند و نیازی به مؤلفه‌ی دوم ندارد.

#### preset نسبت ۴/۳ در حالت Combo
برای استفاده از نسبت گام بلند به کوتاه به‌صورت گام یکنواخت، مقدار `Combo Preset` را روی `COMBO_PRESET_RATIO_4_3` بگذارید. محاسبه‌ی این preset برابر است با:

```text
Step = TH * (4 / 3) = TH * 1.333333...
```

---

### ۶. مدیریت قیمت پایه (Base Price)

#### منطق بروزرسانی
- **بازه زمانی:** هر ۳۰ دقیقه (در دقیقه‌های ۰۰ و ۳۰ هر ساعت).
- **آستانه تغییر (Threshold):** اگر تغییر قیمت نسبت به Base Price قبلی کمتر از **۰.۰۶۶٪** باشد، قیمت پایه بروزرسانی نمی‌شود تا از نویز جلوگیری شود.

---

### ۷. منطق ترازسازی هوشمند زون‌ها (Smart Zone Alignment)

در سیستم جدید `LevelPipeline` (نسخه ۳.۱۰ به بعد)، از یک منطق پیشرفته برای تراز کردن دقیق خطوط و زون‌ها استفاده شده است:

#### چالش جابجایی نیم‌واحدی
در سیستم‌های زون‌محور، معمولاً قیمت لول به عنوان **مرکز زون** در نظر گرفته می‌شود. این کار باعث می‌شد که مرز زون‌ها (محل رسم خطوط تریگر) با نصف واحد فاصله نسبت به قیمت واقعی لول رسم شود. 

#### راه حل: افست هوشمند (Smart Offset)
برای حل این مشکل بدون تغییر در ساختار اصلی، از یک جابجایی ریاضی استفاده شده است:
1. در تابع `CalculateLevels` یک افست معادل **نصفِ گام (0.5 * StepSize)** به قیمت مرکز اضافه می‌شود.
2. این کار باعث می‌شود **مرز زون‌ها** دقیقاً بر روی قیمت‌های اصلی (مثل قیمت TH یا Custom Price) منطبق گردد.
3. **نتیجه نهایی:** خطوط دقیقاً روی سطوح قیمتی قرار می‌گیرند و زون‌ها فضای متقارن بین خطوط را پر می‌کنند.

#### اصلاح ابعاد در مود فاکتور (Factor Mode)
در حالت Factor، به دلیل تفاوت در نحوه محاسبه رنج، زون‌ها بصورت بصری دوبرابر بزرگتر دیده می‌شدند. یک ضریب اصلاحی `0.5` برای هماهنگی کامل ابعاد زون‌ها در این مود اعمال شده است.

---

### ۸. سطوح استراکچر (L1-L5)

این سطوح بر اساس مضربی از گام اصلی محاسبه می‌شوند:
فرمول فواصل: `BaseMultiplier * 2^(Level - 1)`

اگر `BaseMultiplier = 3` باشد:
- **L1:** گام ۳
- **L2:** گام ۶
- **L3:** گام ۱۲
- **L4:** گام ۲۴
- **L5:** گام ۴۸

---

## ویژگی‌های عملکردی (Performance Features)

- **Multi-Level Caching:** استفاده از کش حافظه برای جلوگیری از تکرار محاسبات سنگین ریاضی و تبدیل‌های تایم‌فریم.
- **Throttled Redraw:** کنترل تعداد دفعات بازخوانی و رسم اشیاء روی چارت برای جلوگیری از درگیری بیش از حد CPU.
- **Object Pool Management:** مدیریت هوشمند اشیاء گرافیکی متاتریدر برای جلوگیری از کندی سیستم در تعداد لول‌های بالا.

---
*مستندات بروزرسانی شده در جون ۲۰۲۶ - تیم توسعه Biotak*
